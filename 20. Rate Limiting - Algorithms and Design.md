# Rate Limiting — Algorithms এবং Design

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [Rate Limiting কী এবং কেন](#rate-limiting-কী-এবং-কেন)
* [কোথায় বসাবে](#কোথায়-বসাবে)
* [কার উপর limit দেবে](#কার-উপর-limit-দেবে)
* [৫টা Algorithm](#৫টা-algorithm)
* [Algorithm গুলোর তুলনা](#algorithm-গুলোর-তুলনা)
* [Distributed Rate Limiting — আসল চ্যালেঞ্জ](#distributed-rate-limiting--আসল-চ্যালেঞ্জ)
* [Redis দিয়ে বাস্তবায়ন](#redis-দিয়ে-বাস্তবায়ন)
* [Client-কে কী উত্তর দেবে](#client-কে-কী-উত্তর-দেবে)
* [Throttling-এর ধরন](#throttling-এর-ধরন)
* [বাস্তব design-এর বিবেচনা](#বাস্তব-design-এর-বিবেচনা)
* [Interview Q&A](#interview-qa)
* [এক কথায় মনে রাখো](#এক-কথায়-মনে-রাখো)

---

<!-- TOC END -->

## Rate Limiting কী এবং কেন

Rate limiting মানে — **নির্দিষ্ট সময়ে কেউ কতবার request পাঠাতে পারবে, তার সীমা বেঁধে দেওয়া।**

যেমন: "প্রতি user প্রতি মিনিটে সর্বোচ্চ ১০০টা API call।"

**কেন দরকার:**

**১. Server-কে বাঁচানো** — একজন খারাপ client সেকেন্ডে ১০ হাজার request পাঠিয়ে পুরো system ধসিয়ে দিতে পারে।

**২. DDoS ও abuse ঠেকানো** — Brute force login, scraping, spam — সবই rate limit দিয়ে অনেকটা ঠেকানো যায়।

**৩. খরচ নিয়ন্ত্রণ** — প্রতিটা request-এ যদি একটা দামি external API বা AI model ডাকা হয়, তাহলে সীমা না দিলে বিল আকাশছোঁয়া হবে।

**৪. ন্যায্য ব্যবহার** — একজন যেন সব সম্পদ দখল করে না বসে, সবাই যেন সমান সুযোগ পায়। এটাকে বলে **noisy neighbor** সমস্যা ঠেকানো।

**৫. ব্যবসায়িক স্তর** — Free user-এর জন্য ১০০/ঘণ্টা, Pro user-এর জন্য ১০,০০০/ঘণ্টা।

**৬. Cascading failure ঠেকানো** — একটা service-এ চাপ বাড়লে সেটা যেন পুরো chain-এ ছড়িয়ে না পড়ে।

---

## কোথায় বসাবে

```
Client ──► CDN/WAF ──► Load Balancer ──► API Gateway ──► Service ──► Database
             ①              ②                ③             ④
```

**① CDN/WAF স্তরে (Cloudflare)** — সবচেয়ে সস্তা, কারণ traffic তোমার infrastructure-এ ঢোকেই না। DDoS-এর জন্য আদর্শ, কিন্তু business logic বোঝে না।

**② Load Balancer/Nginx-এ** — সরল IP-ভিত্তিক limit। দ্রুত, কিন্তু user চেনে না।

**③ API Gateway-তে (সবচেয়ে প্রচলিত)** — এখানে authentication হয়ে যায়, তাই user/plan অনুযায়ী limit দেওয়া যায়। **বেশিরভাগ ক্ষেত্রে এটাই সঠিক জায়গা।**

**④ Service-এর ভিতরে** — নির্দিষ্ট কোনো দামি operation-এর জন্য (যেমন report generate)।

**বাস্তবে একাধিক স্তরে দেওয়া হয়** — CDN-এ মোটা দাগে DDoS আটকানো, gateway-তে সূক্ষ্ম per-user limit।

---

## কার উপর limit দেবে

| ভিত্তি | সুবিধা | অসুবিধা |
|---|---|---|
| **IP address** | Login ছাড়াও কাজ করে | অফিস/মোবাইল network-এ হাজার জন একই IP; VPN দিয়ে সহজে এড়ানো যায় |
| **User ID / API Key** | সবচেয়ে নির্ভুল ও ন্যায্য | Login করা user-এর জন্যই কেবল সম্ভব |
| **API endpoint** | দামি endpoint-এ কড়া limit | একা যথেষ্ট না |
| **Device fingerprint** | IP বদলালেও ধরা যায় | নিখুঁত না, privacy প্রশ্ন |
| **Global** | System-কে সামগ্রিকভাবে বাঁচায় | কে দোষী তা আলাদা করা যায় না |

**বাস্তব কৌশল — একাধিক স্তর একসাথে:**

```
IP-ভিত্তিক:         ১০০০ req/ঘণ্টা     (মোটা দাগে abuse ঠেকাতে)
User-ভিত্তিক:       ১০০ req/মিনিট      (ন্যায্য ব্যবহার)
Endpoint-ভিত্তিক:   /login → ৫ req/মিনিট  (brute force ঠেকাতে)
Global:             ৫০,০০০ req/সেকেন্ড   (system বাঁচাতে)
```

---

## ৫টা Algorithm

### ১. Fixed Window Counter

সময়কে নির্দিষ্ট window-এ ভাগ করো (যেমন প্রতি মিনিট), প্রতিটা window-এ counter রাখো।

```
10:00:00 – 10:00:59  →  counter = 0, প্রতি request-এ +1
10:01:00              →  counter reset হয়ে 0
```

```javascript
const key = `rate:${userId}:${Math.floor(Date.now() / 60000)}`;
const count = await redis.incr(key);
if (count === 1) await redis.expire(key, 60);
if (count > LIMIT) return reject();
```

*সুবিধা:* সবচেয়ে সহজ, memory খুব কম (প্রতি user-এ একটা সংখ্যা), বোঝাও সহজ।

*অসুবিধা — **Boundary Problem** (এটাই এর মূল দুর্বলতা):*

```
Limit: প্রতি মিনিটে ১০০

10:00:59 → ১০০টা request  ✅ (প্রথম window-এ)
10:01:00 → ১০০টা request  ✅ (নতুন window শুরু)
────────────────────────────────────
মাত্র ১ সেকেন্ডে ২০০টা request গেলো! 😱
```

মানে বাস্তবে limit-এর **দ্বিগুণ** traffic ঢুকে যেতে পারে।

### ২. Sliding Window Log

প্রতিটা request-এর timestamp জমা রাখো। নতুন request এলে শেষ ১ মিনিটের মধ্যে কতগুলো আছে গুনে দেখো।

```
Request এলো 10:01:30 এ
→ ১০:০০:৩০ এর আগের সব timestamp মুছে দাও
→ বাকি গুলো গোনো
→ ১০০-এর কম হলে অনুমতি দাও, আর এই timestamp টাও যোগ করো
```

*সুবিধা:* **একদম নিখুঁত** — কোনো boundary problem নেই, যেকোনো ১ মিনিটের window-এ ঠিক ১০০-ই থাকবে।

*অসুবিধা:* Memory ব্যয়বহুল — প্রতিটা request-এর timestamp রাখতে হয়। ১ লাখ user × ১০০ request = ১ কোটি entry। বড় scale-এ অবাস্তব।

### ৩. Sliding Window Counter — বাস্তবে সবচেয়ে ভালো ভারসাম্য

Fixed window-এর সরলতা আর sliding window-এর নিখুঁততার মাঝামাঝি। আগের আর বর্তমান window-এর counter মিলিয়ে একটা আনুমানিক হিসাব করা হয়।

```
বর্তমান সময়: 10:01:30 (মানে বর্তমান window-এর ৫০% পার হয়েছে)

আগের window (10:00–10:01) = ৮০টা request
বর্তমান window (10:01–10:02) = ৩০টা request

আনুমানিক হিসাব = ৩০ + (৮০ × ৫০%) = ৩০ + ৪০ = ৭০
```

সূত্র: `বর্তমান count + আগের count × (বর্তমান window-এর বাকি অংশের অনুপাত)`

*সুবিধা:* Memory কম (প্রতি user-এ মাত্র দুটো সংখ্যা), boundary problem প্রায় নেই। Cloudflare-এর পরীক্ষায় দেখা গেছে ভুলের হার ১%-এরও কম।

*অসুবিধা:* একদম নিখুঁত না — ধরে নেয় আগের window-এ request গুলো সমানভাবে ছড়ানো ছিল।

**বেশিরভাগ বাস্তব system-এ এটাই ব্যবহার হয়।**

### ৪. Token Bucket — Burst সামলানোর সেরা উপায়

একটা বালতি কল্পনা করো যাতে token জমা হয়।

```
বালতির ধারণক্ষমতা: ১০টা token
পূরণের হার: প্রতি সেকেন্ডে ২টা token

প্রতিটা request একটা token খরচ করে।
Token না থাকলে request প্রত্যাখ্যাত।
```

```
সময় 0s:  🪙🪙🪙🪙🪙🪙🪙🪙🪙🪙  (১০টা)
১০টা request একসাথে এলো → সব গেলো ✅ (burst allowed)
সময় 0s:  (খালি)
সময় 1s:  🪙🪙                    (২টা যোগ হলো)
সময় 5s:  🪙🪙🪙🪙🪙🪙🪙🪙🪙🪙  (আবার ভরে গেছে, কিন্তু ১০-এর বেশি না)
```

*সুবিধা:* **Burst অনুমোদন করে** — ব্যবহারকারী কিছুক্ষণ চুপ থেকে জমানো token একসাথে খরচ করতে পারে। এটা বাস্তব ব্যবহারের সাথে খুব ভালো মেলে (কেউ একবারে ১০টা page খুলে, তারপর অনেকক্ষণ পড়ে)। Memory-ও কম — শুধু token সংখ্যা আর শেষ refill-এর সময়।

*কোথায় ব্যবহার হয়:* AWS API Gateway, Stripe, GitHub API — প্রায় সব বড় API।

```javascript
function allow(bucket, now) {
  const elapsed = (now - bucket.lastRefill) / 1000;
  bucket.tokens = Math.min(CAPACITY, bucket.tokens + elapsed * REFILL_RATE);
  bucket.lastRefill = now;
  if (bucket.tokens >= 1) { bucket.tokens -= 1; return true; }
  return false;
}
```

### ৫. Leaky Bucket — সমান গতির নিশ্চয়তা

একটা ফুটো বালতি — উপর থেকে যেভাবেই পানি ঢালো, নিচ দিয়ে **সবসময় সমান হারে** বেরোয়।

```
Request গুলো একটা queue-তে জমা হয়
Queue থেকে ঠিক ১০টা/সেকেন্ড হারে process হয়
Queue ভরে গেলে নতুন request বাদ
```

*সুবিধা:* Downstream-এ traffic একদম মসৃণ ও পূর্বানুমেয় থাকে। যে service নির্দিষ্ট গতির বেশি সামলাতে পারে না, তার সামনে আদর্শ।

*অসুবিধা:* Burst একদমই দেয় না — জরুরি request-ও queue-তে অপেক্ষা করে, তাই latency বাড়ে।

**Token Bucket vs Leaky Bucket:** Token bucket **input** নিয়ন্ত্রণ করে আর burst দেয়; leaky bucket **output** নিয়ন্ত্রণ করে আর গতি সমান রাখে। API-র জন্য সাধারণত token bucket ভালো।

---

## Algorithm গুলোর তুলনা

| Algorithm | Memory | নিখুঁততা | Burst | জটিলতা | কখন ব্যবহার |
|---|---|---|---|---|---|
| Fixed Window | খুব কম | কম (boundary সমস্যা) | দুর্ঘটনাক্রমে | খুব সহজ | সরল ক্ষেত্রে, কড়াকড়ি জরুরি না |
| Sliding Log | খুব বেশি | নিখুঁত | না | মাঝারি | ছোট scale, কড়া নিয়ম |
| Sliding Counter | কম | প্রায় নিখুঁত | না | মাঝারি | **বেশিরভাগ API-র জন্য সেরা** |
| Token Bucket | কম | ভালো | **হ্যাঁ** | মাঝারি | **Burst দরকার হলে সেরা** |
| Leaky Bucket | মাঝারি | ভালো | না | মাঝারি | Downstream-কে সমান গতিতে রাখতে |

**সহজ সিদ্ধান্ত:** Burst দরকার হলে **Token Bucket**, নাহলে **Sliding Window Counter**।

---

## Distributed Rate Limiting — আসল চ্যালেঞ্জ

**সমস্যা:** তোমার ১০টা API server আছে। প্রত্যেকে নিজের memory-তে count রাখলে —

```
Limit: ১০০ req/মিনিট
কিন্তু প্রতিটা server আলাদাভাবে ১০০ পর্যন্ত দিচ্ছে
→ বাস্তবে user পাচ্ছে ১০০০ req/মিনিট! ❌
```

**সমাধান ১ — Centralized store (Redis)**

সব server একই Redis-এ count রাখে।

*সুবিধা:* নিখুঁত, সব server একমত।
*অসুবিধা:* প্রতিটা request-এ একটা Redis call (১-২ms যোগ হয়)। Redis down হলে কী হবে সেটা ভাবতে হয়।

**সমাধান ২ — Local cache + পর্যায়ক্রমিক sync**

প্রতিটা server local-এ গোনে, আর প্রতি কয়েক সেকেন্ডে Redis-এর সাথে মিলিয়ে নেয়।

*সুবিধা:* খুব দ্রুত, Redis-এ চাপ কম।
*অসুবিধা:* একটু বেশি request ঢুকে যেতে পারে (eventual consistency)।

**সমাধান ৩ — Limit ভাগ করে দেওয়া**

১০টা server, তাই প্রত্যেকে ১০ করে দিক।

*সমস্যা:* Load balancer সমানভাবে ভাগ না করলে কেউ কেউ অন্যায্যভাবে reject হবে।

**Redis down হলে কী করবে? — এটা interview-র ভালো প্রশ্ন**

- **Fail open** — Redis নেই? সব request যেতে দাও। ✅ Availability রক্ষা পায়, ❌ কিন্তু তখন কেউ abuse করলে আটকানো যাবে না
- **Fail closed** — Redis নেই? সব request আটকাও। ✅ নিরাপদ, ❌ কিন্তু cache down হলেই পুরো service বন্ধ

সাধারণত **fail open**-ই বেছে নেওয়া হয় (rate limiter-এর জন্য service বন্ধ করা অযৌক্তিক), তবে login-এর মতো সংবেদনশীল endpoint-এ fail closed যুক্তিসঙ্গত। আর local fallback limit রাখা সবচেয়ে ভালো — Redis না থাকলে অন্তত মোটামুটি একটা সীমা তো থাকলো।

---

## Redis দিয়ে বাস্তবায়ন

**গুরুত্বপূর্ণ বিষয় — Atomicity।** "গোনো, তারপর বাড়াও" — এই দুটো আলাদা call হলে race condition হবে। তাই Lua script দিয়ে একসাথে করতে হয়।

**Sliding Window Counter (Lua script):**

```lua
-- KEYS[1] = rate limit key, ARGV[1] = limit, ARGV[2] = window (সেকেন্ড)
local current = tonumber(redis.call('GET', KEYS[1]) or '0')
if current >= tonumber(ARGV[1]) then
  return 0                                   -- প্রত্যাখ্যান
end
redis.call('INCR', KEYS[1])
if current == 0 then
  redis.call('EXPIRE', KEYS[1], ARGV[2])
end
return 1                                     -- অনুমোদন
```

**Sliding Window Log (Sorted Set দিয়ে):**

```javascript
const now = Date.now();
const key = `rate:${userId}`;
const pipeline = redis.multi();
pipeline.zremrangebyscore(key, 0, now - WINDOW_MS);  // পুরনো মুছে দাও
pipeline.zcard(key);                                  // এখন কতগুলো আছে
pipeline.zadd(key, now, `${now}-${Math.random()}`);   // এটা যোগ করো
pipeline.expire(key, WINDOW_SEC);
const [, count] = await pipeline.exec();
if (count >= LIMIT) return reject();
```

---

## Client-কে কী উত্তর দেবে

Limit ছাড়ালে **HTTP 429 Too Many Requests** পাঠাও।

সাথে এই header গুলো দেওয়া ভালো অভ্যাস — এতে client বুঝতে পারে তার অবস্থা:

```
HTTP/1.1 429 Too Many Requests
X-RateLimit-Limit: 100            ← মোট কত অনুমতি
X-RateLimit-Remaining: 0          ← আর কত বাকি
X-RateLimit-Reset: 1706539200     ← কখন reset হবে (Unix timestamp)
Retry-After: 30                   ← কত সেকেন্ড পর আবার চেষ্টা করবে
```

`Retry-After` header টা বিশেষভাবে গুরুত্বপূর্ণ — এটা না থাকলে client অন্ধভাবে বারবার চেষ্টা করতে থাকে, আর সমস্যা আরো বাড়ে।

**ভালো client-এর আচরণ:** 429 পেলে exponential backoff দিয়ে অপেক্ষা করা, আর `Retry-After` মেনে চলা।

---

## Throttling-এর ধরন

**Hard Throttling** — সীমা পার হলেই সোজা প্রত্যাখ্যান। কড়া, পূর্বানুমেয়।

**Soft Throttling** — কিছুটা ছাড় দেওয়া (যেমন ১১০ পর্যন্ত সহ্য করা), তারপর আটকানো।

**Elastic/Dynamic Throttling** — System-এ চাপ কম থাকলে সীমা শিথিল করা, চাপ বাড়লে কড়া করা। সবচেয়ে বুদ্ধিমান, কিন্তু client-এর জন্য অপ্রত্যাশিত।

**Adaptive Rate Limiting** — Server-এর নিজের স্বাস্থ্য (CPU, latency, error rate) দেখে নিজে থেকে সীমা কমানো-বাড়ানো। এটাকে বলে **load shedding** — system বাঁচাতে ইচ্ছাকৃতভাবে কিছু request ফেলে দেওয়া।

---

## বাস্তব design-এর বিবেচনা

**১. সব endpoint-এ একই limit দিও না।** `GET /products` সস্তা, `POST /generate-report` অনেক দামি। খরচ অনুযায়ী আলাদা limit দাও, বা প্রতিটা endpoint-কে আলাদা "cost" দাও (token bucket-এ একটা request ৫টা token খেতে পারে)।

**২. Limit গুলো configurable রাখো।** Code-এ hardcode না করে config-এ রাখো, যাতে deploy ছাড়াই বদলানো যায়।

**৩. Whitelist রাখো।** Internal service, monitoring, বিশ্বস্ত partner — এদের limit-এর বাইরে রাখো।

**৪. প্রথমে শুধু নজর রাখো, আটকিও না।** নতুন limit চালু করার আগে কিছুদিন log-only mode-এ চালাও — দেখো কতজন প্রকৃত user ক্ষতিগ্রস্ত হতো। নাহলে হঠাৎ বৈধ user-দের আটকে দিয়ে বসবে।

**৫. Monitoring অপরিহার্য।** কতগুলো request 429 পাচ্ছে, কারা সবচেয়ে বেশি পাচ্ছে — এগুলো নজরে রাখো। হঠাৎ 429-এর হার বাড়া মানে হয় attack, নয়তো তোমার limit ভুল।

**৬. Login-এর মতো endpoint-এ আলাদা কড়াকড়ি।** Brute force ঠেকাতে `/login` এ ৫ req/মিনিট, আর ব্যর্থ চেষ্টার পর ক্রমশ বাড়তে থাকা অপেক্ষা (exponential backoff) রাখা উচিত।

---

## Interview Q&A

**প্রশ্ন ১: Rate limiter design করো — কোথা থেকে শুরু করবে?**

প্রথমে requirement স্পষ্ট করবো — limit কার উপর হবে (IP, user, API key), কোথায় বসবে (gateway না service-এ), কত request কত সময়ে, আর distributed environment-এ নিখুঁততা কতটা জরুরি। তারপর algorithm বাছবো — burst দরকার হলে token bucket, নাহলে sliding window counter। সব server-এ সামঞ্জস্য রাখতে Redis-এ Lua script দিয়ে atomic counter রাখবো। Client-কে 429 এর সাথে `Retry-After` ও `X-RateLimit-*` header দেবো। আর Redis fail করলে fail-open করবো যাতে rate limiter নিজেই outage-এর কারণ না হয়।

**প্রশ্ন ২: Fixed window-এর সমস্যা কী?**

Boundary problem। Window-এর একদম শেষ মুহূর্তে limit পূর্ণ করে, আর নতুন window শুরু হওয়ার সাথে সাথেই আবার limit পূর্ণ করা যায় — ফলে খুব অল্প সময়ের মধ্যে limit-এর প্রায় দ্বিগুণ request ঢুকে যেতে পারে। ১০০/মিনিট limit-এ ১০:০০:৫৯ এ ১০০টা আর ১০:০১:০০ এ আরো ১০০টা মানে এক সেকেন্ডে ২০০টা। Sliding window counter এই সমস্যা সমাধান করে আগের window-এর একটা weighted অংশ হিসাবে ধরে।

**প্রশ্ন ৩: Token bucket আর leaky bucket-এর পার্থক্য?**

Token bucket-এ নির্দিষ্ট হারে token জমা হয় আর প্রতিটা request একটা token খরচ করে; জমে থাকা token একসাথে খরচ করা যায় বলে burst অনুমোদিত। Leaky bucket-এ request গুলো queue-তে জমা হয় এবং সবসময় একটা নির্দিষ্ট সমান হারে বেরোয়, তাই burst হয় না কিন্তু output একদম মসৃণ থাকে। API-র জন্য সাধারণত token bucket ভালো কারণ বাস্তব user আচরণ burst-প্রবণ; আর যে downstream service নির্দিষ্ট গতির বেশি সামলাতে পারে না তার সামনে leaky bucket উপযুক্ত।

**প্রশ্ন ৪: একাধিক server থাকলে rate limiting কীভাবে করবে?**

প্রতিটা server আলাদাভাবে গুনলে মোট limit server সংখ্যার গুণিতক হয়ে যাবে, যা ভুল। তাই একটা centralized store — সাধারণত Redis — ব্যবহার করতে হয় যেখানে সব server একই counter দেখে, এবং race condition এড়াতে Lua script দিয়ে atomic ভাবে check-and-increment করতে হয়। খুব বেশি throughput-এ প্রতি request-এ Redis call ব্যয়বহুল হলে local counter রেখে পর্যায়ক্রমে sync করা যায়, তবে তখন সামান্য বেশি request ঢুকতে পারে — এটা একটা সচেতন tradeoff।

**প্রশ্ন ৫: Rate limiter-এর Redis down হলে কী হবে?**

দুটো পথ আছে। Fail-open মানে Redis না থাকলে সব request যেতে দেওয়া — এতে service চালু থাকে কিন্তু ওই সময়টায় abuse আটকানো যায় না। Fail-closed মানে সব request আটকানো — নিরাপদ কিন্তু cache down হলেই পুরো service অচল, যা সাধারণত অগ্রহণযোগ্য। বেশিরভাগ ক্ষেত্রে fail-open বেছে নেওয়া হয়, তবে login-এর মতো সংবেদনশীল endpoint-এ fail-closed যুক্তিসঙ্গত। সবচেয়ে ভালো সমাধান হলো প্রতিটা server-এ একটা local fallback limit রাখা যাতে Redis না থাকলেও অন্তত মোটামুটি সুরক্ষা থাকে।

**প্রশ্ন ৬: Rate limiting কোথায় বসানো উচিত?**

সাধারণত API Gateway-তে, কারণ ওখানে authentication ইতিমধ্যে হয়ে গেছে বলে user বা plan অনুযায়ী সূক্ষ্ম limit দেওয়া যায়, এবং সব service-এর জন্য এক জায়গায় নিয়ম রাখা যায়। এর পাশাপাশি CDN বা WAF স্তরে মোটা দাগে IP-ভিত্তিক limit রাখা ভালো, কারণ তখন আক্রমণের traffic তোমার infrastructure-এ ঢোকেই না। আর বিশেষ কোনো ব্যয়বহুল operation-এর জন্য service-এর ভিতরেও আলাদা limit থাকতে পারে।

**প্রশ্ন ৭: 429 response-এ কী কী পাঠানো উচিত?**

HTTP 429 status code-এর সাথে `Retry-After` header পাঠানো সবচেয়ে জরুরি — এটা client-কে বলে কত সেকেন্ড পর আবার চেষ্টা করতে হবে, নাহলে client অন্ধভাবে retry করে সমস্যা বাড়ায়। এর সাথে `X-RateLimit-Limit`, `X-RateLimit-Remaining` আর `X-RateLimit-Reset` header দেওয়া ভালো অভ্যাস, কারণ তখন client আগেভাগেই নিজের ব্যবহার সামলাতে পারে এবং limit-এ পৌঁছানোর আগেই ধীর হতে পারে।

**প্রশ্ন ৮: IP-ভিত্তিক rate limiting-এর সমস্যা কী?**

একটা IP-র পিছনে অনেক প্রকৃত user থাকতে পারে — অফিসের NAT, বিশ্ববিদ্যালয়ের network, বা মোবাইল carrier-এর CGNAT-এ হাজার হাজার মানুষ একই public IP ব্যবহার করে, ফলে নির্দোষ user-রা আটকে যায়। উল্টো দিকে আক্রমণকারী proxy, VPN বা botnet ব্যবহার করে সহজেই IP বদলে limit এড়াতে পারে। তাই IP limit কেবল মোটা দাগে সুরক্ষার জন্য রাখা উচিত, আর মূল সূক্ষ্ম limit দেওয়া উচিত authenticated user ID বা API key-র উপর।

---

## এক কথায় মনে রাখো

Rate limiting = **কে কতবার request করতে পারবে তার সীমা** — server বাঁচাতে, abuse ঠেকাতে, খরচ নিয়ন্ত্রণে রাখতে।

Algorithm বাছার নিয়ম: **Burst দরকার → Token Bucket**, **নিখুঁততা দরকার → Sliding Window Counter**।

Distributed system-এ **Redis + Lua script** (atomic), আর Redis মরলে সাধারণত **fail open**।

Client-কে **429 + Retry-After** দাও, আর নতুন limit চালু করার আগে কিছুদিন **শুধু নজর রাখো, আটকিও না**।
