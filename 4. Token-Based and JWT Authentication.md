# Token-Based এবং JWT Authentication

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [সমস্যাটা আগে বুঝি](#সমস্যাটা-আগে-বুঝি)
* [পুরনো পদ্ধতি — Session (দারোয়ানের খাতা)](#পুরনো-পদ্ধতি--session-দারোয়ানের-খাতা)
* [নতুন পদ্ধতি — Token (আইডি কার্ড)](#নতুন-পদ্ধতি--token-আইডি-কার্ড)
* [পুরো Flow — ৭ ধাপে](#পুরো-flow--৭-ধাপে)
* [JWT কী — তিন টুকরার একটা কার্ড](#jwt-কী--তিন-টুকরার-একটা-কার্ড)
* [Signature — জালিয়াতি আটকানোর আসল জাদু](#signature--জালিয়াতি-আটকানোর-আসল-জাদু)
* [⚠️ সবচেয়ে বড় ভুল ধারণা](#️-সবচেয়ে-বড়-ভুল-ধারণা)
* [Access Token ও Refresh Token — দুইটা কেন](#access-token-ও-refresh-token--দুইটা-কেন)
* [Logout-এর সমস্যা](#logout-এর-সমস্যা)
* [Token কোথায় রাখবে](#token-কোথায়-রাখবে)
* [JWT vs Session — তুলনা](#jwt-vs-session--তুলনা)
* [কখন JWT ব্যবহার করবে, কখন করবে না](#কখন-jwt-ব্যবহার-করবে-কখন-করবে-না)
* [যে ভুলগুলো সবাই করে](#যে-ভুলগুলো-সবাই-করে)
* [কোড দিয়ে দেখো](#কোড-দিয়ে-দেখো)
* [ছবিতে ধাপগুলো](#ছবিতে-ধাপগুলো)
* [Interview Q&A](#interview-qa)
* [এক কথায় মনে রাখো](#এক-কথায়-মনে-রাখো)

---

<!-- TOC END -->

## সমস্যাটা আগে বুঝি

তুমি একটা website-এ login করলে। এরপর ২০টা পাতা ঘুরলে।

**প্রশ্ন:** প্রতিটা পাতায় server কীভাবে জানবে তুমি কে?

HTTP-র একটা মৌলিক বৈশিষ্ট্য হলো — **সে কিছু মনে রাখে না**। প্রতিটা request তার কাছে সম্পূর্ণ নতুন, অচেনা। আগের request-এর সাথে এটার কোনো সম্পর্ক আছে, সেটা সে জানেই না।

তাহলে সমাধান কী?

**সবচেয়ে বোকা সমাধান:** প্রতিটা request-এ username আর password পাঠাও।

```
GET /profile     + username & password
GET /settings    + username & password
GET /orders      + username & password
```

**কেন এটা ভয়ংকর:**

- Password ২০ বার network-এ ঘুরছে — একবার ফাঁস হলেই সব শেষ
- প্রতিবার database-এ গিয়ে password যাচাই করতে হবে (bcrypt ইচ্ছাকৃতভাবে ধীর, ~১০০ms) — server মরে যাবে
- Password কোথাও জমা রাখতে হবে browser-এ — মারাত্মক ঝুঁকি

তাই দরকার এমন কিছু — **একবার পরিচয় প্রমাণ করবে, তারপর একটা "প্রমাণপত্র" পাবে**, আর পরের বার সেই প্রমাণপত্রটাই দেখাবে।

এই প্রমাণপত্রের দুটো রূপ আছে। চলো দুটোই দেখি।

---

## পুরনো পদ্ধতি — Session (দারোয়ানের খাতা)

ধরো তুমি একটা অফিসে ঢুকছো। রিসেপশনে গিয়ে পরিচয় দিলে।

রিসেপশনিস্ট একটা **খাতায় লিখে রাখলেন** — *"টোকেন ৪৫৭ = তানহা, তৃতীয় তলা, বিকেল ৫টা পর্যন্ত"* — আর তোমাকে একটা কাগজে শুধু লিখে দিলেন **"৪৫৭"**।

এখন তুমি যেখানেই যাও, ওই "৪৫৭" দেখাও। প্রতিবার দারোয়ান **রিসেপশনে ফোন করে** জিজ্ঞেস করেন — "৪৫৭ কে?"

```
১. Login
   Browser → server: username + password
   Server: যাচাই করলো ✅
           একটা random ID বানালো: "abc123xyz"
           নিজের কাছে (memory/Redis-এ) লিখে রাখলো:
              abc123xyz  →  { userId: 4521, name: "তানহা" }
           Browser-কে দিলো: Set-Cookie: session=abc123xyz

২. পরের প্রতিটা request
   Browser → server: Cookie: session=abc123xyz
   Server: নিজের store-এ খুঁজলো abc123xyz
           পেলো → "ও আচ্ছা, এ তো তানহা" ✅
```

**মূল কথা:** ID টা অর্থহীন — শুধু একটা চাবি। **আসল তথ্যটা server-এর কাছে জমা আছে।**

### সুবিধা

- **Logout তাৎক্ষণিক** — খাতা থেকে লাইনটা কেটে দাও, ব্যস। Token সাথে সাথে অকেজো।
- **Cookie ছোট** — মাত্র কয়েক byte যায় প্রতিবার।
- **যেকোনো সময় বাতিল করা যায়** — কেউ সন্দেহজনক কিছু করলে সাথে সাথে তার session মুছে দাও।

### অসুবিধা

- **Server-কে মনে রাখতে হয়** — মানে server **stateful**।
- আর এখানেই [অধ্যায় ৩-এর](3.%20Stateful%20vs%20Stateless%20Architecture.md) সেই সমস্যা: একাধিক server থাকলে Server-1 এর খাতা Server-2 চেনে না। User একবার login, একবার logout দেখে।

**এর সমাধান:** session টা Redis-এ রাখা, যাতে সব server একই খাতা দেখে। কাজ করে, কিন্তু প্রতিটা request-এ একটা বাড়তি network call।

---

## নতুন পদ্ধতি — Token (আইডি কার্ড)

এবার অন্য একটা অফিস কল্পনা করো।

এখানে রিসেপশনিস্ট কোনো খাতায় কিছু লেখেন না। তিনি তোমাকে একটা **লেমিনেট করা আইডি কার্ড** দেন, যেখানে **তোমার সব তথ্য লেখা আছে** —

```
┌──────────────────────────────┐
│  নাম:    তানহা               │
│  পদ:     Student             │
│  মেয়াদ:  আজ বিকেল ৫টা       │
│                              │
│      🔏 অফিসের সিলমোহর        │
└──────────────────────────────┘
```

এখন দারোয়ান কী করেন? **রিসেপশনে ফোন করেন না।** তিনি শুধু **সিলমোহরটা দেখেন** — আসল কিনা। আসল হলে কার্ডে যা লেখা আছে সেটাই বিশ্বাস করেন।

**এটাই Token-Based Authentication।**

```
১. Login
   Browser → server: username + password
   Server: যাচাই করলো ✅
           একটা token বানালো যার ভিতরেই user-এর তথ্য আছে
           + একটা signature (সিলমোহর) বসালো
           নিজের কাছে কিছুই রাখলো না ❌
           Browser-কে দিলো token

২. পরের প্রতিটা request
   Browser → server: Authorization: Bearer eyJhbGci...
   Server: signature যাচাই করলো
           আসল হলে → token-এর ভিতরের তথ্যই বিশ্বাস করলো ✅
           কোনো database, কোনো Redis লাগলো না
```

### মূল পার্থক্যটা এক লাইনে

> **Session:** ID টা অর্থহীন, তথ্য server-এর কাছে।
> **Token:** তথ্য token-এর ভিতরেই, server কিছুই মনে রাখে না।

### কেন এটা বড় ব্যাপার

Server যেহেতু কিছু মনে রাখে না, তাই **যেকোনো server যেকোনো request সামলাতে পারে**। ১০টা server থাকুক বা ১০০টা — কোনো সমস্যা নেই, কোনো shared storage লাগে না।

এটাই **stateless**, আর এটাই horizontal scaling-এর মূল শর্ত।

---

## পুরো Flow — ৭ ধাপে

```
┌─────────┐                                    ┌─────────┐
│ Browser │                                    │ Server  │
└────┬────┘                                    └────┬────┘
     │                                              │
  ①  │ ─── POST /login {email, password} ────────► │
     │                                              │  ② password যাচাই
     │                                              │     (bcrypt দিয়ে)
     │                                              │
     │                                              │  ③ token বানাও
     │                                              │     + signature দাও
     │                                              │
  ④  │ ◄──── { token: "eyJhbGci..." } ───────────── │
     │                                              │
  ⑤  │ token জমা রাখো                                │
     │ (cookie / memory)                            │
     │                                              │
  ⑥  │ ─── GET /profile ─────────────────────────► │
     │     Authorization: Bearer eyJhbGci...        │
     │                                              │  ⑦ signature যাচাই
     │                                              │     মেয়াদ যাচাই
     │ ◄──── { name: "তানহা", ... } ──────────────── │     → তথ্য পাঠাও
     │                                              │
```

| ধাপ | কী হয় |
|---|---|
| ① | User email আর password পাঠায় |
| ② | Server password যাচাই করে (hash মিলিয়ে) |
| ③ | মিললে token বানায় — ভিতরে user-এর তথ্য, সাথে signature |
| ④ | Token client-কে পাঠিয়ে দেয় |
| ⑤ | Client token জমা রাখে |
| ⑥ | পরের প্রতিটা request-এ header-এ token পাঠায় |
| ⑦ | Server শুধু signature আর মেয়াদ দেখে — **database ছোঁয় না** |

**সবচেয়ে গুরুত্বপূর্ণ ধাপ ⑦** — এখানে কোনো database lookup নেই। এটাই token-এর পুরো শক্তি।

---

## JWT কী — তিন টুকরার একটা কার্ড

**JWT** (JSON Web Token) হলো token-এর একটা নির্দিষ্ট **standard format**।

Token-based auth একটা ধারণা; JWT সেই ধারণার সবচেয়ে জনপ্রিয় বাস্তবায়ন।

একটা JWT দেখতে এরকম — তিনটা অংশ, ডট দিয়ে আলাদা:

```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9  .  eyJzdWIiOiI0NTIxIiwibmFtZSI6IlRhbmhhIn0  .  SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV
└─────────── Header ────────────────┘    └─────────── Payload ──────────────┘    └───────── Signature ─────────┘
       "কোন পদ্ধতিতে সিল মারা"                  "কার্ডে কী লেখা"                      "সিলমোহরটা"
```

### ১. Header — সিলটা কোন পদ্ধতিতে মারা

```json
{
  "alg": "HS256",     // কোন algorithm দিয়ে sign করা
  "typ": "JWT"
}
```

### ২. Payload — আসল তথ্য

```json
{
  "sub": "4521",              // subject — কার token
  "name": "তানহা",
  "role": "student",
  "iat": 1516239022,          // issued at — কখন বানানো
  "exp": 1516242622           // expiry — কখন মেয়াদ শেষ
}
```

**প্রচলিত claim গুলো:**

| Claim | পুরো নাম | কী বোঝায় |
|---|---|---|
| `sub` | subject | কোন user-এর token |
| `iat` | issued at | কখন তৈরি হয়েছে |
| `exp` | expiration | কখন মেয়াদ শেষ ⭐ |
| `iss` | issuer | কে বানিয়েছে |
| `aud` | audience | কার জন্য বানানো |

এর সাথে তুমি নিজের যেকোনো তথ্য যোগ করতে পারো (`role`, `plan`, `department`)।

### ৩. Signature — সিলমোহর

```
signature = HMACSHA256(
                base64(header) + "." + base64(payload),
                গোপন_চাবি
            )
```

মানে — header আর payload একসাথে নিয়ে, **গোপন চাবি দিয়ে** একটা hash বানানো।

---

## Signature — জালিয়াতি আটকানোর আসল জাদু

এখানে একটা প্রশ্ন আসা স্বাভাবিক:

> *"Payload-এ যদি `role: student` লেখা থাকে, আমি তো সেটা বদলে `role: admin` করে দিতে পারি! তাহলে যে কেউ admin হয়ে যাবে না?"*

**খুব ভালো প্রশ্ন। আর এটাই signature-এর পুরো উদ্দেশ্য।**

দেখো কী হবে:

```
আসল token:
  payload = { role: "student" }
  signature = hash("student-এর payload" + গোপন_চাবি) = "abc123..."
  → দুটো মিলছে ✅

তুমি payload বদলালে:
  payload = { role: "admin" }        ← বদলে দিলে
  signature = "abc123..."             ← কিন্তু এটা পুরনোটাই

Server যাচাই করে:
  নতুন payload + গোপন_চাবি দিয়ে hash বানালো → "xyz789..."
  Token-এর signature → "abc123..."

  মিলছে না! ❌ → "Invalid token"
```

**তুমি নতুন signature বানাতে পারবে না, কারণ গোপন চাবিটা শুধু server-এর কাছে আছে।**

ঠিক যেমন — আইডি কার্ডের লেখা তুমি বদলাতে পারো, কিন্তু **অফিসের আসল সিলমোহরটা নকল করতে পারবে না**।

> **এক লাইনে:** Signature বলে না "এটা গোপন"। Signature বলে — **"এটা বদলানো হয়নি, আর আমিই এটা বানিয়েছি।"**

---

## ⚠️ সবচেয়ে বড় ভুল ধারণা

এই অংশটা সবচেয়ে গুরুত্বপূর্ণ, আর এখানেই সবচেয়ে বেশি মানুষ ভুল করে।

> ## **JWT-র payload encrypted না। এটা শুধু Base64 encoded।**

**Base64 কোনো encryption না — এটা শুধু লেখার একটা ভিন্ন রূপ।** যে কেউ ২ সেকেন্ডে খুলে পড়তে পারে:

```javascript
atob("eyJzdWIiOiI0NTIxIiwibmFtZSI6IlRhbmhhIn0")
// → {"sub":"4521","name":"Tanha"}
```

অথবা [jwt.io](https://jwt.io)-তে পেস্ট করলেই সব দেখা যায়। **কোনো চাবি ছাড়াই।**

### তাহলে payload-এ কী রাখা যাবে না

❌ Password (এমনকি hash করাও না)
❌ Credit card নম্বর
❌ জাতীয় পরিচয়পত্র / পাসপোর্ট নম্বর
❌ যেকোনো ব্যক্তিগত সংবেদনশীল তথ্য
❌ API secret key

### কী রাখা যাবে

✅ user id
✅ নাম, ভূমিকা (role), অনুমতি (permission)
✅ মেয়াদ, ইস্যুর সময়

> **মনে রাখার সহজ নিয়ম:** *JWT-র payload-এ এমন কিছু লিখো না, যা তুমি একটা পোস্টকার্ডে লিখে ডাকে পাঠাতে ভয় পাবে।*

**তাহলে token চুরি হলে?** — চোর তথ্যটা পড়তে পারবে **আর token-টা ব্যবহারও করতে পারবে**। এজন্যই HTTPS বাধ্যতামূলক, আর মেয়াদ ছোট রাখা জরুরি।

---

## Access Token ও Refresh Token — দুইটা কেন

এখানে একটা টানাপোড়েন আছে:

- **Token-এর মেয়াদ বড় রাখলে** → চুরি হলে চোর অনেকক্ষণ ব্যবহার করতে পারবে ❌
- **মেয়াদ ছোট রাখলে** → user প্রতি ১৫ মিনিটে আবার login করবে, বিরক্ত হয়ে চলে যাবে ❌

**সমাধান — দুই ধরনের token:**

| | Access Token | Refresh Token |
|---|---|---|
| মেয়াদ | ৫-১৫ মিনিট | ৭-৩০ দিন |
| কাজ | প্রতিটা API call-এ যায় | শুধু নতুন access token আনতে |
| Server-এ জমা থাকে? | ❌ না (stateless) | ✅ হ্যাঁ (database-এ) |
| বাতিল করা যায়? | না | **হ্যাঁ** ⭐ |
| কোথায় রাখে | Memory | httpOnly cookie |

**কীভাবে কাজ করে:**

```
Login
  → access token (১৫ মিনিট) + refresh token (৩০ দিন)

১৫ মিনিট পর access token-এর মেয়াদ শেষ
  → API বলে: 401 Unauthorized
  → Client নীরবে refresh token পাঠায়: POST /refresh
  → Server database-এ refresh token যাচাই করে
  → নতুন access token দেয় ✅
  → User কিছুই টের পায় না
```

**এতে দুই দিকই বাঁচে:**
- Access token চুরি হলে চোর পাবে সর্বোচ্চ ১৫ মিনিট
- User-কে ৩০ দিনে একবারও login করতে হয় না
- আর refresh token যেহেতু database-এ আছে, **সেটা যেকোনো সময় বাতিল করা যায়**

**Refresh Token Rotation:** প্রতিবার refresh করলে পুরনো refresh token বাতিল করে নতুন একটা দেওয়া — যাতে কেউ পুরনোটা চুরি করে বারবার ব্যবহার করতে না পারে।

---

## Logout-এর সমস্যা

এটা JWT-র সবচেয়ে বড় দুর্বলতা, আর interview-তে খুব জিজ্ঞেস করা হয়।

**Session-এ logout সহজ:** server-এর খাতা থেকে লাইনটা কেটে দাও। শেষ।

**JWT-তে?** Server তো কিছু জমাই রাখেনি! Token টা user-এর হাতে, আর সেটা মেয়াদ শেষ না হওয়া পর্যন্ত **বৈধ থাকবেই**।

```
User "Logout" চাপলো
  → Client নিজের কাছ থেকে token মুছে ফেললো
  → কিন্তু কেউ যদি আগেই token-টা copy করে রাখে?
  → সেটা এখনো কাজ করবে! 😱
```

### তিনটা সমাধান

**১. মেয়াদ ছোট রাখো (সবচেয়ে বাস্তবসম্মত)**
Access token ৫-১৫ মিনিটের। Logout করলে refresh token মুছে দাও। তাহলে চুরি হওয়া access token সর্বোচ্চ ১৫ মিনিট বাঁচবে। **বেশিরভাগ ক্ষেত্রে এটাই যথেষ্ট।**

**২. Blocklist রাখো**
বাতিল করা token গুলো Redis-এ রাখো (token-এর মেয়াদ পর্যন্ত), প্রতিটা request-এ মিলিয়ে দেখো।
*দাম:* এতে stateless-এর সুবিধা কিছুটা হারায়, কারণ আবার Redis দেখতে হচ্ছে। তবে Redis lookup খুব হালকা, তাই সহনীয়।

**৩. Token version রাখো**
User-এর row-তে একটা `token_version` column রাখো, আর সেটা token-এর payload-এও বসাও। সব device থেকে logout করাতে চাইলে শুধু version বাড়িয়ে দাও — পুরনো সব token নিজে থেকেই অবৈধ হয়ে যাবে।

> **বাস্তবে:** ১ + ২ একসাথে ব্যবহার করা হয় — ছোট মেয়াদ + জরুরি প্রয়োজনে blocklist।

---

## Token কোথায় রাখবে

এটা নিয়ে অনেক বিতর্ক আছে। সহজ করে দেখি।

| জায়গা | সুবিধা | ঝুঁকি |
|---|---|---|
| **localStorage** | সহজ, page reload-এ থাকে | **XSS** — যেকোনো JS পড়ে নিতে পারে ❌ |
| **sessionStorage** | tab বন্ধ হলে মুছে যায় | XSS-এ একই ঝুঁকি |
| **httpOnly cookie** | JavaScript পড়তেই পারে না ✅ | **CSRF** — তবে `SameSite` দিয়ে ঠেকানো যায় |
| **Memory (variable)** | সবচেয়ে নিরাপদ | reload করলেই হারায় |

### সহজ পরামর্শ

```
Access token   →  Memory-তে (JavaScript variable)
Refresh token  →  httpOnly + Secure + SameSite=Strict cookie
```

**কেন এটাই সেরা:**
- Access token JS-এর নাগালে আছে ঠিকই, কিন্তু reload করলেই হারায় আর মেয়াদও মাত্র ১৫ মিনিট — তাই XSS হলেও ক্ষতি সীমিত
- Refresh token httpOnly cookie-তে, তাই **কোনো JavaScript সেটা ছুঁতেই পারে না** — XSS হলেও চুরি হবে না
- `SameSite=Strict` দিয়ে CSRF ঠেকানো

**localStorage কি একদম নিষিদ্ধ?** না, অনেক বাস্তব app এটা ব্যবহার করে। কিন্তু মনে রাখতে হবে — তোমার site-এ একবার XSS হলে (এমনকি একটা তৃতীয় পক্ষের npm package-এর মাধ্যমেও) সব token চলে যাবে।

**আর একটা কথা যেটা কেউ ভুলে যায় না বললে:** সবসময় **HTTPS**। HTTP-তে token পাঠানো মানে সেটা পোস্টকার্ডে পাঠানো — মাঝের যে কেউ পড়ে নেবে।

---

## JWT vs Session — তুলনা

| বিষয় | JWT (Token) | Session |
|---|---|---|
| তথ্য কোথায় | Token-এর ভিতরে (client-এর কাছে) | Server-এ |
| Server stateful? | ❌ না | ✅ হ্যাঁ |
| একাধিক server-এ | সহজ, কিছু লাগে না | Redis-এর মতো shared store লাগে |
| প্রতি request-এ DB lookup | ❌ লাগে না | ✅ লাগে |
| আকার | বড় (কয়েকশো byte) | ছোট (শুধু একটা ID) |
| **Logout / বাতিল** | **কঠিন** ❌ | **সহজ** ✅ |
| Mobile app-এ | চমৎকার | cookie নিয়ে ঝামেলা |
| ভিন্ন domain (CORS) | সহজ | কঠিন |
| Microservices-এ | প্রতিটা service নিজেই যাচাই করতে পারে ✅ | সবাইকে একই store দেখতে হয় |

---

## কখন JWT ব্যবহার করবে, কখন করবে না

### ✅ JWT নাও যখন

- **Mobile app বা SPA** — cookie নিয়ে ঝামেলা নেই
- **Microservices** — প্রতিটা service নিজেই token যাচাই করতে পারে, কেন্দ্রীয় কোথাও যেতে হয় না
- **একাধিক domain** — `app.com` আর `api.com` আলাদা হলেও কাজ করে
- **তৃতীয় পক্ষকে সীমিত অনুমতি দেওয়া** — যেমন OAuth-এর access token
- **প্রচুর server, দ্রুত scale করতে হবে**

### ❌ JWT নিও না যখন

- **তাৎক্ষণিক বাতিল করা অপরিহার্য** — ব্যাংকিং, চিকিৎসা তথ্য। "৫ মিনিট পরে বাতিল হবে" এখানে চলবে না
- **সাধারণ একটা server-এর ছোট website** — session অনেক সহজ, কেন অকারণে জটিলতা?
- **অনেক বড় তথ্য বহন করতে হবে** — প্রতিটা request-এ ৪ KB token পাঠানো মানে bandwidth-এর অপচয়
- **Server-side rendered traditional app** — cookie + session-ই স্বাভাবিক পছন্দ

> **Boltu ভাইয়ের নিয়ম:** *"সবাই JWT ব্যবহার করছে বলে তুমিও করবে না। প্রশ্নটা হলো — তোমার কি সত্যিই stateless হওয়া দরকার? এক সার্ভারের ছোট app-এ session-ই সঠিক উত্তর।"*

---

## যে ভুলগুলো সবাই করে

**১. `alg: none` মেনে নেওয়া — সবচেয়ে বিখ্যাত JWT দুর্বলতা**
আক্রমণকারী header-এ `"alg": "none"` লিখে signature ফাঁকা রেখে দেয়। কিছু দুর্বল library সেটা মেনে নিয়ে বলে "ঠিক আছে, sign করার দরকার নেই"।
*প্রতিরোধ:* যাচাই করার সময় algorithm-টা **স্পষ্ট করে বলে দাও** — `jwt.verify(token, key, { algorithms: ['HS256'] })`

**২. দুর্বল গোপন চাবি**
`SECRET = "secret123"` — এটা কয়েক মিনিটে brute force করা যায়। কমপক্ষে ৩২ byte-এর random চাবি ব্যবহার করো, আর সেটা environment variable-এ রাখো, code-এ না।

**৩. মেয়াদ (`exp`) না দেওয়া**
মেয়াদহীন token মানে চিরকালের জন্য বৈধ। একবার চুরি হলে আজীবন।

**৪. Payload-এ সংবেদনশীল তথ্য**
উপরে বলা হয়েছে — এটাই সবচেয়ে বেশি ঘটা ভুল।

**৫. Signature যাচাই না করে শুধু decode করা**
`jwt.decode()` আর `jwt.verify()` — এক জিনিস না! `decode` শুধু খুলে পড়ে, **কিছু যাচাই করে না**। সবসময় `verify` ব্যবহার করো।

**৬. HTTP-তে token পাঠানো**
HTTPS ছাড়া পুরো ব্যবস্থাটাই অর্থহীন।

---

## কোড দিয়ে দেখো

```javascript
const jwt = require('jsonwebtoken');

// ── ১. Login — token বানানো ──────────────────────────
app.post('/api/login', async (req, res) => {
  const { email, password } = req.body;

  const user = await db.findUser(email);
  if (!user || !await bcrypt.compare(password, user.passwordHash)) {
    return res.status(401).json({ error: 'ভুল email বা password' });
  }

  // Access token — ছোট মেয়াদ, ভিতরে শুধু দরকারি তথ্য
  const accessToken = jwt.sign(
    { sub: user.id, role: user.role },      // ⚠️ sensitive কিছু না!
    process.env.JWT_SECRET,
    { expiresIn: '15m' }
  );

  // Refresh token — লম্বা মেয়াদ, database-এ জমা থাকে
  const refreshToken = crypto.randomBytes(40).toString('hex');
  await db.saveRefreshToken(user.id, refreshToken, '30d');

  res.cookie('refreshToken', refreshToken, {
    httpOnly: true,      // JavaScript ছুঁতে পারবে না
    secure: true,        // শুধু HTTPS-এ যাবে
    sameSite: 'strict'   // CSRF ঠেকাবে
  });

  res.json({ accessToken });
});

// ── ২. Middleware — প্রতিটা protected route-এ ──────────
function requireAuth(req, res, next) {
  const header = req.headers.authorization;
  if (!header?.startsWith('Bearer ')) {
    return res.status(401).json({ error: 'Token নেই' });
  }

  try {
    const payload = jwt.verify(
      header.slice(7),
      process.env.JWT_SECRET,
      { algorithms: ['HS256'] }        // ⚠️ alg: none আটকাতে
    );
    req.user = payload;                 // কোনো DB lookup লাগলো না ✅
    next();
  } catch (err) {
    if (err.name === 'TokenExpiredError') {
      return res.status(401).json({ error: 'মেয়াদ শেষ', code: 'EXPIRED' });
    }
    return res.status(401).json({ error: 'অবৈধ token' });
  }
}

// ── ৩. ব্যবহার ────────────────────────────────────────
app.get('/api/profile', requireAuth, (req, res) => {
  res.json({ userId: req.user.sub, role: req.user.role });
});

// ── ৪. Refresh — নতুন access token ───────────────────
app.post('/api/refresh', async (req, res) => {
  const token = req.cookies.refreshToken;
  const stored = await db.findRefreshToken(token);

  if (!stored || stored.expiresAt < Date.now()) {
    return res.status(401).json({ error: 'আবার login করুন' });
  }

  await db.deleteRefreshToken(token);              // rotation
  const newRefresh = crypto.randomBytes(40).toString('hex');
  await db.saveRefreshToken(stored.userId, newRefresh, '30d');
  res.cookie('refreshToken', newRefresh, { httpOnly: true, secure: true, sameSite: 'strict' });

  const accessToken = jwt.sign(
    { sub: stored.userId, role: stored.role },
    process.env.JWT_SECRET,
    { expiresIn: '15m' }
  );
  res.json({ accessToken });
});

// ── ৫. Logout ─────────────────────────────────────────
app.post('/api/logout', async (req, res) => {
  await db.deleteRefreshToken(req.cookies.refreshToken);   // এটাই আসল logout
  res.clearCookie('refreshToken');
  res.json({ ok: true });
  // access token টা বাকি কয়েক মিনিট বৈধ থাকবে — সেটা মেনে নেওয়া হয়
});
```

---

## ছবিতে ধাপগুলো

<img width="768" height="406" alt="Overview" src="https://github.com/user-attachments/assets/0e329505-31fd-469b-ad38-da88d557ce4e" />

**ধাপ ১ —** User credentials পাঠায়

<img width="765" height="528" alt="Step 1" src="https://github.com/user-attachments/assets/1fad7ce4-0b6f-4f98-a8bd-f04abe844c50" />

**ধাপ ২ —** Server যাচাই করে

<img width="758" height="417" alt="Step 2" src="https://github.com/user-attachments/assets/74f77d2e-7d89-45d7-b5ae-e334b04154b7" />

**ধাপ ৩ —** Token তৈরি হয়

<img width="757" height="538" alt="Step 3" src="https://github.com/user-attachments/assets/9b8bff16-1300-4d34-a853-c3c714b15742" />

**ধাপ ৪ —** Token client-কে পাঠানো হয়

<img width="757" height="465" alt="Step 4" src="https://github.com/user-attachments/assets/24c2a4ae-9b10-4abd-8768-150de8124773" />

**ধাপ ৫ —** Client token জমা রাখে

<img width="778" height="522" alt="Step 5" src="https://github.com/user-attachments/assets/ac73e0ac-3f94-4af2-9962-5b41d9ec754d" />

**ধাপ ৬ —** পরের request-এ token পাঠানো হয়

<img width="760" height="415" alt="Step 6" src="https://github.com/user-attachments/assets/121b2287-f26f-4ccf-a9d4-d3315f980d6b" />

**ধাপ ৭ —** Server যাচাই করে তথ্য পাঠায়

<img width="768" height="536" alt="Step 7" src="https://github.com/user-attachments/assets/1ca709b5-263b-4df0-8a65-27000957b2df" />

---

## Interview Q&A

**প্রশ্ন ১: Session আর token-based authentication-এর পার্থক্য কী?**

Session-এ server user-এর তথ্য নিজের কাছে (memory বা Redis-এ) জমা রাখে এবং client-কে শুধু একটা অর্থহীন session ID দেয়; তাই server stateful হয়ে যায়। Token-based-এ সব তথ্য token-এর ভিতরেই থাকে এবং signature দিয়ে যাচাই হয়, তাই server কিছুই মনে রাখে না — এটা stateless এবং সহজে horizontally scale করে। বিনিময়ে token বাতিল করা কঠিন, কারণ server-এর কাছে সেটার কোনো record নেই।

**প্রশ্ন ২: JWT-র গঠন কী?**

তিনটা অংশ, ডট দিয়ে আলাদা — Header (কোন algorithm ব্যবহার হয়েছে), Payload (claim গুলো, যেমন user id আর মেয়াদ), আর Signature। Signature হলো header ও payload কে গোপন চাবি দিয়ে sign করার ফল, যা প্রমাণ করে token-টা বদলানো হয়নি এবং সত্যিই server-ই এটা বানিয়েছে।

**প্রশ্ন ৩: JWT-র payload কি নিরাপদ? সেখানে password রাখা যাবে?**

না, একদমই না। Payload শুধু Base64 encoded, encrypted না — যে কেউ jwt.io-তে পেস্ট করে বা `atob()` চালিয়ে কোনো চাবি ছাড়াই পড়ে ফেলতে পারে। Signature শুধু নিশ্চিত করে তথ্যটা **বদলানো হয়নি**, কিন্তু **গোপন রাখে না**। তাই password, কার্ড নম্বর বা যেকোনো সংবেদনশীল তথ্য payload-এ রাখা যাবে না — শুধু user id, role-এর মতো তথ্য রাখা যায়।

**প্রশ্ন ৪: কেউ যদি payload বদলে `role: admin` করে দেয়?**

Signature সেটা ধরে ফেলবে। Server যখন যাচাই করে, তখন সে বদলে যাওয়া payload আর নিজের গোপন চাবি দিয়ে নতুন করে signature হিসাব করে এবং token-এর signature-এর সাথে মেলায়। Payload বদলানো হলে দুটো মিলবে না, তাই token প্রত্যাখ্যাত হবে। আক্রমণকারী নতুন সঠিক signature বানাতে পারবে না, কারণ গোপন চাবিটা শুধু server-এর কাছে।

**প্রশ্ন ৫: JWT-তে logout কীভাবে করবে?**

এটাই JWT-র মূল দুর্বলতা — token নিজে থেকে মেয়াদ শেষ না হওয়া পর্যন্ত বৈধ থাকে, server তাকে "ভুলে যেতে" পারে না। বাস্তব সমাধান হলো access token-এর মেয়াদ খুব ছোট রাখা (৫-১৫ মিনিট) আর একটা দীর্ঘমেয়াদি refresh token রাখা যা database-এ সংরক্ষিত ও বাতিলযোগ্য; logout করলে refresh token মুছে দেওয়া হয়, ফলে access token সর্বোচ্চ কয়েক মিনিট বাঁচে। জরুরি ক্ষেত্রে Redis-এ একটা blocklist রাখা যায়, তবে তাতে statelessness-এর সুবিধা কিছুটা হারায়।

**প্রশ্ন ৬: Access token আর refresh token আলাদা কেন?**

এটা নিরাপত্তা আর সুবিধার মধ্যে ভারসাম্য। মেয়াদ বড় রাখলে token চুরি হলে আক্রমণকারী অনেকক্ষণ ব্যবহার করতে পারে; ছোট রাখলে user-কে বারবার login করতে হয়। দুটো token ব্যবহার করলে access token ছোট মেয়াদের হয় (চুরি হলে ক্ষতি সীমিত) আর refresh token দিয়ে নীরবে নতুন access token নেওয়া যায় (user টেরও পায় না)। আর refresh token যেহেতু server-এ জমা থাকে, সেটা যেকোনো সময় বাতিল করা যায়।

**প্রশ্ন ৭: JWT কোথায় জমা রাখা উচিত?**

সবচেয়ে ভালো সমন্বয় হলো — access token JavaScript-এর memory-তে (reload করলে হারায়, কিন্তু মেয়াদও ছোট বলে ক্ষতি নেই), আর refresh token httpOnly + Secure + SameSite cookie-তে যাতে কোনো JavaScript সেটা ছুঁতে না পারে। localStorage সহজ কিন্তু XSS হলে সব token চলে যায়। শুধু cookie ব্যবহার করলে CSRF-এর কথা ভাবতে হয়, যেটা SameSite attribute দিয়ে সামলানো যায়।

**প্রশ্ন ৮: `alg: none` দুর্বলতাটা কী?**

JWT-র header-এ কোন algorithm ব্যবহার হয়েছে তা লেখা থাকে। কিছু দুর্বল implementation সেই header-টাকেই বিশ্বাস করে — তাই আক্রমণকারী `"alg": "none"` লিখে signature অংশটা ফাঁকা রেখে দিলে server ভাবে "এই token-এ sign করার দরকার নেই" এবং যেকোনো payload মেনে নেয়। প্রতিরোধ হলো যাচাই করার সময় algorithm স্পষ্ট করে নির্দিষ্ট করে দেওয়া, header থেকে না নেওয়া।

**প্রশ্ন ৯: Microservices-এ JWT কেন ভালো?**

কারণ প্রতিটা service নিজেই token যাচাই করতে পারে — শুধু public key বা shared secret থাকলেই হয়, কোনো কেন্দ্রীয় auth service বা session store-এ যেতে হয় না। এতে প্রতিটা request-এ একটা করে network hop বেঁচে যায় এবং auth service একটা bottleneck বা single point of failure হয় না। Session ব্যবহার করলে প্রতিটা service-কে একই session store দেখতে হতো।

**প্রশ্ন ১০: JWT কি সবসময় session-এর চেয়ে ভালো?**

না। JWT-র সুবিধা স্পষ্ট — stateless, scalable, mobile আর microservices-বান্ধব। কিন্তু একটা মাত্র server-এ চলা সাধারণ web app-এ session অনেক সহজ, token আকারে ছোট, আর সবচেয়ে বড় কথা logout তাৎক্ষণিক। যেখানে token তাৎক্ষণিক বাতিল করা অপরিহার্য (ব্যাংকিং, স্বাস্থ্য তথ্য), সেখানে session বা hybrid পদ্ধতিই বেশি উপযুক্ত। প্রশ্নটা "কোনটা আধুনিক" না, "আমার সত্যিই stateless হওয়া দরকার কিনা"।

---

## এক কথায় মনে রাখো

**Session** = দারোয়ানের খাতা। তথ্য server-এর কাছে, তুমি শুধু একটা নম্বর বহন করো। *Logout সহজ, কিন্তু server-কে মনে রাখতে হয়।*

**Token/JWT** = লেমিনেট করা আইডি কার্ড। তথ্য কার্ডেই লেখা, সাথে একটা নকল-অসম্ভব সিলমোহর। *Server কিছু মনে রাখে না, তাই দারুণ scale করে — কিন্তু কার্ড কেড়ে নেওয়া কঠিন।*

**JWT-র তিন অংশ:** Header (কোন পদ্ধতি) · Payload (কী লেখা) · Signature (সিলমোহর)।

**সবচেয়ে জরুরি তিনটা কথা:**
1. ⚠️ **Payload encrypted না** — যে কেউ পড়তে পারে। sensitive কিছু রেখো না।
2. 🔑 **Access token ছোট মেয়াদের + Refresh token দীর্ঘমেয়াদি** — এটাই সঠিক গঠন।
3. 🔒 **সবসময় HTTPS**, আর যাচাইয়ের সময় algorithm স্পষ্ট করে বলে দাও।

📖 *সম্পর্কিত: [3. Stateful vs Stateless](3.%20Stateful%20vs%20Stateless%20Architecture.md) · [5. OAuth2](5.%20How%20OAuth2%20works.md) · [10. Scalability](10.%20Scalability%20-%20Vertical%20vs%20Horizontal%20Scaling.md)*
