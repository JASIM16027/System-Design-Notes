# গল্পে গল্পে System Design — "পড়ুয়া"-র উত্থান (সংক্ষিপ্ত সংস্করণ)

> ### 📚 বিস্তারিত সংস্করণ চাও?
> এই ফাইলটা পুরো গল্পের **সংক্ষিপ্ত রূপ** — এক বসায় পড়ে ফেলার জন্য।
>
> প্রতিটা অধ্যায়ের **পূর্ণাঙ্গ সংস্করণ** (সংলাপ, ডায়াগ্রাম, কোড, সুবিধা-অসুবিধা সহ) আছে এখানে:
> ### ➡️ **[Golpo/ — ১১ অধ্যায়ের পূর্ণ সিরিজ](Golpo/README.md)**

---

> একটা অ্যাপ কীভাবে ৫ জন user থেকে ৫০ লাখে পৌঁছায়, আর প্রতিটা ধাপে কী কী ভাঙে —
> পুরোটা একটা গল্পে। কোনো theory মুখস্থ না, শুধু গল্প পড়ো।
>
> *বিষয়ের তালিকা [montumia.com](https://www.montumia.com/)-এর সিস্টেম ডিজাইন সিরিজ অনুসরণ করে সাজানো — গল্প ও চরিত্র সম্পূর্ণ আলাদা।*

<!-- TOC START -->

**অধ্যায় সমূহ:**

* [প্রলোগ — হলের ৩০৪ নম্বর রুম](#প্রলোগ--হলের-৩০৪-নম্বর-রুম)
* [অধ্যায় ১ — System Design জিনিসটা আসলে কী](#অধ্যায়-১--system-design-জিনিসটা-আসলে-কী)
* [অধ্যায় ২ — পরীক্ষার আগের রাত, প্রথম ধস (Scaling)](#অধ্যায়-২--পরীক্ষার-আগের-রাত-প্রথম-ধস-scaling)
* [অধ্যায় ৩ — দারোয়ান চাচা (Load Balancer)](#অধ্যায়-৩--দারোয়ান-চাচা-load-balancer)
* [অধ্যায় ৪ — চিঠিটা আসলে যায় কীভাবে (Networking Protocol)](#অধ্যায়-৪--চিঠিটা-আসলে-যায়-কীভাবে-networking-protocol)
* [অধ্যায় ৫ — সিলেটের ছেলেটার অভিযোগ (CDN)](#অধ্যায়-৫--সিলেটের-ছেলেটার-অভিযোগ-cdn)
* [অধ্যায় ৬ — ডাটাবেসের জ্যাম (Caching)](#অধ্যায়-৬--ডাটাবেসের-জ্যাম-caching)
* [অধ্যায় ৭ — একটা আলমারিতে আর ধরছে না (Partitioning ও Sharding)](#অধ্যায়-৭--একটা-আলমারিতে-আর-ধরছে-না-partitioning-ও-sharding)
* [অধ্যায় ৮ — যেদিন হার্ডডিস্ক মরে গেলো (Redundancy ও Replication)](#অধ্যায়-৮--যেদিন-হার্ডডিস্ক-মরে-গেলো-redundancy-ও-replication)
* [অধ্যায় ৯ — নতুন বিপত্তি (CAP ও PACELC)](#অধ্যায়-৯--নতুন-বিপত্তি-cap-ও-pacelc)
* [অধ্যায় ১০ — তানহার শেষ শিক্ষা](#অধ্যায়-১০--তানহার-শেষ-শিক্ষা)
* [পুরো গল্পের মানচিত্র](#পুরো-গল্পের-মানচিত্র)

---

<!-- TOC END -->

## প্রলোগ — হলের ৩০৪ নম্বর রুম

রাত দুইটা। তানহার রুমমেট ঘুমাচ্ছে। তানহা জেগে।

সামনে পরীক্ষা, আর তার কাছে নোট নেই। সিনিয়র আপুর নোটের ফটোকপি ঘুরতে ঘুরতে এত ঝাপসা হয়ে গেছে যে পড়াই যায় না। মেসেঞ্জারে ২০০ জনের গ্রুপে কেউ একটা PDF দিয়েছিল, সেটা খুঁজে বের করতে ৪০ মিনিট লেগেছে।

তানহা ল্যাপটপ খুললো।

"এইটা একটা সমস্যা। আর সমস্যার সমাধান বানানোই তো আমার কাজ।"

তিন সপ্তাহ পর জন্ম নিলো **"পড়ুয়া"** — একটা ওয়েব অ্যাপ, যেখানে যে কেউ নোট আর প্রশ্নব্যাংক আপলোড করতে পারে, আর যে কেউ খুঁজে ডাউনলোড করতে পারে।

প্রথম দিন user ছিল ৫ জন — তানহা আর তার চার বন্ধু।

এই গল্পটা সেই ৫ জন থেকে ৫০ লাখে পৌঁছানোর গল্প। আর প্রতিবার যখন user বাড়লো, তখন কী কী ভেঙে পড়লো — সেটার গল্প।

---

## অধ্যায় ১ — System Design জিনিসটা আসলে কী

প্রথম version-টা ছিল অসম্ভব সহজ।

```
┌─────────────────────────────────┐
│   একটা মাত্র কম্পিউটার          │
│                                 │
│   ┌──────────┐  ┌───────────┐   │
│   │ Web App  │  │ Database  │   │
│   └──────────┘  └───────────┘   │
│   ┌──────────────────────────┐  │
│   │ PDF ফাইলগুলো (একটা ফোল্ডারে)│  │
│   └──────────────────────────┘  │
└─────────────────────────────────┘
```

একটাই সার্ভার। তার ভিতরেই অ্যাপ, তার ভিতরেই ডাটাবেস, তার ভিতরেই ফাইল।

আর এটা **দুর্দান্ত কাজ করছিল।**

এখানে প্রথম শিক্ষাটা লুকিয়ে আছে, যেটা বেশিরভাগ মানুষ ভুলে যায় —

> **System Design মানে শুরু থেকেই জটিল কিছু বানানো না। System Design মানে — এখন যা আছে তার সীমা কোথায়, সেটা জানা।**

তানহার এই এক-সার্ভার design ৫ জন user-এর জন্য **নিখুঁত**। ওখানে load balancer বসালে সেটা বোকামি হতো, Kafka বসালে সেটা পাগলামি।

Boltu ভাই — তানহার সিনিয়র, যে একটা কোম্পানিতে কাজ করে — একবার বলেছিল:

> "সবচেয়ে ভালো architecture হলো সেটা, যেটা আজকের সমস্যা সমাধান করে আর কালকের সমস্যার জন্য দরজা খোলা রাখে। আজকের সমস্যার জন্য কালকের সমাধান বানানো — এটার নাম over-engineering, আর এটা একটা রোগ।"

তাহলে System Design আসলে কী?

এটা হলো এই প্রশ্নগুলোর উত্তর খোঁজার শিল্প —
- কত মানুষ ব্যবহার করবে?
- কতটা দ্রুত হতে হবে?
- কতটুকু ভুল সহ্য করা যাবে?
- কোনটা ভেঙে গেলে সবচেয়ে বড় ক্ষতি?
- আর এই সবের জন্য কত টাকা খরচ করা যাবে?

**সব প্রশ্নের একটাই উত্তর নেই। প্রতিটা উত্তরের একটা দাম আছে।** এটাই system design।

📖 *আরো পড়ো: [8. Functional vs Non-Functional Requirements](8.%20Functional%20vs%20Non-Functional%20Requirements.md), [9. Back of the Envelope Estimation](9.%20Back%20of%20the%20Envelope%20Estimation.md)*

---

## অধ্যায় ২ — পরীক্ষার আগের রাত, প্রথম ধস (Scaling)

তিন মাস পর।

কে যেন একটা ফেসবুক গ্রুপে "পড়ুয়া"-র লিংক দিলো। ওই গ্রুপে ৮০ হাজার শিক্ষার্থী।

আর সেদিনই ছিল সেমিস্টার ফাইনালের আগের রাত।

রাত ৯টায় তানহার ফোনে পরপর মেসেজ আসতে শুরু করলো —

> "সাইট খুলছে না"
> "লোডিং ঘুরতেই আছে"
> "৫০৩ error দেখাচ্ছে"

তানহা সার্ভারে ঢুকে দেখলো — CPU ১০০%। RAM শেষ। সার্ভারটা কার্যত অজ্ঞান।

### প্রথম চেষ্টা — সার্ভারটাকে বড় করো (Vertical Scaling)

তানহা ক্লাউড প্যানেলে গিয়ে সার্ভার upgrade করলো। ২ CPU থেকে ৮ CPU, ৪ GB RAM থেকে ৩২ GB।

সাইট আবার চললো। তানহা স্বস্তির নিঃশ্বাস ফেললো।

**এটাকে বলে Vertical Scaling — মেশিনটাকেই বড় করে ফেলা।**

সুবিধা: কোড-এর একটা লাইনও বদলাতে হয়নি। ১৫ মিনিটে সমাধান।

কিন্তু পরের মাসে user আরো বাড়লো। তানহা আবার upgrade করতে গিয়ে দুটো জিনিস দেখলো —

১. পরের ধাপের সার্ভারের দাম আগেরটার **চার গুণ**। ক্ষমতা বেড়েছে মাত্র দ্বিগুণ।
২. একটা জায়গার পর আর কোনো বড় অপশনই নেই। **হার্ডওয়্যারের একটা সিলিং আছে।**

আর সবচেয়ে ভয়ংকর ব্যাপারটা ঘটলো এক শুক্রবার — ওই একটামাত্র সার্ভারটা reboot নিলো, আর ৪০ মিনিট পুরো "পড়ুয়া" পৃথিবী থেকে উধাও।

**একটা সার্ভার মানে একটা মৃত্যুবিন্দু। Single Point of Failure।**

### দ্বিতীয় চেষ্টা — অনেকগুলো সার্ভার (Horizontal Scaling)

Boltu ভাই বললো — "তুই একজন রাঁধুনিকে আরো দ্রুত রাঁধতে বলছিস। কিন্তু একজন মানুষের একটা সীমা আছে। **আরেকজন রাঁধুনি নিয়োগ দে।**"

তানহা তিনটা সার্ভার চালু করলো। একই কোড, তিন জায়গায়।

আর সাথে সাথে একটা নতুন সমস্যায় পড়লো।

User login করে Server-1 এ। Server-1 তার session নিজের memory-তে রাখলো। পরের ক্লিকে request গেলো Server-2 তে। Server-2 বললো — "আপনি কে? Login করুন।"

তানহা মাথায় হাত দিলো।

Boltu ভাই হাসলো — "এইটাই horizontal scaling-এর একমাত্র শর্ত। **সার্ভারকে ভুলে যেতে শেখাতে হবে।**"

সমাধান দুটো —
- Session টা সার্ভারের বাইরে Redis-এ রাখা, যাতে সব সার্ভার একই জায়গা থেকে পড়ে
- অথবা JWT ব্যবহার করা, যেখানে token নিজেই সব তথ্য বহন করে — সার্ভারের কিছু মনে রাখারই দরকার নেই

তানহা JWT নিলো।

এখন তার সার্ভারগুলো **stateless** — কোনো সার্ভার কোনো user-কে চেনে না, তাই যেকোনো সার্ভার যেকোনো request সামলাতে পারে। একটা মরে গেলেও কিছু হারায় না।

> **মূল কথা:** Vertical scaling সহজ কিন্তু সীমাবদ্ধ। Horizontal scaling অসীম কিন্তু তার শর্ত একটাই — **stateless হও।**

📖 *আরো পড়ো: [10. Scalability](10.%20Scalability%20-%20Vertical%20vs%20Horizontal%20Scaling.md), [3. Stateful vs Stateless](3.%20Stateful%20vs%20Stateless%20Architecture.md), [4. Token & JWT](4.%20Token-Based%20and%20JWT%20Authentication.md)*

---

## অধ্যায় ৩ — দারোয়ান চাচা (Load Balancer)

তিনটা সার্ভার তো হলো। কিন্তু user জানবে কী করে কোন সার্ভারে যেতে হবে?

তানহা প্রথমে একটা হাস্যকর কাজ করলো — সে তিনটা আলাদা লিংক দিলো:

```
poruya.com   → Server-1
poruya2.com  → Server-2
poruya3.com  → Server-3
```

"যেটায় ঢুকতে পারো ঢোকো।"

ফলাফল? সবাই প্রথমটাতেই গেলো। প্রথমটা মরে গেলো। বাকি দুটো খালি বসে রইলো।

Boltu ভাই এবার সত্যিই হাসলো।

"তোদের হলের গেটে দারোয়ান চাচা আছে না? তিনটা রেজিস্টার খাতা আছে। উনি কী করেন? সবাইকে এক খাতায় লিখতে দেন? না — উনি দেখেন কোন খাতায় লাইন কম, সেখানে পাঠান।

**সেই দারোয়ান চাচার নাম Load Balancer।**"

```
                    ┌──────────────────┐
   সব User ────────►│  Load Balancer   │  ← একটাই ঠিকানা: poruya.com
                    └────────┬─────────┘
           ┌─────────────────┼─────────────────┐
           ▼                 ▼                 ▼
       Server-1          Server-2          Server-3
```

এখন user শুধু `poruya.com` জানে। ভিতরে তিনটা না তিরিশটা সার্ভার আছে — সে জানেই না।

### দারোয়ান চাচা কীভাবে ঠিক করেন কাকে কোথায় পাঠাবেন

তানহা কয়েকটা নিয়ম শিখলো —

**Round Robin** — একজন একজন করে সিরিয়ালে। সবচেয়ে সহজ। কিন্তু ধরে নেয় সব কাজ সমান ভারী।

**Least Connections** — যার হাতে এখন সবচেয়ে কম কাজ, তাকে দাও। "পড়ুয়া"-তে কেউ ছোট নোট নামায়, কেউ ৫০০ পাতার বই নামায় — তাই তানহার জন্য এটাই ঠিক।

**Weighted** — নতুন শক্তিশালী সার্ভারকে বেশি কাজ, পুরনোটাকে কম।

### আর একটা জিনিস — চাচা খবর রাখেন কে বেঁচে আছে

Load Balancer প্রতি কয়েক সেকেন্ড পর পর প্রতিটা সার্ভারকে টোকা দেয় — "বেঁচে আছো?"

```
GET /health  →  200 OK  ✅ ঠিক আছে, কাজ দাও
GET /health  →  (কোনো উত্তর নেই)  ❌ বাদ দাও
```

যে সার্ভার উত্তর দেয় না, তাকে সে চুপচাপ লাইন থেকে সরিয়ে দেয়। User টেরও পায় না যে একটা সার্ভার মরে গেছে।

**এই একটা জিনিসের কারণেই তানহা এখন রাতে ঘুমাতে পারে।**

### কিন্তু চাচা নিজে অসুস্থ হলে?

তানহার মাথায় প্রশ্নটা এলো — "দারোয়ান চাচা যদি ছুটি নেন?"

Boltu ভাই: "তাহলে গেট বন্ধ। তুই যে সমস্যা সমাধান করলি, সেটাই ফিরে আসলো — এবার দারোয়ানের জায়গায়।"

তাই বাস্তবে **দুইজন দারোয়ান** রাখা হয় — একজন কাজ করে, আরেকজন পাশে দাঁড়িয়ে থাকে। প্রথমজন পড়ে গেলে দ্বিতীয়জন সাথে সাথে গেটের চাবি নিয়ে নেয়।

📖 *আরো পড়ো: [11. Load Balancer](11.%20Load%20Balancer%20-%20Complete%20Guide.md)*

---

## অধ্যায় ৪ — চিঠিটা আসলে যায় কীভাবে (Networking Protocol)

একদিন তানহার সাইটে একটা অদ্ভুত সমস্যা হলো। কিছু user-এর কাছে PDF অর্ধেক ডাউনলোড হয়ে থেমে যায়।

তানহা bug খুঁজতে গিয়ে বুঝলো — সে আসলে জানেই না, তার সার্ভার আর user-এর ব্রাউজারের মধ্যে **আসলে কী ঘটে**।

Boltu ভাই একটা কাগজে এঁকে বোঝালো।

### ধাপ ১ — ঠিকানা খোঁজা (DNS)

User লিখলো `poruya.com`। কিন্তু ইন্টারনেট নাম বোঝে না, সংখ্যা বোঝে।

তাই প্রথমে খোঁজ শুরু হয় — এই নামের পিছনে IP নম্বরটা কী?

```
ব্রাউজারের নিজের মেমোরি → নেই
কম্পিউটারের মেমোরি      → নেই
ISP-র DNS সার্ভার        → নেই
   ↓ তাহলে উপরে জিজ্ঞেস করো
Root সার্ভার  → "'.com' এর দায়িত্ব ওই সার্ভারের"
TLD সার্ভার   → "'poruya.com' এর দায়িত্ব ওই সার্ভারের"
Authoritative → "এই নাও: 103.21.58.12"
```

এই পুরো খোঁজাখুঁজিটা হয় মিলিসেকেন্ডে, আর উত্তরটা সবাই কিছুক্ষণের জন্য মনে রাখে (cache) — যাতে পরেরবার আর খুঁজতে না হয়।

### ধাপ ২ — হাত মেলানো (TCP Handshake)

ঠিকানা পাওয়া গেলো। এবার সংযোগ।

```
User    → "শুনতে পাচ্ছো?"        (SYN)
Server  → "পাচ্ছি, তুমি পাচ্ছো?"  (SYN-ACK)
User    → "হ্যাঁ পাচ্ছি"          (ACK)
                                → এবার কথা শুরু
```

তিন ধাপের এই হাত মেলানোর নাম **Three-way Handshake**। এর পরেই কেবল সত্যিকারের data যায়।

**TCP কেন?** কারণ TCP নির্ভরযোগ্য — প্রতিটা টুকরো পৌঁছেছে কিনা নিশ্চিত করে, হারালে আবার পাঠায়, আর ক্রম ঠিক রাখে। PDF-এর একটা byte হারালেও ফাইল নষ্ট, তাই TCP-ই লাগবে।

**তাহলে UDP কোথায়?** যেখানে দ্রুততা নির্ভুলতার চেয়ে জরুরি। Video call-এ একটা frame হারালে কেউ টেরও পায় না, কিন্তু আধ সেকেন্ড দেরি হলে সবাই টের পায়। তাই ওখানে UDP।

### ধাপ ৩ — তালা লাগানো (TLS/HTTPS)

তানহার সাইটে login আছে, password যায়। খোলা অবস্থায় পাঠালে মাঝখানে যে কেউ পড়ে ফেলতে পারে।

TLS handshake-এ সার্ভার তার certificate দেখায় ("আমি সত্যিই poruya.com, এই দেখো সার্টিফিকেট"), তারপর দুই পক্ষ মিলে একটা গোপন চাবি বানায়। এরপর থেকে সব কথা ওই চাবি দিয়ে তালাবদ্ধ।

`http://` আর `https://` — এই একটা 's'-এর পুরো মানে এটাই।

### ধাপ ৪ — এবার আসল কথা (HTTP)

```
GET /notes/physics-101.pdf HTTP/1.1
Host: poruya.com
Authorization: Bearer eyJhbGci...
```

আর সার্ভার উত্তর দেয় —

```
HTTP/1.1 200 OK
Content-Type: application/pdf
Content-Length: 2048576
```

তানহা এখানেই তার bug-টা খুঁজে পেলো — সে `Content-Length` পাঠাচ্ছিল না, তাই ব্রাউজার জানতোই না ফাইলটা কত বড়, আর connection একটু নড়বড়ে হলেই মাঝপথে থেমে যেতো।

### আর HTTP-র নিজেরও বয়স বেড়েছে

তানহা দেখলো তার সাইটে ৫০টা ছোট ছোট ফাইল (CSS, JS, আইকন) লোড হয়।

**HTTP/1.1** এ একটা connection দিয়ে একবারে একটা জিনিস — লাইন ধরে দাঁড়ানো। তাই ব্রাউজার ৬টা connection খোলে, তাও জ্যাম লাগে।

**HTTP/2** এলো multiplexing নিয়ে — একটা connection দিয়েই একসাথে ৫০টা ফাইল, পাশাপাশি। সাথে header compression।

**HTTP/3** আরো এগিয়ে গেলো — TCP-ই বাদ দিয়ে QUIC (UDP-র উপর) ব্যবহার করে, তাই একটা টুকরো হারালে বাকিরা থেমে থাকে না। মোবাইল নেটওয়ার্ক বদলালেও connection টিকে থাকে।

তানহা শুধু server config-এ HTTP/2 চালু করলো। সাইট **৪০% দ্রুত** হয়ে গেলো — কোডের একটা লাইনও না বদলে।

📖 *আরো পড়ো: [2. HTTP Versions](2.%20HTTP%20Versions%20-%20Complete%20Details.md), [A1. Browser-এ URL লিখলে কী হয়](A1.What%20Happens%20When%20You%20Type%20%60www.google.com%60%20in%20a%20Browser%3F.md)*

---

## অধ্যায় ৫ — সিলেটের ছেলেটার অভিযোগ (CDN)

"পড়ুয়া" এখন সারা দেশে। এমনকি বিদেশেও কিছু বাংলাদেশি শিক্ষার্থী ব্যবহার করে।

একদিন একটা মেসেজ এলো —

> "আপু, আমি কানাডা থেকে ব্যবহার করি। একটা পাতা লোড হতে ৮ সেকেন্ড লাগে। ঢাকার বন্ধুরা বলে ওদের ১ সেকেন্ডে হয়।"

তানহা সার্ভার চেক করলো — CPU ২০%, RAM ঠিক আছে, database দ্রুত। **সার্ভারের কোনো দোষই নেই।**

Boltu ভাই: "দোষ সার্ভারের না। দোষ **দূরত্বের**।"

তানহার সার্ভার সিঙ্গাপুরে। কানাডা থেকে সিঙ্গাপুর প্রায় ১৫ হাজার কিলোমিটার। আলোর গতিতেও একটা সংকেত যেতে-আসতে সময় লাগে। আর প্রতিটা ছবি, প্রতিটা CSS ফাইলের জন্য আলাদা করে সেই যাত্রা।

৫০টা ফাইল × ২৫০ মিলিসেকেন্ড = ভয়াবহ।

### সমাধান — জিনিসটা user-এর কাছে নিয়ে যাও

Boltu ভাই বললো — "ধর, তোর একটা বইয়ের দোকান ঢাকায়। চট্টগ্রামের কেউ বই চাইলে কুরিয়ারে দুই দিন। এখন তুই যদি চট্টগ্রাম, সিলেট, রাজশাহীতে ছোট ছোট গুদাম রাখিস — যেখানে জনপ্রিয় বইগুলো আগে থেকেই আছে — তাহলে দুই ঘণ্টা।"

**এই ছোট গুদামগুলোর নাম CDN-এর Edge Server।**

```
আগে:
  কানাডার user ──── ১৫,০০০ কিমি ────► সিঙ্গাপুরের সার্ভার   (২৫০ms)

এখন:
  কানাডার user ──২০ms──► টরন্টোর Edge Server
                              │
                              └─ না পেলে একবার মূল সার্ভার থেকে এনে
                                 নিজের কাছে রেখে দেয়
```

প্রথম কানাডিয়ান user-এর জন্য একটু দেরি হলো। কিন্তু তার পরের সব কানাডিয়ান user-এর জন্য **২০ মিলিসেকেন্ড**।

### তানহা যা যা CDN-এ দিলো

✅ সব ছবি, আইকন, লোগো
✅ CSS আর JavaScript ফাইল
✅ নোটের PDF গুলো (এগুলোই সবচেয়ে ভারী)
✅ ফন্ট

❌ কিন্তু user-এর নিজের dashboard, তার প্রোফাইল — এগুলো **কখনো না**। কারণ একজনের ব্যক্তিগত পাতা cache হয়ে অন্যজনের কাছে চলে গেলে সেটা মারাত্মক দুর্ঘটনা।

### একটা কৌশল তানহা শিখলো

সে CSS ফাইলে এক বছরের cache সময় দিলো। কিন্তু তারপর ডিজাইন বদলাতে গিয়ে দেখলো — কারো ব্রাউজারে নতুন ডিজাইন আসছেই না, সবাই পুরনোটা দেখছে!

সমাধান: ফাইলের নামেই একটা কোড বসিয়ে দেওয়া।

```
style.a3f8b2.css     ← কোড বদলালে নামও বদলাবে
```

নতুন নাম মানে সম্পূর্ণ নতুন ফাইল, তাই সাথে সাথে সবাই নতুনটা পায়। আর পুরনোটা এক বছর cache-এ পড়ে থাকলেও কারো ক্ষতি নেই।

**বোনাস:** CDN চালু করার পর তানহার নিজের সার্ভারে traffic **৮৫% কমে গেলো**। সার্ভার বিলও কমলো। আর একদিন যখন কেউ তার সাইটে DDoS আক্রমণ করার চেষ্টা করলো, CDN সেটা edge-এই শুষে নিলো — তানহার সার্ভার পর্যন্ত পৌঁছালোই না।

📖 *আরো পড়ো: [13. CDN](13.%20CDN%20-%20Content%20Delivery%20Network.md)*

---

## অধ্যায় ৬ — ডাটাবেসের জ্যাম (Caching)

সার্ভার তিনটা। Load balancer আছে। CDN আছে।

তবু একদিন সাইট আবার ধীর হয়ে গেলো।

তানহা এবার আর অন্ধভাবে সার্ভার বাড়ালো না — সে **মাপলো**। আর দেখলো, প্রতিটা request-এর ৯০% সময়ই যাচ্ছে **একটা জায়গায়** — ডাটাবেসে।

কারণটা বোকার মতো সহজ ছিল।

হোমপেজে "এই সপ্তাহের জনপ্রিয় নোট" নামে একটা তালিকা আছে। সেটা বের করতে একটা ভারী query চালাতে হয় — কয়েক লাখ row ঘেঁটে, গুনে, সাজিয়ে। সময় লাগে **৮০০ মিলিসেকেন্ড**।

আর এই query চলছে **প্রতিটা visitor-এর জন্য, প্রতিবার।**

দিনে ১০ লাখ visitor মানে দিনে ১০ লাখ বার একই হিসাব — যার উত্তর **সারাদিন একই থাকে**।

### Boltu ভাইয়ের রান্নাঘরের উদাহরণ

"তুই রান্না করছিস। প্রতিবার এক চিমটি লবণ লাগলে যদি বাজারে গিয়ে লবণ কিনে আনিস, তাহলে রান্না শেষ হবে কবে? লবণটা চুলার পাশে একটা কৌটায় রেখে দে।

**ডাটাবেস হলো বাজার। Redis হলো চুলার পাশের কৌটা।**"

```
আগে:
  App ──────────► Database    (৮০০ms, প্রতিবার)

এখন:
  App ──► Redis ──► পেলে ফেরত  (২ms) ✅
           │
           └─ না পেলে ──► Database (৮০০ms), তারপর কৌটায় রেখে দাও
```

তানহা লিখলো —

```javascript
async function getPopularNotes() {
  const cached = await redis.get('popular:weekly');
  if (cached) return JSON.parse(cached);         // ২ms

  const notes = await db.query(HEAVY_QUERY);     // ৮০০ms
  await redis.setex('popular:weekly', 600, JSON.stringify(notes));
  return notes;
}
```

মাত্র চার লাইন। ডাটাবেসের load **৯৫% কমে গেলো**। পাতা লোড হতে লাগলো ৮০০ms-এর বদলে ৪০ms।

### কিন্তু তারপর তিনটা বিপদ এলো

**বিপদ ১ — পুরনো তথ্য।** কেউ একটা নোট মুছে দিলো, কিন্তু cache-এ সেটা এখনো দেখাচ্ছে।

*সমাধান:* যখনই কেউ নোট বদলায়, সাথে সাথে ওই cache key-টা **মুছে দাও**। মনে রাখো — নতুন value বসিয়ে দেওয়ার চেয়ে মুছে দেওয়া নিরাপদ, কারণ পরের request নিজেই সঠিকটা এনে বসাবে।

**বিপদ ২ — একসাথে সবার cache শেষ।** ঠিক রাত ১২টায় cache-এর মেয়াদ শেষ হলো, আর ঠিক সেই মুহূর্তে ৫ হাজার request এলো। সবাই cache miss পেয়ে একসাথে ডাটাবেসে ঝাঁপিয়ে পড়লো। **ডাটাবেস মরে গেলো।**

*সমাধান:* মেয়াদের সময়ে একটু এলোমেলো সময় যোগ করো — কেউ ৬০০ সেকেন্ড, কেউ ৬৪৩, কেউ ৫৮৭। তাহলে সবাই একসাথে ভেঙে পড়ে না।

**বিপদ ৩ — যা নেই তা খোঁজা।** কেউ বারবার এমন নোট চাইছে যেটা আছেই না। Cache-এ নেই, ডাটাবেসেও নেই, তাই কিছুই cache হয় না — প্রতিবারই ডাটাবেসে যায়।

*সমাধান:* "এটা নেই" — এই উত্তরটাও অল্প সময়ের জন্য cache করে রাখো।

### আর একটা বড় শিক্ষা

তানহা প্রথমে উৎসাহে **সবকিছু** cache করা শুরু করেছিল। ফল হলো — Redis-এর memory ভরে গেলো এমন সব জিনিসে যা কেউ দ্বিতীয়বার চায়নি, আর যেগুলো সত্যিই দরকার সেগুলো জায়গা না পেয়ে বেরিয়ে গেলো।

> **নিয়ম:** যা **বারবার** পড়া হয় আর **কম** বদলায় — শুধু সেটাই cache করো। বাকিটা memory-র অপচয়।

📖 *আরো পড়ো: [12. Caching, Redis ও Eviction](12.%20Caching%20-%20Strategies,%20Redis%20and%20Eviction.md)*

---

## অধ্যায় ৭ — একটা আলমারিতে আর ধরছে না (Partitioning ও Sharding)

দুই বছর পর। "পড়ুয়া"-তে এখন ৫০ লাখ user, ২ কোটি নোট, আর প্রতিদিন কোটি খানেক পাতা দেখা হয়।

Cache আছে, replica আছে, তবু **লেখার (write) কাজটা** ধীর হয়ে গেছে। কারণ লেখা তো শুধু একটা জায়গাতেই হতে পারে।

আর ডাটাবেসের ডিস্ক ৯২% ভরে গেছে।

### প্রথম ধাপ — একই আলমারিতে তাক ভাগ করা (Partitioning)

তানহার `activity_log` টেবিলে ৩০ কোটি row। যেকোনো query ভয়ংকর ধীর।

কিন্তু সে খেয়াল করলো — ৯৯% query আসলে **শুধু গত এক মাসের** data চায়।

তাই সে টেবিলটা মাস অনুযায়ী ভাগ করে দিলো:

```
activity_log
   ├── activity_2024_11
   ├── activity_2024_12
   └── activity_2025_01   ← বেশিরভাগ query শুধু এখানেই আসে
```

এখন `WHERE created_at >= '2025-01-01'` লিখলে ডাটাবেস বাকি ২৯ কোটি row ছুঁয়েও দেখে না।

**এটা এখনো একটাই ডাটাবেস সার্ভার — শুধু ভিতরে গুছিয়ে রাখা।** পুরনো মাসের partition গুলো মুছে ফেলাও এখন এক লাইনের কাজ।

### দ্বিতীয় ধাপ — আলাদা আলাদা আলমারি (Sharding)

কিন্তু data এত বেড়ে গেলো যে এক মেশিনে আর ধরছেই না।

এবার সত্যিকারের ভাগ — **আলাদা আলাদা সার্ভারে আলাদা আলাদা user-এর data**।

```
              ┌──────────────┐
   App ──────►│ কোন shard?   │
              └──────┬───────┘
       ┌─────────────┼─────────────┐
       ▼             ▼             ▼
   Shard-1       Shard-2       Shard-3
  (user 1-2M)  (user 2-4M)   (user 4-6M)
```

### সবচেয়ে গুরুত্বপূর্ণ সিদ্ধান্ত — কীসের ভিত্তিতে ভাগ করবে

তানহা প্রথমে ভেবেছিল **বিভাগ (department)** দিয়ে ভাগ করবে — CSE, EEE, BBA...

Boltu ভাই থামালো: "কয়টা CSE-র student আর কয়টা Philosophy-র? CSE-র shard-টা ফেটে যাবে, Philosophy-র shard-টা খালি পড়ে থাকবে।"

দ্বিতীয় চেষ্টা — **সাল** দিয়ে ভাগ। কিন্তু তাতে নতুন সব ভর্তি হওয়া student একটা shard-এই পড়ে, আর সেখানেই সব লেখালেখি হয়।

শেষ পর্যন্ত `user_id` দিয়ে ভাগ করা হলো — কারণ এতে —
- অসংখ্য আলাদা মান আছে
- সমানভাবে ছড়ায়
- আর "পড়ুয়া"-র বেশিরভাগ query একজন user-কে নিয়েই, তাই একটা shard-এই কাজ শেষ

> **শিক্ষা:** shard key ভুল বাছলে সেটা ঠিক করতে মাসের পর মাস লাগে। এই সিদ্ধান্তটা সবচেয়ে ভেবে নিতে হয়।

### কিন্তু এখানেই একটা মজার সমস্যা এলো

তানহা যখন আরেকটা shard যোগ করলো (৩ থেকে ৪), তখন `hash(user_id) % 3` বদলে হলো `% 4`।

আর তাতে **প্রায় সব user-এর ঠিকানা বদলে গেলো।** টেরাবাইট data সরাতে হবে!

সমাধান: **Consistent Hashing**। একটা কাল্পনিক বৃত্তে সার্ভারগুলো বসানো হয়, আর প্রতিটা key ঘড়ির কাঁটার দিকে প্রথম যে সার্ভার পায় সেখানে যায়। এখানে সার্ভারের **মোট সংখ্যা কোথাও ব্যবহারই হয় না** — তাই একটা সার্ভার যোগ করলে শুধু তার প্রতিবেশী অংশটুকুই সরে, বাকি সব অপরিবর্তিত।

### Sharding-এর যে দামটা তানহাকে দিতে হলো

- **JOIN আর কাজ করে না** — user Shard-1 এ, তার নোট Shard-3 এ। এখন দুবার query করে নিজে মেলাতে হয়।
- **"মোট কতজন user?"** — এখন সব shard-এ জিজ্ঞেস করে যোগ করতে হয়।
- **ID সংঘর্ষ** — প্রতিটা shard আলাদাভাবে `id = 1` বানাতে চায়! তাই auto-increment বাদ দিয়ে Snowflake ID ব্যবহার করতে হলো।

> **Boltu ভাইয়ের সতর্কবাণী:** "Sharding হলো শেষ অস্ত্র। Index, cache, replica — সব চেষ্টা করার পর, একদম নিরুপায় হয়ে তবেই shard কর।"

📖 *আরো পড়ো: [15. Database Scaling](15.%20Database%20Scaling%20-%20Replication,%20Sharding%20and%20Partitioning.md), [19. Consistent Hashing](19.%20Consistent%20Hashing.md), [14. Database Basics](14.%20Database%20-%20SQL%20vs%20NoSQL,%20Indexing%20and%20Transactions.md)*

---

## অধ্যায় ৮ — যেদিন হার্ডডিস্ক মরে গেলো (Redundancy ও Replication)

তানহার জীবনের সবচেয়ে খারাপ রাত।

ভোর ৪টায় ফোন বাজলো। মনিটরিং সিস্টেমের অ্যালার্ট।

**ডাটাবেস সার্ভারের ডিস্ক নষ্ট হয়ে গেছে।**

সে দৌড়ে ল্যাপটপ খুললো। ডাটাবেস সাড়া দিচ্ছে না। সাইট পুরোপুরি বন্ধ। ২ কোটি নোট, ৫০ লাখ user-এর অ্যাকাউন্ট — সব ওই একটা ডিস্কে।

শেষ backup ছিল **১৮ ঘণ্টা আগের**।

সেই রাতে তানহা ৬ ঘণ্টার data হারালো। ৯ ঘণ্টা সাইট বন্ধ ছিল।

পরদিন Boltu ভাই একটাই কথা বললো —

> "Hardware fail করবেই। এটা 'যদি' না, 'কখন'-এর প্রশ্ন। প্রশ্ন হলো — যখন করবে, তুই প্রস্তুত থাকবি কিনা।"

### সমাধান — একটার বদলে অনেকগুলো কপি

```
                ┌────────────────┐
   সব লেখা ────►│    Primary     │
                └───────┬────────┘
                        │ (সাথে সাথে copy হতে থাকে)
          ┌─────────────┼─────────────┐
          ▼             ▼             ▼
      Replica-1     Replica-2     Replica-3
          ▲             ▲             ▲
          └─────────────┴─────────────┘
                   সব পড়া
```

এখন —
- **Primary মরলে** একটা replica সাথে সাথে তার জায়গা নেয় (failover) — ৯ ঘণ্টার বদলে ৩০ সেকেন্ড
- **পড়ার কাজ** replica-রা ভাগ করে নেয় — "পড়ুয়া"-তে ৯৫% traffic-ই তো পড়া
- **Backup** এখন replica থেকে নেওয়া যায়, primary-কে বিরক্ত না করে

শুধু ডাটাবেস না — তানহা এবার **সবকিছুই জোড়ায় জোড়ায়** রাখলো। দুটো load balancer, একাধিক app server, একাধিক availability zone।

**নিয়মটা হলো — এমন কোনো জায়গা রাখা যাবে না, যেটা মরলে সব মরে।**

### কিন্তু replication-এর নিজস্ব একটা ভূত আছে

চালু করার এক সপ্তাহ পর অভিযোগ আসতে শুরু করলো —

> "আপু, নোট আপলোড করলাম, সফল দেখালো। কিন্তু পাতা refresh করলে নোটটা নাই!"

তানহা ঘাবড়ে গেলো। data কি হারাচ্ছে?

না। **Replication Lag।**

```
১. User নোট আপলোড করলো   → Primary-তে লেখা হলো ✅
২. পাতা refresh হলো       → Replica থেকে পড়া হলো
৩. কিন্তু replica-তে তখনো নোটটা পৌঁছায়নি (আধ সেকেন্ড পিছিয়ে)
৪. User দেখলো — কিছু নেই 😱
```

Data হারায়নি, শুধু **এখনো পৌঁছায়নি**।

**সমাধান:** যে user নিজে কিছু লিখেছে, তাকে পরের কয়েক সেকেন্ড **primary থেকেই পড়াও**। অন্যরা replica থেকে পড়ুক — তাদের আধ সেকেন্ড দেরিতে দেখলে কিছু যায় আসে না।

এটাকে বলে **Read-your-own-writes consistency**।

📖 *আরো পড়ো: [15. Database Scaling](15.%20Database%20Scaling%20-%20Replication,%20Sharding%20and%20Partitioning.md)*

---

## অধ্যায় ৯ — নতুন বিপত্তি (CAP ও PACELC)

তানহা ভেবেছিল সে সব সমস্যার সমাধান করে ফেলেছে।

তারপর এলো সেই দিনটা, যেদিন সে বুঝলো — **কিছু সমস্যার আসলে কোনো সমাধান নেই। শুধু পছন্দ আছে।**

### যেদিন সিঙ্গাপুর আর ফ্রাঙ্কফুর্টের মধ্যে লাইন কেটে গেলো

"পড়ুয়া"-র এখন দুটো অঞ্চলে সার্ভার — এশিয়া আর ইউরোপ। দুই জায়গার data একে অপরের সাথে sync থাকে।

এক দুপুরে সমুদ্রের নিচের একটা ফাইবার কেবলে সমস্যা হলো। দুই অঞ্চল একে অপরের সাথে কথা বলতে পারছে না।

**কিন্তু দুই অঞ্চলেই user আছে, আর তারা কাজ করতে চাইছে।**

ইউরোপের সার্ভারের সামনে এখন দুটো রাস্তা, আর **তৃতীয় কোনো রাস্তা নেই**।

**রাস্তা ১ — সঠিকতা বাঁচাও:**
"এশিয়ার সাথে যোগাযোগ নেই। ওখানে কেউ হয়তো এই নোটটা বদলেছে, আমি জানি না। ভুল তথ্য দেখানোর চেয়ে কিছু না দেখানো ভালো।"
→ **Error দেখাও। সেবা বন্ধ।** (Consistency বাঁচলো, Availability গেলো)

**রাস্তা ২ — সেবা বাঁচাও:**
"আমার কাছে যা আছে তাই দেখাই। লাইন ঠিক হলে মিলিয়ে নেবো।"
→ **কাজ চালু রাখো, হয়তো data একটু পুরনো।** (Availability বাঁচলো, Consistency গেলো)

তানহা রেগে গিয়ে বললো — "দুটোই চাই! সঠিকও হবে, চালুও থাকবে!"

Boltu ভাই মাথা নাড়লো।

> "পাবি না। এটাই **CAP Theorem**। Network ভেঙে গেলে তুই হয় সঠিকতা পাবি, নয় সেবা — দুটো একসাথে না। আর network ভাঙবেই, এটা তোর হাতে নেই।"

### তানহার সিদ্ধান্ত — feature ধরে ধরে

এখানেই তানহা সবচেয়ে বড় শিক্ষাটা পেলো। সিদ্ধান্তটা পুরো সিস্টেমের জন্য একটা না — **প্রতিটা feature-এর জন্য আলাদা**।

| Feature | পছন্দ | কেন |
|---|---|---|
| নোট পড়া, তালিকা দেখা | **Availability** | ২ সেকেন্ড পুরনো তালিকা দেখালে কারো কিছু যায় আসে না |
| Like / view count | **Availability** | সংখ্যাটা একটু পিছিয়ে থাকলে সমস্যা নেই |
| Premium সাবস্ক্রিপশনের পেমেন্ট | **Consistency** | টাকার হিসাবে এক পয়সা ভুলও চলবে না |
| সীমিত আসনের অনলাইন কোর্সে ভর্তি | **Consistency** | ৫০টা আসনে ৫১ জন ভর্তি করা যাবে না |

### আর তারপর Boltu ভাই আরো একটা খবর দিলো

"তোর ধারণা network ভাঙা একটা বিরল ঘটনা, তাই না? বছরে দু-একবার?"

"হ্যাঁ।"

"তাহলে **বাকি ৩৬৪ দিন**? সেদিনও কি কোনো tradeoff নেই?"

তানহা থমকে গেলো।

"আছে। **PACELC**। Partition হলে A না C — সেটা তো CAP বললোই। কিন্তু **Else**, মানে স্বাভাবিক দিনেও একটা পছন্দ করতে হয় — **Latency না Consistency**।

তুই যদি প্রতিটা লেখার সময় ইউরোপ-এশিয়া দুই জায়গায় নিশ্চিত করে তবেই 'সফল' বলিস, তাহলে data সবসময় নিখুঁত — কিন্তু প্রতিটা আপলোডে অতিরিক্ত ৩০০ মিলিসেকেন্ড।

আর যদি নিজের অঞ্চলে লিখেই 'সফল' বলে দিস, তাহলে দ্রুত — কিন্তু কয়েক সেকেন্ডের জন্য দুই অঞ্চলে দুই রকম অবস্থা।

**কোনো ভুল উত্তর নেই। শুধু সচেতন পছন্দ আছে।**"

তানহা নোট আপলোডের জন্য **গতি** বেছে নিলো, আর পেমেন্টের জন্য **নিখুঁততা**।

📖 *আরো পড়ো: [16. CAP Theorem ও Consistency Models](16.%20CAP%20Theorem%20and%20Consistency%20Models.md)*

---

## অধ্যায় ১০ — তানহার শেষ শিক্ষা

তিন বছর পর। "পড়ুয়া" এখন দেশের সবচেয়ে বড় শিক্ষা প্ল্যাটফর্ম।

একদিন একটা ছোট কোম্পানির নতুন developer তানহাকে মেসেজ করলো —

> "আপু, আমি একটা অ্যাপ বানাচ্ছি। প্রথম দিন থেকেই microservices, Kubernetes, Kafka আর multi-region setup রাখতে চাই — যাতে পরে scale-এ সমস্যা না হয়। কীভাবে শুরু করবো?"

তানহা হাসলো। তিন বছর আগে সে-ও ঠিক এটাই ভাবতো।

সে লিখলো —

> "একটা প্রশ্ন করি — এখন তোমার কতজন user?
>
> যদি উত্তর হয় শূন্য, তাহলে আমার পরামর্শ: **একটা সার্ভারে শুরু করো।** একটা ডাটাবেস। কোনো Kafka না, কোনো microservice না।
>
> কারণ আমি যা যা যোগ করেছি — load balancer, cache, CDN, replica, shard — তার একটাও আমি **আগে থেকে** যোগ করিনি। প্রতিটাই যোগ করেছি **যখন সত্যিই কিছু ভেঙে পড়েছে**, তখন।
>
> আর সেটাই ছিল সঠিক পথ। কারণ কোন জিনিসটা ভাঙবে সেটা আগে থেকে অনুমান করা যায় না। আমি ভেবেছিলাম ডাটাবেসের storage আগে শেষ হবে — আসলে আগে শেষ হয়েছিল CPU।
>
> **System design মানে ভবিষ্যদ্বাণী করা না। System design মানে — যখন ভাঙে, তখন কেন ভাঙলো সেটা বুঝতে পারা, আর সবচেয়ে সহজ যে সমাধানটা কাজ করে সেটা বেছে নেওয়া।**
>
> তোমার প্রথম কাজ user খুঁজে বের করা, সার্ভার না।"

---

## পুরো গল্পের মানচিত্র

তানহার "পড়ুয়া" শেষ পর্যন্ত যা দাঁড়ালো —

```
                            🌍 User (সারা পৃথিবীতে)
                                    │
                    ┌───────────────▼───────────────┐
                    │            CDN                │  ← ছবি, PDF, CSS, JS
                    │   (edge server, ২০ms)          │     অধ্যায় ৫
                    └───────────────┬───────────────┘
                                    │ (যা cache-এ নেই)
                    ┌───────────────▼───────────────┐
                    │      Load Balancer × 2        │  ← দারোয়ান চাচা
                    │   (health check, failover)     │     অধ্যায় ৩
                    └───────────────┬───────────────┘
              ┌─────────────────────┼─────────────────────┐
              ▼                     ▼                     ▼
        ┌──────────┐          ┌──────────┐          ┌──────────┐
        │App Server│          │App Server│          │App Server│  ← stateless
        │   (JWT)  │          │   (JWT)  │          │   (JWT)  │     অধ্যায় ২
        └────┬─────┘          └────┬─────┘          └────┬─────┘
             └─────────────────────┼─────────────────────┘
                                   ▼
                        ┌────────────────────┐
                        │   Redis (Cache)    │  ← চুলার পাশের কৌটা
                        │  ৯৫% এখানেই শেষ    │     অধ্যায় ৬
                        └──────────┬─────────┘
                                   │ (miss হলে)
                        ┌──────────▼─────────┐
                        │    Shard Router    │  ← অধ্যায় ৭
                        └──────────┬─────────┘
              ┌────────────────────┼────────────────────┐
              ▼                    ▼                    ▼
        ┌──────────┐         ┌──────────┐         ┌──────────┐
        │ Shard-1  │         │ Shard-2  │         │ Shard-3  │
        │ Primary  │         │ Primary  │         │ Primary  │  ← লেখা
        │    ↓     │         │    ↓     │         │    ↓     │
        │ Replica×2│         │ Replica×2│         │ Replica×2│  ← পড়া
        └──────────┘         └──────────┘         └──────────┘
                                                     অধ্যায় ৮

        সবকিছুর নিচে: TCP, TLS, HTTP/2, DNS  ← অধ্যায় ৪
        আর সবকিছুর উপরে: CAP/PACELC-এর সচেতন পছন্দ  ← অধ্যায় ৯
```

### দশটা লাইনে পুরো গল্প

১. **শুরু করো সবচেয়ে সহজভাবে** — একটা সার্ভার, একটা ডাটাবেস। এটা লজ্জার কিছু না।
২. **অনুমান করো না, মাপো** — bottleneck কখনোই সেখানে থাকে না যেখানে তুমি ভাবো।
৩. **প্রথমে মেশিন বড় করো, তারপর মেশিন বাড়াও** — কিন্তু মেশিন বাড়ানোর শর্ত হলো **stateless** হওয়া।
৪. **Load Balancer** ছাড়া একাধিক সার্ভারের কোনো মানে নেই — আর সে নিজেও যেন একা না হয়।
৫. **দূরত্বই latency** — CDN দিয়ে জিনিসটা user-এর কাছে নিয়ে যাও।
৬. **Cache হলো সবচেয়ে সস্তা জাদু** — কিন্তু শুধু তাই cache করো যা বারবার লাগে আর কম বদলায়।
৭. **Sharding শেষ অস্ত্র** — index, cache, replica সব চেষ্টার পর।
৮. **Hardware মরবেই** — তাই কোনো কিছুরই একটা মাত্র কপি রেখো না।
৯. **Replication lag বাস্তব** — নিজের লেখা জিনিস নিজেকে দেখাতে হলে primary থেকে পড়াও।
১০. **সব একসাথে পাওয়া যায় না** — CAP আর PACELC-এর পছন্দটা feature ধরে ধরে সচেতনভাবে করো।

---

> **তানহার ডায়েরির শেষ লাইন:**
>
> *"System design শেখার সবচেয়ে ভালো উপায় হলো একটা জিনিস বানানো, আর সেটাকে ভাঙতে দেখা।
> আমি প্রতিটা concept শিখেছি রাত তিনটায়, যখন কিছু একটা ভেঙে পড়েছিল।
> বই পড়ে না — ভেঙে পড়া দেখে।"*

📚 *প্রতিটা অধ্যায়ের গভীর ব্যাখ্যার জন্য দেখো [TOC.md](TOC.md) — আর interview-র প্রস্তুতির জন্য [22](22.%20System%20Design%20Interview%20Framework%20and%20Case%20Studies.md) ও [23](23.%20Interview%20Question%20Bank%20-%20120%2B%20Q%26A.md) নম্বর নোট।*
