# Interview Question Bank — ১২০+ প্রশ্ন ও উত্তর

> প্রতিটা উত্তর interview-তে **মুখে বলার মতো করে** লেখা — মুখস্থ করার জন্য না, বোঝার জন্য।
> বিস্তারিত ব্যাখ্যার জন্য সংশ্লিষ্ট note-এ যাও।

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [A. Networking ও Web Basics](#a-networking-ও-web-basics)
* [B. API ও REST](#b-api-ও-rest)
* [C. Authentication ও Security](#c-authentication-ও-security)
* [D. Scalability ও Load Balancing](#d-scalability-ও-load-balancing)
* [E. Caching ও CDN](#e-caching-ও-cdn)
* [F. Database](#f-database)
* [G. Distributed Systems](#g-distributed-systems)
* [H. Message Queue ও Async](#h-message-queue-ও-async)
* [I. Microservices ও Architecture](#i-microservices-ও-architecture)
* [J. Reliability ও Observability](#j-reliability-ও-observability)
* [K. Design Scenario — ছোট প্রশ্ন](#k-design-scenario--ছোট-প্রশ্ন)
* [L. Tricky ও Behavioral](#l-tricky-ও-behavioral)

---

<!-- TOC END -->

## A. Networking ও Web Basics

**১. `www.google.com` টাইপ করলে কী কী হয়?**
প্রথমে browser cache, OS cache, তারপর DNS resolver-এর মাধ্যমে root → TLD → authoritative name server ঘুরে IP address পাওয়া যায়। তারপর TCP three-way handshake হয়, HTTPS হলে TLS handshake, এরপর HTTP request যায়। Server-এর সামনে সাধারণত CDN বা load balancer থাকে, সেখান থেকে application server-এ যায়। Response এলে browser HTML parse করে, CSS/JS/image-এর জন্য আরো request পাঠায়, তারপর render করে। (বিস্তারিত: `A1`)

**২. TCP আর UDP-র পার্থক্য?**
TCP connection-oriented — handshake করে সংযোগ স্থাপন করে, প্রতিটা packet-এর প্রাপ্তি নিশ্চিত করে, হারানো packet আবার পাঠায় এবং ক্রম বজায় রাখে; তাই নির্ভরযোগ্য কিন্তু ধীর। UDP connectionless — শুধু পাঠিয়ে দেয়, কোনো নিশ্চয়তা নেই; তাই দ্রুত কিন্তু packet হারাতে পারে। Web, API, file transfer-এ TCP; video call, live streaming, gaming, DNS-এ UDP।

**৩. HTTP/1.1, HTTP/2 আর HTTP/3-এর পার্থক্য?**
HTTP/1.1-এ একটা connection-এ একবারে একটা request (head-of-line blocking), তাই browser একাধিক connection খোলে। HTTP/2 multiplexing এনেছে — একই connection-এ সমান্তরালে অনেক request, সাথে header compression আর server push। কিন্তু HTTP/2-তেও TCP স্তরে head-of-line blocking থেকে যায়। HTTP/3 সেটা সমাধান করেছে TCP-র বদলে QUIC (UDP-ভিত্তিক) ব্যবহার করে — একটা stream-এ packet হারালে বাকি stream থামে না, আর connection migration-ও সম্ভব। (বিস্তারিত: `2`)

**৪. DNS কীভাবে কাজ করে?**
DNS হলো domain নামকে IP address-এ রূপান্তরের ব্যবস্থা। Browser প্রথমে নিজের cache দেখে, তারপর OS cache, তারপর configured DNS resolver-কে জিজ্ঞেস করে। Resolver-এর কাছে না থাকলে সে root server-এ যায়, root তাকে TLD server (.com) দেখিয়ে দেয়, TLD তাকে authoritative server দেখায়, আর সেখান থেকে আসল IP পাওয়া যায়। প্রতিটা ধাপের ফল TTL অনুযায়ী cache হয়।

**৫. Forward proxy আর reverse proxy-র পার্থক্য?**
Forward proxy client-এর পক্ষে কাজ করে — client-এর পরিচয় লুকায়, corporate network-এ content filter করে। Reverse proxy server-এর পক্ষে কাজ করে — server-এর পরিচয় লুকায়, load balancing, SSL termination, caching করে। Nginx সাধারণত reverse proxy হিসেবে ব্যবহৃত হয়। (বিস্তারিত: `7`)

**৬. Latency আর Throughput-এর পার্থক্য?**
Latency হলো একটা request শেষ হতে কত সময় লাগে (যেমন ২০০ms)। Throughput হলো প্রতি একক সময়ে কতগুলো request সামলানো যায় (যেমন ১০,০০০ req/sec)। এক লেন-এর দ্রুতগতির রাস্তা মানে কম latency কিন্তু কম throughput; বহু লেনের ধীর রাস্তা মানে বেশি latency কিন্তু বেশি throughput। Video call-এ latency জরুরি, batch processing-এ throughput।

**৭. Latency-তে average না percentile দেখা উচিত?**
Percentile। গড় বিভ্রান্তিকর — ৯৫% request ১০ms আর ৫% request ৫ সেকেন্ড হলে গড় ভালো দেখাবে, অথচ প্রতি ২০ জনের একজন ভয়াবহ অভিজ্ঞতা পাচ্ছে। তাই p50 (মধ্যক), p95, p99 দেখা হয়। বড় company-তে p99 বা p99.9-ই আসল SLA, কারণ যে user সবচেয়ে সক্রিয় সে-ই সবচেয়ে বেশি request করে, তাই খারাপ অভিজ্ঞতা পাওয়ার সম্ভাবনাও তার বেশি।

---

## B. API ও REST

**৮. REST-এর মূল নীতিগুলো কী?**
Client-server আলাদা থাকা, statelessness (প্রতিটা request নিজেই সম্পূর্ণ), cacheability, uniform interface (resource-ভিত্তিক URL আর standard HTTP method), layered system, আর ঐচ্ছিকভাবে code-on-demand। মূল ধারণা হলো সবকিছুকে resource হিসেবে দেখা এবং HTTP method দিয়ে তার উপর কাজ করা। (বিস্তারিত: `1`)

**৯. PUT আর PATCH-এর পার্থক্য?**
PUT পুরো resource প্রতিস্থাপন করে — যে field পাঠাওনি সেগুলো মুছে যাবে বা default হবে। PATCH আংশিক update করে — শুধু যা পাঠিয়েছো তাই বদলায়। PUT idempotent (একই request দশবার পাঠালে ফল একই), PATCH সাধারণত idempotent কিন্তু সবসময় না (যেমন "৫ যোগ করো" ধরনের patch)।

**১০. কোন HTTP method গুলো idempotent?**
GET, PUT, DELETE, HEAD, OPTIONS idempotent — একবার বা দশবার করলে server-এর অবস্থা একই থাকে। POST idempotent না — দশবার POST মানে দশটা নতুন resource। এজন্যই payment-এর মতো POST request-এ Idempotency-Key header ব্যবহার করা হয়।

**১১. 401 আর 403-এর পার্থক্য?**
401 Unauthorized মানে তুমি কে তা-ই জানা যায়নি — credential নেই বা ভুল, তাই login করো। 403 Forbidden মানে তুমি কে তা জানা গেছে, কিন্তু এই কাজ করার অনুমতি তোমার নেই। সহজভাবে — 401 হলো "তুমি কে?", 403 হলো "তুমি হতে পারো, কিন্তু ঢুকতে দেবো না"।

**১২. REST আর GraphQL-এর মধ্যে কোনটা বেছে নেবে?**
REST সহজ, cacheable, আর HTTP-র সব সুবিধা পায় — বেশিরভাগ public API-র জন্য এটাই ঠিক। GraphQL তখন ভালো যখন client-ভেদে চাহিদা ভিন্ন এবং over-fetching বা under-fetching বড় সমস্যা — যেমন mobile app-এ কম data দরকার কিন্তু web-এ বেশি। GraphQL-এর দাম হলো caching কঠিন, একটা জটিল query দিয়ে server-কে ধসিয়ে দেওয়া সম্ভব (তাই query depth limit লাগে), আর N+1 problem খুব সহজে হয়।

**১৩. gRPC কখন ব্যবহার করবে?**
Internal service-to-service যোগাযোগে, যেখানে performance গুরুত্বপূর্ণ। gRPC binary Protocol Buffers ব্যবহার করে HTTP/2-র উপর, তাই JSON/REST-এর চেয়ে অনেক দ্রুত ও হালকা, strongly typed contract দেয় এবং streaming support করে। তবে browser থেকে সরাসরি ব্যবহার কঠিন (grpc-web লাগে) আর debug করতে বিশেষ tool দরকার, তাই public-facing API-তে সাধারণত REST-ই রাখা হয়।

**১৪. API versioning কীভাবে করবে?**
সবচেয়ে প্রচলিত হলো URL-এ version রাখা (`/api/v1/users`) — স্পষ্ট, cache-বান্ধব, debug করা সহজ। বিকল্প হলো header-এ version পাঠানো (`Accept: application/vnd.api.v2+json`) — URL পরিষ্কার থাকে কিন্তু কম দৃশ্যমান। মূল নীতি হলো breaking change কখনো পুরনো version-এ করবে না, নতুন version বের করো এবং পুরনোটার জন্য একটা deprecation timeline ঘোষণা করো।

**১৫. Pagination কীভাবে করবে — offset না cursor?**
Offset-based (`?page=3&limit=20`) সহজ এবং যেকোনো page-এ লাফ দেওয়া যায়, কিন্তু দুটো সমস্যা — বড় offset-এ database ধীর হয়ে যায় (আগের সব row গুনতে হয়), আর নতুন data যোগ হলে item এক page থেকে আরেক page-এ সরে গিয়ে বাদ পড়ে বা দুবার আসে। Cursor-based (`?after=xyz&limit=20`) এই দুটো সমস্যাই সমাধান করে এবং বড় dataset-এ অনেক দ্রুত, তাই infinite scroll ধরনের feed-এ এটাই সঠিক পছন্দ।

---

## C. Authentication ও Security

**১৬. Session-based আর Token-based auth-এর পার্থক্য?**
Session-based-এ server session তৈরি করে নিজের কাছে (memory বা Redis-এ) রাখে এবং client-কে একটা session ID cookie দেয় — server stateful হয়ে যায়। Token-based-এ (JWT) সব তথ্য token-এর ভিতরেই থাকে এবং signature দিয়ে যাচাই হয় — server কিছু মনে রাখে না, তাই stateless এবং horizontally scale করা সহজ। বিনিময়ে JWT বাতিল করা কঠিন, কারণ server-এর কাছে সেটার কোনো record নেই। (বিস্তারিত: `4`)

**১৭. JWT-এর গঠন কী?**
তিনটা অংশ, ডট দিয়ে আলাদা — Header (algorithm আর type), Payload (claim গুলো যেমন user id, expiry), আর Signature। Signature হলো header ও payload-কে secret key দিয়ে sign করার ফল, যা নিশ্চিত করে token-টা বদলানো হয়নি। গুরুত্বপূর্ণ কথা — payload শুধু base64 encoded, encrypted না; তাই যে কেউ পড়তে পারে, সেখানে কখনো sensitive তথ্য রাখা যাবে না।

**১৮. JWT logout কীভাবে করবে?**
এটাই JWT-র মূল দুর্বলতা — token নিজে থেকে expire না হওয়া পর্যন্ত বৈধ থাকে। বাস্তব সমাধান হলো access token-এর আয়ু খুব কম রাখা (৫-১৫ মিনিট) আর একটা দীর্ঘমেয়াদি refresh token রাখা যা server-এ সংরক্ষিত ও বাতিলযোগ্য। Logout করলে refresh token মুছে দেওয়া হয়, ফলে access token-টা সর্বোচ্চ কয়েক মিনিট বেঁচে থাকে। জরুরি হলে একটা blocklist রাখা যায়, তবে তাতে statelessness-এর সুবিধা কিছুটা হারায়।

**১৯. OAuth 2.0 কী?**
OAuth 2.0 হলো authorization-এর একটা কাঠামো যেখানে user তার password না দিয়ে একটা third-party application-কে সীমিত অনুমতি দিতে পারে। যেমন একটা app-কে তোমার Google Drive-এর ফাইল পড়ার অনুমতি দেওয়া, কিন্তু Google password না দেওয়া। মূল ধাপ — user provider-এর কাছে গিয়ে সম্মতি দেয়, app একটা authorization code পায়, তারপর সেই code আর নিজের secret দিয়ে access token নেয়। মনে রাখতে হবে OAuth authorization-এর জন্য, authentication-এর জন্য না — সেটার জন্য OpenID Connect। (বিস্তারিত: `5`)

**২০. Authentication আর Authorization-এর পার্থক্য?**
Authentication হলো "তুমি কে" যাচাই করা — login, password, token verification। Authorization হলো "তুমি কী করতে পারবে" ঠিক করা — role, permission, access control। Authentication আগে হয়, তারপর authorization।

**২১. Password কীভাবে সংরক্ষণ করবে?**
কখনোই plain text-এ না, এমনকি সাধারণ hash (MD5, SHA-256) দিয়েও না — ওগুলো দ্রুত বলে brute force সহজ। bcrypt, scrypt বা Argon2 ব্যবহার করতে হবে, যেগুলো ইচ্ছাকৃতভাবে ধীর এবং প্রতিটা password-এর জন্য আলাদা salt ব্যবহার করে, ফলে rainbow table attack অকার্যকর হয়ে যায়। bcrypt-এর cost factor সময়ের সাথে বাড়িয়ে নেওয়া উচিত।

**২২. HTTPS কীভাবে কাজ করে?**
TLS handshake-এ client আর server প্রথমে কোন cipher ব্যবহার হবে তা ঠিক করে, server তার certificate পাঠায় যা একটা বিশ্বস্ত CA দিয়ে সই করা, client সেটা যাচাই করে। তারপর asymmetric encryption ব্যবহার করে দুই পক্ষ একটা shared symmetric key তৈরি করে (আধুনিক TLS-এ Diffie-Hellman দিয়ে), আর এরপর থেকে সব data সেই দ্রুত symmetric key দিয়ে encrypted হয়। এতে গোপনীয়তা, অখণ্ডতা আর server-এর পরিচয় — তিনটাই নিশ্চিত হয়।

**২৩. SQL Injection কী এবং কীভাবে ঠেকাবে?**
User-এর দেওয়া input যদি সরাসরি SQL query-তে জোড়া লাগানো হয়, তাহলে আক্রমণকারী সেই input-এ SQL লিখে দিয়ে পুরো query-র অর্থ বদলে দিতে পারে — data চুরি করতে বা মুছে দিতে পারে। প্রতিরোধের প্রধান উপায় হলো parameterized query বা prepared statement ব্যবহার করা, যেখানে input কখনো code হিসেবে গণ্য হয় না। এর সাথে input validation, least privilege database user, আর ORM ব্যবহার সহায়ক।

**২৪. CORS কী?**
Browser-এর same-origin policy অনুযায়ী এক origin-এর JavaScript অন্য origin-এ request পাঠাতে পারে না। CORS হলো সেই নিয়ম শিথিল করার প্রক্রিয়া — server `Access-Control-Allow-Origin` header দিয়ে বলে দেয় কোন origin গুলোকে অনুমতি দেওয়া হচ্ছে। জটিল request-এর আগে browser একটা OPTIONS preflight request পাঠিয়ে অনুমতি যাচাই করে নেয়। মনে রাখতে হবে CORS হলো browser-এর নিরাপত্তা, server-এর না — curl বা backend থেকে করা request-এ CORS প্রযোজ্য না।

**২৫. CSRF কী এবং কীভাবে ঠেকাবে?**
User যখন কোনো site-এ logged in আছে, তখন আক্রমণকারী তাকে দিয়ে অজান্তে ওই site-এ একটা request পাঠাতে পারে — কারণ browser নিজে থেকেই cookie যুক্ত করে দেয়। ঠেকানোর উপায় হলো CSRF token (প্রতিটা form-এ একটা unpredictable token যা server যাচাই করে), `SameSite=Strict` বা `Lax` cookie attribute, আর গুরুত্বপূর্ণ কাজে Origin/Referer header যাচাই করা। JWT যদি cookie-তে না রেখে header-এ পাঠানো হয়, তাহলে CSRF-এর ঝুঁকি অনেক কমে যায়।

---

## D. Scalability ও Load Balancing

**২৬. Vertical আর Horizontal scaling-এর পার্থক্য?**
Vertical মানে একই machine-এ CPU/RAM বাড়ানো — সহজ, code বদলাতে হয় না, কিন্তু hardware-এর সীমা আছে, দাম exponentially বাড়ে, আর single point of failure থেকে যায়। Horizontal মানে আরো machine যোগ করা — প্রায় unlimited scale আর fault tolerance দেয়, কিন্তু load balancer লাগে এবং application-কে stateless হতে হয়। (বিস্তারিত: `10`)

**২৭. Stateless application কেন গুরুত্বপূর্ণ?**
কারণ stateless হলে যেকোনো server যেকোনো request সামলাতে পারে। এতে load balancer স্বাধীনভাবে traffic ভাগ করতে পারে, কোনো server মরে গেলেও কিছু হারায় না, আর auto-scaling-এ নতুন server সাথে সাথে কাজ শুরু করতে পারে। State রাখতে হলে সেটা server-এর বাইরে রাখতে হয় — Redis-এ session, বা JWT-র মতো self-contained token-এ।

**২৮. Load balancing algorithm গুলো কী কী?**
Round Robin ক্রমানুসারে ভাগ করে — সহজ, কিন্তু সব server ও সব request সমান ধরে নেয়। Weighted Round Robin server-এর ক্ষমতা অনুযায়ী ভাগ করে। Least Connections যার হাতে এখন সবচেয়ে কম কাজ তাকে দেয় — request-এর সময় ভিন্ন হলে এটাই ভালো। Least Response Time সবচেয়ে দ্রুত server বাছে। IP Hash একই client-কে সবসময় একই server-এ পাঠায়। Consistent Hashing distributed cache বা sharding-এ ব্যবহৃত হয়। (বিস্তারিত: `11`)

**২৯. Layer 4 আর Layer 7 load balancer-এর পার্থক্য?**
L4 শুধু IP আর port দেখে packet forward করে — খুব দ্রুত কিন্তু content বোঝে না। L7 পুরো HTTP request পড়ে, তাই URL বা header দেখে routing করতে পারে, SSL termination আর caching করতে পারে — কিন্তু বেশি CPU লাগে। Microservices-এ path-based routing দরকার হয় বলে সাধারণত L7 ব্যবহার হয়।

**৩০. Load balancer নিজে fail করলে?**
তাহলে সেটাই single point of failure হয়ে যাবে, তাই কখনো একটা load balancer রাখা হয় না। Active-passive setup-এ দুটো load balancer একটা virtual IP share করে এবং heartbeat দিয়ে একে অপরকে নজরে রাখে — active মরলে passive সাথে সাথে IP নিয়ে নেয়। অথবা active-active setup-এ দুটোই কাজ করে আর DNS স্তরে traffic ভাগ হয়।

**৩১. Sticky session কী এবং কেন এড়ানো উচিত?**
Sticky session মানে load balancer একই user-কে সবসময় একই server-এ পাঠায়, কারণ সেই server-এ তার session আছে। এটা এড়ানো উচিত কারণ ওই server মরলে user-এর session হারায়, load সমানভাবে ভাগ হয় না, আর auto-scaling ঠিকমতো কাজ করে না — নতুন server খালি বসে থাকে। সঠিক সমাধান হলো session Redis-এ রাখা বা JWT ব্যবহার করা।

**৩২. Auto-scaling কীভাবে design করবে?**
CPU, memory বা request rate-এর মতো একটা metric-এর উপর ভিত্তি করে threshold ঠিক করতে হয় — সীমা ছাড়ালে instance যোগ, নিচে নামলে বাদ। গুরুত্বপূর্ণ বিষয় হলো নতুন instance চালু হতে সময় লাগে (কয়েক মিনিট), তাই sudden spike সামলাতে কিছু buffer capacity রাখতে হয় বা predictive scaling ব্যবহার করতে হয়। পাশাপাশি cooldown period রাখতে হয় যাতে বারবার scale up-down না হয় (thrashing), আর scale-in করার সময় graceful shutdown নিশ্চিত করতে হয়।

**৩৩. একটা system ধাপে ধাপে কীভাবে scale করবে?**
প্রথমে একটা server-এ সব; তারপর database আলাদা করা; তারপর load balancer সহ একাধিক stateless app server; তারপর cache আর CDN; তারপর database read replica; তারপর sharding ও message queue; সবশেষে microservices আর multi-region। প্রতিটা ধাপ আগেরটার সীমায় পৌঁছানোর পর, আগে না।

---

## E. Caching ও CDN

**৩৪. Cache-Aside pattern কীভাবে কাজ করে?**
পড়ার সময় application আগে cache দেখে; পেলে সেটাই ফেরত দেয়, না পেলে database থেকে এনে cache-এ বসিয়ে ফেরত দেয়। লেখার সময় database update করে এবং cache key delete করে দেয়। এটাই সবচেয়ে বেশি ব্যবহৃত pattern কারণ শুধু যা চাওয়া হয় তাই cache হয়, আর cache down থাকলেও system চলে (শুধু ধীর হয়)। (বিস্তারিত: `12`)

**৩৫. Cache invalidate করতে update না করে delete কেন?**
কারণ একসাথে দুটো write হলে কোনটা পরে cache-এ বসবে তার নিশ্চয়তা নেই — একটা পুরনো value শেষে বসে গিয়ে TTL শেষ না হওয়া পর্যন্ত ভুল data দেখাতে পারে। Delete করলে পরের read আপনাআপনি database থেকে সঠিক data এনে বসায়, তাই এটা অনেক নিরাপদ।

**৩৬. Cache stampede কী?**
একটা জনপ্রিয় key expire হওয়ার মুহূর্তে হাজার হাজার request একসাথে cache miss পেয়ে সবাই database-এ ঝাঁপিয়ে পড়ে, ফলে database ধসে পড়তে পারে। ঠেকানোর উপায় — TTL-এ random jitter দেওয়া যাতে সব key একসাথে expire না করে, lock দিয়ে শুধু একটা request-কে database-এ যেতে দেওয়া, বা expire-এর আগেই background-এ refresh করে রাখা।

**৩৭. LRU আর LFU-র পার্থক্য?**
LRU বাদ দেয় সবচেয়ে অনেকক্ষণ আগে ব্যবহৃত item, LFU বাদ দেয় সবচেয়ে কম বার ব্যবহৃত item। LFU-র সমস্যা হলো একসময় জনপ্রিয় ছিল এমন item তার উঁচু count-এর জোরে আটকে থাকে যদিও এখন কেউ চায় না। বেশিরভাগ ক্ষেত্রে LRU ভালো কাজ করে কারণ recency বাস্তব access pattern-এর সাথে বেশি মেলে।

**৩৮. কোন data cache করবে না?**
যা প্রতিবার বদলায় (real-time price — অথবা এক সেকেন্ডের TTL দাও), যা মাত্র একবার পড়া হবে (cache-এ রাখলে শুধু memory নষ্ট), অত্যন্ত sensitive data (অন্তত encryption ছাড়া না), আর যেখানে ১০০% নির্ভুলতা লাগে (account balance, শেষ একটা টিকিট)। মূল নীতি হলো আগে measure করা — যা সত্যিই ধীর এবং সত্যিই বারবার লাগে, শুধু তাই cache করা।

**৩৯. CDN কীভাবে কাজ করে?**
CDN পৃথিবীর নানা জায়গায় edge server রাখে যেখানে content-এর copy জমা থাকে। User যখন domain resolve করে, CDN-এর DNS বা Anycast routing তাকে সবচেয়ে কাছের edge-এ পাঠায়। Content ওখানে থাকলে সাথে সাথে দেয়, না থাকলে origin থেকে এনে cache করে রাখে যাতে পরের user দ্রুত পায়। এতে latency নাটকীয়ভাবে কমে এবং origin-এর load প্রায় ৯০% কমে যায়। (বিস্তারিত: `13`)

**৪০. CDN-এ content update করবে কীভাবে?**
সবচেয়ে ভালো উপায় হলো versioned URL — file-এর নামে content hash রাখা (`app.a3f8b2.js`), তাহলে নতুন deploy মানেই নতুন URL, purge-এর দরকারই নেই আর পুরনো ফাইল এক বছরের TTL নিয়ে cache-এ পড়ে থাকলেও ক্ষতি নেই। বিকল্প হলো CDN-এর purge API, তবে সব edge-এ ছড়াতে সময় লাগে। HTML-এ ছোট TTL রাখতে হয়, কারণ HTML-ই নতুন asset-এর নাম বহন করে।

---

## F. Database

**৪১. SQL না NoSQL — কীভাবে ঠিক করবে?**
Data-র মধ্যে জটিল সম্পর্ক থাকলে, transaction লাগলে আর সঠিকতা সবচেয়ে জরুরি হলে SQL। বিশাল write throughput, নমনীয় schema আর দিনের প্রথম দিন থেকেই horizontal scaling লাগলে এবং eventual consistency মেনে নেওয়া গেলে NoSQL। বাস্তবে বড় system-এ দুটোই ব্যবহার হয় — order-এ PostgreSQL, catalog-এ MongoDB, session-এ Redis, log-এ Cassandra। (বিস্তারিত: `14`)

**৪২. ACID-এর ব্যাখ্যা দাও।**
Atomicity — transaction-এর সব operation একসাথে সফল বা একসাথে বাতিল। Consistency — transaction-এর আগে-পরে database-এর সব নিয়ম বজায় থাকবে। Isolation — একসাথে চলা transaction গুলো একে অপরের অসম্পূর্ণ কাজ দেখবে না। Durability — commit হয়ে গেলে crash হলেও data থাকবে।

**৪৩. Index কীভাবে কাজ করে?**
Index একটা আলাদা sorted data structure, সাধারণত B+ Tree, যেখানে column-এর value আর সংশ্লিষ্ট row-এর ঠিকানা থাকে। Index ছাড়া database প্রতিটা row একে একে দেখে — O(n); index থাকলে গাছে নেমে সরাসরি পৌঁছানো যায় — O(log n)। ১ কোটি row-এ এর মানে ১ কোটি ধাপের বদলে মাত্র ২৩ ধাপ।

**৪৪. তাহলে সব column-এ index দিলেই তো হয়?**
না। প্রতিটা index disk space নেয় এবং প্রতিটা INSERT, UPDATE, DELETE-এ সব সংশ্লিষ্ট index-ও update করতে হয়, তাই write ধীর হয়ে যায়। অতিরিক্ত index থাকলে query planner-এর ভুল সিদ্ধান্তের সম্ভাবনাও বাড়ে। শুধু সেই column-এ index দেওয়া উচিত যেগুলো WHERE, JOIN বা ORDER BY-তে নিয়মিত আসে।

**৪৫. কখন index কাজ করে না?**
Column-এ function প্রয়োগ করলে (`WHERE YEAR(created_at) = 2024`), leading wildcard দিয়ে LIKE করলে (`LIKE '%rahim'`), type mismatch হলে, বা negation ব্যবহার করলে index সাধারণত ব্যবহৃত হয় না। Composite index-এ leftmost prefix না মানলেও কাজ করে না। `EXPLAIN` চালিয়ে দেখা যায় Index Scan হচ্ছে না Seq Scan।

**৪৬. Composite index-এ column-এর ক্রম গুরুত্বপূর্ণ কেন?**
কারণ composite index বাঁ দিক থেকে ক্রমানুসারে sorted — একে বলে leftmost prefix rule। `(user_id, created_at)` index টা `WHERE user_id = ?` এ কাজ করবে, `WHERE user_id = ? AND created_at = ?` এও করবে, কিন্তু শুধু `WHERE created_at = ?` এ করবে না। তাই সবচেয়ে বেশি ব্যবহৃত বা সবচেয়ে selective column আগে রাখতে হয়।

**৪৭. Replication আর Sharding-এর পার্থক্য?**
Replication মানে একই data-র একাধিক copy রাখা — read scale হয়, fault tolerance আসে, কিন্তু প্রতিটা server-এ পুরো data থাকায় write বা storage scale হয় না। Sharding মানে data টুকরো করে আলাদা server-এ ভাগ করা — write আর storage দুটোই scale হয়, কিন্তু cross-shard query ও transaction কঠিন হয়ে যায়। বাস্তবে দুটোই একসাথে ব্যবহার হয়। (বিস্তারিত: `15`)

**৪৮. Replication lag কী সমস্যা তৈরি করে?**
Asynchronous replication-এ replica primary-র চেয়ে কিছুটা পিছিয়ে থাকে। এতে সবচেয়ে দৃশ্যমান সমস্যা হলো "read your own writes" — user কিছু লিখে page reload করলে replica থেকে পুরনো data পড়ে নিজের লেখা জিনিসটাই দেখতে পায় না। সমাধান হলো write-এর পর ওই user-কে কিছুক্ষণ primary থেকে পড়ানো, বা replica কোন position পর্যন্ত sync হয়েছে তা track করা।

**৪৯. Shard key কীভাবে বাছবে?**
তিনটা শর্ত — high cardinality (অনেক ভিন্ন মান), even distribution (কোনো মানে data জমে না যায়), আর query pattern-এর সাথে মিল (বেশিরভাগ query-তে key থাকলে এক shard-এই কাজ শেষ)। `user_id` বা `tenant_id` সাধারণত ভালো; `country` খারাপ কারণ কিছু দেশে অনেক বেশি user, আর `created_at` খারাপ কারণ সব নতুন write এক shard-এ পড়ে।

**৫০. Sharding-এ unique ID কীভাবে তৈরি করবে?**
প্রতিটা shard আলাদা database হওয়ায় auto-increment ব্যবহার করলে ID সংঘর্ষ হবে। বিকল্প — UUID (সহজ কিন্তু বড় আর random হওয়ায় index-এ খারাপ), প্রতি shard-কে আলাদা ID range দেওয়া, অথবা Snowflake ID যেখানে timestamp + machine id + sequence মিলিয়ে ID বানানো হয়। Snowflake সবচেয়ে জনপ্রিয় কারণ এটা unique, roughly sorted এবং কোনো কেন্দ্রীয় coordination ছাড়াই তৈরি হয়।

**৫১. N+1 query problem কী?**
একটা query দিয়ে N টা record আনার পর প্রতিটার সম্পর্কিত data আনতে আলাদা query চালানো — মোট N+1 query। ORM ব্যবহারে এটা খুব সহজে হয়ে যায় এবং ভয়াবহ ধীর করে দেয়। সমাধান হলো JOIN ব্যবহার করা বা eager loading — সব সম্পর্কিত data এক বা দুটো query-তে একসাথে আনা।

**৫২. Deadlock কী এবং কীভাবে এড়াবে?**
দুটো transaction একে অপরের ধরে রাখা lock-এর জন্য অপেক্ষা করলে কেউই এগোতে পারে না — এটাই deadlock। Database সাধারণত এটা শনাক্ত করে একটা transaction বাতিল করে দেয়। এড়ানোর উপায় — সবসময় একই ক্রমে row lock করা, transaction যতটা সম্ভব ছোট রাখা, উপযুক্ত isolation level ব্যবহার করা, আর সম্ভব হলে pessimistic-এর বদলে optimistic locking ব্যবহার করা।

**৫৩. Normalization কখন ভাঙবে?**
যখন read performance-এর প্রয়োজন data duplication-এর খরচকে ছাড়িয়ে যায়। যেমন প্রতিবার count(*) চালিয়ে view count বের করার বদলে video table-এ একটা `view_count` column রেখে দেওয়া। Analytics ও reporting-এ denormalization স্বাভাবিক, আর NoSQL-এ এটাই মূল ধারা। দাম হলো update করার সময় সব জায়গায় করতে হবে, নাহলে data অসামঞ্জস্যপূর্ণ হবে।

**৫৪. Connection pooling কেন দরকার?**
প্রতিটা নতুন database connection তৈরি করতে TCP handshake আর authentication মিলিয়ে ২০-১০০ms লাগে, তাই প্রতি request-এ নতুন connection খুললে সেটাই bottleneck হয়ে যায়। Pool আগে থেকে কিছু connection খুলে রেখে সেগুলো পুনর্ব্যবহার করে। Pool size অন্ধভাবে বাড়ানো উচিত না — বেশি concurrent connection মানে database-এ বেশি context switching, তাই throughput বরং কমে যেতে পারে।

---

## G. Distributed Systems

**৫৫. CAP theorem কী?**
Distributed system-এ Consistency, Availability আর Partition tolerance — তিনটার মধ্যে একসাথে সর্বোচ্চ দুটো পাওয়া যায়। যেহেতু network partition অনিবার্য, P বাধ্যতামূলক; তাই আসল প্রশ্ন হলো partition-এর সময় consistency বাঁচাবে না availability। ব্যাংক বা inventory-তে CP, feed বা catalog-এ AP। (বিস্তারিত: `16`)

**৫৬. PACELC কী?**
CAP-এর সম্প্রসারণ। এটা বলে — partition হলে A না C বেছে নাও, আর else অর্থাৎ স্বাভাবিক সময়ে latency না consistency বেছে নাও। এটা বেশি বাস্তবসম্মত কারণ partition বিরল, কিন্তু latency-consistency tradeoff সবসময় থাকে। Cassandra হলো PA/EL, MongoDB হলো PC/EC।

**৫৭. Eventual consistency কী?**
নতুন write বন্ধ থাকলে কিছু সময় পর সব replica একই data-য় মিলে যাবে, কিন্তু ঠিক কখন তার নিশ্চয়তা নেই। এটা গ্রহণযোগ্য কিনা তা use case-এর উপর নির্ভর করে — like count বা news feed-এ ঠিক আছে, কিন্তু account balance-এ বিপজ্জনক।

**৫৮. Quorum কী এবং W + R > N কেন?**
Quorum মানে কোনো operation সফল বলতে কতগুলো replica-র সম্মতি লাগবে তা নির্ধারণ করা। W + R > N হলে read আর write-এর replica set-এর মধ্যে অন্তত একটা replica common থাকবেই, আর সেই replica-র কাছে সর্বশেষ data আছে — তাই read কখনো পুরনো data দেবে না। N=3 এ W=2, R=2 সবচেয়ে প্রচলিত ভারসাম্য।

**৫৯. Consistent hashing কী?**
Key গুলোকে server-এ ভাগ করার একটা পদ্ধতি যেখানে server যোগ বা বিয়োগ করলে খুব অল্প key-র জায়গা বদলায়। সাধারণ `hash % N` এ N বদলালে প্রায় সব mapping ভেঙে যায় — cache পুরো অকেজো হয়ে যায়। Consistent hashing-এ একটা hash ring ব্যবহার করা হয় এবং key ঘড়ির কাঁটার দিকে প্রথম যে node পায় সেখানে যায়, ফলে গড়ে মাত্র K/N key সরে। (বিস্তারিত: `19`)

**৬০. Virtual node কেন লাগে?**
কারণ অল্প কয়েকটা server ring-এ এলোমেলোভাবে বসলে সমানভাবে ছড়ায় না, কারো ভাগে বিশাল অংশ পড়ে যায় — hotspot হয়। প্রতিটা physical server-কে ১০০-২০০টা virtual node হিসেবে ছড়িয়ে দিলে বণ্টন সমান হয়, আর একটা server মরলে তার ভার একজনের ঘাড়ে না পড়ে সবার মধ্যে ছড়িয়ে যায়। বাড়তি সুবিধা — ক্ষমতা অনুযায়ী বেশি বা কম virtual node দেওয়া যায়।

**৬১. Split brain কী?**
Network partition-এর কারণে দুটো node একসাথে নিজেকে primary ভাবতে শুরু করলে দুই জায়গায় আলাদা write হয় এবং data অসামঞ্জস্যপূর্ণ হয়ে যায়। ঠেকানোর উপায় quorum — সংখ্যাগরিষ্ঠ node যাকে সমর্থন করে সেই primary, তাই বিজোড় সংখ্যক node রাখা হয়। এর সাথে fencing — পুরনো primary-কে জোর করে নিষ্ক্রিয় করা।

**৬২. Leader election কীভাবে হয়?**
Raft বা Paxos-এর মতো consensus algorithm দিয়ে। Raft-এ প্রতিটা node একটা randomized timeout নিয়ে অপেক্ষা করে; leader থেকে heartbeat না এলে সে candidate হয়ে ভোট চায়, আর সংখ্যাগরিষ্ঠ ভোট পেলে leader হয়। Randomized timeout থাকায় সাধারণত একজনই আগে চেষ্টা করে, ফলে সংঘর্ষ কম হয়। বাস্তবে নিজে না লিখে etcd, ZooKeeper বা Consul ব্যবহার করাই বুদ্ধিমানের কাজ।

**৬৩. Distributed lock কীভাবে বানাবে?**
সবচেয়ে সহজ উপায় Redis-এ `SET key value NX EX 30` — শুধু তখনই set হবে যখন key নেই, আর নির্দিষ্ট সময় পর নিজে থেকে expire করবে যাতে lock ধারণকারী crash করলেও deadlock না হয়। গুরুত্বপূর্ণ হলো unique value রাখা এবং Lua script দিয়ে atomic ভাবে release করা, নাহলে একজনের lock আরেকজন মুছে দিতে পারে। তবে distributed lock কখনোই ১০০% নিরাপদ না — যেখানে সঠিকতা অপরিহার্য সেখানে database-এর transaction বা fencing token ব্যবহার করা উচিত।

**৬৪. Heartbeat কী?**
Node গুলো নিয়মিত বিরতিতে একে অপরকে বা একটা coordinator-কে "আমি বেঁচে আছি" সংকেত পাঠায়। নির্দিষ্ট সময়ের মধ্যে heartbeat না এলে ওই node-কে মৃত ধরে নিয়ে তার কাজ অন্যকে দেওয়া হয়। চ্যালেঞ্জ হলো timeout ঠিক করা — খুব ছোট হলে সাময়িক network দেরিতেই সুস্থ node-কে মৃত ঘোষণা করা হবে, খুব বড় হলে প্রকৃত failure শনাক্ত করতে দেরি হবে।

**৬৫. Two-phase commit কী এবং কেন এড়ানো হয়?**
2PC-তে একটা coordinator প্রথমে সব participant-কে জিজ্ঞেস করে "তোমরা কি commit করতে প্রস্তুত?", সবাই হ্যাঁ বললে দ্বিতীয় ধাপে commit করতে বলে। সমস্যা হলো এটা blocking — coordinator মাঝপথে মরে গেলে participant-রা lock ধরে অনির্দিষ্টকাল অপেক্ষা করে, ফলে availability মারাত্মকভাবে কমে যায়। এজন্য microservices-এ 2PC-র বদলে Saga pattern ব্যবহার করা হয়।

---

## H. Message Queue ও Async

**৬৬. Message queue কেন ব্যবহার করবে?**
ধীর কাজ async করে response time কমানো, service গুলোকে decouple করা, traffic spike-এ buffer হিসেবে কাজ করে system বাঁচানো, consumer crash করলেও message না হারানো, আর worker যোগ করে সহজে scale করা — এই পাঁচটা কারণে। (বিস্তারিত: `17`)

**৬৭. Kafka আর RabbitMQ-র পার্থক্য?**
RabbitMQ প্রচলিত broker — message ack পেলে মুছে যায়, আর routing ক্ষমতা খুব শক্তিশালী। Kafka একটা distributed log — message retention period পর্যন্ত থাকে, তাই একাধিক consumer group একই data পড়তে পারে এবং replay সম্ভব। Throughput-এ Kafka অনেক এগিয়ে; per-message priority বা জটিল routing লাগলে RabbitMQ ভালো।

**৬৮. Exactly-once delivery কি সম্ভব?**
তাত্ত্বিকভাবে distributed system-এ সত্যিকারের exactly-once delivery অসম্ভব, কারণ network-এ acknowledgment হারাতে পারে। Kafka transaction দিয়ে exactly-once *processing* সম্ভব, কিন্তু সেটা ধীর ও জটিল। ইন্ডাস্ট্রির স্ট্যান্ডার্ড সমাধান হলো at-least-once delivery + idempotent consumer — duplicate এলেও ফলাফল একই হয়, যা কার্যত exactly-once।

**৬৯. Idempotency কীভাবে নিশ্চিত করবে?**
প্রতিটা message বা request-এ unique ID রেখে Redis বা database-এ যাচাই করা যে এটা আগে process হয়েছে কিনা; database-এ unique constraint দিয়ে duplicate insert আটকানো; upsert (`ON CONFLICT DO UPDATE`) ব্যবহার করা; অথবা state machine দিয়ে ইতিমধ্যে সম্পন্ন অবস্থার event উপেক্ষা করা। Payment API-তে সাধারণত `Idempotency-Key` header ব্যবহার হয়।

**৭০. Dead Letter Queue কী?**
বারবার চেষ্টা করেও process করা যায় না এমন message রাখার আলাদা queue। এটা না থাকলে একটা ত্রুটিপূর্ণ message অসীমবার retry হয়ে পুরো queue আটকে দেয় (poison message)। নির্দিষ্ট সংখ্যক ব্যর্থতার পর message DLQ-তে সরিয়ে দিলে বাকিরা চলতে পারে, আর পরে developer সমস্যা ঠিক করে সেগুলো আবার process করাতে পারে।

**৭১. Kafka-তে ordering কীভাবে নিশ্চিত করবে?**
Kafka শুধু একটা partition-এর ভিতরে ক্রমের নিশ্চয়তা দেয়। তাই যেসব message-এর ক্রম জরুরি সেগুলোর জন্য একই partition key ব্যবহার করতে হয় — যেমন `order_id`। এতে এক order-এর সব event ক্রমে আসে, অথচ বিভিন্ন order সমান্তরালে process হয়।

**৭২. Consumer lag বেড়ে গেলে কী করবে?**
প্রথমে consumer instance বাড়াতে হবে, তবে Kafka-তে partition সংখ্যার বেশি consumer যোগ করে লাভ নেই — তখন partition-ও বাড়াতে হবে। পাশাপাশি প্রতিটা message process করার সময় কমানো (batching, DB query optimize), আর প্রয়োজনে producer-এর দিকে rate limit দেওয়া। স্থায়ীভাবে lag বাড়তে থাকলে বুঝতে হবে capacity planning ভুল ছিল।

**৭৩. Exponential backoff-এ jitter কেন?**
Jitter ছাড়া সব client ঠিক একই ব্যবধানে retry করবে, ফলে service ফিরে আসার মুহূর্তেই সবাই একসাথে হামলে পড়বে — thundering herd — আর service আবার ধসে পড়বে। Random jitter যোগ করলে চেষ্টাগুলো সময়ের সাথে ছড়িয়ে যায় এবং service ধীরে ধীরে load নিতে পারে।

---

## I. Microservices ও Architecture

**৭৪. Monolith না microservices — কীভাবে ঠিক করবে?**
Team-এর আকার, domain-এর স্পষ্টতা আর scaling-এর চাহিদা দেখে। ছোট team, নতুন product আর অস্পষ্ট domain হলে monolith — কারণ তখনো জানা নেই সীমারেখা কোথায়। একাধিক team যখন একই codebase-এ বাধা পাচ্ছে, deploy ঝুঁকিপূর্ণ হয়ে উঠছে আর বিভিন্ন অংশের scaling চাহিদা আলাদা — তখন microservices। Modular monolith প্রায়ই সেরা মাঝামাঝি পথ। (বিস্তারিত: `18`)

**৭৫. Distributed monolith কী?**
যখন service গুলো আলাদা করা হয়েছে কিন্তু এতটাই পরস্পরনির্ভর যে একটাকে আলাদাভাবে deploy করা যায় না। এতে monolith-এর সব সীমাবদ্ধতা থেকেই যায়, তার উপর distributed system-এর সব সমস্যা যোগ হয় — সবচেয়ে খারাপ পরিণতি। সাধারণত এটা হয় ভুল জায়গায় service ভাগ করলে বা database শেয়ার করলে।

**৭৬. Service গুলো কীভাবে ভাগ করবে?**
Technical layer ধরে না, business capability ধরে। Domain-Driven Design-এর bounded context ধরে ভাগ করতে হয় — User, Order, Payment — যেখানে প্রতিটা service একটা পূর্ণ ব্যবসায়িক ক্ষমতার মালিক, নিজের database সহ। ভালো ভাগের লক্ষণ হলো high cohesion, low coupling, আর স্বাধীনভাবে deploy করার ক্ষমতা।

**৭৭. প্রতিটা service-এর আলাদা database কেন?**
Shared database হলে সবচেয়ে শক্ত coupling তৈরি হয় — একটা service schema বদলালে অন্যরা ভেঙে যায়, ফলে স্বাধীন deployment-এর ক্ষমতাই নষ্ট হয়, যা microservices-এর মূল উদ্দেশ্য। এর দাম হলো JOIN আর transaction হারানো, কিন্তু সেটাই microservices-এর মূল্য।

**৭৮. Circuit breaker কী?**
একটা downstream service বারবার fail করলে তাকে ডাকা সাময়িকভাবে বন্ধ করে দেওয়ার কৌশল — CLOSED, OPEN, HALF-OPEN তিনটা অবস্থা থাকে। এটা জরুরি কারণ মৃত service-কে বারবার ডাকলে caller-এর thread গুলোও আটকে যায়, ফলে সে নিজেও মরে যায় এবং cascading failure-এ পুরো system ধসে পড়ে।

**৭৯. Saga pattern কী?**
Microservices-এ একাধিক service জুড়ে transaction চালানোর কৌশল। কাজটাকে ধারাবাহিক local transaction-এ ভাগ করা হয়, প্রতিটা ধাপ নিজের database-এ commit করে; কোনো ধাপ ব্যর্থ হলে আগের ধাপগুলোর compensating transaction চালিয়ে পিছিয়ে আসা হয়। Choreography (event-ভিত্তিক) বা orchestration (কেন্দ্রীয় coordinator) — দুইভাবে করা যায়।

**৮০. Service discovery কী?**
Auto-scaling আর restart-এর কারণে instance গুলোর ঠিকানা সারাক্ষণ বদলায়, তাই IP hardcode করা অসম্ভব। Service discovery track রাখে কোন service-এর কোন instance এখন কোথায় চলছে। Client-side discovery-তে client নিজে registry থেকে ঠিকানা নেয়; server-side-এ একটা load balancer সেটা করে দেয়। Kubernetes-এ এটা built-in।

**৮১. API Gateway কী কাজ করে?**
সব client request-এর একক প্রবেশদ্বার হিসেবে কাজ করে — authentication, rate limiting, routing, request/response transformation, protocol translation, response aggregation, logging আর caching। এতে প্রতিটা microservice-এ এই cross-cutting concern গুলো আলাদা করে লিখতে হয় না। তবে এটা single point of failure হতে পারে, তাই একে redundant রাখতে হয়। (বিস্তারিত: `6`)

**৮২. API Gateway আর Load Balancer কি এক?**
না। Load balancer ঠিক করে request কোন server-এ যাবে — মূলত traffic distribution আর availability। API Gateway ঠিক করে request পাঠানোর আগে-পরে কী কী হবে — auth, rate limit, transformation, aggregation। Gateway সাধারণত load balancer-এর উপরে বসে এবং নিজেও load balancing করতে পারে।

**৮৩. Strangler Fig pattern কী?**
Monolith থেকে microservices-এ ধাপে ধাপে migrate করার কৌশল। সামনে একটা gateway বসিয়ে একটা একটা করে feature নতুন service-এ সরানো হয়, প্রতিবার gateway-তে ওই path নতুন service-এ পাঠানো হয়, আর নিশ্চিত হওয়ার পর monolith থেকে পুরনো code মুছে দেওয়া হয়। একবারে পুরোটা rewrite করা প্রায় সবসময়ই ব্যর্থ হয়।

**৮৪. Sidecar pattern কী?**
মূল application-এর পাশে একটা সহায়ক container চালানো যা cross-cutting কাজ করে — logging, monitoring, service-to-service TLS, retry, circuit breaking। এতে application code-এ এসব logic লিখতে হয় না এবং ভাষা-নিরপেক্ষভাবে সব service একই আচরণ পায়। Service mesh (Istio, Linkerd) এই pattern-এর উপর গড়া।

---

## J. Reliability ও Observability

**৮৫. Availability-র "নয়" গুলো কী বোঝায়?**
99% মানে বছরে প্রায় ৮৭ ঘণ্টা downtime, 99.9% মানে ৮.৭ ঘণ্টা, 99.99% মানে ৫২ মিনিট, আর 99.999% মানে মাত্র ৫ মিনিট। প্রতিটা অতিরিক্ত নয়-এর জন্য খরচ ও জটিলতা নাটকীয়ভাবে বাড়ে, তাই ব্যবসায়িকভাবে কতটা দরকার সেটা আগে ঠিক করা উচিত। (বিস্তারিত: `8`)

**৮৬. SLA, SLO আর SLI-র পার্থক্য?**
SLI হলো পরিমাপ — যেমন প্রকৃত uptime বা p99 latency। SLO হলো নিজেদের ঠিক করা লক্ষ্য — যেমন "৯৯.৯% uptime রাখবো"। SLA হলো গ্রাহকের সাথে চুক্তি, যা ভাঙলে আর্থিক জরিমানা বা ক্ষতিপূরণ দিতে হয়। সাধারণত SLO কে SLA-র চেয়ে কড়া রাখা হয়, যাতে SLO ভাঙলেও SLA বাঁচানোর সময় থাকে।

**৮৭. Single point of failure কীভাবে খুঁজবে?**
Architecture diagram-এ প্রতিটা component ধরে জিজ্ঞেস করতে হয় — "এটা মরে গেলে কী হবে?" যেটার উত্তর "পুরো system বন্ধ", সেটাই SPOF। সাধারণ SPOF হলো একটামাত্র load balancer, একটামাত্র database primary, একটামাত্র API gateway, বা একটামাত্র availability zone। সমাধান হলো প্রতিটা স্তরে redundancy আর স্বয়ংক্রিয় failover।

**৮৮. Graceful degradation কী?**
System-এর একটা অংশ fail করলে পুরো service বন্ধ না করে শুধু ওই feature-টা বন্ধ বা সরল রাখা। যেমন recommendation service down হলে শুধু "জনপ্রিয় পণ্য" দেখানো, কিন্তু কেনাকাটা চালু রাখা। এতে user-এর অভিজ্ঞতা কিছুটা খারাপ হয় কিন্তু ব্যবসা চলতে থাকে।

**৮৯. Observability-র তিন স্তম্ভ কী?**
Logs — কী ঘটেছে তার বিস্তারিত record; Metrics — কত ঘটেছে তার সংখ্যাগত পরিমাপ (rate, latency, error); আর Traces — একটা request কোন কোন service-এ কত সময় নিলো। Microservices-এ তিনটাই লাগে, আর সব log ও trace-এ একটা common correlation ID থাকা অপরিহার্য।

**৯০. কী কী monitor করা উচিত?**
Google-এর "four golden signals" — Latency (কত সময় লাগছে, percentile সহ), Traffic (কত request আসছে), Errors (কত শতাংশ ব্যর্থ হচ্ছে), আর Saturation (resource কতটা পূর্ণ)। এর সাথে ব্যবসায়িক metric — order সংখ্যা, signup — কারণ technical সব ঠিক দেখালেও ব্যবসায়িক metric পড়ে যাওয়া মানে কিছু একটা ভাঙা।

**৯১. Blue-Green আর Canary deployment-এর পার্থক্য?**
Blue-Green-এ দুটো সম্পূর্ণ environment থাকে; নতুন version দ্বিতীয়টায় deploy করে একসাথে সব traffic সরিয়ে দেওয়া হয় — rollback তাৎক্ষণিক, কিন্তু দ্বিগুণ infrastructure লাগে। Canary-তে নতুন version-এ প্রথমে অল্প traffic (যেমন ৫%) পাঠিয়ে metric দেখা হয়, ঠিক থাকলে ধীরে ধীরে বাড়ানো হয় — ঝুঁকি সবচেয়ে কম, কিন্তু ভালো monitoring আর সময় লাগে।

**৯২. Chaos engineering কী?**
ইচ্ছাকৃতভাবে production-এ নিয়ন্ত্রিত ব্যর্থতা তৈরি করে দেখা system সত্যিই সহনশীল কিনা — যেমন Netflix-এর Chaos Monkey এলোমেলোভাবে instance বন্ধ করে দেয়। যুক্তিটা হলো failure যেহেতু হবেই, সেটা রাত ৩টায় হঠাৎ না ঘটে বরং কর্মদিবসে নিয়ন্ত্রিতভাবে ঘটুক, যাতে দুর্বলতা আগেই ধরা পড়ে।

**৯৩. Load shedding কী?**
System অতিরিক্ত চাপে পড়লে ইচ্ছাকৃতভাবে কিছু request প্রত্যাখ্যান করে বাকিদের ঠিকমতো সেবা দেওয়া। সব request ধীরে ধীরে সামলানোর চেষ্টা করলে সবাই timeout পায় এবং কেউই সেবা পায় না; তার চেয়ে কিছু request-কে দ্রুত 503 দিয়ে ফিরিয়ে দেওয়া ভালো। সাধারণত কম গুরুত্বপূর্ণ traffic আগে বাদ দেওয়া হয়।

---

## K. Design Scenario — ছোট প্রশ্ন

**৯৪. একটা URL shortener design করো — সবচেয়ে গুরুত্বপূর্ণ সিদ্ধান্ত কী?**
Short code তৈরির কৌশল। Hash নিলে collision সামলাতে হয়; auto-increment ID-কে Base62 করলে collision নেই কিন্তু অনুমানযোগ্য আর distributed-এ কঠিন; তাই সাধারণত Snowflake ID বা আগে থেকে key তৈরি করে রাখা একটা Key Generation Service ব্যবহার করা হয়। এর পরের গুরুত্বপূর্ণ সিদ্ধান্ত হলো cache — এটা প্রচণ্ড read-heavy system, তাই Redis ছাড়া চলবে না। (বিস্তারিত: `22`)

**৯৫. News feed-এ fan-out on write না read?**
Hybrid। সাধারণ user-এর জন্য fan-out on write — post করার সময়ই সব follower-এর feed-এ লিখে দেওয়া, তাই feed পড়া অতি দ্রুত। কিন্তু celebrity-র কোটি follower থাকলে একটা post-এ কোটি লেখা হবে, তাই তাদের জন্য fan-out on read — feed চাওয়ার সময় celebrity-দের post সরাসরি টেনে এনে merge করা।

**৯৬. Rate limiter design করো।**
Burst দরকার হলে token bucket, নাহলে sliding window counter — এই দুটোই বাস্তবে সবচেয়ে ব্যবহৃত। Distributed environment-এ সব server-এ সামঞ্জস্য রাখতে Redis-এ Lua script দিয়ে atomic check-and-increment করতে হবে। Client-কে 429 এর সাথে `Retry-After` header দিতে হবে, আর Redis fail করলে সাধারণত fail-open করা উচিত যাতে rate limiter নিজেই outage-এর কারণ না হয়। (বিস্তারিত: `20`)

**৯৭. একটা chat app-এ message ordering কীভাবে নিশ্চিত করবে?**
Client-এর ঘড়ির উপর ভরসা করা যাবে না, কারণ device-এর সময় ভুল থাকতে পারে। তাই server প্রতিটা message-এ একটা sequence number বা Snowflake ID বসিয়ে দেয়, আর সেই ID দিয়েই ক্রম নির্ধারিত হয়। এক conversation-এর সব message একই partition বা shard-এ রাখলে ক্রম স্বাভাবিকভাবেই বজায় থাকে।

**৯৮. "কাছের রেস্তোরাঁ খুঁজে দাও" — এটা কীভাবে করবে?**
Geospatial index দিয়ে। সব রেস্তোরাঁর সাথে দূরত্ব হিসাব করা অসম্ভব ধীর, তাই geohash, quadtree বা S2/H3-র মতো পদ্ধতিতে পৃথিবীকে ঘরে ভাগ করে রাখা হয়। তখন শুধু নিজের ঘর আর তার প্রতিবেশী ঘরগুলোতে খুঁজলেই হয়। ছোট থেকে মাঝারি scale-এ Redis-এর `GEOADD`/`GEORADIUS` দিয়েই কাজ চলে যায়।

**৯৯. Live view count (যেমন YouTube) কীভাবে করবে?**
প্রতিটা view-তে database-এ increment করলে DB ধসে পড়বে। তাই view event গুলো Kafka-তে পাঠিয়ে worker দিয়ে batch aggregate করা হয় এবং পর্যায়ক্রমে (যেমন প্রতি ১০ সেকেন্ডে) database update করা হয়। দ্রুত পড়ার জন্য Redis-এ counter রাখা হয়। এখানে eventual consistency সম্পূর্ণ গ্রহণযোগ্য — view count কয়েক সেকেন্ড পিছিয়ে থাকলে কারো কিছু যায় আসে না।

**১০০. Typeahead/autocomplete কীভাবে design করবে?**
Trie data structure ব্যবহার করা হয়, যেখানে প্রতিটা node-এ ওই prefix-এর সবচেয়ে জনপ্রিয় কয়েকটা suggestion আগে থেকে হিসাব করে রাখা থাকে — তাহলে query-র সময় গাছ ঘুরতে হয় না। Trie টা memory-তে রাখা হয় এবং পর্যায়ক্রমে offline job দিয়ে rebuild করা হয়। খুব বড় হলে prefix ধরে shard করা হয়, আর সামনে aggressive caching বসানো হয় কারণ কিছু query-ই বারবার আসে।

**১০১. File upload কীভাবে handle করবে?**
বড় file কখনো নিজের API server দিয়ে পাঠানো উচিত না। Client-কে একটা pre-signed URL দেওয়া হয় যাতে সে সরাসরি S3-র মতো object storage-এ upload করতে পারে, আর upload শেষ হলে server-কে জানায়। বড় file-এর জন্য multipart upload ব্যবহার করা হয় যাতে টুকরো টুকরো করে পাঠানো যায় এবং কোনো টুকরো ব্যর্থ হলে শুধু সেটাই আবার পাঠাতে হয়। এরপর প্রক্রিয়াকরণ (thumbnail, transcode, virus scan) queue-র মাধ্যমে async-এ হয়।

**১০২. Search feature কীভাবে বানাবে?**
Database-এর `LIKE '%keyword%'` দিয়ে করা যাবে না — সেটা index ব্যবহার করে না এবং ভয়াবহ ধীর। Elasticsearch বা OpenSearch-এর মতো inverted index-ভিত্তিক search engine ব্যবহার করতে হয়, যা শব্দ থেকে document-এর mapping রাখে এবং relevance অনুযায়ী rank করতে পারে। মূল database থেকে search engine-এ data sync করা হয় CDC বা event stream দিয়ে, আর মনে রাখতে হয় search index সবসময় eventual consistent।

**১০৩. Payment system-এ কী কী বিশেষভাবে খেয়াল রাখবে?**
Idempotency সবচেয়ে জরুরি — network timeout-এ retry হলে দুবার টাকা কাটা যাবে না, তাই প্রতিটা payment request-এ idempotency key থাকতে হবে। Consistency-কে availability-র চেয়ে অগ্রাধিকার দিতে হবে। প্রতিটা অবস্থার পরিবর্তন audit log-এ রাখতে হবে। External payment provider-এর সাথে যোগাযোগ async আর retry-সহ করতে হবে, আর দিনশেষে reconciliation চালিয়ে মিলিয়ে দেখতে হবে।

---

## L. Tricky ও Behavioral

**১০৪. তুমি কীভাবে ঠিক করবে কোথায় optimize করা দরকার?**
আগে measure করবো, অনুমান করবো না। Profiling আর monitoring দিয়ে দেখবো আসল সময়টা কোথায় যাচ্ছে — প্রায়ই সেটা যেখানে ভাবা হয় সেখানে থাকে না। তারপর যেটা সবচেয়ে বেশি প্রভাব ফেলবে সেটা আগে ঠিক করবো। অকালে optimize করা মানে সময় নষ্ট করা এবং code জটিল করে ফেলা।

**১০৫. একটা system হঠাৎ ধীর হয়ে গেছে — কীভাবে খুঁজবে?**
প্রথমে সাম্প্রতিক পরিবর্তন দেখবো — deploy, config change, traffic-এর হঠাৎ বৃদ্ধি। তারপর dashboard-এ four golden signal দেখবো — latency কোন স্তরে বাড়ছে, error rate বেড়েছে কিনা, কোন resource saturated। Distributed tracing দিয়ে দেখবো কোন service-এ সময় যাচ্ছে। সাধারণ কারণগুলো হলো database-এর slow query বা lock, cache hit ratio পড়ে যাওয়া, connection pool শেষ হয়ে যাওয়া, বা কোনো downstream dependency ধীর হয়ে যাওয়া।

**১০৬. Requirement অস্পষ্ট থাকলে কী করবে?**
প্রশ্ন করবো — কিন্তু সব প্রশ্ন না, যেগুলোর উত্তর design বদলে দেবে সেগুলো। যেমন "কতজন user" আর "consistency না availability" — এই দুটোর উত্তর পুরো architecture বদলে দেয়। উত্তর না পেলে স্পষ্টভাবে একটা assumption ঘোষণা করে এগোবো — "আমি ধরে নিচ্ছি ১ কোটি DAU, read-heavy; ভুল হলে বলবেন।"

**১০৭. একটা design-এর দুর্বলতা জিজ্ঞেস করলে কী বলবে?**
সততার সাথে বলবো, কারণ কোনো design নিখুঁত না — আর নিজের design-এর সীমা জানাটাই পরিণতির লক্ষণ। যেমন — "এই design-এ cache একটা critical dependency; Redis down হলে database-এর উপর হঠাৎ ১০ গুণ load পড়বে, তাই graceful degradation আর circuit breaker দরকার। আর celebrity user-এর ক্ষেত্রে fan-out এখনো hotspot তৈরি করতে পারে।"

**১০৮. Over-engineering কীভাবে চিনবে?**
যখন এমন সমস্যার সমাধান করা হচ্ছে যা এখনো নেই এবং হয়তো কখনো হবেও না। ১০০০ user-এর app-এ Kafka, microservices আর multi-region setup — এটা over-engineering। ভালো নিয়ম হলো বর্তমান scale-এর ১০ গুণ পর্যন্ত সামলানোর মতো design করা, তার বেশি না; কারণ ততদিনে প্রয়োজনটাই বদলে যাবে। সবচেয়ে সহজ যে সমাধান কাজ করে, সেটাই বেছে নেওয়া।

**১০৯. "সবচেয়ে ভালো database কোনটা?" — এই প্রশ্নের উত্তর কী?**
এমন কিছু নেই। প্রশ্নটা হবে — access pattern কী, data-র মধ্যে সম্পর্ক আছে কিনা, transaction লাগবে কিনা, কত scale, consistency কতটা কড়া লাগবে। এই উত্তরগুলো পেলেই database বেছে নেওয়া যায়। আর বাস্তব system-এ একটা database না, কাজভেদে একাধিক database ব্যবহার হয়।

**১১০. Interview-তে যদি কোনো কিছু না জানো?**
সোজাসুজি বলবো যে এটা আমার জানা নেই, তারপর যুক্তি দিয়ে অনুমান করার চেষ্টা করবো — "আমি এই নির্দিষ্ট tool-টা ব্যবহার করিনি, তবে সমস্যাটা যেহেতু X ধরনের, আমার ধারণা এটা Y উপায়ে কাজ করে; আমি এভাবে সমাধান করার চেষ্টা করতাম।" বানিয়ে বলার চেয়ে এটা হাজার গুণ ভালো, আর interviewer সাধারণত এই সততা ও যুক্তির প্রক্রিয়াটাই দেখতে চায়।

---

## দ্রুত পুনরাবৃত্তি — সবচেয়ে বেশি জিজ্ঞাসিত ১৫টা

১. Vertical vs Horizontal scaling → `10`
২. Load balancer-এর algorithm গুলো → `11`
৩. Cache-Aside pattern ও invalidation → `12`
৪. SQL vs NoSQL কখন কোনটা → `14`
৫. Index কীভাবে কাজ করে → `14`
৬. Replication vs Sharding → `15`
৭. CAP Theorem → `16`
৮. At-least-once + Idempotency → `17`
৯. Kafka vs RabbitMQ → `17`
১০. Microservices-এর tradeoff → `18`
১১. Circuit Breaker → `18`
১২. Consistent Hashing → `19`
১৩. Rate Limiter design → `20`
১৪. WebSocket vs SSE ও scaling → `21`
১৫. News feed fan-out (write vs read) → `22`

**সবচেয়ে বড় পরামর্শ:** উত্তর মুখস্থ করো না। প্রতিটা প্রশ্নে **"কেন"** আর **"এর দাম কী"** — এই দুটো বলতে পারলেই যথেষ্ট। Interviewer সঠিক উত্তরের চেয়ে সঠিক চিন্তার প্রক্রিয়াকে বেশি মূল্য দেয়।
