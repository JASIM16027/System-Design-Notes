# Database Scaling — Replication, Sharding এবং Partitioning

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [সমস্যাটা কোথায়](#সমস্যাটা-কোথায়)
* [Database Scaling-এর ধাপ গুলো](#database-scaling-এর-ধাপ-গুলো)
* [Replication — Data-র copy রাখা](#replication--data-র-copy-রাখা)
* [Replication Lag — সবচেয়ে বড় মাথাব্যথা](#replication-lag--সবচেয়ে-বড়-মাথাব্যথা)
* [Failover — Primary মরে গেলে](#failover--primary-মরে-গেলে)
* [Partitioning — এক DB-র ভিতরে টেবিল ভাগ](#partitioning--এক-db-র-ভিতরে-টেবিল-ভাগ)
* [Sharding — অনেক DB-তে data ভাগ](#sharding--অনেক-db-তে-data-ভাগ)
* [Sharding Strategy — কোন data কোন shard-এ](#sharding-strategy--কোন-data-কোন-shard-এ)
* [Sharding-এর সমস্যা গুলো](#sharding-এর-সমস্যা-গুলো)
* [Federation — Feature অনুযায়ী DB ভাগ](#federation--feature-অনুযায়ী-db-ভাগ)
* [সব মিলিয়ে একটা বাস্তব Architecture](#সব-মিলিয়ে-একটা-বাস্তব-architecture)
* [Interview Q&A](#interview-qa)
* [এক কথায় মনে রাখো](#এক-কথায়-মনে-রাখো)

---

<!-- TOC END -->

## সমস্যাটা কোথায়

App server বাড়ানো সহজ — stateless, তাই যত খুশি copy করো।

কিন্তু database-এ **data থাকে**। একটা DB server-এ চাপ বাড়লে তিনটা জিনিস শেষ হয়ে যায়:

1. **CPU/RAM** — query process করতে পারছে না
2. **Disk** — data আর ধরছে না
3. **Connection** — একসাথে এত connection সামলাতে পারছে না

তখনই database scaling-এর কথা আসে। কিন্তু এখানে একটা ধারাবাহিকতা আছে — সহজ থেকে কঠিন।

---

## Database Scaling-এর ধাপ গুলো

এই ক্রমটা মনে রাখা খুব গুরুত্বপূর্ণ। Interview-তে সরাসরি "sharding করবো" বললে খারাপ দেখায় — ধাপে ধাপে যাওয়াটাই সঠিক উত্তর।

**ধাপ ০ — Query optimize করো** (সবচেয়ে সস্তা)
Index দাও, N+1 problem ঠিক করো, slow query খুঁজে বের করো। অনেক সময় এখানেই ৮০% সমস্যা মিটে যায়।

**ধাপ ১ — Cache বসাও**
Redis দিয়ে repeated read আটকে দাও। DB-র read load ৯০% পর্যন্ত কমতে পারে।

**ধাপ ২ — Vertical scaling**
বড় machine নাও। সহজ, দ্রুত, কিন্তু একটা সীমা আছে।

**ধাপ ৩ — Read Replica**
Read গুলো replica-তে পাঠাও। বেশিরভাগ application-এ ৮০-৯৫% traffic-ই read, তাই এটা বিশাল কাজ করে।

**ধাপ ৪ — Partitioning**
একই DB-র ভিতরে বড় table গুলো ছোট ছোট টুকরায় ভাগ করো।

**ধাপ ৫ — Federation (Functional splitting)**
আলাদা feature-এর জন্য আলাদা DB — user DB, order DB, analytics DB।

**ধাপ ৬ — Sharding** (সবচেয়ে কঠিন, সবার শেষে)
Data-কে অনেকগুলো আলাদা DB server-এ ভাগ করে দাও।

> **মূল কথা:** Sharding হলো শেষ অস্ত্র। এর আগে সব চেষ্টা করে দেখো।

---

## Replication — Data-র copy রাখা

একটা DB-র হুবহু copy আরো কয়েকটা server-এ রাখা।

### Primary-Replica (Master-Slave)

```
                ┌────────────┐
   Write ──────►│  Primary   │
                └─────┬──────┘
                      │ replication
          ┌───────────┼───────────┐
          ▼           ▼           ▼
      Replica-1   Replica-2   Replica-3
          ▲           ▲           ▲
          └───────────┴───────────┘
                   Read
```

- **Primary** — সব write এখানে হয়
- **Replica** — Primary থেকে data copy করে নেয়, শুধু read serve করে

**কেন এটা কাজ করে:** বেশিরভাগ application-এ read:write অনুপাত ৯০:১০ বা তারও বেশি। Read গুলো ছড়িয়ে দিলেই বিশাল স্বস্তি।

**সুবিধা:**
- Read scale করা যায় — আরো replica যোগ করো
- Backup নেওয়া যায় replica থেকে, primary-কে বিরক্ত না করে
- Analytics query replica-তে চালানো যায়
- Primary মরলে replica-কে promote করা যায় (disaster recovery)

**সীমাবদ্ধতা:** Write scale হয় না — সব write সেই একটা primary-তেই যায়।

### Multi-Primary (Master-Master)

একাধিক server-এ write করা যায়, তারা একে অপরের সাথে sync করে।

*সুবিধা:* Write-ও scale হয়, একটা region down হলে অন্যটা কাজ করে।
*অসুবিধা:* **Write conflict** — দুই জায়গায় একই row একসাথে বদলালে কোনটা সঠিক? এটা সমাধান করা খুব কঠিন। তাই সাধারণত এড়িয়ে চলা হয়, বা multi-region setup-এ খুব সাবধানে ব্যবহার করা হয়।

### Synchronous vs Asynchronous Replication

**Synchronous** — Primary অপেক্ষা করে যতক্ষণ না replica নিশ্চিত করে যে সে-ও লিখেছে, তারপর user-কে success বলে।
- ✅ কোনো data loss নেই, replica সবসময় up-to-date
- ❌ প্রতিটা write ধীর, replica ধীর হলে primary-ও আটকে যায়

**Asynchronous** — Primary নিজে লিখেই success বলে দেয়, replica-তে পরে যায়।
- ✅ Write দ্রুত
- ❌ Primary হঠাৎ মরলে শেষ কয়েকটা write হারাতে পারে; replica সবসময় একটু পিছিয়ে

**Semi-synchronous** — অন্তত একটা replica নিশ্চিত করলেই যথেষ্ট। মাঝামাঝি একটা সমাধান, বেশ জনপ্রিয়।

---

## Replication Lag — সবচেয়ে বড় মাথাব্যথা

Asynchronous replication-এ replica সবসময় primary-র থেকে কিছুটা পিছিয়ে থাকে — মিলিসেকেন্ড থেকে কখনো কয়েক সেকেন্ড।

**এতে কী সমস্যা হয়:**

```
1. User একটা comment লিখলো   → Primary-তে লেখা হলো
2. Page reload হলো            → Replica থেকে পড়া হলো
3. Replica-তে এখনো comment পৌঁছায়নি
4. User দেখে তার comment হাওয়া! 😱
```

এটাকে বলে **"Read your own writes"** problem।

**সমাধান:**

**১. Read-after-write consistency** — User নিজের data পড়ার সময় primary থেকে পড়াও, অন্যের data পড়ার সময় replica থেকে।

**২. Sticky routing** — Write করার পর ওই user-কে কিছুক্ষণ (যেমন ৫ সেকেন্ড) primary-তেই পাঠাও।

**৩. Timestamp/LSN tracking** — User-এর শেষ write-এর position মনে রাখো; replica ওই position পর্যন্ত পৌঁছেছে কিনা দেখে তবেই তাকে ব্যবহার করো।

**৪. Client-side prediction** — UI-তে সাথে সাথে দেখিয়ে দাও (optimistic update), পিছনে server-এ যাক।

**Monitoring:** Replication lag সবসময় নজরে রাখতে হয়। PostgreSQL-এ `pg_stat_replication`, MySQL-এ `SHOW SLAVE STATUS` দিয়ে দেখা যায়।

---

## Failover — Primary মরে গেলে

Primary down হলে দুটো কাজ করতে হয় — কে নতুন primary হবে সেটা ঠিক করা, আর সবাইকে জানানো।

**Manual failover** — মানুষ সিদ্ধান্ত নেয়। নিরাপদ কিন্তু ধীর (মিনিট থেকে ঘণ্টা)।

**Automatic failover** — একটা monitoring system (যেমন Patroni, Orchestrator) primary-কে নজরে রাখে, মরে গেলে সবচেয়ে up-to-date replica-কে promote করে।

**একটা বিপজ্জনক সমস্যা — Split Brain:**
Network সমস্যায় পুরনো primary ভাবছে সে বেঁচে আছে, এদিকে নতুন primary-ও চালু হয়েছে। এখন দুটো primary — দুই জায়গায় আলাদা write হচ্ছে। Data নষ্ট।

*প্রতিরোধ:* **Quorum** — সংখ্যাগরিষ্ঠ node যাকে মানবে সেই primary। ৩ বা ৫টা node রাখা হয় যাতে সবসময় সংখ্যাগরিষ্ঠতা নির্ধারণ করা যায়। আর **fencing** — পুরনো primary-কে জোর করে বন্ধ করে দেওয়া।

---

## Partitioning — এক DB-র ভিতরে টেবিল ভাগ

একটা বিশাল table কে ছোট ছোট টুকরায় ভাগ করা, কিন্তু **একই database server-এ**।

**Horizontal Partitioning (row ভাগ):**

```
orders (১০ কোটি row)
   │
   ├── orders_2023   (২০২৩ সালের সব order)
   ├── orders_2024   (২০২৪ সালের)
   └── orders_2025   (২০২৫ সালের)
```

Query-তে `WHERE created_at >= '2025-01-01'` দিলে database শুধু ২০২৫-এর partition-টাই দেখবে — বাকি ৭ কোটি row ছুঁয়েও দেখবে না। একে বলে **Partition Pruning**।

**Vertical Partitioning (column ভাগ):**

```
users (৫০টা column)
   │
   ├── users_basic    (id, name, email — বারবার লাগে)
   └── users_details  (bio, preferences, settings — কম লাগে)
```

ছোট table মানে একটা disk page-এ বেশি row ধরে, তাই read দ্রুত।

**Partitioning আর Sharding-এর পার্থক্য:**

| | Partitioning | Sharding |
|---|---|---|
| কোথায় | এক server-এর ভিতরে | আলাদা আলাদা server-এ |
| উদ্দেশ্য | Query দ্রুত করা, maintenance সহজ করা | Data ও load ভাগ করে scale করা |
| Complexity | কম, DB নিজেই সামলায় | অনেক বেশি |
| JOIN | স্বাভাবিকভাবে কাজ করে | Cross-shard JOIN কঠিন |

---

## Sharding — অনেক DB-তে data ভাগ

Data-কে একাধিক আলাদা database server-এ ভাগ করে দেওয়া। প্রতিটা server-কে বলে একটা **shard**।

```
                  ┌─────────────────┐
   App ──────────►│  Shard Router   │
                  └────────┬────────┘
          ┌────────────────┼────────────────┐
          ▼                ▼                ▼
      Shard-1          Shard-2          Shard-3
   user_id 1-1M     user_id 1M-2M    user_id 2M-3M
```

**কেন লাগে:** এটাই একমাত্র উপায় **write** কে horizontally scale করার। Replication দিয়ে read scale হয়, কিন্তু write-এর জন্য sharding ছাড়া উপায় নেই।

---

## Sharding Strategy — কোন data কোন shard-এ

### ১. Range-based Sharding

Key-র range অনুযায়ী ভাগ। যেমন `user_id 1–1M` → Shard-1, `1M–2M` → Shard-2।

*সুবিধা:* খুব সহজ। Range query দক্ষভাবে কাজ করে।
*অসুবিধা:* **Hotspot** — নতুন user-রা সবাই শেষ shard-এ যাচ্ছে, সেটাতেই সব load। আবার `A-M`, `N-Z` দিয়ে নাম ভাগ করলে দেখা যাবে A-M-এ অনেক বেশি মানুষ।

### ২. Hash-based Sharding

```
shard = hash(user_id) % number_of_shards
```

*সুবিধা:* Data সমানভাবে ছড়ায়, hotspot হয় না।
*অসুবিধা:* Range query অসম্ভব (সব shard-এ খুঁজতে হবে)। আর সবচেয়ে বড় সমস্যা — **shard সংখ্যা বদলালে প্রায় সব data-র জায়গা বদলে যায়** (কারণ `% 4` আর `% 5` সম্পূর্ণ আলাদা ফল দেয়)। পুরো data migrate করতে হয়।

### ৩. Consistent Hashing

Hash-এর সমস্যাটার সমাধান। Shard যোগ বা বিয়োগ করলে শুধু `1/n` অংশ data সরাতে হয়, সব না।
(বিস্তারিত: `19. Consistent Hashing.md`)

### ৪. Directory-based Sharding

একটা lookup table রাখা হয় — কোন key কোন shard-এ, সেটা সেখানে লেখা।

*সুবিধা:* সবচেয়ে নমনীয়, ইচ্ছেমতো data সরানো যায়।
*অসুবিধা:* Lookup service নিজেই একটা bottleneck ও single point of failure হয়ে যেতে পারে (তাই সেটাকেও replicate করতে হয়)।

### ৫. Geographic Sharding

Region অনুযায়ী ভাগ — ইউরোপের user-দের data ইউরোপে, এশিয়ার data এশিয়ায়।

*সুবিধা:* Latency কম, আর GDPR-এর মতো data residency আইন মানা যায়।
*অসুবিধা:* User দেশ বদলালে ঝামেলা, আর region গুলোর load সমান হয় না।

### Shard Key বাছার নিয়ম

Shard key হলো যে column দেখে ঠিক হবে data কোন shard-এ যাবে। **এটাই sharding-এর সবচেয়ে গুরুত্বপূর্ণ সিদ্ধান্ত** — একবার ভুল করলে ঠিক করা ভয়ানক কষ্টকর।

ভালো shard key-র তিনটা গুণ:

1. **High cardinality** — অনেক আলাদা মান আছে (user_id ভালো, country খারাপ)
2. **Even distribution** — কোনো একটা মানে বেশি data জমে না
3. **Query-র সাথে মেলে** — বেশিরভাগ query-তে এই key থাকে, তাহলে একটা shard-এই কাজ শেষ

*ভালো উদাহরণ:* `user_id`, `tenant_id`
*খারাপ উদাহরণ:* `country` (চীন/ভারতের shard উপচে পড়বে), `created_at` (সব নতুন লেখা এক shard-এ), `status` (মাত্র কয়েকটা মান)

---

## Sharding-এর সমস্যা গুলো

Sharding-কে শেষ অস্ত্র বলার কারণ এগুলোই।

**১. Cross-shard JOIN অসম্ভব**
User Shard-1-এ, তার order Shard-3-এ — SQL JOIN কাজ করবে না। Application-এ দুবার query করে নিজে মেলাতে হবে।

**২. Cross-shard Transaction ভয়াবহ কঠিন**
দুই shard-এ একসাথে atomic update? Two-Phase Commit লাগবে, যা ধীর ও ভঙ্গুর। বাস্তবে **Saga pattern** ব্যবহার করা হয় — ধাপে ধাপে করা, ব্যর্থ হলে উল্টো কাজ করে ফিরিয়ে আনা।

**৩. Resharding দুঃস্বপ্ন**
Shard সংখ্যা বাড়াতে হলে TB-র পর TB data সরাতে হবে, তাও system চালু রেখে। এজন্যই consistent hashing বা প্রয়োজনের চেয়ে বেশি shard আগেই বানিয়ে রাখা (virtual shard) হয়।

**৪. Hotspot / Celebrity problem**
একজন celebrity-র ১০ কোটি follower — তার shard-টাই ভেঙে পড়ে। সমাধান: ওই key-কে আলাদাভাবে সামলানো, বা key-তে random suffix যোগ করা।

**৫. Global query কঠিন**
"মোট কতজন user আছে?" — সব shard-এ query করে যোগ করতে হবে (scatter-gather)। ধীর।

**৬. Unique constraint ও auto-increment ID**
প্রতিটা shard আলাদাভাবে `id = 1` বানাতে পারে — সংঘর্ষ! সমাধান: UUID, বা **Snowflake ID** (timestamp + machine id + sequence), বা কেন্দ্রীয় ID generator।

**৭. Operational জটিলতা**
Backup, monitoring, schema migration — সব ১০ গুণ কঠিন হয়ে যায়।

---

## Federation — Feature অনুযায়ী DB ভাগ

Sharding-এর তুলনায় সহজ একটা বিকল্প — একই table ভাগ না করে, **আলাদা আলাদা feature-এর জন্য আলাদা database**।

```
                 ┌──────────────┐
   App ─────────►│  Users DB    │
       ├────────►│  Products DB │
       ├────────►│  Orders DB   │
       └────────►│  Analytics DB│
                 └──────────────┘
```

*সুবিধা:* প্রতিটা DB ছোট থাকে, আলাদাভাবে scale করা যায়, আর একটা down হলে পুরো system যায় না। Sharding-এর চেয়ে অনেক সহজ।
*অসুবিধা:* এখানেও cross-database JOIN নেই, আর একটা সীমা পর্যন্তই কাজ করে — কোনো একটা feature-ই যদি বিশাল হয়ে যায়, তখন তো সেটাকে আবার shard করতেই হবে।

Microservices architecture-এ এটাই স্বাভাবিক pattern — প্রতিটা service-এর নিজের database (**Database per Service**)।

---

## সব মিলিয়ে একটা বাস্তব Architecture

একটা বড় system-এ সব কৌশল একসাথে ব্যবহার হয়:

```
                        ┌──────────┐
        Users ─────────►│  Cache   │ (Redis — ৯০% read এখানেই শেষ)
                        └────┬─────┘
                             │ miss
                             ▼
                     ┌───────────────┐
                     │ Shard Router  │
                     └───────┬───────┘
            ┌────────────────┼────────────────┐
            ▼                ▼                ▼
      ┌──────────┐     ┌──────────┐     ┌──────────┐
      │ Shard 1  │     │ Shard 2  │     │ Shard 3  │
      │ Primary  │     │ Primary  │     │ Primary  │  ← Write
      └────┬─────┘     └────┬─────┘     └────┬─────┘
           │                │                │
      ┌────▼─────┐     ┌────▼─────┐     ┌────▼─────┐
      │ Replica  │     │ Replica  │     │ Replica  │  ← Read
      │ Replica  │     │ Replica  │     │ Replica  │
      └──────────┘     └──────────┘     └──────────┘
```

প্রতিটা shard-এর ভিতরে আবার primary-replica setup — sharding দিয়ে write scale, replication দিয়ে read scale, cache দিয়ে দুটোরই চাপ কমানো।

---

## Interview Q&A

**প্রশ্ন ১: Database scaling-এর ধাপ গুলো কী কী?**

আমি সবচেয়ে সস্তা সমাধান থেকে শুরু করবো। প্রথমে query optimization আর indexing — অনেক সময় এখানেই সমস্যা মেটে। তারপর caching বসিয়ে repeated read আটকাবো। এরপর vertical scaling, তারপর read replica যোগ করে read traffic ছড়িয়ে দেবো। তারপরও না হলে partitioning, federation, আর সবার শেষে sharding। Sharding সবচেয়ে জটিল, তাই এটা শেষ অস্ত্র।

**প্রশ্ন ২: Replication আর Sharding-এর পার্থক্য?**

Replication মানে একই data-র একাধিক copy রাখা — এতে read scale হয় এবং fault tolerance আসে, কিন্তু প্রতিটা server-এ পুরো data থাকে বলে storage বা write scale হয় না। Sharding মানে data-কে টুকরো করে আলাদা আলাদা server-এ ভাগ করা — এতে write এবং storage দুটোই scale হয়, কিন্তু প্রতিটা shard-এ data-র একটা অংশ থাকায় cross-shard query কঠিন হয়ে যায়। বাস্তবে দুটোই একসাথে ব্যবহার হয় — প্রতিটা shard-এর নিজস্ব replica থাকে।

**প্রশ্ন ৩: Replication lag কী এবং এর সমস্যা কী?**

Asynchronous replication-এ primary নিজে লিখেই success ফেরত দেয়, replica-তে data পৌঁছাতে কিছুটা সময় লাগে — এই দেরিটাই replication lag। এর সবচেয়ে দৃশ্যমান সমস্যা হলো "read your own writes" — user কিছু লিখে page reload করলে replica থেকে পুরনো data পড়ে নিজের লেখা জিনিসটাই দেখতে পায় না। সমাধান হলো write করার পর ওই user-কে কিছুক্ষণ primary থেকে পড়ানো, অথবা replica কোন position পর্যন্ত sync হয়েছে সেটা track করা।

**প্রশ্ন ৪: Shard key কীভাবে বাছবে?**

তিনটা জিনিস দেখতে হবে। প্রথমত high cardinality — অনেক ভিন্ন মান থাকতে হবে, তাহলে data ছড়াবে। দ্বিতীয়ত even distribution — কোনো একটা মানে অস্বাভাবিক বেশি data জমা হওয়া চলবে না, নয়তো hotspot হবে। তৃতীয়ত query pattern-এর সাথে মিল — বেশিরভাগ query-তে যদি shard key থাকে তাহলে একটা shard-এই কাজ শেষ হয়, নাহলে প্রতিটা query সব shard-এ যেতে হবে। যেমন multi-tenant app-এ `tenant_id` ভালো shard key, কিন্তু `country` খারাপ কারণ কিছু দেশে অনেক বেশি user।

**প্রশ্ন ৫: Sharding-এ auto-increment ID কীভাবে handle করবে?**

প্রতিটা shard আলাদা database, তাই সবাই স্বাধীনভাবে `1, 2, 3` তৈরি করলে ID সংঘর্ষ হবে। সমাধান কয়েকটা — UUID ব্যবহার করা (সহজ, কিন্তু বড় আর sorted না বলে index-এ খারাপ), প্রতিটা shard-কে আলাদা range দেওয়া, অথবা Snowflake-এর মতো ID generator ব্যবহার করা যেখানে ID-তে timestamp + machine id + sequence মিলিয়ে বানানো হয় — এটা unique, sorted এবং distributed, তাই সবচেয়ে জনপ্রিয়।

**প্রশ্ন ৬: Cross-shard query কীভাবে handle করবে?**

সবচেয়ে ভালো উপায় হলো এমনভাবে shard key বাছা যাতে বেশিরভাগ query একটা shard-এই সীমাবদ্ধ থাকে। তবু যেসব query একাধিক shard জুড়ে লাগে, সেগুলোর জন্য scatter-gather করতে হয় — সব shard-এ সমান্তরালে query পাঠিয়ে ফলাফল application-এ মিলিয়ে নেওয়া, যা ধীর। বিকল্প হিসেবে aggregate data আলাদা করে denormalized অবস্থায় রাখা যায়, অথবা analytics-এর জন্য সব data একটা data warehouse-এ পাঠানো যায়।

**প্রশ্ন ৭: Split brain কী এবং কীভাবে ঠেকাবে?**

Network partition-এর কারণে যখন দুটো node একসাথে নিজেকে primary ভাবতে শুরু করে, তখন দুই জায়গায় আলাদা write হয় এবং data অসামঞ্জস্যপূর্ণ হয়ে যায় — এটাই split brain। ঠেকানোর মূল উপায় quorum — সংখ্যাগরিষ্ঠ node যাকে সমর্থন করে সেই primary হয়, তাই বিজোড় সংখ্যক (৩ বা ৫) node রাখা হয়। এর সাথে fencing ব্যবহার করা হয় — পুরনো primary-কে জোর করে বন্ধ করে দেওয়া বা তার write ক্ষমতা কেড়ে নেওয়া।

**প্রশ্ন ৮: Partitioning আর Sharding কি একই জিনিস?**

কাছাকাছি, কিন্তু এক না। Partitioning সাধারণত একই database server-এর ভিতরে একটা বড় table-কে ছোট ছোট অংশে ভাগ করা — উদ্দেশ্য query দ্রুত করা আর maintenance সহজ করা; database নিজেই এটা সামলায় এবং JOIN স্বাভাবিকভাবে কাজ করে। Sharding মানে সেই ভাগগুলোকে আলাদা আলাদা server-এ রাখা — উদ্দেশ্য load আর storage ভাগ করা; এখানে routing, cross-shard query, resharding সব application বা middleware-কে সামলাতে হয়। সহজভাবে বলা যায় sharding হলো একাধিক machine জুড়ে করা horizontal partitioning।

**প্রশ্ন ৯: Read replica ব্যবহার করলে কোন কোন বিষয়ে সাবধান থাকতে হবে?**

মূল বিষয় হলো replica সবসময় কিছুটা পিছিয়ে থাকে, তাই যেখানে সদ্য লেখা data অবশ্যই দেখাতে হবে সেখানে replica ব্যবহার করা যাবে না — যেমন checkout করার পরপরই order দেখানো। এছাড়া replica-র সংখ্যা বাড়ালে primary-র উপর replication-এর চাপও বাড়ে, তাই অসীম replica যোগ করা যায় না। আর application-এ read আর write query আলাদা করে routing করার ব্যবস্থা রাখতে হয়, যা ORM-এ সবসময় সহজ না।

---

## এক কথায় মনে রাখো

**Replication** = একই data-র copy। **Read** scale করে, fault tolerance দেয়।

**Sharding** = আলাদা data আলাদা server-এ। **Write** scale করে, কিন্তু অনেক জটিলতা আনে।

ক্রমটা মনে রাখো: **Index → Cache → Vertical → Replica → Partition → Federation → Shard**।

Sharding-এর আগে দুবার ভাবো — আর shard key একবার ভুল বাছলে সেটা ঠিক করতে বছর লেগে যায়।
