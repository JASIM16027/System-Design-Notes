# CDN — Content Delivery Network

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [CDN কী? — কুরিয়ার সার্ভিসের গল্প](#cdn-কী--কুরিয়ার-সার্ভিসের-গল্প)
* [CDN ছাড়া আর CDN সহ — পার্থক্যটা দেখো](#cdn-ছাড়া-আর-cdn-সহ--পার্থক্যটা-দেখো)
* [CDN কীভাবে কাজ করে — ধাপে ধাপে](#cdn-কীভাবে-কাজ-করে--ধাপে-ধাপে)
* [Push CDN vs Pull CDN](#push-cdn-vs-pull-cdn)
* [কী কী CDN-এ রাখবে](#কী-কী-cdn-এ-রাখবে)
* [Cache Control Header — CDN-কে নির্দেশ দেওয়া](#cache-control-header--cdn-কে-নির্দেশ-দেওয়া)
* [Cache Invalidation — CDN-এর data পুরনো হলে](#cache-invalidation--cdn-এর-data-পুরনো-হলে)
* [CDN-এর অন্যান্য সুবিধা](#cdn-এর-অন্যান্য-সুবিধা)
* [Edge Computing — CDN-এর পরবর্তী ধাপ](#edge-computing--cdn-এর-পরবর্তী-ধাপ)
* [জনপ্রিয় CDN গুলো](#জনপ্রিয়-cdn-গুলো)
* [Interview Q&A](#interview-qa)
* [এক কথায় মনে রাখো](#এক-কথায়-মনে-রাখো)

---

<!-- TOC END -->

## CDN কী? — কুরিয়ার সার্ভিসের গল্প

ধরো একটা কোম্পানির গুদাম শুধু ঢাকায়। চট্টগ্রামের একজন কিছু order করলে ঢাকা থেকে পাঠাতে ২ দিন লাগে। সিলেটের জন্যও তাই।

এখন কোম্পানি যদি চট্টগ্রাম, সিলেট, রাজশাহীতেও ছোট ছোট গুদাম রাখে — যেখানে জনপ্রিয় জিনিসগুলো আগে থেকেই রাখা আছে — তাহলে ২ দিনের বদলে ২ ঘণ্টায় ডেলিভারি হবে।

এই ছোট গুদামগুলোই **CDN-এর Edge Server**, আর মূল গুদামটা হলো **Origin Server**।

**CDN মানে** — পৃথিবীর বিভিন্ন জায়গায় ছড়ানো server-এর একটা নেটওয়ার্ক, যেখানে তোমার website-এর static content (image, CSS, JS, video) copy করে রাখা হয়, যাতে user তার সবচেয়ে কাছের server থেকে দ্রুত জিনিসটা পায়।

---

## CDN ছাড়া আর CDN সহ — পার্থক্যটা দেখো

**CDN ছাড়া:**

```
ঢাকার user  ──── 8,000 km ────►  Origin Server (Virginia, USA)
                                         │
             ◄──── 250ms ────────────────┘
```

প্রতিটা image, প্রতিটা CSS file-এর জন্য আলো-র গতিতেও ২৫০ms লাগবে। ২০টা image মানে পেজ load হতে কয়েক সেকেন্ড।

**CDN সহ:**

```
ঢাকার user ──20ms──► Edge Server (Singapore/Dhaka)
                            │
                            └─ না পেলে ──► Origin (Virginia), তারপর cache করে রাখে
```

প্রথম user-এর জন্য একটু দেরি (cache miss), কিন্তু ঢাকার বাকি সব user-এর জন্য ২০ms।

**একটা বাস্তব হিসাব:** একটা page-এ ৫০টা resource আছে। CDN ছাড়া গড়ে ২০০ms করে = অনেকটা সময়। CDN সহ ২০ms করে — page load time প্রায় **৫-১০ গুণ দ্রুত**।

Amazon-এর একটা বিখ্যাত হিসাব — প্রতি **১০০ms** extra latency-তে তাদের বিক্রি **১%** কমে যায়। তাই CDN শুধু technical বিষয় না, ব্যবসারও বিষয়।

---

## CDN কীভাবে কাজ করে — ধাপে ধাপে

ধরো user `https://cdn.example.com/logo.png` চাইলো।

**ধাপ ১ — DNS Resolution (Anycast)**
User-এর DNS query CDN-এর DNS server-এ যায়। CDN দেখে user কোথা থেকে আসছে, আর তার সবচেয়ে কাছের edge server-এর IP ফেরত দেয়। ঢাকার user পাবে Singapore edge-এর IP, লন্ডনের user পাবে London edge-এর IP — **একই domain, ভিন্ন IP**।

**ধাপ ২ — Edge Server-এ request**
User এখন সরাসরি ওই edge server-এ request পাঠায়।

**ধাপ ৩ — Cache Hit বা Miss**

- **Hit:** File edge-এ আছে → সাথে সাথে ফেরত। ⚡ (২০ms)
- **Miss:** File নেই → edge server নিজে origin server থেকে আনে, নিজের কাছে copy রাখে, তারপর user-কে দেয়। (প্রথমবার ২৫০ms, পরেরবার থেকে ২০ms)

**ধাপ ৪ — পরের সব request**
একই region-এর অন্য user-রা এখন cache থেকেই পাবে। Origin server-এ আর কোনো চাপ নেই।

**Response header দেখে বোঝা যায়:**
```
X-Cache: HIT from edge-singapore
Age: 3421                      ← কতক্ষণ ধরে cache-এ আছে
Cache-Control: max-age=31536000
```

---

## Push CDN vs Pull CDN

**Pull CDN (সবচেয়ে common)**

CDN নিজে থেকে origin থেকে file টেনে আনে, যখন প্রথম কেউ চায়।

- *সুবিধা:* কোনো setup লাগে না, শুধু URL বদলাও। যা কেউ চায় না তা storage-ও নেয় না। Origin update করলে TTL শেষে নিজে থেকেই নতুনটা আসে।
- *অসুবিধা:* প্রতিটা region-এর প্রথম user slow পায় (cold cache)।
- *কখন:* সাধারণ website, blog, e-commerce — প্রায় সব ক্ষেত্রেই।

**Push CDN**

তুমি নিজে file গুলো আগে থেকে CDN-এ upload করে দাও।

- *সুবিধা:* কোনো cold start নেই, প্রথম user-ও দ্রুত পায়। Origin-এ traffic-ই আসে না।
- *অসুবিধা:* Upload আর sync নিজে manage করতে হয়। যা কেউ দেখবে না, তার জন্যও storage খরচ।
- *কখন:* বড় video file, software installer, game asset — যেখানে file কম কিন্তু আকারে বিশাল এবং কবে release হবে জানা।

---

## কী কী CDN-এ রাখবে

**অবশ্যই রাখবে (Static content):**
- Image, icon, logo
- CSS, JavaScript file
- Font
- Video, audio
- PDF, downloadable file

**রাখা যায় (একটু চিন্তা করে):**
- HTML page — যদি সবার জন্য একই হয় (blog post, landing page)
- API response — যদি personalized না হয় (product list, public data), ছোট TTL দিয়ে

**রাখবে না:**
- Personalized content (user-এর dashboard, cart)
- যেসব response-এ auth token/cookie-নির্ভর data আছে
- Real-time data (live score — যদি না ১-২ সেকেন্ডের TTL দাও)
- POST/PUT/DELETE request-এর response

**একটা বড় বিপদ:** ভুল করে personalized page cache করে ফেললে একজন user-এর private data অন্যজন দেখে ফেলতে পারে। তাই authenticated response-এ সবসময় `Cache-Control: private, no-store` দাও।

---

## Cache Control Header — CDN-কে নির্দেশ দেওয়া

CDN কতক্ষণ file রাখবে, সেটা তুমি HTTP header দিয়ে বলে দাও।

```
Cache-Control: public, max-age=31536000, immutable
```

| নির্দেশ | মানে |
|---|---|
| `public` | যে কেউ (CDN, proxy, browser) cache করতে পারে |
| `private` | শুধু browser cache করবে, CDN করবে না |
| `no-cache` | Cache করা যাবে, কিন্তু ব্যবহারের আগে origin-এ যাচাই করতে হবে |
| `no-store` | একদমই cache করা যাবে না (sensitive data) |
| `max-age=3600` | ৩৬০০ সেকেন্ড (১ ঘণ্টা) fresh থাকবে |
| `s-maxage=7200` | CDN-এর জন্য আলাদা TTL (browser-এর থেকে ভিন্ন) |
| `immutable` | এই file কখনো বদলাবে না, যাচাই করারও দরকার নেই |
| `stale-while-revalidate=60` | পুরনোটা দেখাও, পিছনে নতুনটা এনে রাখো |

**বাস্তব কৌশল — Cache Busting:**

Static file-এর জন্য এক বছরের TTL দাও, কিন্তু file-এর নামে version/hash রাখো:

```
app.a3f8b2c1.js     ← code বদলালে নাম বদলাবে
style.9d4e7f.css
```

এতে দুটো লাভ একসাথে — file গুলো চিরকাল cache-এ থাকে (দ্রুত), আর deploy করলেই নতুন নাম হওয়ায় user সাথে সাথে নতুনটা পায় (fresh)। প্রায় সব modern build tool (Webpack, Vite) এটা automatically করে।

আর HTML file-এ ছোট TTL বা `no-cache` দাও — কারণ HTML-ই নতুন file-এর নাম গুলো বহন করে।

---

## Cache Invalidation — CDN-এর data পুরনো হলে

**১. Purge / Invalidate** — CDN-কে API দিয়ে বলে দাও নির্দিষ্ট file মুছে ফেলতে।
```
POST /purge  { "files": ["/images/logo.png"] }
```
*সমস্যা:* সব edge server-এ ছড়াতে কয়েক সেকেন্ড থেকে কয়েক মিনিট লাগে। বেশি purge করলে খরচও লাগে।

**২. Versioned URL (সবচেয়ে ভালো)** — purge-এর দরকারই নেই, নতুন নাম মানেই নতুন file।
```
/logo.png?v=2      বা      /logo.a3f8b2.png
```

**৩. TTL শেষ হওয়ার অপেক্ষা** — সবচেয়ে সহজ, কিন্তু ততক্ষণ পুরনো data দেখাবে।

**পরামর্শ:** Version-based URL কে default বানাও, purge কে জরুরি অবস্থার জন্য রাখো।

---

## CDN-এর অন্যান্য সুবিধা

CDN শুধু গতি বাড়ায় না — আরো অনেক কাজ করে।

**১. Origin-এর load কমায়** — ৯০%+ traffic edge-এই শেষ হয়ে যায়, origin server অনেক হালকা থাকে। মানে কম server, কম খরচ।

**২. DDoS Protection** — Attack-এর traffic পৃথিবীর শত শত edge server-এ ছড়িয়ে যায়, তোমার origin পর্যন্ত পৌঁছায়ই না। Cloudflare-এর সবচেয়ে জনপ্রিয় feature-ই এটা।

**৩. Bandwidth খরচ কমায়** — Cloud provider-এর egress bandwidth অনেক দামি; CDN-এর তুলনামূলক সস্তা।

**৪. HTTPS সহজ করে** — CDN নিজেই free SSL certificate দেয় এবং renew করে।

**৫. Compression ও Optimization** — Gzip/Brotli compression, image কে WebP/AVIF-এ রূপান্তর, minification — সব edge-এ করে দেয়।

**৬. Availability বাড়ায়** — Origin কিছুক্ষণের জন্য down থাকলেও CDN cached version দেখাতে থাকে। User টেরও পায় না।

**৭. WAF (Web Application Firewall)** — SQL injection, XSS-এর মতো attack edge-এই আটকে দেয়।

---

## Edge Computing — CDN-এর পরবর্তী ধাপ

আগে CDN শুধু file রাখতো। এখন CDN-এর edge server-এ **code চালানো** যায়।

**Cloudflare Workers, AWS Lambda@Edge, Vercel Edge Functions** — এগুলো তোমার JavaScript function কে পৃথিবীর ৩০০+ শহরে চালায়।

**কী কাজে লাগে:**
- A/B testing — user-এর কাছেই সিদ্ধান্ত নেওয়া
- Authentication check — origin পর্যন্ত যাওয়ার আগেই token যাচাই
- Personalization — user-এর দেশ অনুযায়ী ভাষা/মুদ্রা বদলে দেওয়া
- Image resize — চাহিদা অনুযায়ী edge-এ image ছোট করা
- Redirect, header manipulation

**লাভটা কী:** Origin-এ ২৫০ms round trip-ই লাগলো না, কাজটা user-এর ২০ms দূরত্বে হয়ে গেলো।

---

## জনপ্রিয় CDN গুলো

| CDN | বিশেষত্ব |
|---|---|
| **Cloudflare** | Free tier আছে, দুর্দান্ত DDoS protection, Workers |
| **AWS CloudFront** | AWS-এর সব service-এর সাথে মিশে যায় |
| **Akamai** | সবচেয়ে পুরনো ও বড় network, enterprise-এর পছন্দ |
| **Fastly** | তাৎক্ষণিক purge (মিলিসেকেন্ডে), developer-friendly |
| **Google Cloud CDN** | GCP-এর সাথে integrated |
| **Bunny CDN** | সস্তা, ছোট project-এর জন্য জনপ্রিয় |

---

## Interview Q&A

**প্রশ্ন ১: CDN কী এবং কেন ব্যবহার করা হয়?**

CDN হলো পৃথিবীর বিভিন্ন জায়গায় ছড়ানো server-এর একটা নেটওয়ার্ক যা website-এর static content-এর copy রাখে। User-এর সবচেয়ে কাছের server থেকে content দেওয়া হয় বলে network latency অনেক কমে যায়। পাশাপাশি origin server-এর load কমে, bandwidth খরচ কমে, আর DDoS protection পাওয়া যায়।

**প্রশ্ন ২: CDN কীভাবে জানে কোন edge server user-এর সবচেয়ে কাছে?**

মূলত DNS আর Anycast routing দিয়ে। User যখন CDN-এর domain resolve করে, CDN-এর DNS server তার IP দেখে ভৌগোলিক অবস্থান আন্দাজ করে সবচেয়ে কাছের edge-এর IP ফেরত দেয়। Anycast-এ আবার একাধিক edge server একই IP address ঘোষণা করে, আর internet-এর routing protocol (BGP) স্বাভাবিকভাবেই packet কে network-এর হিসেবে সবচেয়ে কাছের server-এ পাঠিয়ে দেয়।

**প্রশ্ন ৩: Push আর Pull CDN-এর পার্থক্য?**

Pull CDN-এ content আগে থেকে upload করতে হয় না — প্রথম request এলে CDN নিজেই origin থেকে টেনে এনে cache করে। Push CDN-এ তুমি নিজে আগেভাগে content upload করে দাও। Pull সহজ এবং প্রায় সব website এটাই ব্যবহার করে; Push তখন ভালো যখন file বিশাল (video, installer) এবং প্রথম user-কেও দ্রুত দিতে হবে।

**প্রশ্ন ৪: Dynamic content কি CDN-এ cache করা যায়?**

সাধারণত না, কারণ প্রতিটা user-এর জন্য response আলাদা। তবে কিছু কৌশল আছে — যে dynamic content সবার জন্য এক (যেমন public product list) তা ছোট TTL দিয়ে cache করা যায়; `stale-while-revalidate` দিয়ে পুরনোটা দেখিয়ে পিছনে refresh করা যায়; আর edge computing দিয়ে personalization-এর কাজটাই edge-এ করে ফেলা যায়। তবে user-specific authenticated content কখনোই public cache করা উচিত না — এটা মারাত্মক security bug।

**প্রশ্ন ৫: CDN-এ content update করবে কীভাবে?**

তিনটা উপায়। সবচেয়ে ভালো হলো versioned URL — file-এর নামে hash রাখা (`app.a3f8b2.js`), তাহলে নতুন deploy মানেই নতুন URL, purge-এর দরকারই নেই। দ্বিতীয়ত CDN-এর purge API দিয়ে নির্দিষ্ট file invalidate করা, তবে সব edge-এ ছড়াতে সময় লাগে। তৃতীয়ত শুধু TTL শেষ হওয়ার অপেক্ষা করা। Production-এ সাধারণত static asset-এ এক বছরের TTL + versioned নাম, আর HTML-এ খুব ছোট TTL রাখা হয়।

**প্রশ্ন ৬: CDN আর Load Balancer-এর পার্থক্য?**

Load balancer একটা data center-এর ভিতরে একাধিক server-এর মধ্যে traffic ভাগ করে — লক্ষ্য load distribution আর availability। CDN পৃথিবীজুড়ে ছড়ানো এবং content-এর copy রাখে — লক্ষ্য user-এর কাছাকাছি থেকে দ্রুত content দেওয়া। CDN নিজেও ভিতরে load balancing করে, কিন্তু এর মূল কাজ geographic caching, traffic distribution না।

**প্রশ্ন ৭: CDN কীভাবে DDoS আটকায়?**

Attack traffic তোমার একটা origin server-এ না গিয়ে CDN-এর শত শত edge server-এ ছড়িয়ে পড়ে, ফলে কোনো একটা জায়গায় load কেন্দ্রীভূত হয় না। CDN provider-দের মোট bandwidth capacity বিশাল (কয়েক Tbps), তাই তারা attack শুষে নিতে পারে। তাছাড়া edge-এ rate limiting, bot detection আর WAF থাকে যা সন্দেহজনক traffic origin পর্যন্ত পৌঁছানোর আগেই বাদ দিয়ে দেয়।

**প্রশ্ন ৮: CDN ব্যবহারে কোনো অসুবিধা আছে?**

আছে। খরচ বাড়ে (যদিও bandwidth সাশ্রয়ে অনেক সময় পুষিয়ে যায়)। Cache invalidation-এ দেরি হওয়ায় user কিছুক্ষণ পুরনো content দেখতে পারে। Debugging কঠিন হয় — সমস্যাটা তোমার code-এ না CDN-এর cache-এ বোঝা মুশকিল। ভুল configuration-এ private content public cache-এ চলে যেতে পারে, যা মারাত্মক। আর CDN নিজেই একটা external dependency — তারা down হলে তোমার site-ও প্রভাবিত হয়।

---

## এক কথায় মনে রাখো

CDN = **user-এর কাছাকাছি content-এর copy রাখা।**

Latency কমে (৮০-৯০%), origin-এর load কমে, DDoS protection ফ্রি পাওয়া যায়।

**Static** সব কিছু CDN-এ দাও, **personalized** কিছু কখনো public cache করো না।

Update-এর সেরা কৌশল — **versioned filename + লম্বা TTL**।
