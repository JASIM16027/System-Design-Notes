# Caching — Strategies, Redis এবং Eviction

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [Cache কী? — রান্নাঘরের গল্প](#cache-কী--রান্নাঘরের-গল্প)
* [কেন Cache এত গুরুত্বপূর্ণ — সংখ্যা দিয়ে দেখো](#কেন-cache-এত-গুরুত্বপূর্ণ--সংখ্যা-দিয়ে-দেখো)
* [Cache কোথায় কোথায় থাকে](#cache-কোথায়-কোথায়-থাকে)
* [Cache Hit, Miss আর Hit Ratio](#cache-hit-miss-আর-hit-ratio)
* [Caching Strategies — ৫টা প্যাটার্ন](#caching-strategies--৫টা-প্যাটার্ন)
* [Cache Eviction Policy — জায়গা শেষ হলে কাকে বের করবে](#cache-eviction-policy--জায়গা-শেষ-হলে-কাকে-বের-করবে)
* [TTL — Cache-এর আয়ু](#ttl--cache-এর-আয়ু)
* [Cache Invalidation — সবচেয়ে কঠিন সমস্যা](#cache-invalidation--সবচেয়ে-কঠিন-সমস্যা)
* [তিনটা বিখ্যাত Cache Problem](#তিনটা-বিখ্যাত-cache-problem)
* [Redis vs Memcached](#redis-vs-memcached)
* [Redis-এর Data Structure গুলো](#redis-এর-data-structure-গুলো)
* [কী cache করবে, কী করবে না](#কী-cache-করবে-কী-করবে-না)
* [Interview Q&A](#interview-qa)
* [এক কথায় মনে রাখো](#এক-কথায়-মনে-রাখো)

---

<!-- TOC END -->

## Cache কী? — রান্নাঘরের গল্প

তুমি রান্না করছো। লবণ লাগবে।

**Cache নেই এমন অবস্থা:** প্রতিবার লবণ লাগলে তুমি বাজারে যাও, লবণ কিনে আনো, এক চিমটি দাও, বাকিটা বাজারে ফেরত দিয়ে আসো। পরের বার আবার বাজারে যাও।

**Cache আছে এমন অবস্থা:** তুমি একবার লবণ কিনে চুলার পাশে একটা কৌটায় রাখো। যতবার লাগে, হাত বাড়ালেই পাও।

এটাই cache — **যে জিনিস বারবার লাগে, সেটা কাছে রেখে দাও।**

Software-এ database হলো বাজার (দূরে, ধীর), আর Redis হলো চুলার পাশের কৌটা (কাছে, দ্রুত)।

```
কোনো cache নেই:
  App ──────► Database    (প্রতিবার, 50ms)

Cache আছে:
  App ──► Redis ──► (পেলে ফেরত, 1ms)
           │
           └─ না পেলে ──► Database (50ms), তারপর Redis-এ রেখে দাও
```

---

## কেন Cache এত গুরুত্বপূর্ণ — সংখ্যা দিয়ে দেখো

বিভিন্ন জায়গা থেকে data আনতে কত সময় লাগে, সেটা দেখলেই বুঝবে:

| কোথা থেকে | সময় | তুলনা |
|---|---|---|
| CPU L1 cache | 1 ns | ১ সেকেন্ড ধরলে |
| RAM (memory) | 100 ns | ১.৫ মিনিট |
| SSD | 150,000 ns (0.15 ms) | ১.৭ দিন |
| Network (একই datacenter) | 500,000 ns (0.5 ms) | ৫.৮ দিন |
| HDD disk seek | 10,000,000 ns (10 ms) | ৪ মাস |
| Network (এক মহাদেশ থেকে আরেক) | 150,000,000 ns (150 ms) | ৫ বছর |

মূল কথা — **RAM থেকে পড়া disk থেকে পড়ার চেয়ে হাজার গুণ দ্রুত।** Cache মানেই RAM-এ রাখা।

**একটা বাস্তব হিসাব:** ধরো তোমার API-তে প্রতি সেকেন্ডে ১০,০০০ request আসে এবং প্রতিটাতে ৫০ms-এর DB query লাগে। Cache বসিয়ে যদি 90% hit ratio পাও, তাহলে DB-তে যাচ্ছে মাত্র ১,০০০ request — **DB-র load ১০ গুণ কমে গেলো**, আর গড় response time নামলো প্রায় ৬ms-এ।

---

## Cache কোথায় কোথায় থাকে

Cache শুধু Redis না। User-এর browser থেকে database পর্যন্ত প্রতিটা স্তরে cache থাকে।

```
1. Browser Cache          ← User-এর কম্পিউটারে (0 network call)
        ↓
2. CDN Cache              ← User-এর কাছের edge server-এ
        ↓
3. Load Balancer / Proxy Cache
        ↓
4. Application Cache      ← App server-এর নিজের memory-তে (in-process)
        ↓
5. Distributed Cache      ← Redis / Memcached (সব server share করে)
        ↓
6. Database Query Cache   ← DB-র নিজের cache
        ↓
7. Disk
```

**In-process cache (App-এর memory)** — সবচেয়ে দ্রুত (network call-ই নেই), কিন্তু প্রতিটা server-এর আলাদা copy, তাই inconsistency হতে পারে আর server restart-এ হারিয়ে যায়।

**Distributed cache (Redis)** — একটু ধীর (network call লাগে), কিন্তু সব server একই data দেখে, আর server restart-এ হারায় না। বেশিরভাগ ক্ষেত্রে এটাই ব্যবহার হয়।

---

## Cache Hit, Miss আর Hit Ratio

**Cache Hit** — যা চেয়েছিলে তা cache-এ পাওয়া গেছে। ✅ দ্রুত।

**Cache Miss** — cache-এ নেই, database-এ যেতে হবে। ❌ ধীর।

**Hit Ratio = Hits ÷ (Hits + Misses)**

উদাহরণ: ১০০টা request-এর মধ্যে ৯০টা cache থেকে পাওয়া গেলে hit ratio = 90%।

**ভালো hit ratio কত?** সাধারণত **80% এর উপরে** ভালো ধরা হয়, 95%+ হলে চমৎকার। 50%-এর নিচে হলে বুঝতে হবে হয় ভুল জিনিস cache করছো, নয়তো TTL খুব কম, নয়তো cache-এর size ছোট।

---

## Caching Strategies — ৫টা প্যাটার্ন

এটা interview-এর সবচেয়ে গুরুত্বপূর্ণ অংশ।

### ১. Cache-Aside (Lazy Loading) — সবচেয়ে বেশি ব্যবহৃত

Application নিজে cache manage করে।

```
পড়ার সময়:
  1. Cache-এ খোঁজো
  2. পেলে → ফেরত দাও (HIT)
  3. না পেলে → DB থেকে পড়ো → Cache-এ লিখে রাখো → ফেরত দাও (MISS)

লেখার সময়:
  1. DB-তে লিখো
  2. Cache থেকে ওই key delete করো (invalidate)
```

```javascript
async function getUser(id) {
  let user = await redis.get(`user:${id}`);
  if (user) return JSON.parse(user);           // HIT

  user = await db.query('SELECT * FROM users WHERE id = ?', [id]);  // MISS
  await redis.setex(`user:${id}`, 3600, JSON.stringify(user));
  return user;
}
```

*সুবিধা:* শুধু যা চাওয়া হয় তাই cache হয়, তাই memory নষ্ট হয় না। Cache down হলেও system চলে (শুধু ধীর হয়)।
*অসুবিধা:* প্রথম request সবসময় slow (cache miss)। Data একটু পুরনো হতে পারে।

### ২. Read-Through — Cache নিজেই DB থেকে আনে

Application শুধু cache-এর সাথে কথা বলে। Cache-এ না থাকলে cache নিজেই DB থেকে এনে দেয়।

*সুবিধা:* Application-এর code সহজ, caching logic আলাদা layer-এ।
*অসুবিধা:* Cache library/provider-কে DB চিনতে হয়। প্রথম request তবু slow।

### ৩. Write-Through — লেখার সময়েই cache-এ লিখো

```
লেখার সময়:  App → Cache-এ লিখো → Cache সাথে সাথে DB-তে লিখে → তারপর success
```

*সুবিধা:* Cache আর DB সবসময় sync-এ থাকে, তাই stale data-র ঝুঁকি নেই।
*অসুবিধা:* প্রতিটা write ধীর (দুই জায়গায় লিখতে হয়)। যে data কখনো পড়াই হবে না, সেটাও cache-এ জায়গা নেয়।

### ৪. Write-Behind (Write-Back) — পরে DB-তে লিখো

```
লেখার সময়:  App → Cache-এ লিখো → সাথে সাথে success ফেরত
             Cache পরে (batch করে) DB-তে লিখে দেয়
```

*সুবিধা:* Write অসম্ভব দ্রুত। অনেকগুলো write একসাথে batch করা যায়, DB-র উপর চাপ কম।
*অসুবিধা:* **DB-তে লেখার আগে cache crash করলে data হারিয়ে যাবে।** তাই critical data-র জন্য বিপজ্জনক।
*কোথায় ব্যবহার হয়:* View count, like count, analytics — যেখানে দু-একটা data হারালেও বিরাট ক্ষতি নেই।

### ৫. Write-Around — Cache কে পাশ কাটিয়ে যাও

লেখার সময় সরাসরি DB-তে লিখো, cache-এ কিছু করো না। পরে যখন কেউ পড়বে, তখন cache-এ ঢুকবে।

*কখন ভালো:* যে data লেখা হয় অনেক কিন্তু পড়া হয় কম (যেমন log data)। এতে cache অপ্রয়োজনীয় data-য় ভরে যায় না।

### সারসংক্ষেপ টেবিল

| Strategy | Read speed | Write speed | Consistency | কখন ব্যবহার |
|---|---|---|---|---|
| Cache-Aside | দ্রুত (hit-এ) | স্বাভাবিক | মোটামুটি | সাধারণ read-heavy app (সবচেয়ে common) |
| Read-Through | দ্রুত (hit-এ) | স্বাভাবিক | মোটামুটি | Code simple রাখতে চাইলে |
| Write-Through | দ্রুত | ধীর | খুব ভালো | Data consistency critical |
| Write-Behind | দ্রুত | খুব দ্রুত | দুর্বল | Counter, analytics, high write |
| Write-Around | প্রথমবার ধীর | স্বাভাবিক | ভালো | Write বেশি, read কম |

---

## Cache Eviction Policy — জায়গা শেষ হলে কাকে বের করবে

Cache-এর memory সীমিত। ভরে গেলে নতুন জিনিস রাখতে পুরনো কিছু ফেলতে হয়। কাকে ফেলবে?

**LRU (Least Recently Used)** — যেটা সবচেয়ে অনেকক্ষণ আগে ব্যবহার হয়েছে, সেটা বাদ। **সবচেয়ে জনপ্রিয়** এবং বেশিরভাগ ক্ষেত্রে সেরা।

**LFU (Least Frequently Used)** — যেটা সবচেয়ে কম বার ব্যবহার হয়েছে, সেটা বাদ। যে জিনিস একদিন খুব জনপ্রিয় ছিল কিন্তু এখন না — LFU সেটা ধরে রাখে, এটাই এর দুর্বলতা।

**FIFO (First In First Out)** — যেটা সবার আগে ঢুকেছে সেটা আগে বাদ। সহজ, কিন্তু জনপ্রিয় জিনিসও বাদ পড়ে যায়।

**TTL-based** — যার সময় শেষ, সে বাদ।

**Random** — এলোমেলো একটা বাদ। অবাক লাগলেও, খুব বড় cache-এ এটা LRU-র প্রায় সমান কাজ করে এবং অনেক সস্তা।

**Redis-এর policy গুলো:**

| Policy | কী করে |
|---|---|
| `noeviction` | কিছু বাদ দেবে না, নতুন write-এ error দেবে (default) |
| `allkeys-lru` | সব key-র মধ্যে LRU — **সাধারণ cache-এর জন্য এটাই recommended** |
| `volatile-lru` | শুধু যাদের TTL সেট করা আছে তাদের মধ্যে LRU |
| `allkeys-lfu` | সব key-র মধ্যে LFU |
| `allkeys-random` | এলোমেলো বাদ |
| `volatile-ttl` | যার TTL সবচেয়ে কাছে, সে আগে বাদ |

---

## TTL — Cache-এর আয়ু

TTL (Time To Live) মানে data কতক্ষণ cache-এ থাকবে।

```
SET user:123 "{...}" EX 3600     ← ১ ঘণ্টা পর নিজে থেকে মুছে যাবে
```

**TTL কত রাখবে?** এটা একটা tradeoff —

- **ছোট TTL (৩০ সেকেন্ড – ৫ মিনিট)** — data বেশি fresh, কিন্তু hit ratio কম, DB-তে চাপ বেশি
- **বড় TTL (১ ঘণ্টা – ১ দিন)** — hit ratio বেশি, DB-তে চাপ কম, কিন্তু stale data-র ঝুঁকি

**Data-ভিত্তিক গাইডলাইন:**

| Data-র ধরন | পরামর্শকৃত TTL |
|---|---|
| Stock price, live score | ১–৫ সেকেন্ড |
| News feed, timeline | ১–৫ মিনিট |
| User profile | ১–২৪ ঘণ্টা |
| Product catalog | কয়েক ঘণ্টা |
| Country list, config | কয়েক দিন / TTL-ই লাগবে না |

**একটা জরুরি টিপ:** TTL-এ সবসময় একটু **random jitter** যোগ করো — `3600 + random(0, 300)`। কারণ ঠিক একই সময়ে হাজারটা key expire করলে সবাই একসাথে DB-তে ঝাঁপিয়ে পড়ে। এটাকে বলে cache stampede।

---

## Cache Invalidation — সবচেয়ে কঠিন সমস্যা

> "There are only two hard things in Computer Science: cache invalidation and naming things." — Phil Karlton

**সমস্যাটা কী:** DB-তে data বদলে গেছে, কিন্তু cache-এ পুরনোটাই পড়ে আছে। User পুরনো data দেখছে।

**সমাধানের ৩টা উপায়:**

**১. TTL দিয়ে (Time-based)** — সবচেয়ে সহজ। ধরেই নাও কিছুক্ষণ পুরনো data দেখালে সমস্যা নেই। বেশিরভাগ ক্ষেত্রে এটাই যথেষ্ট।

**২. Write-এ delete (Explicit invalidation)** — DB update করার সাথে সাথে cache key delete করো।

```javascript
await db.updateUser(id, data);
await redis.del(`user:${id}`);        // cache মুছে দাও
```
*সতর্কতা:* Update আর delete-এর মাঝে অন্য কেউ পড়লে পুরনো data পাবে। তাই **আগে DB update, পরে cache delete** — এই ক্রমটা গুরুত্বপূর্ণ।

**৩. Versioned Key** — Key-র মধ্যেই version রাখো।
```
user:123:v5   ← update হলে v6 হয়ে যাবে, পুরনোটা TTL-এ নিজেই মরবে
```
এতে delete করারই দরকার নেই, race condition-ও নেই।

**কেন Update-এর বদলে Delete করবে?** Cache-এ নতুন value লিখে দিলে দুই server একসাথে লিখলে কোনটা শেষে বসবে তার কোনো নিশ্চয়তা নেই — ভুল value আটকে যেতে পারে। Delete করে দিলে পরের read আপনা-আপনি DB থেকে সঠিক data এনে বসাবে। **Delete is safer than update.**

---

## তিনটা বিখ্যাত Cache Problem

### ১. Cache Stampede (Thundering Herd)

**সমস্যা:** একটা জনপ্রিয় key expire করলো। ঠিক সেই মুহূর্তে ১০,০০০ request এলো। সবাই cache miss পেলো, সবাই একসাথে DB-তে গেলো। **DB crash।**

**সমাধান:**
- **Lock / Mutex** — প্রথম request একটা lock নেয় আর DB-তে যায়, বাকিরা অপেক্ষা করে বা পুরনো value পায়
- **TTL-এ jitter** — সব key একই সময়ে expire না করা
- **Probabilistic early expiry** — TTL শেষ হওয়ার একটু আগেই কিছু request কে refresh করতে পাঠানো
- **Background refresh** — expire হওয়ার আগেই cron দিয়ে refresh করে রাখা

### ২. Cache Penetration

**সমস্যা:** কেউ এমন id চাইছে যা DB-তেই নেই (`user:999999999`)। Cache-এ নেই, DB-তেও নেই, তাই কিছুই cache হয় না। প্রতিবারই DB-তে যায়। কেউ ইচ্ছে করে এভাবে attack করতে পারে।

**সমাধান:**
- **Null cache** — "নেই" এটাও cache করো, ছোট TTL দিয়ে (যেমন ৬০ সেকেন্ড)
- **Bloom Filter** — একটা memory-efficient structure যা দ্রুত বলে দেয় "এই key নিশ্চিতভাবে নেই", তাহলে DB-তে যাওয়ারই দরকার নেই
- Input validation — অস্বাভাবিক id আগেই reject করো

### ৩. Cache Avalanche

**সমস্যা:** একসাথে বিশাল সংখ্যক key expire করলো, বা পুরো Redis server down হয়ে গেলো। সব traffic সরাসরি DB-তে। DB শেষ।

**সমাধান:**
- TTL-এ random jitter (একসাথে expire আটকাতে)
- Redis cluster / replica রাখা, যাতে একটা মরলেও cache থাকে
- Circuit breaker + rate limiting — DB-কে বাঁচাতে কিছু request সরাসরি reject করা
- Multi-level cache — local memory cache কে দ্বিতীয় রক্ষাকবচ হিসেবে রাখা

---

## Redis vs Memcached

| বিষয় | Redis | Memcached |
|---|---|---|
| Data structure | String, List, Set, Hash, Sorted Set, Stream, Bitmap | শুধু key-value string |
| Persistence | আছে (RDB, AOF) — restart-এও data থাকে | নেই — restart মানে সব শেষ |
| Replication | আছে | নেই |
| Cluster | Built-in | Client-side করতে হয় |
| Transaction | আছে (MULTI/EXEC) | নেই |
| Pub/Sub | আছে | নেই |
| Multi-threading | মূলত single-threaded (6.0+ এ I/O threading) | Multi-threaded |
| Memory efficiency | একটু বেশি overhead | সামান্য ভালো |
| Use case | Cache + queue + session + leaderboard + rate limiter | শুধু simple cache |

**সিদ্ধান্ত:** ৯৫% ক্ষেত্রে **Redis** নাও — বেশি feature, প্রায় একই speed। Memcached তখনই ভালো যখন শুধু বিশাল আকারের simple key-value cache লাগবে এবং multi-core CPU-কে পুরোপুরি কাজে লাগাতে চাও।

---

## Redis-এর Data Structure গুলো

Redis শুধু cache না — এই structure গুলোর কারণে এটা অনেক কিছু করতে পারে।

**String** — সাধারণ key-value। Session, counter, JSON blob।
```
SET user:1 "{...}"      GET user:1      INCR page:views
```

**Hash** — একটা object-এর আলাদা আলাদা field।
```
HSET user:1 name "Rahim" age 25       HGET user:1 name
```
*সুবিধা:* পুরো object না এনে শুধু একটা field update/read করা যায়।

**List** — ordered list, দুই দিক থেকেই push/pop করা যায়। Simple queue বানানো যায়।
```
LPUSH queue:jobs "job1"      BRPOP queue:jobs 0
```

**Set** — unique জিনিসের সংগ্রহ, order নেই। "কে কে online", "কোন tag গুলো আছে"।
```
SADD online:users "u1"       SISMEMBER online:users "u1"
```

**Sorted Set (ZSet)** — প্রতিটা member-এর সাথে একটা score, score অনুযায়ী sorted। **Leaderboard-এর জন্য perfect।** Rate limiter-এও ব্যবহার হয়।
```
ZADD leaderboard 500 "rahim"     ZREVRANGE leaderboard 0 9 WITHSCORES
```

**Bitmap** — bit-level operation। "গত ৩০ দিনে user কোন কোন দিন active ছিল" — মাত্র কয়েক byte-এ।

**HyperLogLog** — আনুমানিক unique count, মাত্র 12 KB-তে কোটি কোটি unique item গুনতে পারে (0.81% error সহ)। "আজকে কতজন unique visitor" — এর জন্য দারুণ।

**Stream** — Kafka-র মতো append-only log, consumer group সহ।

---

## কী cache করবে, কী করবে না

**Cache করার জন্য ভালো:**
- যা বারবার পড়া হয় কিন্তু কম বদলায় (product info, user profile, config)
- যা বানাতে খরচ বেশি (complex JOIN, aggregation, report)
- External API-র response (rate limit বাঁচাতে)
- Session data
- Static content (এটা CDN-এ)

**Cache করা উচিত না:**
- যা প্রতিবার বদলায় (real-time stock price — অথবা ১ সেকেন্ডের TTL দাও)
- খুব sensitive data (credit card, password) — অন্তত encryption ছাড়া না
- যা মাত্র একবার পড়া হবে (তাহলে cache-এ রেখে লাভ নেই, শুধু memory নষ্ট)
- যেখানে ১০০% accuracy লাগে (bank balance, inventory-র শেষ item)

**Golden rule:** আগে measure করো। যেটা সত্যিই slow আর সত্যিই বারবার লাগে — শুধু সেটাই cache করো। অন্ধভাবে সব cache করা মানে complexity বাড়ানো আর bug ডেকে আনা।

---

## Interview Q&A

**প্রশ্ন ১: Caching কী এবং কেন ব্যবহার করা হয়?**

Caching হলো বারবার প্রয়োজনীয় data-কে দ্রুত-অ্যাক্সেসযোগ্য জায়গায় (সাধারণত RAM-এ) রেখে দেওয়া, যাতে প্রতিবার ধীরগতির উৎস (database, disk, external API) থেকে আনতে না হয়। এতে দুটো লাভ — response time নাটকীয়ভাবে কমে (৫০ms → ১ms), আর backend-এর load কমে যাওয়ায় একই hardware দিয়ে অনেক বেশি traffic সামলানো যায়।

**প্রশ্ন ২: Cache-Aside আর Write-Through-এর পার্থক্য?**

Cache-Aside-এ application নিজে cache manage করে — পড়ার সময় আগে cache দেখে, না পেলে DB থেকে এনে cache-এ বসায়; লেখার সময় DB-তে লিখে cache key delete করে দেয়। Write-Through-এ লেখার সময় cache আর DB দুটোতেই একসাথে লেখা হয়, তাই cache সবসময় DB-র সাথে sync থাকে কিন্তু প্রতিটা write ধীর হয়। Cache-Aside বেশি common কারণ এটা simple এবং cache down হলেও system চলে।

**প্রশ্ন ৩: Cache invalidation-কে কঠিন বলা হয় কেন?**

কারণ কখন cache মুছবে সেটা ঠিক করা কঠিন। খুব তাড়াতাড়ি মুছলে hit ratio কমে যায় আর cache-এর উদ্দেশ্যই নষ্ট হয়; দেরি করলে user পুরনো data দেখে। তার উপর distributed system-এ একাধিক server একসাথে read/write করছে, তাই DB update আর cache delete-এর মাঝের সময়টুকুতে race condition হতে পারে। এজন্য বেশিরভাগ system TTL + write-এ delete — এই দুটো একসাথে ব্যবহার করে, আর কঠিন ক্ষেত্রে versioned key ব্যবহার করে।

**প্রশ্ন ৪: Cache update করার বদলে delete করা ভালো কেন?**

কারণ concurrent write-এ ভুল value আটকে যেতে পারে। দুটো request একসাথে update করলে কোনটা পরে cache-এ বসবে তার নিশ্চয়তা নেই — একটা পুরনো value শেষে বসে গিয়ে TTL শেষ না হওয়া পর্যন্ত ভুল data দেখাতে পারে। Delete করে দিলে পরের read-টা আপনাআপনি DB থেকে সঠিক data এনে বসাবে।

**প্রশ্ন ৫: Cache stampede কী এবং কীভাবে ঠেকাবে?**

একটা জনপ্রিয় key expire হওয়ার সাথে সাথে হাজার হাজার request একসাথে cache miss পেয়ে সবাই DB-তে ঝাঁপিয়ে পড়ে — এটাই cache stampede বা thundering herd। ঠেকানোর উপায় — TTL-এ random jitter যোগ করা যাতে সব key একসাথে expire না করে, lock ব্যবহার করে শুধু একটা request-কে DB-তে যেতে দেওয়া আর বাকিদের অপেক্ষা করানো, অথবা expire হওয়ার আগেই background job দিয়ে refresh করে রাখা।

**প্রশ্ন ৬: LRU আর LFU-র পার্থক্য?**

LRU (Least Recently Used) বাদ দেয় সেটাকে যেটা সবচেয়ে বেশিক্ষণ আগে ব্যবহার হয়েছে — সময় দেখে। LFU (Least Frequently Used) বাদ দেয় সেটাকে যেটা সবচেয়ে কম বার ব্যবহার হয়েছে — গণনা দেখে। LFU-র সমস্যা হলো পুরনো জনপ্রিয় data আটকে থাকে, কারণ তার count অনেক বেশি যদিও এখন আর কেউ চায় না। LRU সাধারণত বেশি ব্যবহৃত হয় কারণ এটা recency-কে ধরে, যা বেশিরভাগ access pattern-এর সাথে মেলে।

**প্রশ্ন ৭: Redis single-threaded হয়েও এত দ্রুত কীভাবে?**

কারণ Redis-এর সব data RAM-এ থাকে, তাই disk I/O-র জন্য অপেক্ষাই করতে হয় না। Single-threaded হওয়ায় lock বা context switching-এর কোনো overhead নেই, আর এটা epoll-ভিত্তিক non-blocking I/O multiplexing ব্যবহার করে হাজার হাজার connection সামলায়। এর operation গুলোও O(1) বা O(log n)। ফলে Redis সহজেই প্রতি সেকেন্ডে ১ লাখের বেশি operation করতে পারে।

**প্রশ্ন ৮: Redis-এ data persist হয় কীভাবে?**

দুটো উপায় আছে। RDB — নির্দিষ্ট সময় পর পর পুরো dataset-এর snapshot disk-এ লেখা; দ্রুত, কিন্তু দুই snapshot-এর মাঝের data crash-এ হারাতে পারে। AOF (Append Only File) — প্রতিটা write command একটা log file-এ লেখা; বেশি নিরাপদ কিন্তু file বড় হয় আর একটু ধীর। Production-এ সাধারণত দুটোই একসাথে ব্যবহার করা হয়।

**প্রশ্ন ৯: Cache-এর hit ratio কম হলে কী করবে?**

প্রথমে দেখতে হবে সমস্যাটা কোথায়। TTL খুব কম হলে বাড়াতে হবে। Cache-এর memory ছোট হলে key গুলো তাড়াতাড়ি evict হয়ে যাচ্ছে — memory বাড়াতে হবে বা কম গুরুত্বপূর্ণ জিনিস cache করা বন্ধ করতে হবে। Key design ভুল হলে (যেমন প্রতিটা query-র unique key তৈরি হচ্ছে) সেটা ঠিক করতে হবে। আর যদি access pattern সত্যিই random হয় — প্রতিটা user সম্পূর্ণ আলাদা data চায় — তাহলে caching এখানে হয়তো কাজেই আসবে না।

**প্রশ্ন ১০: Cache আর CDN-এর পার্থক্য?**

CDN আসলে এক ধরনের cache, কিন্তু ভৌগোলিকভাবে ছড়ানো এবং মূলত static content (image, CSS, JS, video)-এর জন্য। এটা user-এর কাছাকাছি edge server-এ বসে, তাই network latency কমায়। Redis-এর মতো application cache বসে data center-এর ভিতরে এবং dynamic data (DB query result, session) cache করে, latency কমানোর চেয়ে বেশি লক্ষ্য থাকে database-এর load কমানো।

---

## এক কথায় মনে রাখো

Cache মানে — **যা বারবার লাগে, তা কাছে রাখো।** RAM disk-এর চেয়ে হাজার গুণ দ্রুত।

Strategy বাছার নিয়ম: সাধারণ ক্ষেত্রে **Cache-Aside**, consistency জরুরি হলে **Write-Through**, counter/analytics হলে **Write-Behind**।

Eviction-এ **LRU** নাও, TTL-এ **jitter** দাও, আর invalidate করার সময় **update না করে delete করো**।

তিনটা বিপদ মনে রাখো — **Stampede** (একসাথে expire), **Penetration** (যা নেই তা খোঁজা), **Avalanche** (পুরো cache down)।
