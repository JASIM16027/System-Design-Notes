# OAuth 2.0 — সহজ ভাষায়, কোড সহ

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [সমস্যাটা আগে বুঝি — password দেওয়ার বিপদ](#সমস্যাটা-আগে-বুঝি--password-দেওয়ার-বিপদ)
* [Hotel Key Card Analogy](#hotel-key-card-analogy)
* [চারটি Actor — এদের চেনো আগে](#চারটি-actor--এদের-চেনো-আগে)
* [পুরো Flow — ধাপে ধাপে](#পুরো-flow--ধাপে-ধাপে)
* [💻 কোড — সম্পূর্ণ Authorization Code Flow](#-কোড--সম্পূর্ণ-authorization-code-flow)
* [Authorization Code কেন? সরাসরি Token কেন না?](#authorization-code-কেন-সরাসরি-token-কেন-না)
* [`state` — CSRF আটকানোর পাহারাদার](#state--csrf-আটকানোর-পাহারাদার)
* [PKCE — secret ছাড়া কীভাবে নিরাপদ থাকা যায়](#pkce--secret-ছাড়া-কীভাবে-নিরাপদ-থাকা-যায়)
* [Scope — ঠিক যতটুকু দরকার, ততটুকু](#scope--ঠিক-যতটুকু-দরকার-ততটুকু)
* [Grant Type — চারটা রাস্তা, কোনটা কখন](#grant-type--চারটা-রাস্তা-কোনটা-কখন)
* [তিন রকম Token — কে কীসের জন্য](#তিন-রকম-token--কে-কীসের-জন্য)
* [ID Token যাচাই করা — JWKS দিয়ে](#id-token-যাচাই-করা--jwks-দিয়ে)
* [Refresh Token — নীরবে নতুন token নেওয়া](#refresh-token--নীরবে-নতুন-token-নেওয়া)
* [Token বাতিল ও যাচাই — Revocation ও Introspection](#token-বাতিল-ও-যাচাই--revocation-ও-introspection)
* [OIDC Discovery — সব ঠিকানা এক জায়গায়](#oidc-discovery--সব-ঠিকানা-এক-জায়গায়)
* [SPA বা Mobile App-এ কীভাবে](#spa-বা-mobile-app-এ-কীভাবে)
* [Authentication vs Authorization](#authentication-vs-authorization)
* [⚠️ যে নিরাপত্তা-ভুলগুলো হয়](#️-যে-নিরাপত্তা-ভুলগুলো-হয়)
* [Production Checklist](#production-checklist)
* [ছবিতে পুরো flow](#ছবিতে-পুরো-flow)
* [Interview Q&A](#interview-qa)
* [এক কথায় মনে রাখো](#এক-কথায়-মনে-রাখো)

---

<!-- TOC END -->

## সমস্যাটা আগে বুঝি — password দেওয়ার বিপদ

OAuth আসার আগে ব্যাপারটা কেমন ছিল, সেটা জানলে OAuth কেন দরকার তা পরিষ্কার হয়ে যায়।

ধরো একটা app তোমার Gmail-এর contact list পড়তে চায়। ২০০৭ সালে সে কী করতো?

```
"আপনার Gmail-এর email আর password দিন" 😱
```

তুমি password দিতে, আর app সেটা দিয়ে তোমার হয়ে Gmail-এ login করতো। এর নাম ছিল **password anti-pattern**।

**এতে কী কী ভয়ংকর জিনিস ঘটতো:**

| সমস্যা | মানে |
|---|---|
| App তোমার password জানতো | সে সেটা জমা রাখতো, আর তার database হ্যাক হলে তোমার Gmail-ও গেলো |
| App-এর **সব** ক্ষমতা ছিল | শুধু contact পড়তে চেয়েছিল, কিন্তু email মুছেও দিতে পারতো |
| অনুমতি ফেরত নেওয়ার উপায় ছিল না | App-কে আটকাতে হলে **password বদলাতে হতো** — আর তাতে বাকি সব app-ও ভেঙে যেতো |
| কে কী করছে বোঝার উপায় ছিল না | সব কাজ "তুমিই করেছো" বলে দেখাতো |

**OAuth 2.0 এই চারটা সমস্যারই সমাধান:**

> Password না দিয়ে, **সীমিত ক্ষমতার একটা অস্থায়ী চাবি** দেওয়া — যেটা যেকোনো সময় কেড়ে নেওয়া যায়।

---

## Hotel Key Card Analogy

তুমি একটা হোটেলে উঠলে। রিসেপশন তোমাকে একটা key card দিলো।

```
┌────────────────────────────────┐
│   🏨 হোটেল কী-কার্ড             │
│                                │
│   ✅ রুম ৩০৪ খুলবে              │
│   ✅ জিমে ঢুকতে পারবে           │
│   ❌ ম্যানেজারের অফিস না         │
│   ❌ ক্যাশ রেজিস্টার না          │
│                                │
│   মেয়াদ: ৩ দিন                  │
└────────────────────────────────┘
```

লক্ষ্য করো —

- কার্ডে **হোটেলের মাস্টার চাবি নেই**, শুধু সীমিত অনুমতি
- কার্ড হারালে হোটেল সেটা **বাতিল করে দিতে পারে**, তালা বদলাতে হয় না
- **মেয়াদ শেষ হলে নিজে থেকেই অকেজো**
- আর সবচেয়ে বড় কথা — কার্ড যার হাতেই যাক, সে **তোমার পরিচয় জানে না**

OAuth 2.0 হুবহু এভাবেই কাজ করে। Password (মাস্টার চাবি) না দিয়ে, একটা **Access Token** (কী-কার্ড) দেওয়া হয়।

---

## চারটি Actor — এদের চেনো আগে

এই চারজনকে না চিনলে পুরো flow ধোঁয়াশা থেকে যাবে।

| Actor | কে | উদাহরণ |
|---|---|---|
| **Resource Owner** | তুমি, মানে user। Data-টা তোমার, সিদ্ধান্তও তোমার | তুমি |
| **Client** | যে app তোমার data চাইছে | Canva, Notion, যেকোনো "Continue with Google" |
| **Authorization Server** | যে login করায় আর অনুমতি দেয়, token বানায় | `accounts.google.com` |
| **Resource Server** | যেখানে আসল data আছে | `googleapis.com` |

**একটা জিনিস অনেকে গুলিয়ে ফেলে:** Authorization Server আর Resource Server **আলাদা**। একজন পরিচয় যাচাই করে চাবি বানায়, আরেকজন সেই চাবি দেখে দরজা খোলে। বড় সিস্টেমে এরা সম্পূর্ণ আলাদা service।

```
   👤 তুমি                    🎨 Canva                  🔐 Google
(Resource Owner)             (Client)          (Auth Server + Resource Server)
```

---

## পুরো Flow — ধাপে ধাপে

তুমি Canva-তে "Continue with Google" চাপলে ঠিক কী ঘটে:

```
  Browser              Canva Backend           Google Auth        Google API
     │                       │                      │                 │
  ①  │──"Google দিয়ে login"─►│                      │                 │
     │                       │                      │                 │
  ②  │◄──302 Redirect────────│                      │                 │
     │   accounts.google.com/o/oauth2/auth?...      │                 │
     │                       │                      │                 │
  ③  │──────────────────────────────────────────────►│                 │
     │        (তুমি Google-এর পাতায় login করো)       │                 │
     │        "Canva কি আপনার নাম-email দেখতে পারবে?"│                 │
     │        [Allow] চাপলে                          │                 │
     │                       │                      │                 │
  ④  │◄─────────────────────────────────────────────│                 │
     │   302 → canva.com/callback?code=4/0AX...&state=xK9  │           │
     │                       │                      │                 │
  ⑤  │──────────────────────►│                      │                 │
     │   GET /callback?code=..│                      │                 │
     │                       │                      │                 │
     │                       │  ⑥ POST /token ─────►│   🔒 backend-এ,
     │                       │  code + client_secret│   browser দেখে না
     │                       │                      │                 │
     │                       │  ⑦ ◄── access_token ─│                 │
     │                       │      + id_token      │                 │
     │                       │      + refresh_token │                 │
     │                       │                      │                 │
     │                       │  ⑧ GET /userinfo ───────────────────────►│
     │                       │     Authorization: Bearer ya29...       │
     │                       │  ◄────── { name, email } ───────────────│
     │                       │                      │                 │
  ⑨  │◄──Set-Cookie: session─│                      │                 │
     │   (এখন Canva-র নিজের session)                │                 │
```

**সবচেয়ে গুরুত্বপূর্ণ কথা ধাপ ⑥-এ:** এই অংশটা **browser-এ হয় না**। Canva-র backend সরাসরি Google-এর সাথে কথা বলে। তাই `client_secret` আর `access_token` কখনো user-এর browser-এ আসে না।

---

## 💻 কোড — সম্পূর্ণ Authorization Code Flow

এবার পুরো ব্যাপারটা চলমান কোডে দেখি। Node.js + Express দিয়ে।

### ধাপ ০ — সেটআপ

```javascript
// config.js
module.exports = {
  clientId:     process.env.GOOGLE_CLIENT_ID,
  clientSecret: process.env.GOOGLE_CLIENT_SECRET,   // ⚠️ কখনো frontend-এ না
  redirectUri:  'https://myapp.com/auth/google/callback',

  authUrl:      'https://accounts.google.com/o/oauth2/v2/auth',
  tokenUrl:     'https://oauth2.googleapis.com/token',
  userInfoUrl:  'https://www.googleapis.com/oauth2/v3/userinfo',
};
```

### ধাপ ১ — User-কে Google-এ পাঠানো

```javascript
const crypto = require('crypto');

app.get('/auth/google', (req, res) => {
  // CSRF ঠেকাতে একটা random state বানাও
  const state = crypto.randomBytes(16).toString('hex');

  // PKCE-র জন্য verifier আর challenge (নিচে বিস্তারিত ব্যাখ্যা আছে)
  const codeVerifier  = crypto.randomBytes(32).toString('base64url');
  const codeChallenge = crypto.createHash('sha256')
                              .update(codeVerifier)
                              .digest('base64url');

  // দুটোই server-এর session-এ রাখো — callback-এ মিলিয়ে দেখতে হবে
  req.session.oauthState    = state;
  req.session.codeVerifier  = codeVerifier;

  const params = new URLSearchParams({
    client_id:             config.clientId,
    redirect_uri:          config.redirectUri,
    response_type:         'code',              // "আমি code চাই, token না"
    scope:                 'openid email profile',
    state,
    code_challenge:        codeChallenge,
    code_challenge_method: 'S256',
    access_type:           'offline',           // refresh_token পেতে হলে দরকার
    prompt:                'consent',
  });

  res.redirect(`${config.authUrl}?${params}`);
});
```

তৈরি হওয়া URL টা দেখতে এরকম:

```
https://accounts.google.com/o/oauth2/v2/auth
  ?client_id=1234.apps.googleusercontent.com
  &redirect_uri=https://myapp.com/auth/google/callback
  &response_type=code
  &scope=openid%20email%20profile
  &state=a3f8b2c1d4e5f6a7
  &code_challenge=E9Melhoa2OwvFrEMTJguCHaoeK1t8URWbuGJSstw-cM
  &code_challenge_method=S256
```

### ধাপ ২ — Callback: code নিয়ে token আনা

```javascript
app.get('/auth/google/callback', async (req, res) => {
  const { code, state, error } = req.query;

  // ── ক. User "Deny" চাপলে ───────────────────────────
  if (error) {
    return res.redirect('/login?error=' + encodeURIComponent(error));
  }

  // ── খ. state মিলিয়ে দেখো (CSRF সুরক্ষা) ─────────────
  if (!state || state !== req.session.oauthState) {
    return res.status(403).send('Invalid state — সম্ভাব্য CSRF আক্রমণ');
  }
  delete req.session.oauthState;      // একবারই ব্যবহার হবে

  // ── গ. code কে token-এ বদলে নাও (server-to-server) ──
  const tokenRes = await fetch(config.tokenUrl, {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body: new URLSearchParams({
      code,
      client_id:     config.clientId,
      client_secret: config.clientSecret,      // 🔒 শুধু backend জানে
      redirect_uri:  config.redirectUri,       // ⚠️ হুবহু আগেরটাই হতে হবে
      grant_type:    'authorization_code',
      code_verifier: req.session.codeVerifier, // PKCE
    }),
  });

  if (!tokenRes.ok) {
    console.error('Token exchange ব্যর্থ:', await tokenRes.text());
    return res.status(502).send('Login সম্পন্ন করা গেলো না');
  }

  const tokens = await tokenRes.json();
  delete req.session.codeVerifier;
  // tokens = { access_token, refresh_token, id_token, expires_in, scope }

  // ── ঘ. Access token দিয়ে user-এর তথ্য আনো ───────────
  const userRes = await fetch(config.userInfoUrl, {
    headers: { Authorization: `Bearer ${tokens.access_token}` },
  });
  const profile = await userRes.json();
  // { sub: "110248495...", email: "...", name: "...", picture: "..." }

  // ── ঙ. নিজের database-এ user খুঁজো বা বানাও ──────────
  const user = await db.findOrCreateUser({
    googleId: profile.sub,          // ⚠️ email না, sub দিয়ে খুঁজবে (নিচে কারণ আছে)
    email:    profile.email,
    name:     profile.name,
    avatar:   profile.picture,
  });

  // refresh_token পেলে সেটা encrypt করে জমা রাখো
  if (tokens.refresh_token) {
    await db.saveGoogleRefreshToken(user.id, encrypt(tokens.refresh_token));
  }

  // ── চ. এখন থেকে নিজের session/JWT ───────────────────
  req.session.userId = user.id;
  res.redirect('/dashboard');
});
```

> **⚠️ একটা অতি গুরুত্বপূর্ণ কথা:** user খুঁজতে **`email` ব্যবহার করবে না, `sub` ব্যবহার করবে।**
> কারণ email বদলানো যায়, আর একই email পরে অন্য কাউকে দেওয়া হতে পারে (বিশেষ করে প্রতিষ্ঠানের Google Workspace-এ)। `sub` কখনো বদলায় না। আর `email_verified: true` না হলে সেই email-কে বিশ্বাস করা যাবে না।

### Token response দেখতে যেমন

```json
{
  "access_token":  "ya29.a0AfH6SMBx...",
  "expires_in":    3599,
  "refresh_token": "1//0gLm3xK9...",
  "scope":         "openid https://www.googleapis.com/auth/userinfo.email",
  "token_type":    "Bearer",
  "id_token":      "eyJhbGciOiJSUzI1NiIsImtpZCI6..."
}
```

---

## Authorization Code কেন? সরাসরি Token কেন না?

খুব স্বাভাবিক প্রশ্ন — Google সরাসরি token দিলেই তো হতো।

**সমস্যাটা হলো browser redirect।**

```
❌ যদি token সরাসরি আসতো:
   https://myapp.com/callback#access_token=ya29.SECRET...

   এই URL টা যেখানে যেখানে থেকে যায়:
   ├── Browser-এর history
   ├── Server-এর access log
   ├── Referer header (পরের পাতায় গেলে)
   ├── Browser extension
   └── কাঁধের উপর দিয়ে তাকিয়ে থাকা লোকটা
```

**Authorization code ইচ্ছাকৃতভাবে প্রায় অকেজো:**

```
✅ Code URL-এ আসে, কিন্তু:
   ├── মেয়াদ মাত্র ৩০-৬০ সেকেন্ড
   ├── একবারই ব্যবহার করা যায় (দ্বিতীয়বার চেষ্টা করলে Google সব token বাতিল করে দেয়)
   ├── client_secret ছাড়া কোনো কাজেই আসে না
   └── redirect_uri হুবহু না মিললে কাজ করবে না
```

আর আসল token বিনিময় হয় **server-to-server**, HTTPS-এ, browser সম্পূর্ণ বাইপাস করে।

> **সংক্ষেপে:** Code হলো একটা "ক্লোকরুমের টোকেন" — টোকেনটা হারালেও কেউ ব্যাগ নিতে পারবে না, কারণ পরিচয়পত্রও লাগবে।

---

## `state` — CSRF আটকানোর পাহারাদার

`state` না রাখলে যে আক্রমণটা সম্ভব, সেটাকে বলে **Login CSRF**। কীভাবে কাজ করে দেখো:

```
১. আক্রমণকারী নিজে "Login with Google" শুরু করে
২. Google তাকে code দেয় — কিন্তু সে callback শেষ করে না
৩. সে তোমাকে একটা লিংকে ক্লিক করায়:
      https://myapp.com/callback?code=তার_কোড

৪. তোমার browser সেটা খোলে
৫. App ভাবলো তুমিই login করেছো
   → তোমার session এখন আক্রমণকারীর Google account-এ যুক্ত! 😱

৬. এখন তুমি যা কিছু save করবে (নোট, ছবি, কার্ড তথ্য),
   সব তার account-এ জমা হবে — সে যেকোনো সময় login করে দেখে নেবে
```

**`state` কীভাবে এটা ঠেকায়:**

```javascript
// পাঠানোর সময়
const state = crypto.randomBytes(16).toString('hex');
req.session.oauthState = state;              // আমার session-এ রাখলাম

// ফেরার সময়
if (req.query.state !== req.session.oauthState) {
  return res.status(403).send('Invalid state');
}
```

আক্রমণকারীর কাছে **তোমার session-এর state টা নেই**, তাই সে মিলাতে পারবে না।

**তিনটা নিয়ম:**
1. `state` অবশ্যই **অনুমান-অযোগ্য random** হতে হবে (`Math.random()` না, `crypto.randomBytes()`)
2. **Server-side session বা signed cookie-তে** রাখতে হবে, URL-এ না
3. একবার ব্যবহারের পর **মুছে ফেলতে হবে**

---

## PKCE — secret ছাড়া কীভাবে নিরাপদ থাকা যায়

### সমস্যাটা

`client_secret` backend-এ রাখা যায়। কিন্তু **mobile app বা SPA-তে?**

```
❌ React app-এ:  const SECRET = "abc123"  → bundle.js এ চলে যাবে, যে কেউ দেখবে
❌ Android app-এ: APK unpack করলেই বেরিয়ে আসবে
```

এদেরকে বলে **public client** — এরা কোনো গোপন কিছু রাখতে পারে না।

তাহলে code চুরি হলে কী হবে? Mobile-এ একটা দূষিত app যদি একই `redirect_uri` scheme দখল করে বসে, সে তোমার code-টা ধরে ফেলতে পারে। আর secret যেহেতু কারো লাগেই না, সে সেই code দিয়ে token নিয়ে নেবে।

### PKCE-র সমাধান — "যে চেয়েছিল, সে-ই নিচ্ছে তো?"

**PKCE** (উচ্চারণ "পিক্সি") = Proof Key for Code Exchange।

মূল বুদ্ধি — শুরুতে একটা গোপন সংখ্যার **তালা** পাঠাও, আর শেষে **চাবি** দেখিয়ে প্রমাণ করো তুমিই সেই লোক।

```
১. Client একটা random গোপন সংখ্যা বানায়:
      code_verifier = "dBjftJeZ4CVP-mB92K27uhbUJU1p1r_wW1gFWFOEjXk"

২. তার SHA-256 hash পাঠায় (তালা):
      code_challenge = SHA256(code_verifier)

   Auth server এটা মনে রাখে।

৩. Token চাওয়ার সময় আসল verifier পাঠায় (চাবি):
      code_verifier = "dBjftJeZ4..."

   Auth server নিজে hash করে মিলিয়ে দেখে ✅

   আক্রমণকারী code চুরি করলেও verifier জানে না
   → hash মিলবে না → token পাবে না ❌
```

**hash একমুখী** — challenge দেখে verifier বের করা অসম্ভব। তাই challenge টা URL-এ প্রকাশ্যে গেলেও ক্ষতি নেই।

### কোড

```javascript
const crypto = require('crypto');

function generatePKCE() {
  // ৪৩-১২৮ অক্ষরের random string
  const verifier = crypto.randomBytes(32).toString('base64url');

  const challenge = crypto
    .createHash('sha256')
    .update(verifier)
    .digest('base64url');       // ⚠️ base64url, সাধারণ base64 না

  return { verifier, challenge };
}

// ── Browser-এ (SPA), Web Crypto API দিয়ে ──────────────
async function generatePKCEBrowser() {
  const bytes = crypto.getRandomValues(new Uint8Array(32));
  const verifier = base64url(bytes);

  const digest = await crypto.subtle.digest(
    'SHA-256',
    new TextEncoder().encode(verifier)
  );
  return { verifier, challenge: base64url(new Uint8Array(digest)) };
}

function base64url(bytes) {
  return btoa(String.fromCharCode(...bytes))
    .replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
}
```

> **আজকের নিয়ম:** OAuth 2.1-এ **সব client-এর জন্য PKCE বাধ্যতামূলক** — এমনকি যাদের `client_secret` আছে তাদের জন্যও। তাই সবসময় PKCE ব্যবহার করো, খরচ কিছুই না।

> **`code_challenge_method` সবসময় `S256` দেবে**, `plain` না। `plain` মানে verifier টাই সরাসরি পাঠানো — তাতে পুরো উদ্দেশ্যই নষ্ট।

---

## Scope — ঠিক যতটুকু দরকার, ততটুকু

Scope হলো OAuth-এর হৃদয় — **তুমি পুরো account দিচ্ছো না, নির্দিষ্ট কিছু অনুমতি দিচ্ছো।**

```javascript
// ✅ ভালো — শুধু যা দরকার
scope: 'openid email profile'

// ❌ খারাপ — অকারণে সব চাওয়া
scope: 'openid email profile https://www.googleapis.com/auth/drive ' +
       'https://www.googleapis.com/auth/gmail.modify'
```

**কেন কম চাওয়াই ভালো:**

| কারণ | ব্যাখ্যা |
|---|---|
| User ভয় পায় | "এই app আমার Drive-এর সব ফাইল মুছতে চায়?" → বেশিরভাগ মানুষ Deny চাপবে |
| যাচাই লাগে | Google-এর মতো provider সংবেদনশীল scope-এর জন্য নিরাপত্তা অডিট করায় (সপ্তাহ লেগে যায়) |
| দায় কমে | তোমার token চুরি হলে ক্ষতির পরিসরও ছোট থাকে |

**Incremental Authorization — সবচেয়ে ভালো কৌশল:**

শুরুতে শুধু `email profile` চাও। পরে যখন user সত্যিই "Drive থেকে import করুন" চাপবে, **তখন** অতিরিক্ত scope চাও।

```javascript
// পরে, যখন সত্যিই দরকার
const params = new URLSearchParams({
  ...baseParams,
  scope: 'https://www.googleapis.com/auth/drive.readonly',
  include_granted_scopes: 'true',    // আগের অনুমতিগুলোও ধরে রাখো
});
```

**দেওয়া scope আর চাওয়া scope এক না** — user কিছু অনুমতি না-ও দিতে পারে। তাই সবসময় response-এর `scope` field মিলিয়ে দেখো:

```javascript
const granted = tokens.scope.split(' ');
if (!granted.includes('https://www.googleapis.com/auth/drive.readonly')) {
  // Drive-এর feature টা লুকিয়ে রাখো বা বুঝিয়ে বলো
}
```

---

## Grant Type — চারটা রাস্তা, কোনটা কখন

OAuth-এ token পাওয়ার কয়েকটা আলাদা পথ আছে। কোনটা কখন — সেটা জানা জরুরি।

| Grant Type | কার জন্য | এখনো ব্যবহার হয়? |
|---|---|---|
| **Authorization Code + PKCE** | Web app, SPA, mobile — user জড়িত | ✅ **সবসময় এটাই** |
| **Client Credentials** | Machine-to-machine, কোনো user নেই | ✅ হ্যাঁ |
| **Device Code** | TV, CLI, IoT — যেখানে ব্রাউজার/কীবোর্ড নেই | ✅ হ্যাঁ |
| **Refresh Token** | নতুন access token নিতে | ✅ হ্যাঁ |
| ~~Implicit~~ | পুরনো SPA | ❌ **বাতিল** (token URL-এ আসে) |
| ~~Password (ROPC)~~ | পুরনো first-party app | ❌ **বাতিল** (password আবার app-এর হাতে!) |

### Client Credentials — কোনো user নেই

তোমার একটা backend service আরেকটা API-কে ডাকবে। কোনো মানুষ জড়িত না।

```javascript
async function getServiceToken() {
  const res = await fetch('https://auth.example.com/oauth/token', {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body: new URLSearchParams({
      grant_type:    'client_credentials',
      client_id:     process.env.SERVICE_CLIENT_ID,
      client_secret: process.env.SERVICE_CLIENT_SECRET,
      scope:         'reports:read invoices:write',
    }),
  });
  return res.json();     // { access_token, expires_in }
}

// ⚠️ প্রতিবার নতুন token চেয়ো না — cache করো
let cached = null;
async function getToken() {
  if (cached && cached.expiresAt > Date.now() + 60_000) return cached.token;

  const { access_token, expires_in } = await getServiceToken();
  cached = { token: access_token, expiresAt: Date.now() + expires_in * 1000 };
  return access_token;
}
```

লক্ষ্য করো — এখানে কোনো redirect নেই, কোনো user নেই, কোনো consent screen নেই। শুধু "আমি এই service, আমার secret এই নাও, token দাও।"

### Device Code — টিভিতে login

Netflix বা YouTube টিভিতে খুললে যা দেখো, সেটাই এটা।

```javascript
// ধাপ ১ — টিভি একটা code চায়
const init = await fetch('https://oauth.example.com/device/code', {
  method: 'POST',
  body: new URLSearchParams({ client_id: CLIENT_ID, scope: 'profile' }),
}).then(r => r.json());

// টিভির পর্দায় দেখাও:
console.log(`ফোনে যান: ${init.verification_uri}`);
console.log(`কোড লিখুন: ${init.user_code}`);      // যেমন "WDJB-MJHT"

// ধাপ ২ — টিভি অপেক্ষা করে, বারবার জিজ্ঞেস করে
while (true) {
  await sleep(init.interval * 1000);

  const res = await fetch('https://oauth.example.com/token', {
    method: 'POST',
    body: new URLSearchParams({
      grant_type:  'urn:ietf:params:oauth:grant-type:device_code',
      device_code: init.device_code,
      client_id:   CLIENT_ID,
    }),
  }).then(r => r.json());

  if (res.access_token) return res;                  // user অনুমতি দিয়েছে ✅
  if (res.error === 'authorization_pending') continue;  // এখনো করেনি
  if (res.error === 'slow_down') { init.interval += 5; continue; }
  throw new Error(res.error);                        // বাতিল বা মেয়াদ শেষ
}
```

---

## তিন রকম Token — কে কীসের জন্য

এখানে অনেকে গুলিয়ে ফেলে, তাই পরিষ্কার করে বলি।

| Token | কী বলে | কে পড়বে | ফরম্যাট |
|---|---|---|---|
| **ID Token** | *"এই user কে"* | **তোমার app** | সবসময় JWT |
| **Access Token** | *"এই কাজগুলোর অনুমতি আছে"* | **Resource Server (API)** | যেকোনো কিছু |
| **Refresh Token** | *"নতুন access token দাও"* | **Authorization Server** | Opaque string |

**সবচেয়ে সাধারণ ভুল:**

```javascript
// ❌ ভুল — ID token দিয়ে API call
fetch('https://api.example.com/data', {
  headers: { Authorization: `Bearer ${idToken}` }
});

// ✅ সঠিক
fetch('https://api.example.com/data', {
  headers: { Authorization: `Bearer ${accessToken}` }
});
```

**দ্বিতীয় সাধারণ ভুল — access token কে "খুলে পড়ার" চেষ্টা:**

```javascript
// ❌ কখনো করবে না
const info = JSON.parse(atob(accessToken.split('.')[1]));
```

Access token টা **তোমার জন্য না, API-র জন্য**। এটা JWT না-ও হতে পারে (Google-এর গুলো opaque string), আর তার ফরম্যাট যেকোনো দিন বদলে যেতে পারে। User-এর তথ্য লাগলে **ID token** পড়ো, বা `/userinfo` endpoint ডাকো।

---

## ID Token যাচাই করা — JWKS দিয়ে

ID token একটা JWT, কিন্তু **সেটা অন্ধভাবে বিশ্বাস করা যাবে না**। যাচাই করতেই হবে।

কেন? কারণ কেউ নিজে একটা JWT বানিয়ে `{ "email": "admin@company.com" }` লিখে পাঠিয়ে দিতে পারে।

```javascript
const { createRemoteJWKSet, jwtVerify } = require('jose');

// Google-এর public key গুলো এখান থেকে আসে (নিজে থেকেই cache হয়)
const JWKS = createRemoteJWKSet(
  new URL('https://www.googleapis.com/oauth2/v3/certs')
);

async function verifyIdToken(idToken) {
  const { payload } = await jwtVerify(idToken, JWKS, {
    issuer:   'https://accounts.google.com',   // কে বানিয়েছে
    audience: config.clientId,                 // ⚠️ আমার জন্যই বানানো তো?
  });

  // jose নিজেই signature, exp, iss, aud যাচাই করে ফেলে
  // কিন্তু এই দুটো নিজে দেখতে হবে:
  if (payload.nonce !== expectedNonce) throw new Error('nonce মিলছে না');
  if (!payload.email_verified)          throw new Error('email যাচাই করা হয়নি');

  return payload;
}
```

**যা যা অবশ্যই যাচাই করতে হবে:**

| যাচাই | কেন |
|---|---|
| **Signature** | জাল token আটকাতে |
| **`iss`** | সত্যিই Google-ই পাঠিয়েছে তো? |
| **`aud`** | ⚠️ **অন্য app-এর জন্য বানানো token আমার এখানে চালানো যাবে না** |
| **`exp`** | মেয়াদ শেষ হয়ে যায়নি তো |
| **`nonce`** | Replay আক্রমণ ঠেকাতে |
| **`email_verified`** | না দেখলে যে কেউ যেকোনো email দাবি করতে পারে |

> **`aud` যাচাই না করা একটা মারাত্মক দুর্বলতা।** আক্রমণকারী তার নিজের app-এ Google দিয়ে login করে সেই ID token তোমার app-এ পাঠাতে পারে — `aud` না দেখলে তুমি মেনে নেবে।

---

## Refresh Token — নীরবে নতুন token নেওয়া

Access token-এর মেয়াদ সাধারণত ১ ঘণ্টা। তারপর?

```javascript
async function callGoogleApi(userId, url) {
  let { accessToken, expiresAt } = await db.getTokens(userId);

  // মেয়াদ শেষ হওয়ার ১ মিনিট আগেই refresh করে ফেলো
  if (Date.now() > expiresAt - 60_000) {
    accessToken = await refreshAccessToken(userId);
  }

  const res = await fetch(url, {
    headers: { Authorization: `Bearer ${accessToken}` },
  });

  // তবু 401 এলে একবার চেষ্টা করে দেখো
  if (res.status === 401) {
    accessToken = await refreshAccessToken(userId);
    return fetch(url, { headers: { Authorization: `Bearer ${accessToken}` } });
  }
  return res;
}

async function refreshAccessToken(userId) {
  const refreshToken = decrypt(await db.getGoogleRefreshToken(userId));

  const res = await fetch(config.tokenUrl, {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body: new URLSearchParams({
      grant_type:    'refresh_token',
      refresh_token: refreshToken,
      client_id:     config.clientId,
      client_secret: config.clientSecret,
    }),
  });

  if (!res.ok) {
    // User অনুমতি প্রত্যাহার করেছে, বা password বদলেছে
    await db.clearGoogleTokens(userId);
    throw new Error('REAUTH_REQUIRED');       // আবার login করাতে হবে
  }

  const data = await res.json();
  await db.saveTokens(userId, {
    accessToken: data.access_token,
    expiresAt:   Date.now() + data.expires_in * 1000,
    // কিছু provider প্রতিবার নতুন refresh token দেয় (rotation)
    ...(data.refresh_token && { refreshToken: encrypt(data.refresh_token) }),
  });

  return data.access_token;
}
```

**যেসব ব্যাপারে সাবধান:**

- **Refresh token সবসময় encrypt করে জমা রাখো।** এটা কার্যত দীর্ঘমেয়াদি password।
- **Google শুধু প্রথমবার refresh token দেয়** — `access_type=offline` আর `prompt=consent` না দিলে পাবেই না।
- **Rotation:** কিছু provider প্রতিবার নতুন refresh token দেয় আর পুরনোটা বাতিল করে। নতুনটা জমা রাখতে ভুললে পরেরবার login ভেঙে যাবে।
- **Refresh ব্যর্থ হওয়া স্বাভাবিক** — user যেকোনো সময় অনুমতি প্রত্যাহার করতে পারে। সেটা সুন্দরভাবে সামলাও।

---

## Token বাতিল ও যাচাই — Revocation ও Introspection

### Revocation — user logout করলে

```javascript
app.post('/auth/disconnect', async (req, res) => {
  const { refreshToken } = await db.getTokens(req.session.userId);

  // Google-কে বলো token বাতিল করতে
  await fetch('https://oauth2.googleapis.com/revoke', {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body: new URLSearchParams({ token: refreshToken }),
  });

  await db.clearGoogleTokens(req.session.userId);
  req.session.destroy();
  res.json({ ok: true });
});
```

**Refresh token বাতিল করলে তার থেকে বানানো সব access token-ও বাতিল হয়ে যায়।**

শুধু নিজের database থেকে মুছে ফেলা যথেষ্ট না — Google-এর দিকেও বাতিল করা উচিত, নাহলে user-এর Google account-এ তোমার app "যুক্ত" হয়ে পড়ে থাকবে।

### Introspection — Resource Server-এর দিক থেকে

তুমি যদি নিজেই API বানাও আর opaque access token ব্যবহার করো, তাহলে যাচাই করবে কীভাবে?

```javascript
// Resource Server-এ middleware
async function verifyToken(req, res, next) {
  const token = req.headers.authorization?.replace('Bearer ', '');
  if (!token) return res.status(401).json({ error: 'token নেই' });

  // ছোট সময়ের জন্য cache করো — নাহলে প্রতিটা request-এ একটা network call
  const cached = await redis.get(`introspect:${hash(token)}`);
  if (cached) { req.token = JSON.parse(cached); return next(); }

  const result = await fetch('https://auth.example.com/oauth/introspect', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/x-www-form-urlencoded',
      Authorization: 'Basic ' + btoa(`${RS_ID}:${RS_SECRET}`),
    },
    body: new URLSearchParams({ token }),
  }).then(r => r.json());

  if (!result.active) return res.status(401).json({ error: 'token অবৈধ' });

  await redis.setex(`introspect:${hash(token)}`, 60, JSON.stringify(result));
  req.token = result;      // { active, scope, client_id, sub, exp }
  next();
}

// Scope অনুযায়ী অনুমতি যাচাই
function requireScope(...needed) {
  return (req, res, next) => {
    const have = (req.token.scope || '').split(' ');
    if (!needed.every(s => have.includes(s))) {
      return res.status(403).json({ error: 'insufficient_scope', needed });
    }
    next();
  };
}

app.get('/api/invoices', verifyToken, requireScope('invoices:read'), handler);
```

**JWT access token হলে introspection লাগে না** — signature যাচাই করলেই হয়, কোনো network call নেই। কিন্তু তখন তাৎক্ষণিক বাতিল করা যায় না। এটাই সেই চেনা tradeoff।

---

## OIDC Discovery — সব ঠিকানা এক জায়গায়

প্রতিটা URL হাতে লেখার দরকার নেই। OIDC provider-রা একটা "নির্দেশিকা" রাখে:

```
https://accounts.google.com/.well-known/openid-configuration
```

```json
{
  "issuer": "https://accounts.google.com",
  "authorization_endpoint": "https://accounts.google.com/o/oauth2/v2/auth",
  "token_endpoint": "https://oauth2.googleapis.com/token",
  "userinfo_endpoint": "https://openidconnect.googleapis.com/v1/userinfo",
  "revocation_endpoint": "https://oauth2.googleapis.com/revoke",
  "jwks_uri": "https://www.googleapis.com/oauth2/v3/certs",
  "scopes_supported": ["openid", "email", "profile"],
  "code_challenge_methods_supported": ["plain", "S256"]
}
```

```javascript
let discoveryCache = null;

async function discover(issuer) {
  if (discoveryCache) return discoveryCache;
  const res = await fetch(`${issuer}/.well-known/openid-configuration`);
  discoveryCache = await res.json();
  return discoveryCache;
}

// এখন provider বদলানো মানে শুধু issuer URL বদলানো
const cfg = await discover('https://accounts.google.com');
res.redirect(`${cfg.authorization_endpoint}?${params}`);
```

**লাভ:** Provider যদি কখনো তার endpoint বদলায় (হয়ে থাকে), তোমার কোড নিজে থেকেই মানিয়ে নেবে।

---

## SPA বা Mobile App-এ কীভাবে

Frontend-এ `client_secret` রাখা যায় না। তাই দুটো পথ।

### পথ ১ — BFF (Backend for Frontend) ⭐ সবচেয়ে নিরাপদ

```
Browser ──► তোমার Backend ──► Google
   │              │
   │              └── token গুলো backend-এ থাকে, browser কখনো দেখে না
   │
   └── শুধু একটা httpOnly session cookie
```

```javascript
// Frontend-এ শুধু এইটুকু
function login() {
  window.location.href = '/auth/google';    // backend সব সামলাবে
}

// API call-এ token নিয়ে ভাবতেই হয় না
fetch('/api/me', { credentials: 'include' });
```

**কেন এটাই সেরা:** কোনো token JavaScript-এর নাগালেই আসে না, তাই XSS হলেও token চুরি হবে না।

### পথ ২ — Authorization Code + PKCE, backend ছাড়া

```javascript
// ── Login শুরু ────────────────────────────────────────
async function startLogin() {
  const { verifier, challenge } = await generatePKCEBrowser();
  const state = crypto.randomUUID();

  sessionStorage.setItem('pkce_verifier', verifier);
  sessionStorage.setItem('oauth_state', state);

  const params = new URLSearchParams({
    client_id: CLIENT_ID,
    redirect_uri: window.location.origin + '/callback',
    response_type: 'code',
    scope: 'openid email profile',
    state,
    code_challenge: challenge,
    code_challenge_method: 'S256',
  });
  window.location.href = `${AUTH_URL}?${params}`;
}

// ── Callback পাতায় ───────────────────────────────────
async function handleCallback() {
  const p = new URLSearchParams(window.location.search);

  if (p.get('state') !== sessionStorage.getItem('oauth_state')) {
    throw new Error('Invalid state');
  }

  const res = await fetch(TOKEN_URL, {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body: new URLSearchParams({
      grant_type: 'authorization_code',
      code: p.get('code'),
      client_id: CLIENT_ID,                  // secret নেই — PKCE-ই যথেষ্ট
      redirect_uri: window.location.origin + '/callback',
      code_verifier: sessionStorage.getItem('pkce_verifier'),
    }),
  });

  const tokens = await res.json();
  sessionStorage.removeItem('pkce_verifier');
  sessionStorage.removeItem('oauth_state');

  // ⚠️ memory-তে রাখো, localStorage-এ না
  setAccessToken(tokens.access_token);

  window.history.replaceState({}, '', '/');   // URL থেকে code মুছে দাও
}
```

### Mobile app-এ

- **কখনো WebView ব্যবহার করবে না** — app সেখানে user-এর password দেখে ফেলতে পারে, আর SSO-ও কাজ করে না
- ব্যবহার করো **ASWebAuthenticationSession** (iOS) বা **Chrome Custom Tabs** (Android)
- Redirect-এর জন্য custom scheme নয়, **App Links / Universal Links** ব্যবহার করো — custom scheme অন্য app দখল করে নিতে পারে
- Token রাখো **Keychain / Keystore**-এ, `UserDefaults` বা `SharedPreferences`-এ না

---

## Authentication vs Authorization

| | Authentication | Authorization |
|---|---|---|
| প্রশ্ন | **"তুমি কে?"** | **"তুমি কী করতে পারবে?"** |
| Protocol | OpenID Connect (OIDC) | OAuth 2.0 |
| ফল | ID Token | Access Token |

**গুরুত্বপূর্ণ কথা: OAuth 2.0 নিজে কিন্তু authentication protocol না।** এটা শুধু অনুমতি দেওয়ার ব্যবস্থা।

"Login with Google" কাজ করে কারণ তার উপরে **OpenID Connect** নামে একটা স্তর বসানো হয়েছে, যেটা `openid` scope দিলে একটা **ID Token** ফেরত দেয়।

```javascript
scope: 'email profile'          // শুধু OAuth2 — ID token পাবে না
scope: 'openid email profile'   // OIDC — ID token পাবে ✅
```

> **এক লাইনে:** *OAuth চাবি দেয়। OIDC পরিচয়পত্র দেয়।*

---

## ⚠️ যে নিরাপত্তা-ভুলগুলো হয়

### ১. `redirect_uri` ঢিলেঢালা রাখা — সবচেয়ে বিপজ্জনক

```javascript
// ❌ ভয়ংকর
if (redirectUri.startsWith('https://myapp.com')) { ... }
// আক্রমণকারী: https://myapp.com.evil.com/steal  → পাস করে যাবে!

// ❌ এটাও খারাপ
allowedPatterns: ['https://myapp.com/*']
// open redirect থাকলে: https://myapp.com/go?to=evil.com

// ✅ সঠিক — হুবহু মিল
const ALLOWED = new Set([
  'https://myapp.com/auth/callback',
  'https://staging.myapp.com/auth/callback',
]);
if (!ALLOWED.has(redirectUri)) throw new Error('Invalid redirect_uri');
```

Authorization code ওই URI-তেই পাঠানো হয় — তাই এখানে ঢিল দেওয়া মানে **আক্রমণকারীর হাতে code তুলে দেওয়া**।

### ২. `state` যাচাই না করা

উপরে দেখানো Login CSRF আক্রমণ। খুব সাধারণ ভুল, খুব সহজ প্রতিরোধ।

### ৩. `aud` যাচাই না করা

অন্য কোনো app-এর জন্য বানানো ID token তোমার এখানে চালিয়ে দেওয়া যাবে। **সবসময় `aud` মিলিয়ে দেখো।**

### ৪. Access token URL-এ পাঠানো

```
❌ GET /api/data?access_token=ya29...     // log, history, referer সবখানে
✅ Authorization: Bearer ya29...
```

### ৫. Token localStorage-এ রাখা

একটা XSS হলেই সব token চলে যাবে। Memory বা httpOnly cookie ব্যবহার করো।

### ৬. Email দিয়ে user মেলানো

```javascript
// ❌ বিপজ্জনক
const user = await db.findByEmail(profile.email);

// ✅ সঠিক
const user = await db.findByProviderId('google', profile.sub);
```

Email বদলানো যায়, আর প্রতিষ্ঠানের account-এ একই email পরে অন্য কর্মীকে দেওয়া হতে পারে। `sub` কখনো বদলায় না।

### ৭. `email_verified` না দেখা

কিছু provider-এ যাচাই না করা email দিয়েও account খোলা যায়। কেউ `victim@company.com` দিয়ে account বানিয়ে তোমার app-এ ঢুকে পড়তে পারে।

---

## Production Checklist

**Authorization request:**
- [ ] `state` — crypto-random, session-এ রাখা, ফেরার সময় মিলিয়ে দেখা
- [ ] PKCE — `S256` পদ্ধতিতে (সব client-এ)
- [ ] `nonce` — OIDC ব্যবহার করলে
- [ ] শুধু দরকারি scope, বেশি না

**Token exchange:**
- [ ] শুধু backend-এ (public client হলে PKCE দিয়ে)
- [ ] `client_secret` environment variable-এ, code-এ না
- [ ] `redirect_uri` হুবহু মিল

**Token যাচাই:**
- [ ] ID token-এর signature, `iss`, `aud`, `exp`, `nonce`
- [ ] `email_verified` দেখা
- [ ] `sub` দিয়ে user মেলানো

**সংরক্ষণ ও ব্যবহার:**
- [ ] Refresh token encrypt করে জমা
- [ ] Access token memory-তে (localStorage-এ না)
- [ ] সব যোগাযোগ HTTPS-এ
- [ ] Logout-এ provider-এর revoke endpoint ডাকা
- [ ] Refresh ব্যর্থ হলে সুন্দরভাবে আবার login করানো

---

## ছবিতে পুরো flow

<img width="745" height="421" alt="OAuth2 flow 1" src="https://github.com/user-attachments/assets/08b527bb-3467-41f5-a8e5-e5f77851f10b" />

<img width="776" height="479" alt="OAuth2 flow 2" src="https://github.com/user-attachments/assets/8b3a5255-023f-4142-a890-8e5076838b95" />

<img width="751" height="443" alt="OAuth2 flow 3" src="https://github.com/user-attachments/assets/e11ce822-d122-417f-9334-3393fa509349" />

<img width="739" height="398" alt="OAuth2 flow 4" src="https://github.com/user-attachments/assets/bc5f5d62-bfbb-4b40-8363-04f1a56c40cf" />

<img width="752" height="403" alt="OAuth2 flow 5" src="https://github.com/user-attachments/assets/cff6c0bd-6851-48d4-8ca4-962067483925" />

<img width="764" height="523" alt="OAuth2 flow 6" src="https://github.com/user-attachments/assets/f818ba84-eab0-4fd7-8b5c-adac3ac707de" />

<img width="750" height="513" alt="OAuth2 flow 7" src="https://github.com/user-attachments/assets/1c729dd5-4d60-4c4a-8a48-a0d6fa8d3955" />

<img width="755" height="483" alt="OAuth2 flow 8" src="https://github.com/user-attachments/assets/713304d0-8445-4e26-9837-d66f0d4a72e3" />

<img width="763" height="401" alt="OAuth2 flow 9" src="https://github.com/user-attachments/assets/c2fb8440-bac7-46fa-8c58-0d85e8df9360" />

<img width="838" height="712" alt="OAuth2 full architecture" src="https://github.com/user-attachments/assets/7c664216-dabb-4bde-b68d-6a0db52b222d" />

---

## Interview Q&A

**প্রশ্ন ১: OAuth 2.0 কী এবং কেন দরকার?**

OAuth 2.0 একটা authorization framework যা user-কে তার password না দিয়েই কোনো third-party application-কে সীমিত অনুমতি দিতে দেয়। এর আগে app গুলো সরাসরি user-এর password চাইতো, ফলে app-এর হাতে পূর্ণ ক্ষমতা চলে যেতো, অনুমতি ফেরত নিতে হলে password বদলাতে হতো, আর app-এর database হ্যাক হলে user-এর মূল account-ও ঝুঁকিতে পড়তো। OAuth এর বদলে একটা সীমিত ক্ষমতার, মেয়াদযুক্ত access token দেয় যা যেকোনো সময় বাতিল করা যায়।

**প্রশ্ন ২: চারটা actor কারা?**

Resource Owner হলো user, যার data। Client হলো যে application সেই data চাইছে। Authorization Server user-কে login করায়, সম্মতি নেয় আর token ইস্যু করে। Resource Server হলো যেখানে আসল data আছে এবং যে access token দেখে সিদ্ধান্ত নেয়। Google-এর ক্ষেত্রে `accounts.google.com` হলো Authorization Server আর `googleapis.com` হলো Resource Server।

**প্রশ্ন ৩: Authorization code লাগে কেন, সরাসরি token দিলে সমস্যা কী?**

Token যদি browser redirect-এ আসতো, তাহলে সেটা URL-এ থাকতো — আর URL browser history, server log, referer header সবখানে ফাঁস হতে পারে। Authorization code ইচ্ছাকৃতভাবে প্রায় অকেজো রাখা হয়েছে: এর মেয়াদ কয়েক সেকেন্ড, একবারই ব্যবহার করা যায়, আর `client_secret` বা PKCE verifier ছাড়া তা দিয়ে token পাওয়া যায় না। আসল token বিনিময়টা হয় server-to-server, browser সম্পূর্ণ বাইপাস করে।

**প্রশ্ন ৪: PKCE কী এবং কেন দরকার?**

Mobile app বা SPA-র মতো public client-এ `client_secret` নিরাপদে রাখা যায় না, কারণ যে কেউ app unpack বা JS bundle পড়ে সেটা বের করতে পারে। PKCE এই ফাঁকটা পূরণ করে — client শুরুতে একটা random `code_verifier` বানায় এবং তার SHA-256 hash কে `code_challenge` হিসেবে পাঠায়; পরে token চাওয়ার সময় আসল verifier পাঠিয়ে প্রমাণ করে যে সে-ই সেই client। কেউ code চুরি করলেও verifier জানে না, তাই token পাবে না। OAuth 2.1-এ এটা সব client-এর জন্যই বাধ্যতামূলক।

**প্রশ্ন ৫: `state` parameter কী আটকায়?**

Login CSRF। `state` না থাকলে আক্রমণকারী নিজে OAuth flow শুরু করে নিজের authorization code নিয়ে একটা লিংক বানাতে পারে, তারপর ভুক্তভোগীকে সেই লিংকে ক্লিক করাতে পারে — ফলে ভুক্তভোগীর session আক্রমণকারীর account-এ যুক্ত হয়ে যায়, আর ভুক্তভোগী যা কিছু save করে সব আক্রমণকারী পরে দেখতে পায়। `state` একটা অনুমান-অযোগ্য random মান যা client নিজের session-এ রাখে আর callback-এ মিলিয়ে দেখে; আক্রমণকারী সেটা জানে না।

**প্রশ্ন ৬: Access token, ID token আর refresh token-এর পার্থক্য?**

Access token বলে কী কী করার অনুমতি আছে — এটা API-কে পাঠানো হয়, আর client-এর এটা খুলে পড়া উচিত না। ID token (OIDC-এর অংশ) বলে user কে — এটা সবসময় JWT এবং এটা client application-এর জন্য, API-র জন্য না। Refresh token দীর্ঘমেয়াদি এবং শুধু Authorization Server-এ পাঠিয়ে নতুন access token নেওয়ার কাজে লাগে। খুব সাধারণ একটা ভুল হলো ID token দিয়ে API call করা।

**প্রশ্ন ৭: Implicit grant বাতিল হলো কেন?**

Implicit flow-তে access token সরাসরি browser-এ URL fragment হিসেবে আসতো, ফলে সেটা history, log আর extension-এর নাগালে চলে যেতো, আর refresh token পাওয়ার কোনো নিরাপদ উপায়ও ছিল না। এটা তৈরি হয়েছিল যখন browser-এ cross-origin request কঠিন ছিল, কিন্তু CORS আসার পর সেই সীমাবদ্ধতা আর নেই। এখন SPA-র জন্যও Authorization Code + PKCE ব্যবহার করা হয়, যা অনেক বেশি নিরাপদ।

**প্রশ্ন ৮: OAuth 2.0 আর OpenID Connect-এর পার্থক্য?**

OAuth 2.0 হলো authorization-এর জন্য — এটা বলে তুমি কী করতে পারবে, কিন্তু তুমি কে সেটা বলার কোনো মানসম্মত উপায় এতে নেই। OpenID Connect হলো OAuth 2.0-এর উপরে বসানো একটা পাতলা স্তর যা authentication যোগ করে — `openid` scope চাইলে এটা একটা ID Token (JWT) দেয় যেখানে user-এর পরিচয় থাকে, সাথে একটা মানসম্মত `/userinfo` endpoint আর discovery document দেয়। "Login with Google" আসলে OAuth 2.0 + OIDC দুটোই ব্যবহার করে।

**প্রশ্ন ৯: `redirect_uri` যাচাই করা এত গুরুত্বপূর্ণ কেন?**

কারণ authorization code ওই URI-তেই পাঠানো হয়। যাচাই ঢিলেঢালা হলে আক্রমণকারী নিজের নিয়ন্ত্রণাধীন একটা URL দিয়ে code হাতিয়ে নিতে পারে। শুধু prefix মিলিয়ে দেখা বিপজ্জনক — `https://myapp.com` দিয়ে শুরু হচ্ছে কিনা দেখলে `https://myapp.com.evil.com` পাস করে যাবে। তাই আগে থেকে নিবন্ধিত URI-র সাথে **হুবহু** মিল যাচাই করতে হয়, আর নিজের site-এ কোনো open redirect আছে কিনা তাও দেখতে হয়।

**প্রশ্ন ১০: Access token যাচাই করার দুটো উপায় কী কী, কোনটা কখন?**

যদি access token একটা JWT হয়, তাহলে Resource Server শুধু signature আর claim যাচাই করলেই হয় — কোনো network call লাগে না, তাই খুব দ্রুত, কিন্তু token তাৎক্ষণিক বাতিল করা যায় না। যদি opaque token হয়, তাহলে introspection endpoint-এ জিজ্ঞেস করতে হয় token টা এখনো সক্রিয় কিনা — এতে তাৎক্ষণিক বাতিল সম্ভব, কিন্তু প্রতিটা request-এ একটা বাড়তি call লাগে (যা অল্প সময়ের জন্য cache করে কমানো যায়)। এটাই সেই চেনা performance বনাম নিয়ন্ত্রণের tradeoff।

**প্রশ্ন ১১: User-কে database-এ email দিয়ে খুঁজবে না `sub` দিয়ে?**

`sub` দিয়ে। Email বদলানো যায়, আর প্রতিষ্ঠানের Google Workspace-এ একজন কর্মী চলে গেলে তার email ঠিকানা নতুন কাউকে দেওয়া হতে পারে — তখন email দিয়ে মেলালে নতুন লোক পুরনো লোকের account-এ ঢুকে পড়বে। `sub` হলো ওই provider-এর কাছে user-এর স্থায়ী, অপরিবর্তনীয় পরিচয়। তাই `(provider, sub)` জোড়া দিয়ে user মেলানো উচিত, আর email-কে শুধু প্রদর্শনের তথ্য হিসেবে দেখা উচিত — তাও `email_verified` সত্য হলে।

**প্রশ্ন ১২: SPA-তে token কোথায় রাখবে?**

সবচেয়ে নিরাপদ পদ্ধতি হলো BFF (Backend for Frontend) — token গুলো backend-এ থাকবে আর browser শুধু একটা httpOnly session cookie পাবে, ফলে কোনো token JavaScript-এর নাগালেই আসবে না। যদি backend রাখা সম্ভব না হয়, তাহলে Authorization Code + PKCE ব্যবহার করে access token শুধু memory-তে (JavaScript variable-এ) রাখতে হবে, localStorage-এ না — কারণ একটা XSS হলেই localStorage-এর সব token চলে যাবে।

---

## এক কথায় মনে রাখো

**OAuth 2.0 = হোটেলের কী-কার্ড।** Password (মাস্টার চাবি) না দিয়ে, সীমিত ক্ষমতার একটা অস্থায়ী চাবি দেওয়া — যা যেকোনো সময় বাতিল করা যায়।

**চারজন:** Resource Owner (তুমি) · Client (app) · Authorization Server (চাবি বানায়) · Resource Server (দরজা খোলে)।

**সঠিক flow সবসময় একটাই:** **Authorization Code + PKCE**। Implicit আর Password grant মৃত।

**তিনটা জিনিস কখনো ভুলবে না:**
1. 🔐 **`state`** যাচাই করো — নাহলে Login CSRF
2. 🔑 **PKCE** ব্যবহার করো — সব client-এ, ব্যতিক্রম ছাড়া
3. 🎯 **`redirect_uri` হুবহু মেলাও** আর ID token-এর **`aud`** যাচাই করো

আর মনে রেখো — **OAuth চাবি দেয়, OIDC পরিচয়পত্র দেয়।** "Login with Google" আসলে দুটোই।

📖 *সম্পর্কিত: [4. Token ও JWT](4.%20Token-Based%20and%20JWT%20Authentication.md) · [3. Stateful vs Stateless](3.%20Stateful%20vs%20Stateless%20Architecture.md) · [6. API Gateway](6.%20API%20Gateway%20Details.md)*
