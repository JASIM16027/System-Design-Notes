# Consistent Hashing

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [সমস্যাটা আগে বুঝি — সাধারণ Hashing কেন ব্যর্থ](#সমস্যাটা-আগে-বুঝি--সাধারণ-hashing-কেন-ব্যর্থ)
* [Consistent Hashing — মূল ধারণা](#consistent-hashing--মূল-ধারণা)
* [Node যোগ করলে কী হয়](#node-যোগ-করলে-কী-হয়)
* [Node মরে গেলে কী হয়](#node-মরে-গেলে-কী-হয়)
* [একটা সমস্যা রয়ে গেছে — অসম বণ্টন](#একটা-সমস্যা-রয়ে-গেছে--অসম-বণ্টন)
* [Virtual Node — আসল সমাধান](#virtual-node--আসল-সমাধান)
* [Replication কীভাবে কাজ করে](#replication-কীভাবে-কাজ-করে)
* [কোড দিয়ে দেখো](#কোড-দিয়ে-দেখো)
* [কোথায় কোথায় ব্যবহার হয়](#কোথায়-কোথায়-ব্যবহার-হয়)
* [সীমাবদ্ধতা](#সীমাবদ্ধতা)
* [Interview Q&A](#interview-qa)
* [এক কথায় মনে রাখো](#এক-কথায়-মনে-রাখো)

---

<!-- TOC END -->

## সমস্যাটা আগে বুঝি — সাধারণ Hashing কেন ব্যর্থ

ধরো তোমার ৪টা cache server আছে। কোন key কোন server-এ যাবে?

সবচেয়ে সহজ উপায়:

```
server = hash(key) % 4
```

চমৎকার কাজ করে। `hash("user_1") % 4 = 2` → Server-2 তে যাও। পরেরবারও একই ফল।

**কিন্তু এখন একটা server যোগ করলে?**

```
আগে (৪টা server):        এখন (৫টা server):
hash("user_1") % 4 = 2    hash("user_1") % 5 = 1   ← বদলে গেছে!
hash("user_2") % 4 = 3    hash("user_2") % 5 = 4   ← বদলে গেছে!
hash("user_3") % 4 = 0    hash("user_3") % 5 = 3   ← বদলে গেছে!
```

**প্রায় সব key-র জায়গা বদলে গেলো।**

গণিতে হিসাব করলে — `% N` থেকে `% (N+1)` এ গেলে প্রায় **(N-1)/N অংশ** key-র mapping বদলায়। ৪ থেকে ৫ server-এ গেলে **৭৫-৮০% key** ভুল জায়গায়।

**এর ফল কী হয়:**

- Cache-এ প্রায় সব key **miss** হবে
- সব traffic একসাথে database-এ যাবে
- **Database crash** — এটাকেই বলে cache avalanche
- Sharded database হলে **TB-র পর TB data migrate** করতে হবে

একই ঘটনা ঘটে যখন একটা server **মরে যায়** — তখন `% 4` হয়ে যায় `% 3`।

এই সমস্যার সমাধানই **Consistent Hashing**।

---

## Consistent Hashing — মূল ধারণা

মূল বুদ্ধিটা সরল: **modulo ব্যবহার করা বন্ধ করো। বরং একটা বৃত্ত কল্পনা করো।**

**ধাপ ১ — একটা বৃত্ত (Hash Ring) কল্পনা করো**

০ থেকে 2³²−1 পর্যন্ত সংখ্যাগুলো একটা বৃত্তে সাজানো — শেষ সংখ্যার পরেই আবার শূন্য।

**ধাপ ২ — Server গুলোকে বৃত্তে বসাও**

প্রতিটা server-এর নাম hash করে যে সংখ্যা পাও, বৃত্তের সেই জায়গায় তাকে বসাও।

```
                    0
              ┌─────┴─────┐
         ┌────┘           └────┐
      ┌──┘      Node-A         └──┐
    ┌─┘         (hash=50)         └─┐
   │                                 │
 Node-D                           Node-B
(hash=800)                       (hash=300)
   │                                 │
    └─┐                            ┌─┘
      └──┐      Node-C         ┌───┘
         └────┐ (hash=600) ┌───┘
              └────────────┘
```

**ধাপ ৩ — Key গুলোকেও বৃত্তে বসাও**

Key-ও hash করে বৃত্তে বসাও।

**ধাপ ৪ — নিয়মটা এই:**

> **প্রতিটা key ঘড়ির কাঁটার দিকে এগিয়ে যে server-কে প্রথম পায়, সেটাই তার server।**

```
key "user_1" → hash = 150 → ঘড়ির কাঁটার দিকে এগোও → Node-B (300) পেলাম ✅
key "user_2" → hash = 700 → এগোও → Node-D (800) পেলাম ✅
key "user_3" → hash = 900 → এগোও → বৃত্ত ঘুরে → Node-A (50) পেলাম ✅
```

লক্ষ্য করো — **এখানে কোথাও server-এর মোট সংখ্যা ব্যবহারই হয়নি।** এটাই মূল চাবিকাঠি।

---

## Node যোগ করলে কী হয়

ধরো Node-E যোগ করলাম, তার hash = 450। সে বসবে Node-B (300) আর Node-C (600)-এর মাঝখানে।

```
আগে:  hash 300–600 এর সব key → Node-C
এখন:  hash 300–450 এর key → Node-E   ← শুধু এই অংশটুকু সরলো
      hash 450–600 এর key → Node-C   ← অপরিবর্তিত
```

**শুধু Node-C-র কিছু key সরলো। বাকি সব node-এর কোনো পরিবর্তন হয়নি।**

গড়ে মাত্র **K/N** সংখ্যক key সরে (K = মোট key, N = মোট node)। ৫টা node হলে মাত্র **২০%**, ১০০টা node হলে মাত্র **১%**।

সাধারণ hashing-এ যেখানে ৮০% key সরতো, এখানে সেখানে ২০% — এটাই বিশাল পার্থক্য।

---

## Node মরে গেলে কী হয়

Node-C (600) মরে গেলো।

```
আগে:  hash 450–600 → Node-C
এখন:  hash 450–600 → Node-D (পরের node)
```

Node-C-র key গুলো ঘড়ির কাঁটার দিকের পরের node-এ চলে গেলো। **বাকি সব node অপরিবর্তিত।**

---

## একটা সমস্যা রয়ে গেছে — অসম বণ্টন

Hash function যত ভালোই হোক, ৪টা এলোমেলো সংখ্যা বৃত্তে সমানভাবে বসবে না।

```
   Node-A (10)  Node-B (50)  Node-C (90)         Node-D (700)
   │───────────│────────────│───────────────────│─────────────│
      40 ফাঁক     40 ফাঁক        610 ফাঁক!         বৃত্তের বাকিটা
```

Node-D পাচ্ছে বৃত্তের বিশাল অংশ, তাই তার কাছে অনেক বেশি key। এটাই **hotspot**।

আরো খারাপ ব্যাপার — Node-D মরে গেলে তার পুরো বিশাল ভার একটামাত্র পরের node-এর ঘাড়ে পড়বে, সে-ও ভেঙে পড়তে পারে। এভাবে **cascading failure**।

---

## Virtual Node — আসল সমাধান

প্রতিটা physical server-কে বৃত্তে **একবার না, অনেকবার** বসাও।

```
Node-A → "A#1", "A#2", "A#3", ... "A#150"  → ১৫০টা আলাদা জায়গায়
Node-B → "B#1", "B#2", "B#3", ... "B#150"  → ১৫০টা আলাদা জায়গায়
```

```
বৃত্তে এখন এভাবে ছড়ানো:
A#1 → B#7 → C#3 → A#22 → D#9 → B#41 → C#88 → A#5 → ...
```

**এতে যা যা লাভ হয়:**

**১. সমান বণ্টন** — অনেক ছোট ছোট টুকরায় ভাগ হওয়ায় গড়ে সবাই প্রায় সমান পায়। ১০০-২০০ virtual node হলে বণ্টনের অসমতা ৫%-এর নিচে নেমে আসে।

**২. Failure-এর ভার ছড়িয়ে পড়ে** — Node-A মরলে তার ১৫০টা টুকরার key ১৫০টা আলাদা জায়গায় ছড়িয়ে যায়, মানে **সব বাকি node একটু একটু করে ভাগ নেয়**। একজনের ঘাড়ে পুরো বোঝা পড়ে না।

**৩. ভিন্ন ক্ষমতার server** — শক্তিশালী server-কে ৩০০টা virtual node দাও, দুর্বলটাকে ১০০টা। তাহলে load ক্ষমতা অনুযায়ী ভাগ হবে।

**বাস্তব সংখ্যা:** সাধারণত প্রতি physical node-এ **১০০–২০০টা** virtual node রাখা হয়। Amazon Dynamo-তে ছিল ১০০-এর মতো, Cassandra-তে default ২৫৬।

---

## Replication কীভাবে কাজ করে

Consistent hashing-এ replication যোগ করা খুব স্বাভাবিক — শুধু একটা না, **ঘড়ির কাঁটার দিকের পরের N টা node**-এ copy রাখো।

```
key "user_1" (hash=150)
    → প্রথম node পেলাম: Node-B     (primary)
    → পরের node: Node-C            (replica 1)
    → তার পরের node: Node-D        (replica 2)
```

**একটা সূক্ষ্ম বিষয়:** পরের ৩টা *virtual* node একই physical server-এর হতে পারে! তখন ৩টা copy আসলে একটাই machine-এ — কোনো নিরাপত্তাই থাকলো না। তাই বাস্তব implementation-এ **আলাদা physical node** (এমনকি আলাদা rack বা data center) খুঁজে নেওয়া হয়।

---

## কোড দিয়ে দেখো

```javascript
const crypto = require('crypto');

class ConsistentHash {
  constructor(nodes = [], virtualNodes = 150) {
    this.virtualNodes = virtualNodes;
    this.ring = new Map();        // hash → physical node
    this.sortedHashes = [];       // sorted hash গুলো, binary search-এর জন্য
    nodes.forEach(node => this.addNode(node));
  }

  hash(key) {
    return parseInt(
      crypto.createHash('md5').update(key).digest('hex').slice(0, 8), 16
    );
  }

  addNode(node) {
    for (let i = 0; i < this.virtualNodes; i++) {
      const h = this.hash(`${node}#${i}`);
      this.ring.set(h, node);
    }
    this.sortedHashes = [...this.ring.keys()].sort((a, b) => a - b);
  }

  removeNode(node) {
    for (let i = 0; i < this.virtualNodes; i++) {
      this.ring.delete(this.hash(`${node}#${i}`));
    }
    this.sortedHashes = [...this.ring.keys()].sort((a, b) => a - b);
  }

  // key-টা কোন node-এ যাবে — ঘড়ির কাঁটার দিকে প্রথম node
  getNode(key) {
    if (this.ring.size === 0) return null;
    const h = this.hash(key);

    // binary search: h-এর চেয়ে বড় প্রথম hash খোঁজো
    let lo = 0, hi = this.sortedHashes.length - 1;
    while (lo < hi) {
      const mid = Math.floor((lo + hi) / 2);
      if (this.sortedHashes[mid] < h) lo = mid + 1;
      else hi = mid;
    }
    // বৃত্তের শেষ পেরিয়ে গেলে আবার শুরুতে ফিরে যাও
    const idx = this.sortedHashes[lo] >= h ? lo : 0;
    return this.ring.get(this.sortedHashes[idx]);
  }
}

// ব্যবহার
const ch = new ConsistentHash(['server-1', 'server-2', 'server-3']);
console.log(ch.getNode('user:1001'));   // → server-2

ch.addNode('server-4');                 // মাত্র ~২৫% key সরবে
console.log(ch.getNode('user:1001'));   // → হয়তো একই, হয়তো server-4
```

**সময়ের হিসাব:** Key খোঁজা O(log N) — binary search, যেখানে N = মোট virtual node।

---

## কোথায় কোথায় ব্যবহার হয়

| ব্যবস্থা | কীভাবে ব্যবহার করে |
|---|---|
| **Amazon DynamoDB** | Dynamo paper-এই এই ধারণা জনপ্রিয় হয়েছিল |
| **Apache Cassandra** | Data কোন node-এ যাবে তা ঠিক করতে (default ২৫৬ vnode) |
| **Memcached** | Client-side sharding — কোন server-এ key যাবে |
| **Redis Cluster** | কিছুটা ভিন্ন — ১৬,৩৮৪টা স্থির hash slot ব্যবহার করে |
| **Riak, Voldemort** | Data distribution |
| **CDN (Akamai)** | কোন edge server কোন content রাখবে |
| **Load Balancer** | Session/cache locality বজায় রাখতে |
| **Kubernetes / Envoy** | Ring hash load balancing policy |

**Redis Cluster-এর ব্যতিক্রম:** এটা ঠিক consistent hashing না — এটা ১৬,৩৮৪টা নির্দিষ্ট slot রাখে আর প্রতিটা node কিছু slot-এর দায়িত্ব নেয়। Node যোগ করলে কিছু slot হাতবদল হয়। ফলাফল একই (কম data সরে), কিন্তু পদ্ধতিটা সরল ও পূর্বানুমেয়।

---

## সীমাবদ্ধতা

**১. নিখুঁত সমতা দেয় না** — Virtual node দিয়েও ৫-১০% হেরফের থাকে।

**২. Hotspot key-র সমাধান না** — একটামাত্র key যদি অসম্ভব জনপ্রিয় হয় (celebrity-র profile), তবে consistent hashing কিছুই করতে পারে না — সেই একটা node-ই সব চাপ নেবে। এর জন্য আলাদা কৌশল লাগে (ওই key-কে বিশেষভাবে cache করা, বা key-তে random suffix যোগ করা)।

**৩. Range query কঠিন** — Hash করার পর ক্রম নষ্ট হয়ে যায়, তাই "১০০ থেকে ২০০ পর্যন্ত সব id" ধরনের query-তে সব node-এ খুঁজতে হবে।

**৪. Metadata সব client-এ রাখতে হয়** — কোন node কোথায় আছে, সবাইকে জানতে হয়। Node যোগ/বিয়োগ হলে সবাইকে জানাতে হয় (gossip protocol বা coordination service দিয়ে)।

**৫. উন্নত বিকল্প আছে** — **Rendezvous Hashing (HRW)** একই ফল দেয় virtual node ছাড়াই, আর **Jump Consistent Hash** (Google) খুব দ্রুত ও memory-বান্ধব, তবে node সরানোয় সীমাবদ্ধতা আছে।

---

## Interview Q&A

**প্রশ্ন ১: Consistent hashing কী এবং কেন দরকার?**

Consistent hashing হলো key গুলোকে server-এ ভাগ করার একটা পদ্ধতি যেখানে server যোগ বা বিয়োগ করলে খুব অল্প সংখ্যক key-র জায়গা বদলায়। সাধারণ `hash(key) % N` পদ্ধতিতে N বদলালে প্রায় সব key-র mapping বদলে যায় — মানে পুরো cache অকেজো হয়ে যায় বা পুরো database migrate করতে হয়। Consistent hashing-এ modulo ব্যবহার না করে একটা hash ring ব্যবহার করা হয়, ফলে গড়ে মাত্র K/N সংখ্যক key সরে।

**প্রশ্ন ২: এটা কীভাবে কাজ করে, সহজ করে বলো।**

০ থেকে 2³²−১ পর্যন্ত একটা কাল্পনিক বৃত্ত ধরা হয়। প্রতিটা server-এর নাম hash করে বৃত্তের একটা নির্দিষ্ট বিন্দুতে বসানো হয়। একটা key কোথায় যাবে জানতে হলে key-টাও hash করে বৃত্তে বসানো হয়, তারপর ঘড়ির কাঁটার দিকে এগিয়ে প্রথম যে server পাওয়া যায় সেটাই তার গন্তব্য। এখানে কোথাও server-এর মোট সংখ্যা ব্যবহার হয় না — এজন্যই একটা server যোগ বা বিয়োগ করলে শুধু তার প্রতিবেশী অংশটুকুই প্রভাবিত হয়, বাকি সব অপরিবর্তিত থাকে।

**প্রশ্ন ৩: Virtual node কেন দরকার?**

দুটো কারণে। প্রথমত, অল্প কয়েকটা server বৃত্তে এলোমেলোভাবে বসলে তারা সমানভাবে ছড়ায় না — কারো ভাগে বৃত্তের বিশাল অংশ পড়ে যায়, ফলে hotspot হয়। প্রতিটা physical server-কে ১০০-২০০টা virtual node হিসেবে বৃত্তের নানা জায়গায় বসালে বণ্টন অনেক সমান হয়। দ্বিতীয়ত, কোনো server মরে গেলে তার ভার একটামাত্র প্রতিবেশীর ঘাড়ে না পড়ে সব বাকি server-এর মধ্যে ছড়িয়ে যায়, ফলে cascading failure এড়ানো যায়। বাড়তি সুবিধা — শক্তিশালী server-কে বেশি virtual node দিয়ে ক্ষমতা অনুযায়ী load ভাগ করা যায়।

**প্রশ্ন ৪: একটা node যোগ করলে কত শতাংশ key সরে?**

গড়ে K/N — অর্থাৎ মোট key-র 1/N অংশ, যেখানে N হলো node সংখ্যা। ৫টা node থাকলে নতুন একটা যোগ করলে প্রায় ২০% key সরবে, ১০০টা node থাকলে মাত্র ১%। তুলনায় সাধারণ modulo hashing-এ ৪ থেকে ৫ node-এ গেলেই প্রায় ৮০% key-র mapping বদলে যায়।

**প্রশ্ন ৫: Consistent hashing দিয়ে replication কীভাবে করবে?**

Key-র জন্য ঘড়ির কাঁটার দিকে প্রথম node-টা primary হয়, আর তার পরের N-১টা node-এ replica রাখা হয়। তবে একটা গুরুত্বপূর্ণ সতর্কতা আছে — পরপর কয়েকটা virtual node একই physical server-এর হতে পারে, তখন সব copy একটাই machine-এ পড়ে যাবে এবং replication-এর উদ্দেশ্যই ব্যর্থ হবে। তাই বাস্তব implementation-এ পরবর্তী *স্বতন্ত্র* physical node খোঁজা হয়, এমনকি আলাদা rack বা availability zone-এ রাখা হয়।

**প্রশ্ন ৬: Consistent hashing-এর সীমাবদ্ধতা কী?**

এটা key-র বণ্টন সমান করে, কিন্তু *access* সমান করে না — একটা key যদি অস্বাভাবিক জনপ্রিয় হয় (যেমন celebrity-র profile), তবে সেই একটা node-এই সব চাপ পড়বে, consistent hashing এতে কিছুই করতে পারে না। এছাড়া hash করার পর key-র স্বাভাবিক ক্রম নষ্ট হয়ে যায়, তাই range query করতে হলে সব node-এ খুঁজতে হয়। আর ring-এর অবস্থা সব client-কে জানতে হয়, তাই node যোগ-বিয়োগের তথ্য ছড়ানোর জন্য gossip protocol বা coordination service লাগে।

**প্রশ্ন ৭: Redis Cluster কি consistent hashing ব্যবহার করে?**

ঠিক প্রচলিত consistent hashing না। Redis Cluster ১৬,৩৮৪টা নির্দিষ্ট hash slot ব্যবহার করে — `CRC16(key) % 16384` দিয়ে key-র slot বের হয়, আর প্রতিটা node কিছু slot-এর দায়িত্ব নেয়। Node যোগ করলে কিছু slot নতুন node-এ হস্তান্তর করা হয়, বাকি সব অপরিবর্তিত থাকে। ফলাফল consistent hashing-এর মতোই (অল্প data সরে), কিন্তু পদ্ধতিটা সরল এবং কোন slot কোথায় আছে তা স্পষ্টভাবে জানা ও নিয়ন্ত্রণ করা যায়।

---

## এক কথায় মনে রাখো

সমস্যা: `hash(key) % N` — **N বদলালে প্রায় সব key-র জায়গা বদলে যায়।**

সমাধান: একটা **বৃত্ত** কল্পনা করো, server আর key দুটোকেই বসাও, প্রতিটা key ঘড়ির কাঁটার দিকে **প্রথম যে server পায় সেখানে যায়**।

ফলাফল: Node যোগ/বিয়োগে মাত্র **K/N** key সরে, সব না।

আর অবশ্যই **Virtual Node** ব্যবহার করো (প্রতি server-এ ১০০-২০০টা) — নাহলে বণ্টন অসম হবে আর একটা node মরলে তার পুরো ভার একজনের ঘাড়ে পড়বে।
