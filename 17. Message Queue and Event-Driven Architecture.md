# Message Queue এবং Event-Driven Architecture

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [সমস্যাটা আগে বুঝি — Synchronous-এর যন্ত্রণা](#সমস্যাটা-আগে-বুঝি--synchronous-এর-যন্ত্রণা)
* [Message Queue কী](#message-queue-কী)
* [Queue ব্যবহারের ৫টা কারণ](#queue-ব্যবহারের-৫টা-কারণ)
* [দুটো মূল Model — Queue vs Pub/Sub](#দুটো-মূল-model--queue-vs-pubsub)
* [Kafka — Log-based Streaming](#kafka--log-based-streaming)
* [Kafka vs RabbitMQ — কোনটা কখন](#kafka-vs-rabbitmq--কোনটা-কখন)
* [Delivery Guarantee — কতবার পৌঁছাবে](#delivery-guarantee--কতবার-পৌঁছাবে)
* [Idempotency — দুবার এলেও যেন সমস্যা না হয়](#idempotency--দুবার-এলেও-যেন-সমস্যা-না-হয়)
* [Dead Letter Queue — যে message কিছুতেই process হয় না](#dead-letter-queue--যে-message-কিছুতেই-process-হয়-না)
* [Message Ordering](#message-ordering)
* [Backpressure ও Consumer Lag](#backpressure-ও-consumer-lag)
* [Event-Driven Architecture](#event-driven-architecture)
* [Saga Pattern — Distributed Transaction](#saga-pattern--distributed-transaction)
* [Interview Q&A](#interview-qa)
* [এক কথায় মনে রাখো](#এক-কথায়-মনে-রাখো)

---

<!-- TOC END -->

## সমস্যাটা আগে বুঝি — Synchronous-এর যন্ত্রণা

ধরো একজন user তোমার site-এ সাইন আপ করলো। Server-এ যা যা করতে হবে:

```
1. Database-এ user save করো        →  50ms
2. Welcome email পাঠাও             →  2000ms  (SMTP ধীর)
3. Profile picture resize করো      →  3000ms
4. Analytics-এ পাঠাও               →  500ms
5. Admin-কে notify করো             →  800ms
────────────────────────────────────────────
মোট: user ৬.৩ সেকেন্ড বসে আছে 😤
```

আরো খারাপ ব্যাপার — **email service down থাকলে পুরো signup-ই fail করবে।** অথচ user তো account পেয়ে গেছে!

**Message Queue দিয়ে সমাধান:**

```
1. Database-এ user save করো        →  50ms
2. Queue-তে ৪টা message ফেলে দাও   →  5ms
────────────────────────────────────────────
মোট: ৫৫ms — user সাথে সাথেই সাইন আপ শেষ ✅

বাকি কাজগুলো background worker গুলো করবে,
আর email service down থাকলেও message queue-তেই থাকবে,
service ফিরলে আবার চেষ্টা হবে।
```

এটাই message queue-র মূল কথা — **যে কাজ এখনই করতে হবে না, তা পরে করো।**

---

## Message Queue কী

দুটো system-এর মাঝখানে বসা একটা বাফার — যেখানে message জমা হয় এবং পরে process হয়।

```
┌──────────┐   message   ┌─────────┐   message   ┌──────────┐
│ Producer │ ──────────► │  Queue  │ ──────────► │ Consumer │
└──────────┘             └─────────┘             └──────────┘
  (পাঠায়)                  (জমায়)                 (কাজ করে)
```

- **Producer** — যে message তৈরি করে queue-তে পাঠায়
- **Queue/Broker** — যে message জমা রাখে
- **Consumer/Worker** — যে message নিয়ে কাজ করে

**গুরুত্বপূর্ণ:** Producer আর Consumer একে অপরকে চেনে না, একই সময়ে চালু থাকারও দরকার নেই। এটাকে বলে **decoupling**।

---

## Queue ব্যবহারের ৫টা কারণ

**১. Asynchronous Processing** — ধীর কাজ পরে করো, user-কে অপেক্ষা করিও না।

**২. Decoupling** — Order service জানেই না email service আছে কি নেই। Email service বন্ধ করে দিলেও order service-এর code বদলাতে হবে না।

**৩. Load Leveling (Buffering)** — Flash sale-এ হঠাৎ ১০ লাখ order এলো, কিন্তু তোমার worker সেকেন্ডে ১০০০টা সামলাতে পারে। Queue সব ধরে রাখবে, worker নিজের গতিতে করে যাবে। System crash করবে না, শুধু একটু দেরি হবে।

**৪. Reliability** — Consumer crash করলে message queue-তেই থাকে, আরেকটা consumer নিয়ে নেয়। কিছুই হারায় না।

**৫. Scalability** — Queue-তে জট লেগে গেছে? আরো worker যোগ করে দাও। Worker গুলো নিজেরাই ভাগ করে নেবে।

---

## দুটো মূল Model — Queue vs Pub/Sub

### Point-to-Point (Queue)

একটা message **শুধু একজন** consumer পাবে।

```
                    ┌── Worker-1  (message 1, 4, 7)
Producer ──► Queue ─┼── Worker-2  (message 2, 5, 8)
                    └── Worker-3  (message 3, 6, 9)
```

*কোথায়:* কাজ ভাগ করে দেওয়া — image resize, email পাঠানো, PDF তৈরি। একই কাজ দুজন করলে অপচয়।

### Publish/Subscribe (Pub/Sub)

একটা message **সব** subscriber পাবে।

```
                       ┌── Email Service     (সবাই একই message পায়)
Publisher ──► Topic ───┼── Analytics Service
                       ├── Inventory Service
                       └── Notification Service
```

*কোথায়:* একটা ঘটনা ঘটেছে, অনেকের জানা দরকার — "order_placed" event-এ email যাবে, stock কমবে, analytics-এ যাবে, warehouse-এ যাবে।

**Kafka-তে দুটোই সম্ভব** — একই consumer group-এর সদস্যরা কাজ ভাগ করে নেয় (queue-র মতো), আর আলাদা আলাদা consumer group সবাই পুরো data পায় (pub/sub-এর মতো)।

---

## Kafka — Log-based Streaming

Kafka সাধারণ queue না, এটা একটা **distributed commit log**। পার্থক্যটা মৌলিক।

**সাধারণ queue-তে:** message পড়া হলে মুছে যায়।
**Kafka-তে:** message একটা append-only log-এ থাকে, retention period (যেমন ৭ দিন) পর্যন্ত থাকে। যে কেউ, যতবার খুশি পড়তে পারে।

### গঠন

```
Topic: "orders"
┌─────────────────────────────────────────────┐
│ Partition 0 │ m0 │ m1 │ m2 │ m3 │ m4 │      │
│ Partition 1 │ m0 │ m1 │ m2 │                │
│ Partition 2 │ m0 │ m1 │ m2 │ m3 │           │
└─────────────────────────────────────────────┘
                      ▲
              Consumer এখানে (offset = 2)
```

**Topic** — message-এর শ্রেণী, যেমন "orders", "user-events"।

**Partition** — Topic-এর ভিতরের ভাগ। **এটাই Kafka-র scaling-এর চাবি** — যত partition, তত সমান্তরাল consumer।

**Offset** — প্রতিটা message-এর ক্রমিক নম্বর। Consumer নিজে মনে রাখে সে কত পর্যন্ত পড়েছে। এর মানে সে চাইলে **পিছিয়ে গিয়ে আবার পড়তে পারে** (replay) — এটা Kafka-র বিশাল শক্তি।

**Consumer Group** — একদল consumer যারা কাজ ভাগ করে নেয়। একটা partition এক সময়ে group-এর একজনকেই দেওয়া হয়।

> **জরুরি নিয়ম:** Consumer-এর সংখ্যা partition-এর চেয়ে বেশি হলে অতিরিক্ত consumer গুলো বসে থাকবে। তাই partition সংখ্যা ঠিক করা গুরুত্বপূর্ণ — এটাই সর্বোচ্চ parallelism ঠিক করে দেয়।

**Replication** — প্রতিটা partition-এর কয়েকটা copy আলাদা broker-এ থাকে। একটা broker মরলে data হারায় না।

### Kafka কেন এত দ্রুত

- **Sequential disk write** — Disk-এ এলোমেলো না লিখে সারিবদ্ধভাবে লেখে, যা এলোমেলো লেখার চেয়ে শতগুণ দ্রুত (SSD-তেও)
- **Zero-copy** — Disk থেকে network-এ data যায় application memory ছুঁয়ে না গিয়ে
- **Batching ও compression** — অনেক message একসাথে পাঠায়
- **কোনো complex routing নেই** — শুধু append আর read

---

## Kafka vs RabbitMQ — কোনটা কখন

| বিষয় | Kafka | RabbitMQ |
|---|---|---|
| ধরন | Distributed log | Traditional message broker |
| Message পড়ার পর | থেকে যায় (retention পর্যন্ত) | মুছে যায় (ack-এর পর) |
| Replay | সম্ভব | সম্ভব না |
| Throughput | অতি উচ্চ (লাখ লাখ/সেকেন্ড) | মাঝারি (হাজার হাজার/সেকেন্ড) |
| Routing | সরল (topic + partition) | খুব শক্তিশালী (exchange, binding, header) |
| Ordering | Partition-এর ভিতরে নিশ্চিত | Queue-তে নিশ্চিত |
| Per-message priority | নেই | আছে |
| Protocol | নিজস্ব | AMQP, MQTT, STOMP |
| শেখা | কঠিন | সহজ |
| উপযুক্ত | Event streaming, log, analytics | Task queue, RPC, জটিল routing |

**সহজ নিয়ম:**
- **Kafka** নাও যখন — বিশাল পরিমাণ event, একাধিক consumer একই data চায়, replay দরকার, event sourcing।
- **RabbitMQ** নাও যখন — background task, জটিল routing, per-message priority, বা তুলনামূলক কম volume।

**অন্যান্য:** AWS SQS (সহজ, managed), Google Pub/Sub, Redis Streams (হালকা), NATS (অতি দ্রুত), Apache Pulsar (Kafka-র প্রতিদ্বন্দ্বী)।

---

## Delivery Guarantee — কতবার পৌঁছাবে

তিনটা স্তর আছে, আর এটা interview-র প্রিয় প্রশ্ন।

**১. At-most-once (সর্বোচ্চ একবার)**
Message হারাতে পারে, কিন্তু কখনো দুবার আসবে না। Consumer কাজ শুরুর আগেই ack দিয়ে দেয়।
*কোথায়:* Metrics, log — দু-একটা হারালে ক্ষতি নেই।

**২. At-least-once (অন্তত একবার)** — সবচেয়ে বেশি ব্যবহৃত
Message কখনো হারাবে না, কিন্তু কখনো দুবার আসতে পারে। Consumer কাজ শেষ করে তবেই ack দেয়; ack পাঠানোর আগে crash করলে message আবার আসবে।
*শর্ত:* Consumer-কে **idempotent** হতে হবে।

**৩. Exactly-once (ঠিক একবার)**
আদর্শ, কিন্তু ব্যয়বহুল ও জটিল। Kafka transaction দিয়ে এটা দেয়, কিন্তু throughput কমে যায়।

**বাস্তব পরামর্শ:** Exactly-once-এর পিছনে না ছুটে **at-least-once + idempotent consumer** ব্যবহার করো। এটাই ইন্ডাস্ট্রির স্ট্যান্ডার্ড সমাধান, এবং কার্যত exactly-once-এর সমান ফল দেয়।

---

## Idempotency — দুবার এলেও যেন সমস্যা না হয়

**Idempotent** মানে — একই কাজ একবার করলে যা হয়, দশবার করলেও তাই হয়।

```
❌ Idempotent না:  balance = balance + 100      (দুবার চললে ২০০ যোগ হবে!)
✅ Idempotent:     balance = 500                (যতবার চালাও, ৫০০-ই থাকবে)
```

**কীভাবে বানাবে:**

**১. Unique ID দিয়ে duplicate ধরা**
```javascript
async function handleMessage(msg) {
  const exists = await redis.set(`processed:${msg.id}`, '1', 'NX', 'EX', 86400);
  if (!exists) return;              // আগেই করা হয়ে গেছে, বাদ দাও
  await doWork(msg);
}
```

**২. Database-এর unique constraint কাজে লাগাও**
`order_id` কে unique করে দাও। দ্বিতীয়বার insert করতে গেলে error দেবে — সেটাকে সফল ধরে নাও।

**৩. Upsert ব্যবহার করো**
`INSERT ... ON CONFLICT DO UPDATE` — একবার হোক বা দশবার, ফল একই।

**৪. State machine দিয়ে**
Order যদি ইতিমধ্যেই "shipped" অবস্থায় থাকে, তাহলে "confirm" event আবার এলে সেটা উপেক্ষা করো।

**একটা বাস্তব উদাহরণ:** Payment API-তে সবসময় `Idempotency-Key` header পাঠানো হয় (Stripe এটা করে)। Network timeout-এ client আবার চেষ্টা করলে server ওই key দেখে বুঝে যায় "এই payment তো হয়েই গেছে" — দুবার টাকা কাটে না।

---

## Dead Letter Queue — যে message কিছুতেই process হয় না

কিছু message বারবার fail করে — data-ই ভুল, বা code-এ bug।

Retry করতে থাকলে সেটা **poison message** হয়ে queue আটকে রাখে, বাকি সব message পিছনে দাঁড়িয়ে থাকে।

**সমাধান — কয়েকবার চেষ্টার পর আলাদা queue-তে সরিয়ে রাখো:**

```
Queue ──► Consumer ──► ব্যর্থ (১ম বার) ──► ২ সেকেন্ড পর retry
                   ──► ব্যর্থ (২য় বার) ──► ৪ সেকেন্ড পর retry
                   ──► ব্যর্থ (৩য় বার) ──► ৮ সেকেন্ড পর retry
                   ──► ব্যর্থ (৪র্থ বার) ──► Dead Letter Queue 📮
```

DLQ-তে পড়ে থাকা message গুলো পরে মানুষ দেখে, সমস্যা ঠিক করে, তারপর আবার queue-তে ফেরত পাঠায়।

**Exponential Backoff:** প্রতিবার retry-র মাঝে দ্বিগুণ সময় অপেক্ষা করা (২, ৪, ৮, ১৬ সেকেন্ড)। এর সাথে একটু random **jitter** যোগ করা হয়, যাতে সব consumer একই মুহূর্তে আবার চেষ্টা না করে।

**DLQ-তে message জমছে কিনা — এটা অবশ্যই monitor করতে হবে।** DLQ ভরে যাওয়া মানে কোথাও কিছু ভাঙা।

---

## Message Ordering

**সমস্যা:** একাধিক consumer সমান্তরালে কাজ করলে ক্রম নষ্ট হতে পারে।

```
Producer পাঠালো:  [order_created] → [order_paid] → [order_shipped]
Consumer পেলো:    [order_shipped] → [order_created] → [order_paid]  😱
```

**সমাধান:**

**১. Partition Key ব্যবহার করো (Kafka-র উপায়)**
একই key-র সব message একই partition-এ যায়, আর একটা partition এক consumer-ই পড়ে — তাই ক্রম বজায় থাকে।
```
partition = hash(order_id) % partition_count
```
এক order-এর সব event ক্রমে আসবে, আর আলাদা order-গুলো সমান্তরালে চলবে। **সেরা সমাধান** — ক্রমও থাকলো, parallelism-ও থাকলো।

**২. একটাই partition / একটাই consumer** — ক্রম নিশ্চিত, কিন্তু scale করে না।

**৩. Message-এ sequence number দাও** — Consumer নিজে সাজিয়ে নেবে, বা ক্রম ভুল হলে অপেক্ষা করবে।

**৪. এমনভাবে design করো যাতে ক্রম না লাগে** — সবচেয়ে ভালো উপায় যদি সম্ভব হয়। প্রতিটা event-এ সম্পূর্ণ state পাঠাও, আর timestamp দেখে পুরনো event বাদ দাও।

---

## Backpressure ও Consumer Lag

**Consumer Lag** = Producer কত message পাঠিয়েছে − Consumer কত পড়েছে।

Lag বাড়তে থাকা মানে consumer পিছিয়ে পড়ছে। **এটাই queue system-এর সবচেয়ে গুরুত্বপূর্ণ metric।**

**Lag বাড়লে কী করবে:**
- আরো consumer যোগ করো (তবে partition-এর বেশি না)
- Consumer-এর কাজ দ্রুত করো (batch processing, DB query optimize)
- Partition সংখ্যা বাড়াও
- Producer-কে ধীর করো (rate limit)

**Backpressure** — Consumer সামলাতে না পারলে producer-কে সংকেত দিয়ে ধীর করা। না হলে queue অসীম বড় হতে থাকবে আর একসময় memory/disk শেষ হয়ে যাবে।

---

## Event-Driven Architecture

Message queue-র উপর ভিত্তি করে গড়ে ওঠা একটা পূর্ণাঙ্গ স্থাপত্য।

**প্রচলিত (Request-driven):**
```
Order Service ──HTTP──► Payment Service
              ──HTTP──► Inventory Service
              ──HTTP──► Email Service
```
Order service সবাইকে চেনে। কেউ down থাকলে order আটকে যায়। নতুন service যোগ করতে order service-এর code বদলাতে হয়।

**Event-driven:**
```
Order Service ──► "OrderPlaced" event ──► Event Bus
                                             │
                        ┌────────────────────┼────────────────────┐
                        ▼                    ▼                    ▼
                 Payment Service     Inventory Service      Email Service
```
Order service শুধু ঘোষণা করে "একটা order হয়েছে"। কে শুনছে, কে কী করছে — তার জানার দরকার নেই। নতুন service যোগ করতে **কোনো পুরনো code বদলাতে হয় না** — শুধু নতুন subscriber যোগ করো।

**সুবিধা:** পূর্ণ decoupling, স্বাধীনভাবে scale, নতুন feature যোগ করা সহজ, একটা service down হলেও বাকিরা চলে।

**অসুবিধা:** Debug করা কঠিন (কোন event কোথায় গেলো?), eventual consistency মেনে নিতে হয়, distributed tracing লাগে, "সব ঠিক আছে কিনা" বোঝা কঠিন।

### Event Sourcing

State না রেখে **সব ঘটনা** রাখা। বর্তমান অবস্থা event গুলো পরপর চালিয়ে বের করা হয়।

```
প্রচলিত:      balance = 1500

Event Sourcing:
  Deposited 1000
  Deposited 800
  Withdrew  300
  ────────────────
  balance = 1500  (হিসাব করে বের করা)
```

*সুবিধা:* সম্পূর্ণ ইতিহাস থাকে, যেকোনো সময়ের অবস্থা দেখা যায়, audit-এর জন্য নিখুঁত, bug ঠিক করে event গুলো আবার চালানো যায়।
*অসুবিধা:* জটিল, বর্তমান অবস্থা জানতে হিসাব লাগে (তাই snapshot রাখতে হয়), storage বেশি লাগে।

**CQRS** — প্রায়ই এর সাথে ব্যবহার হয়। Write-এর model আর read-এর model আলাদা করে দেওয়া। Write যায় event log-এ, read আসে আগে থেকে হিসাব করে রাখা view থেকে।

---

## Saga Pattern — Distributed Transaction

**সমস্যা:** Microservices-এ প্রতিটা service-এর নিজের database। একটা order-এ payment, inventory, shipping — তিনটা service জড়িত। ACID transaction তো এখানে কাজ করবে না!

**সমাধান — Saga:** কাজটাকে ছোট ছোট ধাপে ভাগ করো, প্রতিটা ধাপ নিজের DB-তে commit করে। কোনো ধাপ fail করলে আগের ধাপগুলোর **উল্টো কাজ (compensating transaction)** চালাও।

```
✅ সফল পথ:
   Order তৈরি → Payment কাটা → Stock কমানো → Shipping শুরু

❌ Stock না থাকলে:
   Order তৈরি → Payment কাটা → Stock ব্যর্থ ✗
                      ↓
              Payment ফেরত (compensating)
                      ↓
              Order বাতিল (compensating)
```

**দুই ধরনের Saga:**

**Choreography** — কোনো কেন্দ্রীয় নিয়ন্ত্রক নেই। প্রতিটা service event শোনে আর নিজের কাজ করে পরের event ছাড়ে।
*ভালো:* সরল, decoupled। *খারাপ:* পুরো flow কোথাও লেখা নেই, বোঝা কঠিন।

**Orchestration** — একটা কেন্দ্রীয় orchestrator পুরো flow পরিচালনা করে।
*ভালো:* Flow স্পষ্ট, debug সহজ। *খারাপ:* Orchestrator একটা কেন্দ্রীয় নির্ভরতা।

---

## Interview Q&A

**প্রশ্ন ১: Message queue কেন ব্যবহার করবে?**

মূলত পাঁচটা কারণে। এক, ধীর কাজগুলো asynchronous করে user-এর response time কমানো। দুই, service গুলোকে decouple করা যাতে একটার পরিবর্তন বা downtime অন্যটাকে না ভাঙে। তিন, হঠাৎ traffic spike-এ buffer হিসেবে কাজ করে system-কে বাঁচানো। চার, consumer crash করলেও message হারায় না তাই reliability বাড়ে। পাঁচ, worker যোগ করে সহজে scale করা যায়।

**প্রশ্ন ২: Kafka আর RabbitMQ-র মধ্যে পার্থক্য?**

RabbitMQ একটা প্রচলিত message broker — message consume হয়ে ack পেলে মুছে যায়, এবং এর routing ক্ষমতা খুব শক্তিশালী। Kafka একটা distributed log — message retention period পর্যন্ত থেকে যায়, তাই একাধিক consumer group একই data পড়তে পারে এবং পিছিয়ে গিয়ে replay করা যায়। Throughput-এ Kafka অনেক এগিয়ে, কিন্তু per-message priority বা জটিল routing দরকার হলে RabbitMQ ভালো। সহজভাবে — event streaming ও analytics-এ Kafka, task queue ও RPC-তে RabbitMQ।

**প্রশ্ন ৩: At-least-once আর exactly-once-এর পার্থক্য?**

At-least-once মানে message কখনো হারাবে না, তবে duplicate আসতে পারে — কারণ consumer কাজ শেষ করে ack দেওয়ার আগে crash করলে message আবার delivered হয়। Exactly-once মানে ঠিক একবারই process হবে, কিন্তু এটা নিশ্চিত করতে distributed transaction লাগে যা ধীর ও জটিল। বাস্তবে বেশিরভাগ system at-least-once ব্যবহার করে এবং consumer-কে idempotent বানায় — এতে duplicate এলেও ফলাফল একই থাকে, কার্যত exactly-once-এর সমান।

**প্রশ্ন ৪: Idempotency কী এবং কীভাবে নিশ্চিত করবে?**

Idempotent operation মানে একই কাজ যতবারই করা হোক, ফলাফল একবার করার মতোই থাকবে। নিশ্চিত করার কয়েকটা উপায় — প্রতিটা message-এ unique ID রেখে Redis বা database-এ "এটা আগে process হয়েছে কিনা" যাচাই করা; database-এ unique constraint দিয়ে duplicate insert আটকানো; `INSERT ... ON CONFLICT DO UPDATE`-এর মতো upsert ব্যবহার করা; অথবা state machine দিয়ে ইতিমধ্যে সম্পন্ন অবস্থার event উপেক্ষা করা।

**প্রশ্ন ৫: Kafka-তে message-এর ক্রম কীভাবে বজায় রাখবে?**

Kafka শুধু একটা partition-এর ভিতরে ক্রমের নিশ্চয়তা দেয়। তাই যেসব message-এর ক্রম গুরুত্বপূর্ণ, সেগুলোকে একই partition-এ পাঠাতে হয় — এজন্য partition key হিসেবে এমন কিছু ব্যবহার করা হয় যা ওই সম্পর্কিত message গুলোতে একই থাকে, যেমন `order_id` বা `user_id`। এতে এক order-এর সব event ক্রমে আসে, অথচ বিভিন্ন order সমান্তরালে process হতে পারে — ক্রম আর parallelism দুটোই পাওয়া যায়।

**প্রশ্ন ৬: Dead Letter Queue কী?**

যেসব message বারবার চেষ্টা করেও process করা যায় না, সেগুলোকে সরিয়ে রাখার আলাদা queue। এটা না থাকলে একটা ত্রুটিপূর্ণ message অসীমবার retry হতে থাকে এবং পুরো queue আটকে দেয়। নির্দিষ্ট সংখ্যক ব্যর্থ চেষ্টার পর message DLQ-তে পাঠিয়ে দেওয়া হয়, যাতে বাকি message গুলো চলতে পারে; পরে developer সেগুলো পরীক্ষা করে সমস্যা ঠিক করে আবার process করাতে পারে। DLQ-তে message জমা হচ্ছে কিনা তা monitor করা জরুরি।

**প্রশ্ন ৭: Consumer lag কী এবং বেড়ে গেলে কী করবে?**

Consumer lag হলো producer যত message লিখেছে আর consumer যত পড়েছে তার পার্থক্য। এটা বাড়তে থাকা মানে consumer producer-এর গতির সাথে তাল মেলাতে পারছে না। সমাধানে প্রথমে consumer instance বাড়াতে হবে, তবে Kafka-তে partition সংখ্যার বেশি consumer যোগ করে লাভ নেই — তখন partition সংখ্যাও বাড়াতে হবে। পাশাপাশি consumer-এর প্রতিটা message process করার সময় কমানো (batching, DB query optimization) এবং প্রয়োজনে producer-এর দিকে rate limit দেওয়া যেতে পারে।

**প্রশ্ন ৮: Event-driven architecture-এর অসুবিধা কী?**

সবচেয়ে বড় অসুবিধা হলো debug করা কঠিন — একটা request কোন কোন service-এ কী কী event তৈরি করলো তা বোঝা মুশকিল, তাই distributed tracing লাগে। দ্বিতীয়ত eventual consistency মেনে নিতে হয়, কারণ event process হতে সময় লাগে। তৃতীয়ত event-এর schema বদলানো ঝুঁকিপূর্ণ, কারণ কোন কোন service সেটা শুনছে তা সবসময় জানা থাকে না। আর operational জটিলতাও বাড়ে — message broker নিজেই একটা critical infrastructure যা maintain করতে হয়।

**প্রশ্ন ৯: Saga pattern কী?**

Microservices-এ প্রতিটা service-এর নিজস্ব database থাকায় একাধিক service জুড়ে ACID transaction চালানো যায় না। Saga হলো এর সমাধান — পুরো কাজটাকে ছোট ছোট local transaction-এর ধারাবাহিকতায় ভাগ করা হয়, প্রতিটা ধাপ নিজের database-এ commit করে। কোনো ধাপ ব্যর্থ হলে আগের সফল ধাপগুলোর জন্য compensating transaction চালানো হয় — যেমন payment নেওয়ার পর stock না থাকলে payment refund করা। এটা choreography (event-ভিত্তিক, কেন্দ্রীয় নিয়ন্ত্রক নেই) বা orchestration (একটা কেন্দ্রীয় coordinator) — দুইভাবে করা যায়।

**প্রশ্ন ১০: Queue-তে message জমে গেলে (backlog) কী করবে?**

প্রথমে বুঝতে হবে এটা সাময়িক spike নাকি স্থায়ী সমস্যা। সাময়িক হলে queue-র কাজই তো সেটা শুষে নেওয়া — শুধু নজর রাখতে হবে। স্থায়ী হলে consumer capacity বাড়াতে হবে — instance যোগ করা, partition বাড়ানো, বা consumer-এর processing দ্রুত করা। জরুরি অবস্থায় message গুলোকে গুরুত্ব অনুযায়ী আলাদা queue-তে ভাগ করা যায় যাতে গুরুত্বপূর্ণগুলো আগে হয়, আর কম গুরুত্বপূর্ণ message drop করাও একটা বৈধ সিদ্ধান্ত হতে পারে (যেমন analytics event)।

---

## এক কথায় মনে রাখো

Message Queue = **"এখন না, পরে করবো"** — user-কে অপেক্ষা করানোর দরকার নেই।

তিনটা মূল লাভ: **Async** (দ্রুত response), **Decoupling** (একে অপরকে চেনার দরকার নেই), **Buffering** (spike সামলানো)।

**Kafka** = বিশাল event stream, replay দরকার। **RabbitMQ** = task queue, জটিল routing।

সবচেয়ে জরুরি বাস্তব নিয়ম: **At-least-once delivery + Idempotent consumer**। এটাই সঠিক সমাধান, exactly-once-এর পিছনে ছুটো না।
