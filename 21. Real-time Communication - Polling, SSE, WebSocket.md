# Real-time Communication — Polling, SSE, WebSocket

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [সমস্যাটা কী — HTTP-র মূল সীমাবদ্ধতা](#সমস্যাটা-কী--http-র-মূল-সীমাবদ্ধতা)
* [১. Short Polling — বারবার জিজ্ঞেস করা](#১-short-polling--বারবার-জিজ্ঞেস-করা)
* [২. Long Polling — জিজ্ঞেস করে অপেক্ষা করা](#২-long-polling--জিজ্ঞেস-করে-অপেক্ষা-করা)
* [৩. Server-Sent Events (SSE) — একমুখী স্রোত](#৩-server-sent-events-sse--একমুখী-স্রোত)
* [৪. WebSocket — পূর্ণ দ্বিমুখী সংযোগ](#৪-websocket--পূর্ণ-দ্বিমুখী-সংযোগ)
* [৫. WebRTC — সরাসরি peer-to-peer](#৫-webrtc--সরাসরি-peer-to-peer)
* [চারটার তুলনা এক নজরে](#চারটার-তুলনা-এক-নজরে)
* [কখন কোনটা বেছে নেবে](#কখন-কোনটা-বেছে-নেবে)
* [WebSocket Scale করার চ্যালেঞ্জ](#websocket-scale-করার-চ্যালেঞ্জ)
* [বাস্তব সমস্যা ও সমাধান](#বাস্তব-সমস্যা-ও-সমাধান)
* [Interview Q&A](#interview-qa)
* [এক কথায় মনে রাখো](#এক-কথায়-মনে-রাখো)

---

<!-- TOC END -->

## সমস্যাটা কী — HTTP-র মূল সীমাবদ্ধতা

সাধারণ HTTP একমুখী — **client চায়, server দেয়।** Server নিজে থেকে কিছু পাঠাতে পারে না।

কিন্তু অনেক application-এ ঠিক উল্টোটা দরকার:

- নতুন message এলে সাথে সাথে দেখাতে হবে (WhatsApp)
- Stock-এর দাম বদলালে সাথে সাথে দেখাতে হবে
- কেউ live document-এ টাইপ করলে সাথে সাথে দেখা যাবে (Google Docs)
- খেলার স্কোর, notification, live location

**তাহলে server কীভাবে client-কে খবর দেবে?** এই সমস্যা সমাধানের কয়েকটা উপায় আছে — সহজ থেকে জটিল।

---

## ১. Short Polling — বারবার জিজ্ঞেস করা

Client প্রতি কয়েক সেকেন্ড পর পর server-কে জিজ্ঞেস করে — "নতুন কিছু আছে?"

```
Client ──"নতুন কিছু?"──► Server
       ◄──"না"──────────
       (৩ সেকেন্ড অপেক্ষা)
Client ──"নতুন কিছু?"──► Server
       ◄──"না"──────────
       (৩ সেকেন্ড অপেক্ষা)
Client ──"নতুন কিছু?"──► Server
       ◄──"হ্যাঁ, এই নাও"─
```

```javascript
setInterval(async () => {
  const res = await fetch('/api/messages?since=' + lastId);
  const data = await res.json();
  if (data.length) render(data);
}, 3000);
```

**সুবিধা:** অসম্ভব সহজ, যেকোনো জায়গায় কাজ করে, কোনো বিশেষ infrastructure লাগে না, stateless তাই scale করা সহজ।

**অসুবিধা:**
- **বিপুল অপচয়** — ৯৫% request-এর উত্তরই "না, নতুন কিছু নেই"
- **Latency** — সবচেয়ে খারাপ ক্ষেত্রে পুরো interval-এর সময় দেরি
- Server-এ অপ্রয়োজনীয় চাপ, mobile-এ ব্যাটারি ক্ষয়

**একটা হিসাব:** ১০,০০০ user × প্রতি ৩ সেকেন্ডে ১টা request = **৩,৩৩৩ req/sec** — অথচ হয়তো আসল update মিনিটে কয়েকটা।

**কখন ঠিক আছে:** যেখানে ৫-৩০ সেকেন্ড দেরি হলেও চলে — dashboard refresh, background sync, order status।

---

## ২. Long Polling — জিজ্ঞেস করে অপেক্ষা করা

Client request পাঠায়, কিন্তু server **সাথে সাথে উত্তর দেয় না** — নতুন data আসা পর্যন্ত connection ধরে রাখে।

```
Client ──"নতুন কিছু?"──► Server
                          (৩০ সেকেন্ড ধরে রাখলো...)
                          (নতুন message এলো!)
       ◄──"এই নাও"────────
Client ──"নতুন কিছু?"──► Server   (সাথে সাথে আবার জিজ্ঞেস)
                          (অপেক্ষা...)
```

Data না এলে timeout-এ (যেমন ৩০ সেকেন্ড) খালি উত্তর দিয়ে দেয়, client আবার জিজ্ঞেস করে।

**সুবিধা:** প্রায় তাৎক্ষণিক update, অথচ সাধারণ HTTP-তেই কাজ করে — কোনো নতুন protocol লাগে না। পুরনো browser আর কড়া firewall-এও চলে।

**অসুবিধা:**
- প্রতিটা connection একটা server resource আটকে রাখে
- Traditional (thread-per-request) server-এ ভয়াবহ — ১০,০০০ user মানে ১০,০০০ thread! (Node.js, Go-তে সমস্যা কম কারণ ওরা event-driven)
- বারবার connection খোলা-বন্ধের overhead
- Proxy বা load balancer timeout দিয়ে connection কেটে দিতে পারে

**কখন ব্যবহার:** WebSocket সম্ভব না হলে fallback হিসেবে। এক সময় Facebook chat আর Gmail এটা ব্যবহার করতো।

---

## ৩. Server-Sent Events (SSE) — একমুখী স্রোত

একটা HTTP connection খোলা থাকে, আর server সেটা দিয়ে **যখন খুশি** data পাঠাতে থাকে। Client শুধু শোনে।

```
Client ──একবার connect──► Server
       ◄──event 1────────
       ◄──event 2────────
       ◄──event 3────────
       (connection খোলাই থাকে)
```

**Client-এর দিক:**
```javascript
const es = new EventSource('/api/stream');

es.onmessage = (e) => console.log(JSON.parse(e.data));
es.addEventListener('price', (e) => updatePrice(e.data));
es.onerror = () => console.log('সংযোগ বিচ্ছিন্ন, নিজে থেকেই আবার চেষ্টা করবে');
```

**Server-এর দিক:**
```javascript
app.get('/api/stream', (req, res) => {
  res.writeHead(200, {
    'Content-Type': 'text/event-stream',
    'Cache-Control': 'no-cache',
    'Connection': 'keep-alive'
  });
  const timer = setInterval(() => {
    res.write(`event: price\ndata: ${JSON.stringify({ price: 100 })}\n\n`);
  }, 1000);
  req.on('close', () => clearInterval(timer));
});
```

**সুবিধা:**
- **স্বয়ংক্রিয় পুনঃসংযোগ** — connection কাটলে browser নিজেই আবার জুড়ে নেয়, কোনো code লাগে না
- **Event ID দিয়ে ধারাবাহিকতা** — `Last-Event-ID` header দিয়ে বিচ্ছিন্ন হওয়ার পর বাদ পড়া event গুলো পাওয়া যায়
- সাধারণ HTTP, তাই firewall/proxy-তে সমস্যা কম
- WebSocket-এর চেয়ে অনেক সহজ
- HTTP/2-তে multiplexing-এর সুবিধা পায়

**অসুবিধা:**
- **একমুখী** — client server-কে কিছু পাঠাতে পারে না (পাঠাতে হলে আলাদা POST করতে হবে)
- শুধু **text** পাঠানো যায় (binary না)
- HTTP/1.1-এ browser-এর প্রতি domain-এ সর্বোচ্চ ৬টা connection-এর সীমায় আটকে যায় (HTTP/2-তে এই সমস্যা নেই)
- IE/পুরনো Edge-এ নেই (তবে এখন আর প্রাসঙ্গিক না)

**কখন সেরা:** যেখানে শুধু server থেকে client-এ data যায় — **notification, live score, stock ticker, log stream, progress bar, AI-র token-by-token response**। (ChatGPT-র streaming উত্তর SSE দিয়েই হয়!)

---

## ৪. WebSocket — পূর্ণ দ্বিমুখী সংযোগ

একটা স্থায়ী TCP connection যেখানে **দুই পক্ষই যখন খুশি** data পাঠাতে পারে।

**কীভাবে শুরু হয় — Handshake:**

```
Client → GET /chat HTTP/1.1
         Upgrade: websocket
         Connection: Upgrade
         Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==

Server → HTTP/1.1 101 Switching Protocols
         Upgrade: websocket
         Connection: Upgrade
         Sec-WebSocket-Accept: s3pPLMBiTxaQ9kYGzzhZRbK+xOo=

এরপর থেকে আর HTTP না — ws:// protocol-এ দ্বিমুখী যোগাযোগ
```

লক্ষ্য করো — শুরুটা HTTP দিয়েই হয় (তাই firewall সমস্যা করে না), তারপর protocol **upgrade** হয়ে যায়।

```javascript
const ws = new WebSocket('wss://example.com/chat');

ws.onopen = () => ws.send(JSON.stringify({ type: 'join', room: 'general' }));
ws.onmessage = (e) => renderMessage(JSON.parse(e.data));
ws.onclose = () => reconnectWithBackoff();     // নিজে handle করতে হবে!
```

**সুবিধা:**
- **সত্যিকারের দ্বিমুখী** — দুই দিক থেকেই তাৎক্ষণিক
- **সর্বনিম্ন overhead** — প্রতিটা message-এ মাত্র ২-১৪ byte header (HTTP-তে প্রতিবার শত শত byte header যায়)
- **সর্বনিম্ন latency**
- Text ও binary দুটোই পাঠানো যায়

**অসুবিধা:**
- **Stateful** — এটাই সবচেয়ে বড় সমস্যা। Server-কে প্রতিটা connection মনে রাখতে হয়, তাই horizontal scaling জটিল
- **Reconnection নিজে লিখতে হয়** (SSE-র মতো স্বয়ংক্রিয় না)
- Load balancer, proxy-তে বিশেষ configuration লাগে
- কিছু কড়া corporate firewall ব্লক করে
- সাধারণ HTTP-র caching, compression — কিছুই পাওয়া যায় না

**কখন সেরা:** **Chat, multiplayer game, collaborative editing (Google Docs), trading platform, live location sharing** — যেখানে দুই দিক থেকেই ঘন ঘন data যায়।

---

## ৫. WebRTC — সরাসরি peer-to-peer

Browser থেকে browser-এ **সরাসরি** সংযোগ, server-কে মাঝখানে না রেখে।

```
        ┌── Signaling Server (শুধু পরিচয় করিয়ে দেয়) ──┐
        │                                              │
   Browser A ◄══════ সরাসরি media/data ══════► Browser B
```

Server শুধু শুরুতে দুজনকে একে অপরের ঠিকানা জানিয়ে দেয় (signaling), তারপর সরে যায়।

**সুবিধা:** সর্বনিম্ন latency, server-এ প্রায় কোনো খরচ নেই, video/audio-র জন্য বিশেষভাবে তৈরি।

**অসুবিধা:** খুবই জটিল (NAT traversal, STUN/TURN server লাগে), অনেক network-এ সরাসরি সংযোগ হয় না তাই TURN relay লাগে (যা আবার ব্যয়বহুল)।

**কখন:** Video/audio call (Google Meet, Discord voice), screen sharing, P2P file transfer।

---

## চারটার তুলনা এক নজরে

| বিষয় | Short Polling | Long Polling | SSE | WebSocket |
|---|---|---|---|---|
| দিক | Client → Server | Client → Server | **Server → Client** | **দ্বিমুখী** |
| Protocol | HTTP | HTTP | HTTP | ws/wss (upgrade) |
| Latency | খারাপ (interval অনুযায়ী) | ভালো | খুব ভালো | **সেরা** |
| Overhead | সবচেয়ে বেশি | বেশি | কম | **সবচেয়ে কম** |
| স্বয়ংক্রিয় reconnect | প্রযোজ্য না | নিজে করতে হয় | **আছে ✅** | নিজে করতে হয় |
| Binary data | হ্যাঁ | হ্যাঁ | **না** | হ্যাঁ |
| Scaling | সবচেয়ে সহজ | মাঝারি | মাঝারি | **কঠিন** |
| Firewall-এ সমস্যা | নেই | নেই | নেই | কখনো কখনো |
| জটিলতা | খুব কম | কম | কম | বেশি |
| উদাহরণ | Dashboard refresh | পুরনো chat app | Notification, live score, AI streaming | Chat, game, Google Docs |

---

## কখন কোনটা বেছে নেবে

একটা সহজ সিদ্ধান্তবৃক্ষ:

```
Client-কে কি server-এ ঘন ঘন data পাঠাতে হবে?
│
├── হ্যাঁ ──► WebSocket
│              (chat, game, collaborative editing)
│
└── না, শুধু server থেকে data আসবে
    │
    ├── কত দ্রুত লাগবে?
    │   │
    │   ├── তাৎক্ষণিক (< ১ সেকেন্ড) ──► SSE
    │   │                                (notification, live score, streaming)
    │   │
    │   └── কয়েক সেকেন্ড দেরি চলবে ──► Short Polling
    │                                     (সহজ, সস্তা, যথেষ্ট)
    │
    └── SSE support নেই / পুরনো client ──► Long Polling
```

**একটা গুরুত্বপূর্ণ পরামর্শ:** সবাই WebSocket-এর দিকে ছোটে, কিন্তু বেশিরভাগ ক্ষেত্রে **SSE-ই যথেষ্ট এবং অনেক সহজ**। শুধু server থেকে client-এ data গেলে WebSocket ব্যবহার করা অপ্রয়োজনীয় জটিলতা। Interview-তে এই বিচারবোধ দেখাতে পারলে ভালো ছাপ পড়ে।

---

## WebSocket Scale করার চ্যালেঞ্জ

এটাই interview-র সবচেয়ে গুরুত্বপূর্ণ অংশ।

### সমস্যা ১ — Connection stateful

```
User-A ──► Server-1 এ connected
User-B ──► Server-2 এ connected

User-A একটা message পাঠালো User-B কে।
কিন্তু Server-1 তো User-B-র connection চেনে না! 😱
```

**সমাধান — Pub/Sub backplane:**

```
User-A ──► Server-1 ──publish──► Redis Pub/Sub ──subscribe──► Server-2 ──► User-B
```

সব server একটা shared Redis (বা Kafka, NATS) channel-এ subscribe করে থাকে। কোনো server message পেলে সে publish করে, আর যে server-এ প্রাপক আছে সে সেটা তুলে নিয়ে পাঠিয়ে দেয়।

### সমস্যা ২ — কোন user কোন server-এ, তা জানা

একটা **connection registry** রাখতে হয়:

```
Redis:  user:1001 → server-3
        user:1002 → server-7
```

Connect হলে লেখো, disconnect হলে মুছো। তাহলে target করে পাঠানো যায় (broadcast করে সবার কাছে না পাঠিয়ে)।

### সমস্যা ৩ — Load Balancer-এর configuration

- L4 (TCP) load balancer হলে সহজ; L7 হলে WebSocket upgrade বুঝতে হবে
- **Idle timeout বাড়াতে হবে** — নাহলে LB চুপচাপ থাকা connection কেটে দেবে
- সাধারণত sticky session লাগে (একই connection একই server-এ)
- Nginx-এ `proxy_set_header Upgrade $http_upgrade;` লাগবেই

### সমস্যা ৪ — একটা server কতগুলো connection ধরতে পারে

প্রতিটা connection memory নেয় (~১০-৫০ KB)। ৬৪ GB RAM-এর একটা server তাত্ত্বিকভাবে কয়েক লাখ connection ধরতে পারে, কিন্তু —

- OS-এর **file descriptor limit** বাড়াতে হবে (`ulimit -n`)
- Ephemeral port-এর সীমা আছে
- বাস্তবে ভালোভাবে tuned একটা server-এ **১-৫ লাখ** connection চলে (Node.js, Go, Elixir-এ)

### সমস্যা ৫ — Deploy করলে সবাই বিচ্ছিন্ন হয়ে যায়

Server restart মানে হাজার হাজার connection একসাথে কেটে যাওয়া, তারপর সবাই একসাথে আবার connect করার চেষ্টা — **thundering herd**।

*সমাধান:* Graceful shutdown (নতুন connection নেওয়া বন্ধ করে পুরনোগুলোকে ধীরে ধীরে সরানো), rolling deploy, আর client-এ **jitter সহ exponential backoff** reconnection।

---

## বাস্তব সমস্যা ও সমাধান

**১. Connection বেঁচে আছে কিনা বোঝা — Heartbeat**

TCP connection "নীরবে মরে" যেতে পারে — দুই পক্ষই ভাবছে সংযোগ আছে, আসলে নেই। তাই নিয়মিত ping/pong পাঠাতে হয় (সাধারণত প্রতি ৩০ সেকেন্ডে)। উত্তর না এলে connection মৃত ধরে নিয়ে আবার জোড়া লাগাও।

**২. Reconnection — Exponential Backoff সহ**

```javascript
let delay = 1000;
function connect() {
  const ws = new WebSocket(URL);
  ws.onopen = () => { delay = 1000; };          // সফল হলে reset
  ws.onclose = () => {
    setTimeout(connect, delay + Math.random() * 1000);   // jitter জরুরি!
    delay = Math.min(delay * 2, 30000);          // সর্বোচ্চ ৩০ সেকেন্ড
  };
}
```

Jitter না দিলে সব client একই মুহূর্তে আবার connect করার চেষ্টা করবে আর server-কে ধসিয়ে দেবে।

**৩. বিচ্ছিন্ন থাকাকালীন message হারানো**

Connection কাটা অবস্থায় পাঠানো message গুলো হারিয়ে যায়। সমাধান — প্রতিটা message-এ ক্রমিক নম্বর দাও, আর পুনঃসংযোগের সময় client বলবে "আমি ১০৫ পর্যন্ত পেয়েছি", server ১০৬ থেকে পাঠাবে। SSE-তে এটা `Last-Event-ID` দিয়ে স্বয়ংক্রিয়ভাবে হয়।

**৪. Authentication**

WebSocket handshake-এ custom header পাঠানো যায় না (browser API-র সীমাবদ্ধতা)। তাই সাধারণত — cookie ব্যবহার করা হয়, বা query parameter-এ একটা স্বল্পমেয়াদি token পাঠানো হয় (URL log-এ চলে যায় বলে ঝুঁকিপূর্ণ), বা connection হওয়ার পর প্রথম message হিসেবে token পাঠিয়ে authenticate করা হয় — **শেষটাই সবচেয়ে নিরাপদ**।

**৫. Backpressure**

Client ধীর হলে server-এর send buffer ভরতে থাকে, একসময় memory শেষ। Buffer-এর আকার নজরে রাখো, সীমা ছাড়ালে message বাদ দাও বা connection কেটে দাও।

**৬. সবসময় `wss://` ব্যবহার করো**

`ws://` unencrypted। তাছাড়া অনেক proxy কেবল `wss://` (TLS) traffic-কে ঠিকমতো যেতে দেয়।

---

## Interview Q&A

**প্রশ্ন ১: WebSocket আর HTTP-র মধ্যে পার্থক্য কী?**

HTTP request-response ভিত্তিক এবং একমুখী — client চাইলে তবেই server উত্তর দেয়, server নিজে থেকে কিছু পাঠাতে পারে না, আর প্রতিটা request-এ নতুন করে header যায়। WebSocket একটা স্থায়ী দ্বিমুখী সংযোগ — একবার handshake হয়ে গেলে দুই পক্ষই যখন খুশি data পাঠাতে পারে, আর প্রতিটা message-এ header overhead মাত্র কয়েক byte। তবে WebSocket stateful বলে scale করা অনেক কঠিন, আর HTTP-র caching বা standard tooling-এর সুবিধা পাওয়া যায় না।

**প্রশ্ন ২: SSE আর WebSocket-এর মধ্যে কোনটা বেছে নেবে?**

মূল প্রশ্ন হলো client-কে server-এ ঘন ঘন data পাঠাতে হবে কিনা। যদি data শুধু server থেকে client-এ যায় — notification, live score, dashboard update, AI-র streaming response — তাহলে SSE যথেষ্ট এবং অনেক সহজ, কারণ এটা সাধারণ HTTP-র উপর চলে এবং স্বয়ংক্রিয় reconnection ও event ID দিয়ে ধারাবাহিকতা দেয়। কিন্তু chat, multiplayer game বা collaborative editing-এর মতো যেখানে দুই দিক থেকেই ঘন ঘন data যায়, সেখানে WebSocket লাগবে।

**প্রশ্ন ৩: Long polling কীভাবে কাজ করে?**

Client একটা সাধারণ HTTP request পাঠায়, কিন্তু server সাথে সাথে উত্তর না দিয়ে connection খোলা রেখে অপেক্ষা করে যতক্ষণ না নতুন data আসে বা timeout হয়। Data এলে server উত্তর দেয়, client সেটা process করে সাথে সাথে আবার নতুন request পাঠায়। এতে প্রায় তাৎক্ষণিক update পাওয়া যায় short polling-এর অপচয় ছাড়াই, তবে প্রতিটা অপেক্ষমাণ connection server-এর resource আটকে রাখে, তাই thread-ভিত্তিক server-এ এটা ব্যয়বহুল।

**প্রশ্ন ৪: WebSocket দিয়ে ১ কোটি user-এর chat app কীভাবে scale করবে?**

একটা server সব connection ধরতে পারবে না, তাই অনেকগুলো WebSocket server লাগবে — ভালোভাবে tuned প্রতিটা server ১-৫ লাখ connection ধরতে পারে। কিন্তু তখন সমস্যা হলো user-A যদি Server-1-এ আর user-B Server-2-এ থাকে, Server-1 সরাসরি user-B-কে পাঠাতে পারবে না। এর সমাধান হলো একটা pub/sub backplane — Redis বা Kafka — যেখানে সব server subscribe করে থাকে, আর message ওই channel দিয়ে সঠিক server-এ পৌঁছে যায়। পাশাপাশি একটা registry রাখতে হয় (যেমন Redis-এ `user_id → server_id`) যাতে broadcast না করে সরাসরি target করা যায়। আর message গুলো database-এ (Cassandra-র মতো write-heavy DB) জমা রাখতে হয় offline user-দের জন্য।

**প্রশ্ন ৫: WebSocket connection বেঁচে আছে কিনা কীভাবে বুঝবে?**

Heartbeat বা ping/pong দিয়ে। TCP connection অনেক সময় "নীরবে" মরে যায় — কোনো close event আসে না, দুই পক্ষই ভাবে সংযোগ আছে। তাই নির্দিষ্ট বিরতিতে (সাধারণত ৩০ সেকেন্ড) একটা ping পাঠানো হয়; নির্দিষ্ট সময়ের মধ্যে pong না এলে connection মৃত ধরে নিয়ে বন্ধ করে আবার সংযোগের চেষ্টা করা হয়। এটা idle connection-কে proxy বা load balancer-এর timeout থেকেও বাঁচায়।

**প্রশ্ন ৬: Reconnection logic-এ jitter কেন দরকার?**

Server restart বা network সমস্যায় হাজার হাজার client একসাথে বিচ্ছিন্ন হয়। সবাই যদি ঠিক একই ব্যবধানে আবার connect করার চেষ্টা করে, তাহলে server ফিরে আসার মুহূর্তেই সবাই একসাথে ঝাঁপিয়ে পড়ে — thundering herd — আর server আবার ধসে পড়ে। Exponential backoff-এর সাথে random jitter যোগ করলে চেষ্টাগুলো সময়ের সাথে ছড়িয়ে যায় এবং server ধীরে ধীরে load নিতে পারে।

**প্রশ্ন ৭: WebSocket-এ authentication কীভাবে করবে?**

Browser-এর WebSocket API-তে custom header পাঠানো যায় না, তাই Authorization header ব্যবহার করা যায় না। তিনটা প্রচলিত উপায় আছে — cookie ব্যবহার করা (same-origin হলে কাজ করে, কিন্তু CSRF-এর দিকটা দেখতে হয়); query parameter-এ স্বল্পমেয়াদি token পাঠানো (সহজ, কিন্তু URL server log-এ চলে যেতে পারে); অথবা connection স্থাপনের পর প্রথম message হিসেবে token পাঠিয়ে authenticate করা এবং নির্দিষ্ট সময়ের মধ্যে authenticate না হলে connection কেটে দেওয়া। শেষ পদ্ধতিটাই সাধারণত সবচেয়ে নিরাপদ।

**প্রশ্ন ৮: Short polling কি কখনো ভালো পছন্দ হতে পারে?**

অবশ্যই। যেখানে update-এর জন্য কয়েক সেকেন্ড দেরি গ্রহণযোগ্য, সেখানে short polling-ই সবচেয়ে যুক্তিসঙ্গত — এটা stateless, তাই যেকোনো load balancer-এর পিছনে কোনো বিশেষ configuration ছাড়াই scale করে, কোনো connection management বা reconnection logic লাগে না, এবং debug করা সহজ। যেমন প্রতি ৩০ সেকেন্ডে একবার order status বা background job-এর অগ্রগতি দেখানো — এখানে WebSocket ব্যবহার করা অপ্রয়োজনীয় জটিলতা মাত্র।

**প্রশ্ন ৯: SSE-র সীমাবদ্ধতা কী?**

সবচেয়ে বড় সীমাবদ্ধতা হলো এটা একমুখী — client server-কে কিছু পাঠাতে পারে না, তার জন্য আলাদা HTTP request করতে হয়। দ্বিতীয়ত এটা শুধু text পাঠাতে পারে, binary data-র জন্য base64 encode করতে হয় যা আকার বাড়িয়ে দেয়। তৃতীয়ত HTTP/1.1-এ browser প্রতি domain-এ সর্বোচ্চ ৬টা connection রাখে, তাই একাধিক tab খোলা থাকলে সমস্যা হতে পারে — যদিও HTTP/2-তে multiplexing-এর কারণে এটা আর সমস্যা না।

---

## এক কথায় মনে রাখো

**Short Polling** = বারবার জিজ্ঞেস করা। সহজ, অপচয়ী, কিন্তু অনেক ক্ষেত্রেই যথেষ্ট।

**Long Polling** = জিজ্ঞেস করে অপেক্ষা করা। WebSocket-এর পুরনো বিকল্প।

**SSE** = server থেকে একমুখী স্রোত। **Auto-reconnect সহ, আর বেশিরভাগ real-time প্রয়োজনের জন্য এটাই যথেষ্ট।**

**WebSocket** = পূর্ণ দ্বিমুখী। Chat, game, collaborative editing-এর জন্য অপরিহার্য — কিন্তু stateful বলে scale করতে **Redis pub/sub backplane** লাগবেই।

সবচেয়ে বড় শিক্ষা: **সবকিছুতে WebSocket লাগে না।** প্রয়োজনের চেয়ে সহজতম সমাধানটাই নাও।
