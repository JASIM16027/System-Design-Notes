# Database — SQL vs NoSQL, Indexing এবং Transactions

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [শুরুর কথা — Database কেন এত গুরুত্বপূর্ণ](#শুরুর-কথা--database-কেন-এত-গুরুত্বপূর্ণ)
* [SQL (Relational) Database](#sql-relational-database)
* [NoSQL Database — ৪টা ধরন](#nosql-database--৪টা-ধরন)
* [SQL vs NoSQL — পাশাপাশি তুলনা](#sql-vs-nosql--পাশাপাশি-তুলনা)
* [কখন কোনটা বেছে নেবে](#কখন-কোনটা-বেছে-নেবে)
* [ACID — SQL-এর প্রতিশ্রুতি](#acid--sql-এর-প্রতিশ্রুতি)
* [BASE — NoSQL-এর দর্শন](#base--nosql-এর-দর্শন)
* [Indexing — Database-এর সূচিপত্র](#indexing--database-এর-সূচিপত্র)
* [Index-এর ধরন](#index-এর-ধরন)
* [Transaction ও Isolation Level](#transaction-ও-isolation-level)
* [Normalization vs Denormalization](#normalization-vs-denormalization)
* [Connection Pooling](#connection-pooling)
* [N+1 Query Problem](#n1-query-problem)
* [Interview Q&A](#interview-qa)
* [এক কথায় মনে রাখো](#এক-কথায়-মনে-রাখো)

---

<!-- TOC END -->

## শুরুর কথা — Database কেন এত গুরুত্বপূর্ণ

System design-এ প্রায় সব সময় **database-ই প্রথম bottleneck** হয়।

App server গুলো stateless, তাই যত খুশি বাড়ানো যায়। কিন্তু database-এ data থাকে — সেটাকে ভাগ করা, copy করা, sync রাখা কঠিন।

তাই interview-তে "কোন database ব্যবহার করবে এবং কেন" — এই প্রশ্নের উত্তর ভালোভাবে দিতে পারলে অর্ধেক কাজ হয়ে যায়।

---

## SQL (Relational) Database

Data থাকে **table**-এ — row আর column-এ সাজানো, ঠিক Excel-এর মতো। Table গুলো একে অপরের সাথে **relation** দিয়ে যুক্ত।

```
users                          orders
┌────┬────────┬───────────┐    ┌────┬─────────┬────────┐
│ id │ name   │ email     │    │ id │ user_id │ amount │
├────┼────────┼───────────┤    ├────┼─────────┼────────┤
│ 1  │ Rahim  │ r@x.com   │◄───┤ 1  │    1    │  500   │
│ 2  │ Karim  │ k@x.com   │    │ 2  │    1    │  300   │
└────┴────────┴───────────┘    └────┴─────────┴────────┘
                                      foreign key
```

**মূল বৈশিষ্ট্য:**

- **Fixed Schema** — আগে থেকে ঠিক করতে হয় কোন column থাকবে, কী type হবে
- **Relations** — Foreign key দিয়ে table জোড়া, JOIN দিয়ে একসাথে query
- **ACID** — Transaction-এর পূর্ণ নিরাপত্তা
- **SQL ভাষা** — শক্তিশালী query language, complex analysis সহজ
- **Vertical scaling** — সাধারণত machine বড় করে scale করা হয়

**উদাহরণ:** PostgreSQL, MySQL, Oracle, SQL Server, SQLite

**কখন সেরা:** যেখানে data-র মধ্যে সম্পর্ক জটিল এবং সঠিকতা সবচেয়ে জরুরি — banking, e-commerce order, inventory, booking system।

---

## NoSQL Database — ৪টা ধরন

NoSQL মানে "No SQL" না, বরং "**Not Only SQL**"। এগুলো relational model-এর বাইরে গিয়ে অন্যভাবে data রাখে।

### ১. Document Database

Data থাকে JSON-এর মতো document হিসেবে। প্রতিটা document আলাদা structure-এর হতে পারে।

```json
{
  "_id": "user_1",
  "name": "Rahim",
  "address": { "city": "Dhaka", "zip": "1200" },
  "orders": [ { "id": 1, "amount": 500 } ]
}
```

*সুবিধা:* Schema নমনীয়, একটা object পুরোটা এক জায়গায় থাকে তাই JOIN লাগে না।
*উদাহরণ:* MongoDB, CouchDB, Firestore
*কোথায় ভালো:* Content management, product catalog, user profile — যেখানে প্রতিটা item-এর গঠন আলাদা হতে পারে।

### ২. Key-Value Store

সবচেয়ে সহজ model — একটা key, একটা value। ব্যস।

```
"session:abc123"  →  "{userId: 1, expires: ...}"
"cart:user_5"     →  "[{item: 'shoe', qty: 2}]"
```

*সুবিধা:* অবিশ্বাস্য দ্রুত, সহজে scale করে।
*অসুবিধা:* Key ছাড়া কিছু খোঁজা যায় না।
*উদাহরণ:* Redis, DynamoDB, Memcached
*কোথায় ভালো:* Cache, session, shopping cart, rate limiter, real-time leaderboard।

### ৩. Column-Family (Wide-Column)

Data থাকে column ধরে, row ধরে না। বিশাল পরিমাণ data-য় নির্দিষ্ট কিছু column পড়তে দুর্দান্ত।

*সুবিধা:* বিশাল write throughput, horizontally scale করে চমৎকার, time-series data-য় সেরা।
*উদাহরণ:* Cassandra, HBase, ScyllaDB
*কোথায় ভালো:* IoT sensor data, log, analytics, message history (WhatsApp, Discord এটা ব্যবহার করে)।

### ৪. Graph Database

Data থাকে node আর edge হিসেবে — সম্পর্কটাই এখানে মুখ্য।

```
(Rahim) ──friend──► (Karim) ──friend──► (Jamil)
   │                                        ▲
   └──────────── friend ────────────────────┘
```

*সুবিধা:* "বন্ধুর বন্ধুর বন্ধু" ধরনের query তাৎক্ষণিক — SQL-এ এটা করতে গেলে ভয়াবহ ধীর JOIN লাগে।
*উদাহরণ:* Neo4j, Amazon Neptune, ArangoDB
*কোথায় ভালো:* Social network, recommendation engine, fraud detection, knowledge graph।

---

## SQL vs NoSQL — পাশাপাশি তুলনা

| বিষয় | SQL | NoSQL |
|---|---|---|
| Schema | Fixed, আগে ঠিক করতে হয় | Flexible, চলার পথে বদলানো যায় |
| Data model | Table, row, column | Document / Key-Value / Column / Graph |
| Relation | Foreign key + JOIN | সাধারণত embed বা application-এ handle |
| Consistency | Strong (ACID) | সাধারণত Eventual (BASE) |
| Scaling | Vertical (horizontal কঠিন) | Horizontal (সহজেই) |
| Query | শক্তিশালী SQL, যেকোনো complex query | সীমিত, model-এর উপর নির্ভরশীল |
| Transaction | পূর্ণাঙ্গ, multi-table | সীমিত (আধুনিক গুলোতে কিছুটা আছে) |
| Write speed | তুলনামূলক ধীর | সাধারণত দ্রুত |
| পরিপক্বতা | ৪০+ বছরের | তুলনামূলক নতুন |
| উদাহরণ | PostgreSQL, MySQL | MongoDB, Redis, Cassandra, Neo4j |

---

## কখন কোনটা বেছে নেবে

**SQL বেছে নাও যখন:**

- Data-র মধ্যে জটিল সম্পর্ক আছে (user → order → product → payment)
- Transaction লাগবে — টাকা কাটা আর যোগ করা দুটোই হতে হবে, নয়তো কোনোটাই না
- Data structure মোটামুটি স্থির
- Complex query, report, analytics লাগবে
- Data-র সঠিকতা সবচেয়ে গুরুত্বপূর্ণ (ব্যাংক, হিসাব, inventory)

**NoSQL বেছে নাও যখন:**

- বিশাল পরিমাণ data আর write, যা এক machine-এ ধরবে না
- Schema বারবার বদলায় বা প্রতিটা record আলাদা রকম
- Horizontal scaling দিনের প্রথম দিন থেকেই লাগবে
- Query pattern সহজ ও জানা (key দিয়ে খোঁজা)
- একটু পুরনো data দেখালেও চলবে (like count, view count)

**সবচেয়ে গুরুত্বপূর্ণ বাস্তবতা — Polyglot Persistence:**

আসল system-এ একটাই database থাকে না। যেমন একটা e-commerce site-এ:

```
User, Order, Payment   →  PostgreSQL   (transaction লাগে)
Product catalog        →  MongoDB      (প্রতিটা product-এর আলাদা attribute)
Session, Cart          →  Redis        (দ্রুত, ক্ষণস্থায়ী)
Search                 →  Elasticsearch (full-text search)
Activity log           →  Cassandra    (বিশাল write)
Recommendation         →  Neo4j        (সম্পর্ক ভিত্তিক)
```

**Interview-এর টিপ:** "কোন DB নেবে" জিজ্ঞেস করলে সরাসরি একটা নাম বলে দিও না। বলো — "এই data-র access pattern কী, consistency কতটা লাগবে, scale কত — সেটার উপর নির্ভর করে। Order-এর জন্য PostgreSQL নেবো কারণ transaction লাগবে, কিন্তু activity feed-এর জন্য Cassandra নেবো কারণ write অনেক বেশি।"

---

## ACID — SQL-এর প্রতিশ্রুতি

Transaction মানে কতগুলো operation যা একসাথে সফল হবে অথবা একসাথে বাতিল হবে।

**A — Atomicity (সব অথবা কিছুই না)**

Rahim-এর account থেকে ৫০০ টাকা কেটে Karim-এর account-এ যোগ করতে হবে। প্রথমটা হলো, দ্বিতীয়টার আগেই server crash করলো — তাহলে ৫০০ টাকা হাওয়া! Atomicity বলে: হয় দুটোই হবে, নয় কোনোটাই না। মাঝপথে থেমে গেলে সব rollback।

**C — Consistency (নিয়ম ভাঙবে না)**

Database-এর সব নিয়ম (constraint, foreign key, unique) transaction-এর আগে ও পরে ঠিক থাকবে। Balance কখনো negative হবে না যদি সেই rule থাকে।

**I — Isolation (একে অপরের পথে দাঁড়াবে না)**

দুটো transaction একসাথে চললে একটা আরেকটার অসম্পূর্ণ কাজ দেখতে পাবে না। মনে হবে যেন একটার পর একটা চলছে।

**D — Durability (একবার হলে চিরকাল)**

Commit হয়ে গেলে সেটা disk-এ পাকাপাকি লেখা হয়ে যায়। এর পর বিদ্যুৎ চলে গেলেও data থাকবে।

---

## BASE — NoSQL-এর দর্শন

ACID-এর বিপরীতে NoSQL অনেক সময় BASE অনুসরণ করে।

**BA — Basically Available** — System সবসময় উত্তর দেবে, হয়তো data একটু পুরনো হবে।

**S — Soft state** — Data-র অবস্থা সময়ের সাথে নিজে নিজে বদলাতে পারে (replication চলছে বলে)।

**E — Eventual consistency** — এখন সব node একই data না দেখালেও, কিছুক্ষণ পরে সবাই মিলে যাবে।

**সহজ উদাহরণ:** Facebook-এ তুমি like দিলে, তোমার বন্ধু হয়তো ২ সেকেন্ড পরে দেখবে। এতে পৃথিবী ধ্বংস হয় না — কিন্তু system অনেক দ্রুত আর available থাকে। ব্যাংকে এটা চলবে না, ওখানে ACID লাগবেই।

---

## Indexing — Database-এর সূচিপত্র

### সমস্যাটা কী

১ কোটি row-এর একটা table-এ query করছো:

```sql
SELECT * FROM users WHERE email = 'rahim@example.com';
```

Index না থাকলে database প্রথম row থেকে শেষ row পর্যন্ত একটা একটা করে দেখবে — একে বলে **Full Table Scan**। ১ কোটি row মানে কয়েক সেকেন্ড।

Index থাকলে? **মিলিসেকেন্ড।**

### Index আসলে কী

একটা বইয়ের শেষে যে index থাকে — "Caching ... পৃষ্ঠা ৪৫" — সেটার মতোই। পুরো বই না পড়ে সরাসরি পৃষ্ঠায় চলে যাও।

Database-এ index হলো একটা আলাদা data structure (সাধারণত **B+ Tree**) যেখানে column-এর value গুলো sorted অবস্থায় থাকে, আর প্রতিটার সাথে থাকে আসল row-এর ঠিকানা।

```
Index on email (B+ Tree):

              [ m ]
             /     \
        [ f ]       [ s ]
       /    \       /    \
  a@..  f@..    m@..   s@..  → প্রতিটা pointer করে আসল row-কে
```

B+ Tree-তে খোঁজা O(log n) — ১ কোটি row-এ মাত্র ~২৩টা ধাপ।

### Index-এর দাম

Index শুধু সুবিধা না, খরচও আছে —

- **Storage লাগে** — প্রতিটা index আলাদা জায়গা নেয়
- **Write ধীর হয়** — INSERT/UPDATE/DELETE-এ index গুলোও update করতে হয়
- **বেশি index = বেশি ধীর write**

**নিয়ম:** যে column গুলো `WHERE`, `JOIN`, `ORDER BY`-তে বারবার আসে — শুধু সেগুলোতে index দাও। অন্ধভাবে সব column-এ index দেওয়া মানে write-কে হত্যা করা।

---

## Index-এর ধরন

**Primary Index (Clustered)** — Table-এর data নিজেই এই order-এ disk-এ সাজানো থাকে। প্রতি table-এ একটাই হতে পারে (সাধারণত primary key)।

**Secondary Index (Non-clustered)** — আলাদা structure, যা মূল row-এর দিকে pointer রাখে। যত খুশি বানানো যায়।

**Composite Index** — একাধিক column মিলিয়ে এক index।
```sql
CREATE INDEX idx_user_date ON orders(user_id, created_at);
```
**গুরুত্বপূর্ণ নিয়ম — Leftmost Prefix:** এই index কাজ করবে `WHERE user_id = ?` তে, আর `WHERE user_id = ? AND created_at = ?` তে। কিন্তু শুধু `WHERE created_at = ?` তে **কাজ করবে না**। বাঁ দিক থেকে ক্রম মেনে চলতে হয়।

**Unique Index** — একই value দুবার ঢুকতে দেয় না (email, username)।

**Full-text Index** — শব্দ খোঁজার জন্য। তবে গুরুতর search-এর জন্য Elasticsearch ভালো।

**Covering Index** — query-র সব column-ই index-এ আছে, তাই আসল table-এ যাওয়ারই দরকার নেই। খুব দ্রুত।

**Index কাজ করবে না যেসব ক্ষেত্রে:**

```sql
WHERE YEAR(created_at) = 2024        -- column-এ function দিলে index নষ্ট
WHERE name LIKE '%rahim'             -- শুরুতে % দিলে index অকেজো
WHERE status != 'active'             -- negation-এ প্রায়ই কাজে লাগে না
WHERE user_id = '123'                -- type mismatch (int column-এ string)
```

**Query ধীর কেন — বোঝার উপায়:**
```sql
EXPLAIN ANALYZE SELECT * FROM users WHERE email = 'x@y.com';
```
এতে দেখবে `Seq Scan` (খারাপ) নাকি `Index Scan` (ভালো) হচ্ছে।

---

## Transaction ও Isolation Level

একসাথে অনেক transaction চললে কিছু অদ্ভুত সমস্যা হতে পারে।

**তিনটা সমস্যা:**

**Dirty Read** — অন্য transaction-এর এখনো commit না হওয়া data পড়ে ফেলা। সে যদি rollback করে, তুমি ভুল data নিয়ে বসে আছো।

**Non-repeatable Read** — একই query দুবার চালালে দুই রকম উত্তর, কারণ মাঝখানে অন্য কেউ update করে commit করেছে।

**Phantom Read** — একই condition-এ দুবার query করলে দ্বিতীয়বার নতুন row চলে এসেছে, কারণ মাঝে কেউ insert করেছে।

**চারটা Isolation Level:**

| Level | Dirty Read | Non-repeatable | Phantom | গতি |
|---|---|---|---|---|
| Read Uncommitted | হতে পারে | হতে পারে | হতে পারে | দ্রুততম |
| Read Committed | ❌ হবে না | হতে পারে | হতে পারে | দ্রুত (PostgreSQL default) |
| Repeatable Read | ❌ | ❌ | হতে পারে | মাঝারি (MySQL default) |
| Serializable | ❌ | ❌ | ❌ | ধীরতম, সবচেয়ে নিরাপদ |

যত উপরে ওঠো, তত নিরাপদ কিন্তু তত ধীর। বেশিরভাগ application-এর জন্য **Read Committed** যথেষ্ট।

**Deadlock:** দুটো transaction একে অপরের lock-এর জন্য অপেক্ষা করছে, কেউ এগোতে পারছে না। Database সাধারণত এটা ধরে ফেলে আর একটাকে বাতিল করে দেয়। এড়ানোর উপায় — সবসময় একই ক্রমে row lock করা, আর transaction ছোট রাখা।

**Optimistic vs Pessimistic Locking:**

- **Pessimistic** — "কেউ নিশ্চয়ই একই সময়ে বদলাবে" ধরে নিয়ে আগেই lock করা (`SELECT ... FOR UPDATE`)। Conflict বেশি হলে ভালো।
- **Optimistic** — "সম্ভবত কেউ বদলাবে না" ধরে নিয়ে কাজ করা, শুধু save করার সময় version number মিলিয়ে দেখা। মিল না হলে আবার চেষ্টা। Conflict কম হলে অনেক দ্রুত।

---

## Normalization vs Denormalization

**Normalization** — Data-র পুনরাবৃত্তি কমানো, আলাদা আলাদা table-এ ভাগ করা।

```
❌ Denormalized: প্রতিটা order-এর সাথে user-এর নাম, ঠিকানা লেখা
✅ Normalized:   orders table-এ শুধু user_id, বাকি তথ্য users table-এ
```

*সুবিধা:* Data একবারই লেখা, তাই update সহজ, inconsistency হয় না, storage কম।
*অসুবিধা:* পড়ার সময় JOIN লাগে, যা ধীর।

**Denormalization** — ইচ্ছে করে data duplicate রাখা, যাতে JOIN না লাগে।

*সুবিধা:* Read খুব দ্রুত।
*অসুবিধা:* Update করলে সব জায়গায় করতে হবে, নয়তো data inconsistent।

**নিয়ম:** OLTP (সাধারণ application) → normalize করো। OLAP/reporting/read-heavy → denormalize করো। NoSQL-এ সাধারণত denormalization-ই স্বাভাবিক।

**বাস্তব উদাহরণ:** YouTube-এ video-র view count আলাদা table-এ normalize করে রাখলে প্রতিবার count করতে হবে। তার বদলে video table-এই `view_count` column রেখে দেওয়া হয় — এটা denormalization।

---

## Connection Pooling

Database-এ প্রতিটা connection খুলতে ২০-১০০ms লাগে (TCP handshake, authentication)। প্রতিটা request-এ নতুন connection খোলা মানে বিপর্যয়।

**Connection Pool** হলো আগে থেকে খুলে রাখা কতগুলো connection-এর একটা ভাণ্ডার। Request এলে একটা ধার নাও, কাজ শেষে ফেরত দাও।

```
App ──► [ Pool: 20টা খোলা connection ] ──► Database
```

**Pool size কত রাখবে?** একটা প্রচলিত সূত্র: `connections = (core সংখ্যা × 2) + disk সংখ্যা`। বেশিরভাগ app-এর জন্য ১০-৩০ যথেষ্ট। বেশি connection মানেই বেশি throughput না — বরং DB-তে context switching বেড়ে ধীর হয়ে যায়।

**PgBouncer, HikariCP** — জনপ্রিয় pooler।

---

## N+1 Query Problem

সবচেয়ে বেশি ঘটা performance bug, বিশেষ করে ORM ব্যবহার করলে।

```javascript
const users = await User.findAll();          // ১টা query
for (const user of users) {
  user.orders = await Order.findByUser(user.id);   // N টা query!
}
```

১০০ জন user মানে **১০১টা query**। ভয়ানক ধীর।

**সমাধান — একটা query-তে সব আনো:**

```javascript
// JOIN দিয়ে
SELECT * FROM users u LEFT JOIN orders o ON u.id = o.user_id;

// অথবা দুটো query-তে (IN clause)
const users = await User.findAll();
const orders = await Order.findAll({ where: { userId: users.map(u => u.id) } });
```

ORM-এ একে বলে **eager loading** — Sequelize-এ `include`, Django-তে `select_related`/`prefetch_related`, Rails-এ `includes`।

---

## Interview Q&A

**প্রশ্ন ১: SQL আর NoSQL-এর মধ্যে কোনটা বেছে নেবে, কীভাবে ঠিক করবে?**

এটা data-র প্রকৃতি আর access pattern-এর উপর নির্ভর করে। যদি data-র মধ্যে জটিল সম্পর্ক থাকে, transaction লাগে, এবং সঠিকতা সবচেয়ে জরুরি হয় — তাহলে SQL। যদি বিশাল write throughput লাগে, schema বারবার বদলায়, horizontal scaling দিনের প্রথম দিন থেকেই দরকার হয় এবং eventual consistency মেনে নেওয়া যায় — তাহলে NoSQL। বাস্তবে বড় system-এ দুটোই ব্যবহার হয়, আলাদা আলাদা কাজের জন্য।

**প্রশ্ন ২: ACID কী?**

Atomicity মানে transaction-এর সব operation একসাথে সফল হবে বা একসাথে বাতিল হবে। Consistency মানে transaction-এর আগে ও পরে database-এর সব নিয়ম বজায় থাকবে। Isolation মানে একসাথে চলা transaction গুলো একে অপরের অসম্পূর্ণ কাজ দেখবে না। Durability মানে commit হয়ে গেলে সেটা স্থায়ীভাবে সংরক্ষিত, crash হলেও হারাবে না।

**প্রশ্ন ৩: Index কীভাবে query দ্রুত করে?**

Index একটা আলাদা sorted data structure (সাধারণত B+ Tree) যেখানে column-এর value আর সংশ্লিষ্ট row-এর ঠিকানা থাকে। Index ছাড়া database-কে প্রতিটা row একে একে দেখতে হয় — O(n)। Index থাকলে B+ Tree-তে binary search-এর মতো খুঁজে সরাসরি row-এ পৌঁছানো যায় — O(log n)। ১ কোটি row-এ এর মানে ১ কোটি ধাপের বদলে মাত্র ২৩ ধাপ।

**প্রশ্ন ৪: তাহলে সব column-এ index দিলেই তো হয়?**

না, কারণ প্রতিটা index-এর দাম আছে। এগুলো disk space নেয়, আর প্রতিটা INSERT, UPDATE, DELETE-এ সংশ্লিষ্ট সব index-ও update করতে হয় — তাই write ধীর হয়ে যায়। তাছাড়া অতিরিক্ত index থাকলে query planner-এর ভুল সিদ্ধান্ত নেওয়ার সম্ভাবনাও বাড়ে। তাই শুধু সেই column গুলোতে index দেওয়া উচিত যেগুলো WHERE, JOIN বা ORDER BY-তে নিয়মিত ব্যবহার হয়।

**প্রশ্ন ৫: Clustered আর Non-clustered index-এর পার্থক্য?**

Clustered index-এ table-এর আসল data disk-এ ওই index-এর ক্রমেই সাজানো থাকে, তাই প্রতি table-এ একটাই clustered index সম্ভব — সাধারণত primary key। Non-clustered index একটা আলাদা structure যা মূল row-এর দিকে pointer রাখে; এগুলো যত খুশি বানানো যায়, তবে data পেতে একটা অতিরিক্ত lookup লাগে।

**প্রশ্ন ৬: Composite index-এ column-এর ক্রম গুরুত্বপূর্ণ কেন?**

কারণ composite index বাঁ দিক থেকে ক্রমানুসারে sorted থাকে — একে বলে leftmost prefix rule। `(user_id, created_at)` index টা `WHERE user_id = ?` এবং `WHERE user_id = ? AND created_at = ?` — দুটোতেই কাজ করবে, কিন্তু শুধু `WHERE created_at = ?` তে কাজ করবে না। তাই সবচেয়ে বেশি ব্যবহৃত বা সবচেয়ে selective column আগে রাখতে হয়।

**প্রশ্ন ৭: Normalization আর Denormalization কখন করবে?**

Normalization data duplication কমায়, তাই update সহজ হয় আর inconsistency এড়ানো যায় — সাধারণ transactional application-এ (OLTP) এটাই ঠিক। Denormalization ইচ্ছাকৃতভাবে data duplicate রাখে যাতে JOIN না লাগে এবং read দ্রুত হয় — read-heavy system, analytics, বা NoSQL-এ এটা করা হয়। মূল tradeoff হলো read speed বনাম write complexity ও data consistency।

**প্রশ্ন ৮: Optimistic আর Pessimistic locking-এর পার্থক্য?**

Pessimistic locking ধরে নেয় conflict হবেই, তাই আগেই row lock করে রাখে (`SELECT FOR UPDATE`) — অন্যরা অপেক্ষা করে। এটা নিরাপদ কিন্তু throughput কমায় এবং deadlock-এর ঝুঁকি থাকে। Optimistic locking ধরে নেয় conflict বিরল, তাই lock না করেই কাজ করে; শুধু save করার সময় version number মিলিয়ে দেখে — না মিললে ব্যর্থ হয়ে আবার চেষ্টা করে। Conflict কম হলে optimistic অনেক বেশি দ্রুত।

**প্রশ্ন ৯: N+1 query problem কী?**

যখন একটা query দিয়ে N টা record আনা হয়, তারপর প্রতিটা record-এর সম্পর্কিত data আনতে আলাদা আলাদা query চালানো হয় — মোট N+1 টা query। ORM ব্যবহারে এটা খুব সহজে হয়ে যায় এবং ভয়াবহ ধীর করে দেয়। সমাধান হলো JOIN ব্যবহার করা অথবা eager loading — সব সম্পর্কিত data একটা বা দুটো query-তে একসাথে আনা।

**প্রশ্ন ১০: Database connection pooling কেন দরকার?**

প্রতিটা নতুন database connection তৈরি করতে TCP handshake আর authentication মিলিয়ে উল্লেখযোগ্য সময় লাগে (২০-১০০ms)। প্রতিটা request-এ নতুন connection খুললে সেটাই bottleneck হয়ে দাঁড়ায়। Connection pool আগে থেকে কিছু connection খুলে রাখে এবং request গুলোর মধ্যে সেগুলো পুনর্ব্যবহার করে, ফলে overhead প্রায় শূন্য হয়ে যায় এবং database-এ concurrent connection-এর সংখ্যাও নিয়ন্ত্রণে থাকে।

**প্রশ্ন ১১: Isolation level কী এবং কোনটা ব্যবহার করবে?**

Isolation level ঠিক করে একসাথে চলা transaction গুলো একে অপরের কাজ কতটা দেখতে পাবে। Read Uncommitted সবচেয়ে শিথিল — dirty read পর্যন্ত হতে পারে। Read Committed শুধু commit হওয়া data পড়তে দেয় (PostgreSQL-এর default)। Repeatable Read নিশ্চিত করে একই query বারবার একই ফল দেবে। Serializable সবচেয়ে কড়া, যেন transaction গুলো একটার পর একটা চলছে। যত কড়া, তত ধীর — বেশিরভাগ application-এ Read Committed যথেষ্ট, আর আর্থিক লেনদেনের মতো critical জায়গায় Serializable লাগতে পারে।

**প্রশ্ন ১২: MongoDB কি ACID support করে?**

আধুনিক MongoDB (4.0+) multi-document transaction support করে, তাই সীমিত অর্থে করে। তবে এটা সেই কাজের জন্য optimize করা না — transaction ব্যবহার করলে MongoDB-র performance-এর মূল সুবিধাটাই কমে যায়। MongoDB-র আসল শক্তি হলো একটা document-এর মধ্যেই সব সম্পর্কিত data রাখা, যাতে একটা document-level atomic operation-ই যথেষ্ট হয়। যদি সত্যিই ঘন ঘন multi-document transaction লাগে, তাহলে সম্ভবত relational database-ই সঠিক পছন্দ ছিল।

---

## এক কথায় মনে রাখো

**SQL** = সম্পর্ক জটিল, transaction লাগে, সঠিকতা সবার আগে। **NoSQL** = বিশাল scale, নমনীয় schema, দ্রুত write।

**ACID** = Atomicity, Consistency, Isolation, Durability — SQL-এর নিরাপত্তা। **BASE** = পাওয়া যাবেই, তবে data একটু পরে মিলবে — NoSQL-এর দর্শন।

**Index** = বইয়ের সূচিপত্র। Read দ্রুত করে, write ধীর করে। শুধু দরকারি column-এ দাও।

আর বাস্তব system-এ একটা DB না — **প্রতিটা কাজের জন্য উপযুক্ত DB** (polyglot persistence)।
