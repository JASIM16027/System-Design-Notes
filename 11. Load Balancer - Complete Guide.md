# Load Balancer — Complete Guide

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [Load Balancer কী? — ট্রাফিক পুলিশের গল্প](#load-balancer-কী--ট্রাফিক-পুলিশের-গল্প)
* [Load Balancer আসলে কী কী কাজ করে](#load-balancer-আসলে-কী-কী-কাজ-করে)
* [Load Balancing Algorithms — কে কোন server পাবে](#load-balancing-algorithms--কে-কোন-server-পাবে)
* [Health Check — মরা server চেনার উপায়](#health-check--মরা-server-চেনার-উপায়)
* [Layer 4 vs Layer 7 Load Balancer](#layer-4-vs-layer-7-load-balancer)
* [Load Balancer নিজেই যদি মরে যায়?](#load-balancer-নিজেই-যদি-মরে-যায়)
* [Sticky Session — দরকার আছে কি?](#sticky-session--দরকার-আছে-কি)
* [SSL Termination](#ssl-termination)
* [Load Balancer কোথায় কোথায় বসে](#load-balancer-কোথায়-কোথায়-বসে)
* [জনপ্রিয় Load Balancer গুলো](#জনপ্রিয়-load-balancer-গুলো)
* [Interview Q&A](#interview-qa)
* [এক কথায় মনে রাখো](#এক-কথায়-মনে-রাখো)

---

<!-- TOC END -->

## Load Balancer কী? — ট্রাফিক পুলিশের গল্প

ধরো একটা ব্যাংকে ৫টা কাউন্টার আছে। কাস্টমাররা এসে যদি নিজেরাই কাউন্টার বেছে নেয়, তাহলে দেখবে ১ নম্বর কাউন্টারে ৫০ জনের লাইন, আর ৫ নম্বর কাউন্টার খালি।

তাই ব্যাংক একজন লোক রাখে দরজায়, যে বলে দেয় — "আপনি ৩ নম্বরে যান, ওটা খালি।"

এই লোকটাই **Load Balancer**।

Server-এর দুনিয়ায় ব্যাপারটা এমন —

```
                    ┌──────────────────┐
   Users  ────────► │  Load Balancer   │  ← একটাই public IP
                    └────────┬─────────┘
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
         Server-1       Server-2       Server-3
        (10.0.0.1)     (10.0.0.2)     (10.0.0.3)
```

User জানেই না পিছনে কয়টা server আছে। সে শুধু `api.example.com` এ request পাঠায়। Load balancer ঠিক করে দেয় কোন server কাজটা করবে।

---

## Load Balancer আসলে কী কী কাজ করে

শুধু traffic ভাগ করাই এর একমাত্র কাজ না। আরো অনেক কিছু করে —

**১. Traffic distribution** — Request গুলো সব server-এ সমানভাবে ভাগ করে দেয়, যাতে কোনো server অতিরিক্ত চাপে না পড়ে।

**২. Health check** — নিয়মিত প্রতিটা server-কে জিজ্ঞেস করে "তুমি বেঁচে আছো?" যে উত্তর দেয় না, তাকে rotation থেকে বাদ দেয়।

**৩. High Availability** — একটা server crash করলে load balancer তাকে বাদ দিয়ে বাকিদের কাছে traffic পাঠায়। User কিছুই টের পায় না।

**৪. SSL Termination** — HTTPS-এর encryption/decryption-এর ভারী কাজটা নিজে করে নেয়, backend server-কে হালকা রাখে।

**৫. Scaling সহজ করে** — নতুন server যোগ করলে শুধু load balancer-এর pool-এ যোগ করে দাও, আর কিছু লাগে না।

**৬. Zero-downtime deployment** — একটা একটা করে server pool থেকে সরিয়ে update করে আবার যোগ করা যায়। Site কখনো বন্ধ হয় না। এটাকে বলে **Rolling Deployment**।

---

## Load Balancing Algorithms — কে কোন server পাবে

এটা interview-তে খুব common প্রশ্ন। মোট কয়েকটা পদ্ধতি আছে।

### ১. Round Robin — সিরিয়াল ধরে

Request-১ যাবে Server-১ এ, Request-২ যাবে Server-২ এ, Request-৩ যাবে Server-৩ এ, আবার Request-৪ যাবে Server-১ এ।

*সুবিধা:* সবচেয়ে সহজ, কোনো হিসাব রাখতে হয় না।
*অসুবিধা:* সব server সমান শক্তিশালী ধরে নেয়, আর সব request সমান ভারী ধরে নেয়। বাস্তবে তা হয় না।

### ২. Weighted Round Robin — শক্তি অনুযায়ী ভাগ

প্রতিটা server-কে একটা weight দেওয়া হয়। Server-1 এর weight 3, Server-2 এর weight 1 মানে Server-1 তিনগুণ বেশি request পাবে।

*কখন লাগে:* যখন server গুলো আলাদা আলাদা configuration-এর। পুরনো server কম কাজ পাক, নতুন শক্তিশালী server বেশি কাজ পাক।

### ৩. Least Connections — যার কাজ কম, তাকে দাও

যে server-এ এই মুহূর্তে সবচেয়ে কম active connection আছে, সেখানে পাঠাও।

*কখন লাগে:* যখন request গুলোর সময় আলাদা আলাদা। কিছু request 10ms লাগে, কিছু 30 সেকেন্ড। Round robin এখানে খারাপ কাজ করবে, least connections ভালো করবে।

### ৪. Least Response Time — যে সবচেয়ে দ্রুত

Active connection **আর** response time — দুটোই দেখে সবচেয়ে ভালো server বেছে নেয়। সবচেয়ে বুদ্ধিমান, কিন্তু সবচেয়ে বেশি overhead।

### ৫. IP Hash — একই user সবসময় একই server-এ

User-এর IP address কে hash করে server ঠিক করা হয়। একই IP মানে সবসময় একই server।

*কখন লাগে:* যখন sticky session দরকার (যদিও stateless design-ই ভালো)। Cache locality-র জন্যও কাজে লাগে।

### ৬. Consistent Hashing — Server যোগ/বিয়োগেও কম ধাক্কা

সাধারণ hash-এ একটা server যোগ করলে প্রায় সব mapping বদলে যায়। Consistent hashing এই সমস্যা সমাধান করে — server যোগ হলে শুধু অল্প কিছু key-র mapping বদলায়।

*কখন লাগে:* Distributed cache (Redis cluster), database sharding-এ।
(বিস্তারিত: `19. Consistent Hashing.md`)

### কোনটা কখন — সারসংক্ষেপ

| Algorithm | কখন ব্যবহার করবে |
|---|---|
| Round Robin | সব server একই রকম, request একই রকম |
| Weighted RR | Server গুলোর ক্ষমতা আলাদা |
| Least Connections | Request-এর সময় ভিন্ন ভিন্ন (long-running) |
| Least Response Time | Performance সবচেয়ে critical |
| IP Hash | Session affinity লাগবে |
| Consistent Hashing | Distributed cache / sharding |

---

## Health Check — মরা server চেনার উপায়

Load balancer প্রতি কয়েক সেকেন্ড পর পর প্রতিটা server-এ একটা request পাঠায়, সাধারণত এইরকম:

```
GET /health  HTTP/1.1
Host: 10.0.0.2
```

Server যদি `200 OK` দেয়, সে সুস্থ। যদি না দেয় বা timeout হয়, load balancer তাকে "unhealthy" mark করে rotation থেকে বাদ দেয়।

**দুই ধরনের health check:**

**Passive (Shallow)** — শুধু দেখে server response দিচ্ছে কি না। দ্রুত, কিন্তু ভাসা ভাসা। Server চালু আছে কিন্তু database-এর connection নষ্ট — এটা ধরতে পারবে না।

**Active (Deep)** — `/health` endpoint নিজে database, cache, external API সব check করে তারপর উত্তর দেয়। বেশি নির্ভরযোগ্য, কিন্তু বেশি খরচ।

**বাস্তব পরামর্শ:** দুটো endpoint রাখো —
- `/health/live` — শুধু "আমি চালু আছি" (shallow)
- `/health/ready` — "আমি কাজ নিতে প্রস্তুত, আমার DB-ও ঠিক আছে" (deep)

Kubernetes-এ এদেরকে বলে **liveness probe** আর **readiness probe**।

---

## Layer 4 vs Layer 7 Load Balancer

এটা interview-তে খুবই common।

**Layer 4 (Transport Layer)** — শুধু IP address আর port দেখে সিদ্ধান্ত নেয়। ভিতরে কী আছে (URL, header, cookie) সেটা খোলেও দেখে না। শুধু packet forward করে দেয়।

**Layer 7 (Application Layer)** — পুরো HTTP request খুলে পড়ে। URL কী, header-এ কী আছে, cookie কী — সব দেখে তারপর সিদ্ধান্ত নেয়।

| বিষয় | Layer 4 | Layer 7 |
|---|---|---|
| কী দেখে | IP + Port | URL, Header, Cookie, Body |
| গতি | খুব দ্রুত | তুলনামূলক ধীর |
| বুদ্ধি | কম | বেশি |
| Content-based routing | পারে না | পারে |
| SSL termination | পারে না | পারে |
| Caching | পারে না | পারে |
| খরচ (CPU) | কম | বেশি |
| উদাহরণ | AWS NLB, HAProxy (TCP mode) | AWS ALB, Nginx, HAProxy (HTTP mode) |

**Layer 7 দিয়ে যা করা যায় কিন্তু Layer 4 দিয়ে যায় না:**

```
/api/users/*    →  User Service
/api/orders/*   →  Order Service
/images/*       →  Static/CDN Server
```

এটাকে বলে **Path-based routing**। মূলত এই কারণেই API Gateway গুলো Layer 7-এ কাজ করে।

**কখন কোনটা:** Raw speed লাগলে (gaming, video streaming, database proxy) → Layer 4। Smart routing লাগলে (web app, microservices) → Layer 7।

---

## Load Balancer নিজেই যদি মরে যায়?

খুব ভালো প্রশ্ন — এটা interview-তে জিজ্ঞেস করা হয়।

Load balancer যদি একটাই হয়, তাহলে সে নিজেই তো **Single Point of Failure** হয়ে গেলো! আমরা যে সমস্যা সমাধান করতে চাইলাম, সেটাই ফিরে আসলো।

**সমাধান — Load Balancer-এর জোড়া রাখা:**

```
        ┌─────────────────┐
        │  Virtual IP     │  ← DNS এই IP-তে point করে
        └────────┬────────┘
        ┌────────┴────────┐
        ▼                 ▼
   LB-Active  ◄────►  LB-Passive
   (কাজ করছে)  heartbeat  (অপেক্ষায়)
```

**Active-Passive:** একটা কাজ করে, আরেকটা অপেক্ষায় থাকে। Active মরলে passive সাথে সাথে Virtual IP নিয়ে নেয়। এটাকে বলে **Failover**।

**Active-Active:** দুটোই একসাথে কাজ করে, DNS round-robin দিয়ে traffic ভাগ হয়। বেশি efficient।

**DNS Load Balancing:** এক domain-এর জন্য একাধিক IP return করা — সবচেয়ে উপরের স্তরের load balancing, সাধারণত multi-region setup-এ ব্যবহার হয়।

Cloud-এ (AWS ELB, GCP Load Balancer) এটা নিয়ে চিন্তা করতে হয় না — cloud provider নিজেই redundancy দিয়ে রাখে।

---

## Sticky Session — দরকার আছে কি?

Sticky session (session affinity) মানে — একই user-কে সবসময় একই server-এ পাঠানো।

**কীভাবে হয়:** Load balancer একটা cookie বসিয়ে দেয় (`SERVERID=server-2`), পরের বার সেই cookie দেখে একই server-এ পাঠায়।

**কেন লাগে:** যখন server নিজের memory-তে session রাখে।

**কেন এটা খারাপ:**
- ওই server মরে গেলে user-এর session হারিয়ে যায় — logout হয়ে যায়
- Load সমানভাবে ভাগ হয় না — কিছু server-এ বেশি চাপ
- Auto-scaling ঠিকমতো কাজ করে না
- নতুন server যোগ করলে সে খালি বসে থাকে

**ভালো সমাধান:** Session কে server থেকে বের করে আনো —
- Redis-এ session রাখো (centralized), অথবা
- JWT ব্যবহার করো (কোথাও কিছু রাখতেই হবে না)

**এক লাইনে:** Sticky session একটা workaround, solution না। যদি possible হয়, stateless বানাও।

---

## SSL Termination

HTTPS request encrypted অবস্থায় আসে। এই encryption খোলা (decrypt) CPU-এর জন্য ভারী কাজ।

**SSL Termination মানে** — Load balancer নিজে decrypt করে ফেলে, তারপর backend server-এ plain HTTP পাঠায়।

```
User ──HTTPS(encrypted)──► Load Balancer ──HTTP(plain)──► Servers
```

**সুবিধা:**
- Backend server-এর CPU বাঁচে, তারা শুধু business logic নিয়ে ব্যস্ত থাকে
- SSL certificate একটা জায়গায় রাখলেই হয়, প্রতিটা server-এ না
- Certificate renew করা সহজ
- Layer 7 routing সম্ভব হয় (decrypt না করলে URL দেখা যায় না)

**অসুবিধা:** Load balancer আর server-এর মাঝের traffic unencrypted। Private network-এ সাধারণত সমস্যা না, কিন্তু bank/health data হলে **SSL Passthrough** বা **re-encryption** ব্যবহার করতে হয়।

---

## Load Balancer কোথায় কোথায় বসে

একটা বড় system-এ load balancer একাধিক জায়গায় থাকে —

```
Internet
   │
   ▼
[ DNS Load Balancing ]        ← কোন region-এ যাবে
   │
   ▼
[ Global Load Balancer ]      ← কোন data center-এ যাবে
   │
   ▼
[ API Gateway / L7 LB ]       ← কোন service-এ যাবে
   │
   ▼
[ Internal LB ]               ← Service-এর কোন instance-এ যাবে
   │
   ▼
[ Database LB / Proxy ]       ← কোন DB replica-তে যাবে
```

মানে load balancer শুধু "সামনে" থাকে না — system-এর প্রতিটা স্তরেই থাকতে পারে।

---

## জনপ্রিয় Load Balancer গুলো

| নাম | ধরন | বিশেষত্ব |
|---|---|---|
| **Nginx** | Software, L7 | সবচেয়ে জনপ্রিয়, web server হিসেবেও কাজ করে |
| **HAProxy** | Software, L4+L7 | Performance-এ সেরা, খুব configurable |
| **AWS ALB** | Cloud, L7 | Path/host routing, auto-scaling সহ |
| **AWS NLB** | Cloud, L4 | অতি উচ্চ performance, লক্ষ লক্ষ connection |
| **Envoy** | Software, L7 | Service mesh (Istio)-এর ভিত্তি |
| **Traefik** | Software, L7 | Container/Kubernetes-এর জন্য, auto service discovery |
| **Cloudflare** | Cloud, Global | CDN + LB + DDoS protection একসাথে |

---

## Interview Q&A

**প্রশ্ন ১: Load Balancer কী এবং কেন দরকার?**

Load balancer হলো একটা component যা incoming traffic কে একাধিক backend server-এর মধ্যে ভাগ করে দেয়। দরকার তিনটা কারণে — এক, কোনো একটা server অতিরিক্ত চাপে পড়ে না; দুই, একটা server fail করলে বাকিরা কাজ চালিয়ে যায় ফলে high availability পাওয়া যায়; তিন, নতুন server যোগ করে সহজে horizontally scale করা যায়।

**প্রশ্ন ২: Layer 4 আর Layer 7 load balancer-এর পার্থক্য?**

Layer 4 শুধু IP আর port দেখে packet forward করে — খুব দ্রুত কিন্তু বোকা। Layer 7 পুরো HTTP request পড়ে — URL, header, cookie দেখে সিদ্ধান্ত নিতে পারে, তাই path-based routing, SSL termination, caching সব করতে পারে, কিন্তু বেশি CPU লাগে। Microservices-এ সাধারণত L7 লাগে কারণ `/api/users` কে user service-এ পাঠাতে হয়।

**প্রশ্ন ৩: Load balancer কীভাবে জানে কোন server down?**

Health check দিয়ে। প্রতি কয়েক সেকেন্ড পর পর প্রতিটা server-এ একটা request পাঠায় (যেমন `GET /health`)। পরপর কয়েকবার fail করলে সেই server-কে unhealthy mark করে rotation থেকে বাদ দেয়। Server আবার সুস্থ হয়ে 200 দিলে আবার rotation-এ ফিরিয়ে আনে।

**প্রশ্ন ৪: Load balancer নিজে fail করলে কী হবে?**

সেটা single point of failure হয়ে যাবে, তাই বাস্তবে কখনো একটা load balancer রাখা হয় না। Active-passive setup-এ দুটো load balancer একটা virtual IP share করে, heartbeat দিয়ে একে অপরকে monitor করে — active মরলে passive সাথে সাথে IP নিয়ে নেয়। অথবা active-active setup-এ দুটোই কাজ করে আর DNS level-এ traffic ভাগ হয়। Cloud load balancer (AWS ELB) এই redundancy নিজেই manage করে।

**প্রশ্ন ৫: Round Robin আর Least Connections — কোনটা কখন?**

Round Robin ভালো যখন সব request প্রায় একই সময় নেয় এবং সব server একই ক্ষমতার। কিন্তু যদি কিছু request 10ms আর কিছু 30 সেকেন্ড নেয়, তাহলে Round Robin-এ একটা server অনেক long request পেয়ে আটকে যেতে পারে যদিও তার turn এসেছে। তখন Least Connections ভালো — যার হাতে এখন সবচেয়ে কম কাজ, তাকে দাও।

**প্রশ্ন ৬: Load Balancer আর API Gateway কি একই জিনিস?**

না। Load balancer-এর কাজ traffic distribute করা — কোন server পাবে সেটা ঠিক করা। API Gateway-এর কাজ অনেক বেশি — authentication, rate limiting, request transformation, protocol translation, response aggregation, logging। API Gateway সাধারণত load balancer-এর উপরে বসে এবং নিজেও load balancing করতে পারে। এক লাইনে — LB "কোথায় পাঠাবো" ঠিক করে, Gateway "পাঠানোর আগে কী কী করবো" ঠিক করে।

**প্রশ্ন ৭: Sticky session কেন এড়িয়ে চলা উচিত?**

কারণ এটা server-কে stateful বানিয়ে রাখে। ওই server crash করলে user-এর session হারায়, load সমানভাবে distribute হয় না, আর auto-scaling ঠিকমতো কাজ করে না — নতুন server খালি বসে থাকে কারণ পুরনো user-রা তাদের আগের server-এই আটকে আছে। এর বদলে session Redis-এ রাখা বা JWT ব্যবহার করা উচিত।

**প্রশ্ন ৮: SSL termination কী এবং কোথায় করা উচিত?**

SSL termination মানে HTTPS-এর decryption load balancer-এ করে ফেলা, তারপর backend-এ plain HTTP পাঠানো। এতে backend server-এর CPU বাঁচে আর certificate management একটা জায়গায় centralized হয়। তবে LB আর backend-এর মাঝের traffic unencrypted থাকে — তাই highly sensitive data (payment, health record) হলে ওই segment-ও re-encrypt করা উচিত।

---

## এক কথায় মনে রাখো

Load Balancer হলো **ট্রাফিক পুলিশ** — কোন request কোন server-এ যাবে সেটা ঠিক করে।

তিনটা মূল সুবিধা: **load ভাগ করা**, **fail হওয়া server বাদ দেওয়া**, আর **সহজে scale করা**।

Algorithm বাছার নিয়ম: সব সমান হলে **Round Robin**, কাজের সময় ভিন্ন হলে **Least Connections**, cache/shard হলে **Consistent Hashing**।

আর সবচেয়ে জরুরি কথা — Load balancer নিজেও যেন single point of failure না হয়।
