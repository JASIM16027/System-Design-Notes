<!-- TOC START -->

**আলোচ্য বিষয়:**

* [Stateful Architecture](#stateful-architecture)
* [Stateless Architecture](#stateless-architecture)
* [মূল পার্থক্য এক কথায়](#মূল-পার্থক্য-এক-কথায়)
* [১. HTTP-এর মূল nature: Stateless by design](#১-http-এর-মূল-nature-stateless-by-design)
* [২. Scaling problem — এখানেই stateful ভেঙে পড়ে](#২-scaling-problem--এখানেই-stateful-ভেঙে-পড়ে)
* [৩. JWT — Statelessness-এর technical backbone](#৩-jwt--statelessness-এর-technical-backbone)
* [৪. Real-world architecture — Modern hybrid approach](#৪-real-world-architecture--modern-hybrid-approach)
* [Technical summary যা মনে রাখা দরকার](#technical-summary-যা-মনে-রাখা-দরকার)
* [আরো Real-World Examples](#আরো-real-world-examples)
  * [TCP Handshake — Stateful protocol-এর সবচেয়ে ভালো উদাহরণ](#tcp-handshake--stateful-protocol-এর-সবচেয়ে-ভালো-উদাহরণ)
  * [Shopping Cart — Stateful session-এর classic উদাহরণ](#shopping-cart--stateful-session-এর-classic-উদাহরণ)
  * [DNS Lookup — Stateless-এর পারফেক্ট উদাহরণ](#dns-lookup--stateless-এর-পারফেক্ট-উদাহরণ)
  * [Online Gaming vs REST API — পাশাপাশি দেখো](#online-gaming-vs-rest-api--পাশাপাশি-দেখো)
  * [Video Streaming — দুটোর hybrid](#video-streaming--দুটোর-hybrid)
  * [Email — আরেকটি hybrid](#email--আরেকটি-hybrid)

---

<!-- TOC END -->

## Stateful Architecture

<img width="1440" height="1040" alt="image" src="https://github.com/user-attachments/assets/56c90cf8-6173-4859-9104-a4f38bb8c498" />

## Stateful Architecture 

**State মানে কী?**
Server মনে রাখে — তুমি কে, তুমি কী করেছো, তোমার কাছে কী আছে।

**উদাহরণ:** ATM-এ card দিলে, PIN verify হলে, তারপর balance check বা withdraw করলে ATM প্রতিবার তোমাকে চেনে — কারণ সে state ধরে রেখেছে।

---

### কীভাবে কাজ করে (৩টা ধাপ)

1. **Connect** — Client প্রথমবার server-এ আসে
2. **Session তৈরি** — Server একটা session বানায়, তাকে একটা ID দেয় (যেমন টোকেন নম্বর)
3. **পরের প্রতিটি request** — Client শুধু সেই ID পাঠায়, server ID দিয়ে বাকি সব তথ্য খুঁজে বের করে

👉 Client কিছু মনে রাখে না, শুধু ID বহন করে। **Server-ই সব জানে।**

---

### কোথায় ব্যবহার হয়
TCP, WebSocket, SSH, online গেম — যেখানে continuous, দুইদিকের যোগাযোগ দরকার।

---

### সমস্যা — একাধিক server হলে

যদি ৩টা server থাকে আর Rahim-এর session শুধু Server A-তে থাকে, তাহলে Load Balancer তার request Server B-তে পাঠালে B বলবে "তোমাকে চিনি না" — error।

### দুইটা সমাধান

| সমাধান | মানে কী | ভালো দিক | খারাপ দিক |
|---|---|---|---|
| **Sticky session** | একই user সবসময় একই server-এ যাবে | দ্রুত | Server crash করলে session হারায়, load অসম হতে পারে |
| **Shared store (Redis)** | সব server এক জায়গা থেকে session পড়ে | Session নিরাপদ, load সমান | একটু ধীর, বাড়তি জিনিস maintain করতে হয় |

---

### এক লাইনে মনে রাখো
Stateful = server তোমাকে মনে রাখে, কিন্তু বেশি server হলে সেই "মনে রাখা"টাই ঝামেলা তৈরি করে — আর তার সমাধান হলো হয় সবসময় একই জায়গায় পাঠানো (sticky), নাহলে সবার জন্য একটা common খাতা রাখা (shared store)।




## Sticky Session — সহজে বুঝি

**মূল আইডিয়া:** একই user-এর request সবসময় একই server-এ পাঠানো হবে, অন্য server-এ যেতে দেওয়া হবে না।

**কীভাবে হয়:**
Load Balancer request পাঠানোর সময় মনে রাখে "এই user আগে কোন server-এ গিয়েছিল"। এটা করার দুইটা common উপায়:

- **IP hash** — client-এর IP address hash করে সবসময় একই server বেছে নেওয়া হয়
- **Cookie based** — Load Balancer নিজেই একটা cookie বসিয়ে দেয় (যেমন `AWSALB=serverA`), প্রতি request-এ সেই cookie দেখে বুঝে নেয় কোথায় পাঠাবে

**উদাহরণ:** ধরো তোমার অফিসে ৩টা caunter আছে। রিসেপশনিস্ট প্রথমবার তোমাকে Counter 2-তে পাঠালো। এরপর তুমি যতবারই আসো, রিসেপশনিস্ট মনে রেখে তোমাকে সবসময় Counter 2-তেই পাঠায় — কারণ তোমার ফাইল ওখানেই আছে।

**সুবিধা:** দ্রুত, simple, কোনো বাড়তি tool লাগে না।

**সমস্যা:**
- Counter 2 (Server A) বন্ধ হয়ে গেলে বা crash করলে তোমার ফাইলটাই হারিয়ে যায় — আবার নতুন করে শুরু করতে হয় (re-login)
- একই Counter-এ অনেক "ভারী" user জমে গেলে সেটা overload হয়, অন্য counter খালি বসে থাকে — load সমান ভাগ হয় না

---

## Shared Store (Redis) — সহজে বুঝি

**মূল আইডিয়া:** কোনো server নিজের কাছে session রাখে না। সবার session একটা কেন্দ্রীয় জায়গায় (Redis) থাকে, আর যেকোনো server সেখান থেকে পড়ে নেয়।

**কীভাবে হয়:**
Server A, B, C — সবাই Redis-এর সাথে connected। User request যে server-এই আসুক না কেন, সেই server Redis-কে জিজ্ঞেস করে "এই ID-র মালিক কে, তার data কী" — Redis উত্তর দেয়, server কাজ করে।

**উদাহরণ:** এবার অফিসে একটা centralized filing cabinet আছে যেটা সব counter থেকে access করা যায়। তুমি Counter 1-এ গেলেও, Counter 3-এ গেলেও — সমস্যা নেই, কারণ ফাইলটা কোনো নির্দিষ্ট counter-এর না, সবার জন্য common cabinet-এ আছে।

**সুবিধা:**
- যেকোনো server, যেকোনো user-কে চিনতে পারে
- একটা server crash করলেও session হারায় না (Redis-এ safe আছে)
- Load সমানভাবে ভাগ করা যায়

**সমস্যা:**
- প্রতিটি request-এ এখন Redis-এর সাথে একটা বাড়তি network trip লাগে — তাই একটু ধীর (RAM-এর তুলনায়)
- Redis নিজে down হয়ে গেলে সব server-ই বিপদে পড়বে — তাই Redis-কেও backup রাখতে হয় (replica)
- দুইটা request একসাথে একই session update করতে চাইলে conflict হতে পারে (race condition)

---

### এক লাইনে পার্থক্য
- **Sticky session** = "তুমি সবসময় এই একটা টেবিলেই যাবে"
- **Shared store** = "টেবিল যেটাই হোক, সবার হাতে একটা কমন খাতা আছে"





Stateful architecture বলতে বোঝায় এমন একটি system যেখানে **server নিজেই client-এর পূর্ববর্তী interaction-এর তথ্য মনে রাখে।** "State" মানে হলো একটি session-এর current condition — কে connected আছে, সে কী করেছে, তার কাছে কী data আছে।

ধরো তুমি একটি bank-এর ATM-এ card ঢোকালে। তুমি PIN দিলে, ATM সেটা verify করলো এবং তোমাকে "authenticated" হিসেবে চিনে রাখলো। এরপর তুমি balance check করলে, তারপর withdraw করলে — প্রতিটি step-এ ATM জানে তুমি কে, কারণ সে state maintain করছে। তুমি কিছু না বললেও সে বোঝে।

Technical ভাবে বলতে গেলে, stateful system-এ server memory বা database-এ একটি session object রাখে। Client প্রথমবার connect করলে server এই session তৈরি করে এবং client-কে একটি session ID দেয়। এরপর প্রতিটি request-এ client শুধু সেই ID পাঠায় — server সেই ID দিয়ে পুরো context খুঁজে বের করে। Client নিজে কোনো information বহন করে না, server-ই সব জানে।

এই approach-এর সবচেয়ে বড় সুবিধা হলো real-time interaction। TCP connection, WebSocket, SSH session, online multiplayer game — এগুলো সবই stateful কারণ এখানে continuous, দ্বিমুখী যোগাযোগ দরকার। Server জানে কে কখন connected, কার কাছে কী packet পাঠানো হয়েছে, connection-এর current state কী।

সমস্যা হলো scalability। যদি তোমার application তিনটি server-এ চলে এবং Rahim-এর session শুধু Server A-তে থাকে, তাহলে Load Balancer যদি Rahim-এর পরের request Server B-তে পাঠায়, সে "তোমাকে চিনি না" বলে error দেবে। এটা solve করতে হয় হয় sticky session দিয়ে (একই user সবসময় একই server-এ যাবে), অথবা Redis-এর মতো shared session store দিয়ে যেখানে সব server একই session data দেখতে পাবে। দুটোই complexity বাড়ায়।

---

## Stateless Architecture

## Stateless Architecture — সহজ নোট

**মূল আইডিয়া:** Server কিছুই মনে রাখে না। প্রতিটি request একা একাই সম্পূর্ণ — দরকারি সব তথ্য request-এর সাথেই আসে।

**উদাহরণ:** Vending machine। প্রতিবার পুরো instruction দাও (item + টাকা)। Machine তোমাকে চেনে না, আগের কেনাকাটা মনে রাখে না — যেকোনো machine-এ গেলেও একই কাজ হবে।

---

### কীভাবে কাজ করে (JWT দিয়ে)

1. **Login** — User লগইন করলে server একটা **signed token** (JWT) বানিয়ে দেয়
2. **Token-এর ভেতরে সব থাকে** — user ID, role, expiry time — encode করা অবস্থায়
3. **Server ভুলে যায়** — কিছুই save করে না
4. **প্রতি request-এ** — Client পুরো token পাঠায়
5. **Verify** — Server শুধু signature check করে বোঝে token আসল কিনা, tamper হয়েছে কিনা — কোনো database lookup লাগে না

👉 তথ্য client-এর কাছে (token-এ), server-এর কাছে না।

---

### সুবিধা
- যেকোনো server যেকোনো request handle করতে পারে (কোনো shared state লাগে না)
- নতুন server যোগ করলেই capacity বাড়ে — কোনো session sync-এর ঝামেলা নেই
- এক server crash করলে অন্যটা সহজে replace করে নেয়

তাই REST API, microservices, cloud-native app-এ এটাই default choice।

---

### সমস্যা — Token revoke করা কঠিন

Token একবার issue হলে, তার expiry না আসা পর্যন্ত server সেটা বাতিল করতে পারে না — কারণ server কিছুই store করেনি চেক করার জন্য।

**Logout করলেও** token technically valid থেকে যায়, যতক্ষণ না নিজে থেকে expire হয়।

**সমাধান:** Redis-এ একটা **blacklist** রাখা — logout হওয়া token-গুলো সেখানে রাখা হয়, verify করার সময় সেটাও check হয়।

⚠ কিন্তু এই blacklist রাখা মানেই server আবার কিছু একটা মনে রাখছে — অর্থাৎ পুরোপুরি stateless আর থাকল না, **partially stateful** হয়ে গেল।

---

### Stateful vs Stateless — এক নজরে

| দিক | Stateful | Stateless (JWT) |
|---|---|---|
| তথ্য থাকে | Server-এ | Client-এর token-এ |
| Scaling | কঠিন (session sync লাগে) | সহজ |
| Logout | সাথে সাথে কাজ করে | Token expire না হওয়া পর্যন্ত valid থাকে |
| Database lookup | প্রতি request-এ লাগতে পারে | লাগে না (শুধু signature verify) |



JWT দিয়ে stateless authentication-এর একটা পূর্ণ example — Node.js + Express।

## ১. Login — token ইস্যু করা

```javascript
// server.js
const express = require('express');
const jwt = require('jsonwebtoken');

const app = express();
app.use(express.json());

const JWT_SECRET = 'my-super-secret-key'; // production-এ .env-এ রাখতে হয়

app.post('/login', (req, res) => {
  const { username, password } = req.body;

  // ধরো verify হলো (database check)
  if (username !== 'rahim' || password !== '1234') {
    return res.status(401).send('Invalid credentials');
  }

  // Token-এর ভেতরে দরকারি সব তথ্য ঢুকিয়ে দিচ্ছি
  const token = jwt.sign(
    { userId: 'rahim123', role: 'customer' },
    JWT_SECRET,
    { expiresIn: '30m' } // ৩০ মিনিট পর নিজে থেকে expire
  );

  // server কিছুই save করছে না — শুধু token পাঠিয়ে দিচ্ছে
  res.json({ token });
});
```

## ২. পরের request-এ token verify করা

```javascript
function verifyToken(req, res, next) {
  const authHeader = req.headers['authorization']; // "Bearer <token>"
  const token = authHeader && authHeader.split(' ')[1];

  if (!token) return res.status(401).send('Token নেই');

  try {
    // শুধু signature check হচ্ছে — কোনো database call নেই
    const decoded = jwt.verify(token, JWT_SECRET);
    req.user = decoded; // { userId: 'rahim123', role: 'customer' }
    next();
  } catch (err) {
    return res.status(403).send('Token invalid বা expired');
  }
}

app.get('/balance', verifyToken, (req, res) => {
  // req.user থেকেই সব পেয়ে গেলাম — server কিছু মনে রাখেনি
  res.send(`User ${req.user.userId}-এর balance: 5000`);
});

app.listen(3000, () => console.log('Server running on 3000'));
```

## ৩. Client-এর দিক থেকে (প্রতি request-এ token পাঠানো)

```javascript
// login করে token সংরক্ষণ (client-এর memory/localStorage-এ)
const loginRes = await fetch('http://localhost:3000/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ username: 'rahim', password: '1234' })
});
const { token } = await loginRes.json();

// পরের প্রতিটা request-এ এই token পাঠাতে হবে
const balanceRes = await fetch('http://localhost:3000/balance', {
  headers: { 'Authorization': `Bearer ${token}` }
});
console.log(await balanceRes.text());
```

## ৪. Logout blacklist (partially stateful সমাধান)

```javascript
const { createClient } = require('redis');
const redisClient = createClient();
redisClient.connect();

app.post('/logout', verifyToken, async (req, res) => {
  const token = req.headers['authorization'].split(' ')[1];
  const decoded = jwt.decode(token);
  const remainingTime = decoded.exp - Math.floor(Date.now() / 1000);

  // token-টাকে blacklist-এ রাখছি, যতক্ষণ তার নিজের expiry বাকি আছে ততক্ষণের জন্য
  await redisClient.setEx(`blacklist:${token}`, remainingTime, 'true');
  res.send('Logged out');
});

// verifyToken function-এ এই check যোগ করতে হবে
async function verifyToken(req, res, next) {
  const token = req.headers['authorization']?.split(' ')[1];
  if (!token) return res.status(401).send('Token নেই');

  const isBlacklisted = await redisClient.get(`blacklist:${token}`);
  if (isBlacklisted) return res.status(403).send('Token revoked হয়ে গেছে');

  try {
    req.user = jwt.verify(token, JWT_SECRET);
    next();
  } catch {
    return res.status(403).send('Token invalid');
  }
}
```

### লক্ষ্য করার বিষয়
`/login` আর `/balance` route-এ কোনো session, কোনো shared store লাগেনি — শুধু token verify। এটাই আসল stateless। কিন্তু `/logout`-এ Redis লাগলো কারণ সেটাই একমাত্র উপায় token-কে "মনে করে বাতিল" করার — এখানেই সিস্টেমটা সামান্য stateful হয়ে যাচ্ছে, যেমনটা আগে বলেছিলাম।



Stateless architecture-এ server কিছুই মনে রাখে না। প্রতিটি request সম্পূর্ণ নিজের মধ্যে — যতটুকু information দরকার সব request-এর সাথেই পাঠাতে হয়। Server একটি request দেখে, process করে, response দেয় এবং সম্পূর্ণ ভুলে যায়।

Vending machine-এর কথা মনে করো। তুমি প্রতিবার পুরো instruction দাও — কোন item, কত টাকা। Machine তোমাকে চেনে না, আগে কী কিনেছ মনে নেই, কিন্তু যেকোনো machine-এ যাও একই কাজ করবে।

আধুনিক web-এ statelessness সাধারণত JWT (JSON Web Token) দিয়ে implement হয়। User login করলে server একটি signed token তৈরি করে দেয়। এই token-এর ভেতরে user ID, role, expiry time সহ দরকারি সব information encoded থাকে। Server এরপর কিছু সংরক্ষণ করে না। Client প্রতিটি request-এ এই পুরো token পাঠায়। Server শুধু cryptographic signature verify করে বুঝতে পারে token টি valid কিনা এবং tamper করা হয়েছে কিনা। কোনো database lookup লাগে না।

এর সবচেয়ে বড় সুবিধা scaling-এ। যেকোনো server যেকোনো request handle করতে পারে কারণ token-এই সব আছে, কোনো shared state নেই। নতুন server যোগ করা মানেই capacity বাড়া। একটি server crash করলে অন্য server seamlessly request নেয়। এই কারণেই REST API, microservices, এবং cloud-native application সব stateless।

একটি গুরুত্বপূর্ণ সমস্যা আছে — token revoke করা কঠিন। JWT একবার issue হলে সেটার expiry আসার আগে server সেটা invalid করতে পারে না, কারণ server কিছুই store করেনি। User logout করলেও token technically valid থাকে। এটা solve করতে Redis-এ একটি blacklist রাখতে হয়, যা আবার partially stateful হয়ে যায়।

---

## মূল পার্থক্য এক কথায়

Stateful মানে — **server মনে রাখে, client ভুলে যায়।** Stateless মানে — **client মনে রাখে, server ভুলে যায়।**

আর modern production system-এ সাধারণত দুটোই থাকে। HTTP layer stateless (JWT দিয়ে), WebSocket layer stateful (real-time connection), আর database layer সবসময় stateful — কারণ actual data তো কোথাও না কোথাও থাকতেই হবে।


---

## ১. HTTP-এর মূল nature: Stateless by design

HTTP protocol জন্মগতভাবে stateless। প্রতিটা HTTP request সম্পূর্ণ independent। এটা বোঝার জন্য দেখো request-response cycle টা কীভাবে কাজ করে:---

<img width="730" height="619" alt="image" src="https://github.com/user-attachments/assets/fdd8e332-dc95-4224-b36e-6f7e3f7d3ce5" />


## ২. Scaling problem — এখানেই stateful ভেঙে পড়ে

৩ টা user request আসছে। Stateful system-এ কী হয় দেখো:---


<img width="704" height="601" alt="image" src="https://github.com/user-attachments/assets/fee6678d-fe47-48c4-85c9-6d9062bc4536" />


## ৩. JWT — Statelessness-এর technical backbone

Stateless authentication-এর সবচেয়ে popular implementation হলো JWT (JSON Web Token)। এটা তিনটা part নিয়ে তৈরি:---

<img width="769" height="491" alt="image" src="https://github.com/user-attachments/assets/eadf2fb2-6719-48f0-9d30-b35582303fc4" />


## ৪. Real-world architecture — Modern hybrid approach

Production-grade system-এ usually দুটোর combination ব্যবহার হয়:---

<img width="754" height="634" alt="image" src="https://github.com/user-attachments/assets/11ab3f6b-ecda-4f82-a4f2-be8b8bf6e306" />


## Technical summary যা মনে রাখা দরকার

**Stateless-এর গভীর সমস্যা:** Token revoke করা কঠিন। JWT একবার issue হলে expire হওয়া পর্যন্ত valid থাকে — user logout করলেও server জানে না। এটা solve করতে Redis-এ token blacklist রাখতে হয়, যা partially stateful হয়ে যায়।

**Stateful-এর গভীর সমস্যা:** WebSocket connection যদি একটা নির্দিষ্ট server-এ থাকে, সেই server crash করলে সব active connection শেষ। তাই production-এ session replication বা shared state store (Redis Cluster) দরকার।

**Protocol-level পার্থক্য:**
- TCP — stateful, handshake করে connection establish করে (SYN→SYN-ACK→ACK), তারপর state track করে
- UDP — stateless, প্রতিটা packet independent, delivery guarantee নেই
- HTTP/1.1 — stateless, তবে `Connection: keep-alive` দিয়ে TCP connection reuse করে
- HTTP/2 — stateless request কিন্তু stateful connection (multiplexing, server push)



## আরো Real-World Examples

### TCP Handshake — Stateful protocol-এর সবচেয়ে ভালো উদাহরণ

দুইজন মানুষ phone call করলে যেমন প্রথমে "হ্যালো, শুনতে পাচ্ছ?" বলে connection confirm করে, TCP-ও ঠিক তেমন। এটাকে বলে Three-Way Handshake।

Client পাঠায় **SYN** — "আমি connect করতে চাই।" Server জবাব দেয় **SYN-ACK** — "ঠিক আছে, আমি ready, তুমিও ready তো?" Client confirm করে **ACK** — "হ্যাঁ, শুরু করা যাক।" এরপর থেকে দুই পক্ষই এই connection-এর state track করে — কতটুকু data গেছে, কোন packet হারিয়েছে, sequence কী। Connection শেষ না হওয়া পর্যন্ত এই state বজায় থাকে। এটাই stateful।

---

### Shopping Cart — Stateful session-এর classic উদাহরণ

Daraz বা Chaldal-এ কিছু কিনতে গেলে তুমি login করো, cart-এ product যোগ করো, তারপর checkout করো। এই পুরো journey-টা একটি session। Server তোমার cart-এ কী আছে, তুমি কোন address দিয়েছ, payment step-এ আছ নাকি confirmation-এ — সব মনে রাখে। তুমি browser বন্ধ করে আবার খুললে cart-এ সব থাকে, কারণ server-side session বা database-এ state persist করা ছিল।

---

### DNS Lookup — Stateless-এর পারফেক্ট উদাহরণ

তুমি browser-এ `google.com` লিখলে তোমার computer DNS server-কে জিজ্ঞেস করে "এই নামের IP address কী?" DNS server জবাব দেয় "142.250.190.46।" ব্যস, কথা শেষ। DNS server তোমাকে চেনে না, আগে কী জিজ্ঞেস করেছিলে মনে নেই, পরে কী জিজ্ঞেস করবে সেটা নিয়েও চিন্তা নেই। প্রতিটি query সম্পূর্ণ independent। এই কারণেই DNS এত fast এবং এত বিশাল scale-এ কাজ করে — বিশ্বের কোটি কোটি query handle করে কোনো shared state ছাড়াই।

---

### Online Gaming vs REST API — পাশাপাশি দেখো

PUBG বা Free Fire-এর কথা ভাবো। তুমি map-এ কোথায় আছ, তোমার health কত, কতটা ammo আছে, অন্য players কোথায় — এই সব information server প্রতি millisecond-এ track করছে। তুমি move করলে server জানে, তুমি shoot করলে server জানে। Server যদি এই state হারায়, সবাই game থেকে disconnect হয়ে যায়। এটা stateful হতেই হবে।

অন্যদিকে bKash-এর "balance check" API ধরো। তুমি request পাঠাও — token সহ। Server token verify করে, database থেকে balance টেনে আনে, response দেয়। Server কিছু মনে রাখেনি। পরের second-এ আরেকজন একই server-এ request করলে সে আগের কোনো কিছু জানে না। এটাই stateless।

---

### Video Streaming — দুটোর hybrid

Netflix বা YouTube-এ movie দেখলে interesting একটা জিনিস হয়। তুমি 45 মিনিটে pause করলে, কাল এসে play করলে সেখান থেকেই শুরু হয় — এটা stateful, তোমার watch progress database-এ আছে। কিন্তু actual video stream করার সময় HTTP range request ব্যবহার হয়, যেটা stateless। প্রতিটি chunk আলাদা আলাদা request। একটি CDN server ব্যর্থ হলে পরের chunk অন্য server থেকে আসে, তুমি বুঝতেও পারো না। User data stateful, content delivery stateless।

---

### Email — আরেকটি hybrid

Gmail-এ email পাঠানোর সময় SMTP protocol ব্যবহার হয়, যেটা stateful connection — server-এর সাথে একটি session establish হয়, তারপর email যায়, তারপর connection বন্ধ হয়। কিন্তু তুমি browser-এ Gmail খুলে inbox দেখলে সেটা HTTPS REST call, যেটা stateless। আর তোমার email সব সময় inbox-এ থাকে কারণ সেটা database-এ stored state।

---

একটু লক্ষ্য করলে দেখবে যে **real world-এ pure stateful বা pure stateless খুব কম।** বেশিরভাগ system-ই এই দুটোকে layer করে ব্যবহার করে — কোন layer-এ কী দরকার সেটা বুঝে সিদ্ধান্ত নেয়। Communication stateless রাখো scaling-এর জন্য, data stateful রাখো persistence-এর জন্য, real-time feature stateful রাখো responsiveness-এর জন্য।

