# System Design Interview — Framework এবং Case Studies

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [Interviewer আসলে কী দেখতে চায়](#interviewer-আসলে-কী-দেখতে-চায়)
* [৪৫ মিনিটের RESHADED Framework](#৪৫-মিনিটের-reshaded-framework)
* [যে ভুলগুলো সবাই করে](#যে-ভুলগুলো-সবাই-করে)
* [Case Study 1 — URL Shortener (TinyURL)](#case-study-1--url-shortener-tinyurl)
* [Case Study 2 — Chat System (WhatsApp)](#case-study-2--chat-system-whatsapp)
* [Case Study 3 — News Feed (Facebook/Twitter)](#case-study-3--news-feed-facebooktwitter)
* [Case Study 4 — Video Streaming (YouTube)](#case-study-4--video-streaming-youtube)
* [Case Study 5 — Ride Sharing (Uber)](#case-study-5--ride-sharing-uber)
* [Case Study 6 — Notification System](#case-study-6--notification-system)
* [সব design-এ কাজে লাগে এমন Building Block](#সব-design-এ-কাজে-লাগে-এমন-building-block)
* [শেষ মুহূর্তের Checklist](#শেষ-মুহূর্তের-checklist)

---

<!-- TOC END -->

## Interviewer আসলে কী দেখতে চায়

এটা আগে বোঝা জরুরি। Interviewer দেখতে চায় না তুমি Facebook-এর আসল architecture মুখস্থ করেছো কিনা।

সে দেখতে চায় —

**১. তুমি কি প্রশ্ন করো?** — অস্পষ্ট requirement নিয়ে সরাসরি design শুরু করা সবচেয়ে বড় লাল পতাকা।

**২. তুমি কি scale বোঝো?** — ১০০০ user আর ১০ কোটি user-এর design সম্পূর্ণ আলাদা।

**৩. তুমি কি tradeoff নিয়ে কথা বলতে পারো?** — "SQL ভালো" না বলে "এই ক্ষেত্রে SQL ভালো কারণ..., তবে এতে এই সমস্যা হবে" বলা।

**৪. তুমি কি ধাপে ধাপে এগোতে পারো?** — সহজ কিছু দিয়ে শুরু করে ধীরে ধীরে জটিল করা।

**৫. তুমি কি জানো কোথায় ভাঙবে?** — Bottleneck চিহ্নিত করতে পারা।

**৬. তুমি কি যোগাযোগ করতে পারো?** — চুপ করে ভাবা না, জোরে জোরে ভাবা।

> **সবচেয়ে গুরুত্বপূর্ণ কথা:** এটা একটা **আলোচনা**, পরীক্ষা না। Interviewer তোমার সহকর্মী হিসেবে কেমন হবে সেটাই বোঝার চেষ্টা করছে।

---

## ৪৫ মিনিটের RESHADED Framework

### ধাপ ১ — Requirements (৫-৭ মিনিট) 🔴 সবচেয়ে গুরুত্বপূর্ণ

কখনো সরাসরি design শুরু করো না। আগে প্রশ্ন করো।

**Functional requirements — কী কী করবে:**
- "মূল feature গুলো কী কী? আমি কি শুধু X, Y, Z ধরে নেবো?"
- "এটা কি এই feature-টাও করবে?" (scope ছোট রাখতে চেষ্টা করো)

**Non-functional requirements — কেমনভাবে করবে:**
- কতজন user? Daily active কত?
- Read-heavy না write-heavy? অনুপাত কত?
- Latency কত লাগবে?
- Consistency না availability — কোনটা বেশি জরুরি?
- Data কতদিন রাখতে হবে?

**Scope ঠিক করো:** "সময় সীমিত, তাই আমি core feature-এ মনোযোগ দিচ্ছি — X, Y, Z। Analytics আর admin panel বাদ রাখছি, আপনি চাইলে পরে যোগ করবো।"

### ধাপ ২ — Estimation (৩-৫ মিনিট)

Back-of-the-envelope হিসাব। (বিস্তারিত: `9. Back of the Envelope Estimation.md`)

```
DAU, QPS (average আর peak — peak সাধারণত ২-৩ গুণ)
Storage per day, per year
Bandwidth
Cache-এর আকার (সাধারণত ২০% data ৮০% traffic — তাই hot data-র ২০%)
```

**কেন জরুরি:** এই সংখ্যাগুলোই পরে তোমার সিদ্ধান্তের ভিত্তি হবে। "১০ TB/দিন, তাই একটা DB-তে হবে না — sharding লাগবে।"

### ধাপ ৩ — System Interface / API (৩ মিনিট)

মূল API গুলো লিখে ফেলো:

```
POST /api/v1/urls          { longUrl, customAlias? } → { shortUrl }
GET  /{shortCode}                                     → 301 Redirect
GET  /api/v1/urls/{code}/stats                        → { clicks, ... }
```

এতে সবার কাছে স্পষ্ট হয়ে যায় system-টা আসলে কী করে।

### ধাপ ৪ — Data Model (৫ মিনিট)

মূল table/collection গুলো, আর **কোন database কেন**।

```
URLs: short_code (PK), long_url, user_id, created_at, expires_at
Users: id, email, plan

DB পছন্দ: read-heavy, key-value access → Cassandra বা DynamoDB
```

### ধাপ ৫ — High-Level Design (১০ মিনিট)

সহজ একটা ছবি আঁকো — box আর arrow।

```
Client → CDN → Load Balancer → API Gateway → Services → Cache → Database
                                                ↓
                                        Message Queue → Workers
```

প্রতিটা box কী করে, সংক্ষেপে বলো। **এখনো গভীরে যেও না।**

### ধাপ ৬ — Deep Dive (১০-১৫ মিনিট)

Interviewer সাধারণত এখানে একটা দিক বেছে দেয়। না দিলে তুমি প্রস্তাব করো:

"আমি তিনটা জায়গায় গভীরে যেতে পারি — unique ID তৈরির কৌশল, database sharding, বা cache strategy। কোনটা নিয়ে কথা বলবো?"

### ধাপ ৭ — Bottleneck ও Scaling (৫ মিনিট)

- Single point of failure কোথায়?
- ১০ গুণ traffic বাড়লে প্রথমে কী ভাঙবে?
- Monitoring-এ কী কী দেখবে?
- Failure হলে কী হবে?

**সবসময় শেষ করো নিজের design-এর সমালোচনা দিয়ে** — "এই design-এর দুর্বলতা হলো X, আরো সময় থাকলে আমি Y করতাম।" এটা পরিণত মনে হয়।

---

## যে ভুলগুলো সবাই করে

**❌ সরাসরি design শুরু করা** — আগে requirement স্পষ্ট করো।

**❌ প্রথমেই সবচেয়ে জটিল সমাধান** — সহজ দিয়ে শুরু করো, তারপর প্রয়োজন অনুযায়ী জটিল করো।

**❌ চুপচাপ ভাবা** — জোরে জোরে ভাবো। Interviewer তোমার চিন্তা শুনতে চায়।

**❌ Tradeoff না বলা** — প্রতিটা সিদ্ধান্তের একটা দাম আছে, সেটা স্বীকার করো।

**❌ Buzzword ছোড়া** — "Kubernetes-এ microservices চালাবো" বলে থেমে যাওয়া। কেন, সেটা বলো।

**❌ Estimation এড়িয়ে যাওয়া** — সংখ্যা ছাড়া design মানে অনুমানে design।

**❌ একটা অংশে আটকে থাকা** — সময়ের হিসাব রাখো।

**❌ Interviewer-এর ইঙ্গিত উপেক্ষা করা** — সে যদি বলে "এখানে একটা সমস্যা আছে না?" — সেটা সাহায্য, আক্রমণ না।

**❌ "জানি না" বলতে না পারা** — না জানলে বলো "এটা আমি নিশ্চিত নই, তবে আমার ধারণা এভাবে কাজ করে..." — এটা বানিয়ে বলার চেয়ে অনেক ভালো।

---

## Case Study 1 — URL Shortener (TinyURL)

### Requirements

**Functional:** লম্বা URL দিলে ছোট URL দেবে; ছোট URL-এ গেলে মূল URL-এ redirect করবে; custom alias (ঐচ্ছিক); expiry।

**Non-functional:** অতি উচ্চ availability (redirect fail করা যাবে না); redirect latency < ১০০ms; short URL অনুমান করা যাবে না।

### Estimation

```
নতুন URL: ১০ কোটি/মাস → ~৪০ write/sec
Read:Write = ১০০:১        → ~৪,০০০ read/sec
Storage: ১০ কোটি × ৫০০ byte × ১২ মাস × ৫ বছর ≈ ৩ TB
```

**সিদ্ধান্ত:** প্রচণ্ড read-heavy → cache অপরিহার্য।

### Short Code তৈরির কৌশল — এটাই মূল প্রশ্ন

**উপায় ১: Hash (MD5/SHA) নিয়ে প্রথম ৭ অক্ষর**
সহজ, কিন্তু **collision হতে পারে** — দুটো URL-এর একই code। প্রতিবার DB-তে চেক করতে হবে।

**উপায় ২: Base62 encoding সহ auto-increment ID** ⭐
```
ID = 1000000  →  Base62 এ  →  "4c92"
```
৬২টা অক্ষর (a-z, A-Z, 0-9)। ৭ অক্ষরে **৬২⁷ ≈ ৩.৫ ট্রিলিয়ন** URL — যথেষ্ট।
কোনো collision নেই। কিন্তু ID **অনুমানযোগ্য** (পরেরটা কী হবে বোঝা যায়), আর distributed system-এ auto-increment কঠিন।

**উপায় ৩: আগে থেকে key তৈরি করে রাখা (Key Generation Service)** ⭐⭐
একটা আলাদা service আগেই লাখ লাখ unique key বানিয়ে রেখে দেয়। App শুধু একটা নিয়ে নেয়।
*সুবিধা:* কোনো collision নেই, কোনো অপেক্ষা নেই, distributed-এ ভালো কাজ করে।
*চ্যালেঞ্জ:* KGS-কেই replicate করতে হবে, আর একই key দুজনকে না দেওয়া নিশ্চিত করতে হবে।

**উপায় ৪: Snowflake ID → Base62**
Timestamp + machine ID + sequence। Distributed, unique, sorted। তবে code একটু লম্বা হয়।

**আমার সুপারিশ:** Base62 + **Snowflake বা KGS** — সাথে অনুমান ঠেকাতে কিছুটা randomization।

### Design

```
                    ┌─────┐
   Write path:      │ API │──► ID Gen (Snowflake/KGS)
                    └──┬──┘         │
                       └────────────┴──► DB (short_code → long_url)

                    ┌─────┐
   Read path:  ────►│ CDN │──► LB ──► API ──► Redis ──(miss)──► DB
                    └─────┘                    ↓
                                            301 Redirect
```

**গুরুত্বপূর্ণ খুঁটিনাটি:**

**301 না 302?** — 301 (permanent) হলে browser নিজেই cache করে, তাই server-এ চাপ কম কিন্তু click count পাওয়া যায় না। 302 (temporary) হলে প্রতিবার server-এ আসে, তাই analytics পাওয়া যায় কিন্তু load বেশি। **Analytics লাগলে 302, নাহলে 301।**

**Database:** Key-value access pattern, কোনো JOIN নেই → **Cassandra বা DynamoDB** ভালো। Shard key = short_code।

**Cache:** ৮০/২০ নিয়ম — জনপ্রিয় ২০% URL-ই ৮০% traffic পায়। LRU eviction সহ Redis।

**Analytics:** Click count সরাসরি DB-তে না লিখে **Kafka-তে পাঠাও**, worker গুলো batch করে aggregate করবে।

---

## Case Study 2 — Chat System (WhatsApp)

### Requirements

**Functional:** ১-১ chat, group chat (সর্বোচ্চ ১০০ জন), online status, delivery receipt (sent/delivered/read), offline থাকলে পরে পাওয়া, media পাঠানো।

**Non-functional:** কম latency, message কখনো হারাবে না, ৫ কোটি DAU।

### Estimation

```
৫ কোটি DAU × ৪০ message/দিন = ২০০ কোটি message/দিন
QPS ≈ ২৩,০০০ (average), peak ≈ ৭০,০০০
Storage: ২০০ কোটি × ১০০ byte = ২০০ GB/দিন = ৭৩ TB/বছর
Concurrent connection: DAU-র ~২০% = ১ কোটি
```

### Design

```
                   ┌──────────────────┐
   Mobile ─WSS──►  │ WebSocket Servers│ ◄── ১ কোটি connection
                   │  (অনেকগুলো)      │      (server প্রতি ~২ লাখ)
                   └────────┬─────────┘
                            │
              ┌─────────────┼─────────────┐
              ▼             ▼             ▼
      ┌──────────────┐ ┌─────────┐ ┌──────────────┐
      │ Redis Pub/Sub│ │ Message │ │ Presence     │
      │ (backplane)  │ │ Service │ │ Service      │
      └──────────────┘ └────┬────┘ └──────────────┘
                            ▼
                   ┌─────────────────┐
                   │ Cassandra       │  ← message history
                   │ (write-heavy)   │
                   └─────────────────┘
```

**মূল সিদ্ধান্তগুলো:**

**১. WebSocket কেন:** দুই দিক থেকেই message যায়, আর তাৎক্ষণিক হতে হবে। (`21. Real-time Communication` দেখো)

**২. কোন user কোন server-এ:** Redis-এ registry — `user_id → server_id`। Message পাঠানোর সময় সেখানে দেখে সঠিক server-এ pub/sub দিয়ে পাঠাও।

**৩. Database কেন Cassandra:**
- বিশাল write throughput (২০০ কোটি/দিন)
- Access pattern সহজ: "এই chat-এর শেষ N টা message" — এটাই Cassandra-র শক্তি
- Time-series data, horizontally scale করে চমৎকার
- Schema: `partition key = chat_id`, `clustering key = message_id (timestamp সহ)` — এতে এক chat-এর message একসাথে sorted অবস্থায় থাকে

**৪. Offline user:** Message DB-তে লেখা হয়ে যায়, সাথে একটা "undelivered" queue-তেও যায়। User online হলে সেগুলো পাঠানো হয়।

**৫. Delivery receipt:** তিনটা অবস্থা — sent (server পেয়েছে), delivered (প্রাপকের device পেয়েছে), read (প্রাপক খুলেছে)। প্রতিটা ধাপে একটা ছোট acknowledgment message যায়।

**৬. Group chat:** ১০০ জনের group-এ একটা message মানে ১০০টা delivery। ছোট group হলে fan-out on write ঠিক আছে। খুব বড় group (broadcast channel) হলে fan-out on read করতে হবে।

**৭. Media:** ফাইল WebSocket দিয়ে পাঠিও না। Client S3-তে সরাসরি upload করে (pre-signed URL দিয়ে), তারপর শুধু **URL টা** message হিসেবে পাঠায়।

**৮. Message ordering:** প্রতিটা message-এ server-এর দেওয়া sequence number বা Snowflake ID দাও — device-এর ঘড়ির উপর ভরসা করা যায় না।

---

## Case Study 3 — News Feed (Facebook/Twitter)

### Requirements

**Functional:** Post করা, অনুসরণ করা, feed দেখা (সময় বা প্রাসঙ্গিকতা অনুযায়ী)।

**Non-functional:** Feed load < ২০০ms, প্রচণ্ড read-heavy, eventual consistency চলবে।

### মূল প্রশ্ন — Fan-out on Write না Fan-out on Read

এটাই এই design-এর হৃদয়, আর interview-তে এটাই আসল আলোচনা।

**Fan-out on Write (Push model):**
Post করার সাথে সাথে সব follower-এর feed-এ লিখে দাও।

```
User post করলো → ১০০০ follower-এর প্রত্যেকের Redis feed list-এ যোগ করো
```

*সুবিধা:* Feed পড়া অতি দ্রুত — শুধু নিজের list পড়ে নাও (O(1))।
*অসুবিধা:* Celebrity-র ১ কোটি follower মানে একটা post-এ **১ কোটি লেখা!** এটাকে বলে **hotkey/celebrity problem**।

**Fan-out on Read (Pull model):**
Feed চাওয়ার সময় সবার post এনে merge করো।

*সুবিধা:* লেখা সস্তা, storage কম।
*অসুবিধা:* Feed পড়া ধীর — ১০০০ জনকে follow করলে ১০০০ জায়গা থেকে আনতে হবে।

**✅ সঠিক উত্তর — Hybrid:**

```
সাধারণ user (< ১০,০০০ follower)  →  Fan-out on Write
Celebrity (> ১০,০০০ follower)      →  Fan-out on Read

Feed তৈরি = আগে থেকে লেখা feed + celebrity-দের post সাথে সাথে টেনে merge
```

এটাই Twitter আসলে করে। এই উত্তরটা দিতে পারলে interview-তে বড় নম্বর।

### Design

```
Write path:
  Post → Post Service → DB → Kafka → Fanout Workers → Redis (প্রতি user-এর feed list)

Read path:
  Feed request → Feed Service → Redis (আগে থেকে বানানো feed)
                                  + Celebrity post (সরাসরি)
                                  → merge → rank → hydrate (post-এর বিস্তারিত আনা)
```

**অন্যান্য বিবেচনা:**

- **Feed cache** — প্রতি user-এর সর্বশেষ ~৫০০টা post-এর ID রাখো, পুরোটা না। ID থেকে বিস্তারিত আনা (hydration) আলাদা ধাপ।
- **নিষ্ক্রিয় user** — ৩০ দিন login করেনি? তার জন্য fan-out করে লাভ নেই, resource নষ্ট।
- **Pagination** — offset ব্যবহার করো না (নতুন post এলে সব সরে যায়), **cursor-based pagination** ব্যবহার করো।
- **Ranking** — সময় ছাড়াও engagement, সম্পর্কের ঘনিষ্ঠতা, ধরন — ML model দিয়ে।

---

## Case Study 4 — Video Streaming (YouTube)

### মূল চ্যালেঞ্জ — Storage আর Bandwidth

```
৫ লাখ upload/দিন × ১০০ MB = ৫০ TB/দিন (raw)
একাধিক resolution-এ transcode → ~৩ গুণ = ১৫০ TB/দিন
Streaming bandwidth: ১ কোটি concurrent viewer × ৫ Mbps = ৫০ Tbps
```

### Upload Pipeline

```
Client
  │ (S3-তে সরাসরি upload, pre-signed URL দিয়ে —
  │  কখনো নিজের API server দিয়ে ১০০ MB পাঠিও না)
  ▼
Raw Storage (S3)
  │
  ▼ Kafka event: "নতুন video এসেছে"
  │
  ▼
Transcoding Workers (DAG হিসেবে সাজানো)
  ├── ভাগে ভাগ করা (chunking) — ১০ সেকেন্ডের টুকরো
  ├── প্রতিটা resolution-এ encode (240p...4K) — সমান্তরালে
  ├── Thumbnail তৈরি
  └── Content moderation (ML)
  │
  ▼
Processed Storage → CDN-এ পাঠানো
  │
  ▼ Metadata DB update: status = "ready"
```

**কেন chunk করা হয়:** ১ ঘণ্টার video একবারে encode করলে ব্যর্থ হলে পুরোটা আবার করতে হবে। ১০ সেকেন্ডের টুকরো করলে হাজারটা worker সমান্তরালে কাজ করতে পারে, আর একটা টুকরো ব্যর্থ হলে শুধু সেটাই আবার করতে হয়।

### Streaming — Adaptive Bitrate (ABR)

Video কে ছোট ছোট segment-এ ভেঙে প্রতিটা resolution-এ রাখা হয়। Player নিজের network speed দেখে প্রতিটা segment-এর জন্য আলাদাভাবে ঠিক করে কোন quality নেবে।

```
Manifest file (HLS-এ .m3u8):
  240p  → segment_001.ts, segment_002.ts, ...
  720p  → segment_001.ts, segment_002.ts, ...
  1080p → segment_001.ts, segment_002.ts, ...

Network ভালো → 1080p নাও
Network খারাপ হলো → পরের segment-এ 480p তে নেমে যাও (buffer হবে না)
```

Protocol: **HLS** (Apple, সবচেয়ে প্রচলিত) বা **DASH**।

### বাকি অংশ

- **CDN-ই সব** — ৯৯% video traffic CDN থেকে যায়। জনপ্রিয় video edge-এ push করা হয়, কম জনপ্রিয়টা pull।
- **Metadata DB** — video-র তথ্য MySQL/Vitess-এ (relational, কারণ user-video-channel সম্পর্ক আছে)
- **View count** — সরাসরি DB-তে increment করলে DB মরে যাবে। Kafka → batch aggregate → পর্যায়ক্রমে update।
- **Search** — Elasticsearch।
- **Recommendation** — আলাদা ML pipeline, আগে থেকে হিসাব করে রাখা।

---

## Case Study 5 — Ride Sharing (Uber)

### মূল চ্যালেঞ্জ — কাছের চালক খোঁজা

লাখ লাখ চালকের অবস্থান সারাক্ষণ বদলাচ্ছে। "আমার ৫ কিমি-র মধ্যে কারা আছে?" — এটা দ্রুত বের করতে হবে।

**সরল উপায় (খারাপ):** সব চালকের সাথে দূরত্ব হিসাব করা — ১০ লাখ চালকে অসম্ভব ধীর।

**সঠিক উপায় — Geospatial Index:**

**Geohash** — পৃথিবীকে গ্রিডে ভাগ করে প্রতিটা ঘরকে একটা string দেওয়া। কাছাকাছি জায়গার geohash-এর শুরুটা একই হয়, তাই prefix দিয়ে খোঁজা যায়।
```
"wh0r1" এর ভিতরের সব চালক = prefix "wh0r1" দিয়ে খুঁজলেই হলো
```

**Quadtree** — এলাকাকে বারবার ৪ ভাগে ভাগ করা, যেখানে ঘনত্ব বেশি সেখানে বেশি ভাগ। ঘন শহরে ভালো কাজ করে।

**S2 (Google)** বা **H3 (Uber-এর নিজের)** — hexagon ব্যবহার করে, প্রতিবেশী ঘরগুলোর দূরত্ব সমান হয় বলে এটা বেশি নিখুঁত। **Uber এটাই ব্যবহার করে।**

**Redis GEO command** — ছোট থেকে মাঝারি scale-এ `GEOADD` আর `GEORADIUS` দিয়েই কাজ চলে যায়।

### Design

```
Driver App ──প্রতি ৪ সেকেন্ডে location──► Location Service ──► Redis (geo index)
                                                              │
                                                              └──► Kafka (ইতিহাসের জন্য)

Rider App ──"ride চাই"──► Matching Service
                              │
                              ├─► Redis-এ কাছের চালক খোঁজো
                              ├─► ranking (দূরত্ব, rating, ETA)
                              ├─► ক্রমানুসারে চালকদের offer পাঠাও (WebSocket)
                              └─► প্রথম যে গ্রহণ করলো → trip শুরু
```

**গুরুত্বপূর্ণ বিবেচনা:**

**১. Location update বিশাল write load** — ১০ লাখ চালক ÷ ৪ সেকেন্ড = **২.৫ লাখ write/sec**। এগুলো DB-তে না লিখে Redis-এ রাখো (in-memory, দ্রুত), আর ইতিহাসের জন্য Kafka-তে পাঠাও।

**২. একই চালক দুজনকে দেওয়া যাবে না** — এখানে **strong consistency** লাগবে। Distributed lock (Redis `SETNX`) বা conditional update দিয়ে নিশ্চিত করো যে একটা চালক একবারই assign হয়।

**৩. Payment-এ Saga pattern** — ride শেষ → ভাড়া হিসাব → payment → চালকের হিসাবে জমা। কোনো ধাপ ব্যর্থ হলে compensating transaction।

**৪. Surge pricing** — এলাকা ধরে চাহিদা/সরবরাহের অনুপাত হিসাব করে দাম বাড়ানো, আলাদা service হিসেবে।

**৫. CAP-এর সিদ্ধান্ত** — চালকের অবস্থান দেখানো **AP** (১ সেকেন্ড পুরনো হলেও চলবে), কিন্তু ride assignment **CP** (দুজনকে এক চালক দেওয়া চলবে না)। এই পার্থক্যটা বলতে পারলে চমৎকার।

---

## Case Study 6 — Notification System

### Requirements

Push notification (iOS/Android), SMS, Email — তিন মাধ্যমেই পাঠাতে হবে। দিনে কোটি কোটি।

### Design

```
Services ──► Notification API ──► Kafka (topic per channel)
                                     │
              ┌──────────────────────┼──────────────────────┐
              ▼                      ▼                      ▼
        Push Workers            SMS Workers           Email Workers
              │                      │                      │
              ▼                      ▼                      ▼
         APNS / FCM              Twilio                SendGrid
```

**গুরুত্বপূর্ণ খুঁটিনাটি:**

**১. কেন queue:** External provider (APNS, Twilio) ধীর ও অনির্ভরযোগ্য। Queue না থাকলে তাদের downtime তোমার service-কে টেনে নামাবে।

**২. Retry + DLQ** — ব্যর্থ হলে exponential backoff দিয়ে আবার চেষ্টা, বারবার ব্যর্থ হলে Dead Letter Queue।

**৩. Idempotency অপরিহার্য** — একই notification দুবার পাঠানো user-এর কাছে সবচেয়ে বিরক্তিকর। প্রতিটা notification-এ unique ID রেখে duplicate ঠেকাও।

**৪. Rate limiting** — একজন user-কে ঘণ্টায় ৫টার বেশি না। নাহলে user app uninstall করবে।

**৫. User preference** — কে কোন ধরনের notification চায়, কোন সময়ে চায় না (quiet hours) — সব মানতে হবে।

**৬. অগ্রাধিকার** — OTP আর marketing notification একই queue-তে থাকা উচিত না। আলাদা priority queue রাখো।

**৭. Template service** — বার্তার ছাঁচ আলাদা রাখো, যাতে ভাষা বা লেখা বদলাতে deploy না লাগে।

---

## সব design-এ কাজে লাগে এমন Building Block

প্রায় প্রতিটা system design-এ এই উপাদানগুলো ঘুরেফিরে আসে:

| উপাদান | কেন |
|---|---|
| **Load Balancer** | Traffic ভাগ, high availability |
| **CDN** | Static content, latency কমানো |
| **API Gateway** | Auth, rate limit, routing |
| **Cache (Redis)** | Read কমানো, latency কমানো |
| **Database** | SQL না NoSQL — সবসময় যুক্তি দিয়ে বলো |
| **Message Queue** | Async, decoupling, buffering |
| **Blob Storage (S3)** | ছবি, video, file |
| **Search (Elasticsearch)** | Full-text search |
| **Monitoring** | Metrics, logs, tracing |

---

## শেষ মুহূর্তের Checklist

Interview-এ এই কথাগুলো বলতে ভুলো না:

- [ ] Requirement স্পষ্ট করার জন্য অন্তত ৩-৪টা প্রশ্ন করেছি
- [ ] Estimation করেছি (QPS, storage) এবং সেটা থেকে সিদ্ধান্ত নিয়েছি
- [ ] API স্পষ্ট করে লিখেছি
- [ ] Database পছন্দের পিছনে যুক্তি দিয়েছি
- [ ] Cache কোথায় বসবে বলেছি
- [ ] Single point of failure চিহ্নিত করেছি
- [ ] Read-heavy না write-heavy — সেটা অনুযায়ী design করেছি
- [ ] অন্তত একটা tradeoff স্পষ্টভাবে আলোচনা করেছি
- [ ] Peak traffic-এর কথা বলেছি (average-এর ২-৩ গুণ)
- [ ] Monitoring/alerting-এর কথা বলেছি
- [ ] নিজের design-এর দুর্বলতা নিজেই বলেছি

**সবচেয়ে ভালো শেষ বাক্য:**

> "এই design টা [X] scale পর্যন্ত কাজ করবে। এর মূল দুর্বলতা হলো [Y]। আরো সময় থাকলে আমি [Z] নিয়ে কাজ করতাম।"

এই একটা বাক্য দেখায় — তুমি জানো কোনো design নিখুঁত না, এবং তুমি নিজের কাজের সীমা বোঝো। এটাই একজন senior engineer-এর লক্ষণ।
