# Monolith vs Microservices

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [দুটো জিনিস আগে সহজ করে বুঝি](#দুটো-জিনিস-আগে-সহজ-করে-বুঝি)
* [Monolithic Architecture](#monolithic-architecture)
* [Microservices Architecture](#microservices-architecture)
* [পাশাপাশি তুলনা](#পাশাপাশি-তুলনা)
* [Microservices-এর লুকানো খরচ](#microservices-এর-লুকানো-খরচ)
* [কখন কোনটা বেছে নেবে](#কখন-কোনটা-বেছে-নেবে)
* [Service কীভাবে ভাগ করবে](#service-কীভাবে-ভাগ-করবে)
* [Service-রা একে অপরের সাথে কীভাবে কথা বলে](#service-রা-একে-অপরের-সাথে-কীভাবে-কথা-বলে)
* [Database per Service](#database-per-service)
* [Resilience Pattern — ভাঙা ঠেকানোর কৌশল](#resilience-pattern--ভাঙা-ঠেকানোর-কৌশল)
* [Observability — তিন স্তম্ভ](#observability--তিন-স্তম্ভ)
* [Monolith থেকে Microservices-এ যাওয়ার পথ](#monolith-থেকে-microservices-এ-যাওয়ার-পথ)
* [Interview Q&A](#interview-qa)
* [এক কথায় মনে রাখো](#এক-কথায়-মনে-রাখো)

---

<!-- TOC END -->

## দুটো জিনিস আগে সহজ করে বুঝি

ধরো তুমি একটা রেস্তোরাঁ চালাও।

**Monolith:** একটাই বিশাল রান্নাঘর। সব রাঁধুনি একসাথে, একই চুলা, একই বাসনপত্র। কাউকে খুঁজতে হয় না, সবাই পাশাপাশি — যোগাযোগ সহজ। কিন্তু একজন রাঁধুনি চুলা জ্বালিয়ে রেখে চলে গেলে পুরো রান্নাঘরে ধোঁয়া।

**Microservices:** আলাদা আলাদা ছোট রান্নাঘর — একটা শুধু বিরিয়ানির, একটা শুধু মিষ্টির, একটা শুধু পানীয়ের। মিষ্টির ঘরে আগুন লাগলে বিরিয়ানি ঠিকই তৈরি হবে। কিন্তু একটা order-এ তিনটাই লাগলে ওদের মধ্যে সমন্বয় করা কঠিন।

এই দুটোরই জায়গা আছে। কোনটা "ভালো" — এই প্রশ্নটাই ভুল। প্রশ্নটা হলো **"তোমার এখনকার অবস্থার জন্য কোনটা ঠিক?"**

---

## Monolithic Architecture

পুরো application একটাই codebase, একটাই deployable unit।

```
┌──────────────────────────────────────┐
│         Single Application           │
│  ┌────────┐ ┌────────┐ ┌─────────┐   │
│  │  User  │ │ Order  │ │ Payment │   │
│  │ Module │ │ Module │ │ Module  │   │
│  └────────┘ └────────┘ └─────────┘   │
│              ↓                       │
│         একটাই Database               │
└──────────────────────────────────────┘
```

**সুবিধা:**

- **Development সহজ** — একটা repo, একটা IDE-তে খোলো, সব দেখতে পাও
- **Debug সহজ** — একটা stack trace-এই পুরো ছবি
- **Deploy সহজ** — একটা artifact, একটা deploy
- **Transaction সহজ** — সব একই DB-তে, তাই ACID transaction স্বাভাবিকভাবেই কাজ করে
- **কোনো network latency নেই** — module গুলো সরাসরি function call করে
- **Testing সহজ** — end-to-end test চালানো সহজ
- **খরচ কম** — কম infrastructure, কম tooling

**অসুবিধা:**

- **বড় হলে ধীর হয়ে যায়** — build-এ ২০ মিনিট, IDE ঝুলে যায়
- **একসাথে scale করতে হয়** — শুধু search feature-এ চাপ? তবু পুরো app-ই copy করতে হবে
- **একটা bug পুরো app নামিয়ে দিতে পারে** — একটা module-এ memory leak মানে সব শেষ
- **Technology-তে আটকে যাওয়া** — পুরোটা Java-তে লেখা মানে একটা feature-এর জন্যও Python ব্যবহার করা যাবে না
- **Team-এ সংঘর্ষ** — ৫০ জন developer একই codebase-এ, merge conflict-এর যুদ্ধ
- **Deploy ভয়ের ব্যাপার** — ছোট একটা পরিবর্তনের জন্যও পুরো system deploy করতে হয়

---

## Microservices Architecture

Application কে অনেকগুলো ছোট, স্বাধীন service-এ ভাগ করা। প্রতিটার নিজস্ব codebase, নিজস্ব database, নিজস্ব deployment।

```
                    ┌─────────────┐
    Client ────────►│ API Gateway │
                    └──────┬──────┘
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
  ┌───────────┐     ┌───────────┐     ┌────────────┐
  │   User    │     │   Order   │     │  Payment   │
  │  Service  │     │  Service  │     │  Service   │
  ├───────────┤     ├───────────┤     ├────────────┤
  │  User DB  │     │ Order DB  │     │ Payment DB │
  └───────────┘     └───────────┘     └────────────┘
```

**সুবিধা:**

- **স্বাধীনভাবে scale** — শুধু search service-এ ১০টা instance, বাকিদের ২টা করে
- **স্বাধীনভাবে deploy** — Payment team তাদের অংশ deploy করলো, বাকিরা টেরও পেলো না
- **Fault isolation** — Recommendation service মরলে user তবু কিনতে পারবে
- **Technology-র স্বাধীনতা** — ML service Python-এ, API Go-তে, real-time অংশ Node-এ
- **ছোট, স্বাধীন team** — প্রতিটা team তাদের service-এর পূর্ণ মালিক
- **Codebase ছোট** — নতুন developer দ্রুত বুঝে ফেলে

**অসুবিধা:**

- **Distributed system-এর সব যন্ত্রণা** — network fail করে, latency আছে, partial failure হয়
- **Debug ভয়ানক কঠিন** — একটা request ৮টা service ঘুরেছে, কোথায় ভাঙলো?
- **Data consistency কঠিন** — ACID transaction নেই, Saga pattern লাগে
- **Operational জটিলতা** — ৫০টা service মানে ৫০টা CI/CD pipeline, monitoring, alert
- **Network latency যোগ হয়** — function call ছিল ন্যানোসেকেন্ডে, এখন HTTP call মিলিসেকেন্ডে
- **খরচ অনেক বেশি** — infrastructure, tooling, আর DevOps-এর লোকবল

---

## পাশাপাশি তুলনা

| বিষয় | Monolith | Microservices |
|---|---|---|
| Codebase | একটা | অনেকগুলো |
| Deployment | একসাথে সব | আলাদা আলাদা |
| Scaling | পুরোটা একসাথে | যেটা দরকার সেটা |
| Database | সাধারণত একটা | প্রতি service-এ একটা |
| Transaction | ACID, সহজ | Saga, কঠিন |
| Technology | একটাই stack | যার যা দরকার |
| Failure | একটা গেলে সব যায় | আলাদা করা যায় |
| Debug | সহজ | কঠিন (tracing লাগে) |
| Team | সবাই একসাথে | ছোট স্বাধীন team |
| শুরু করা | দ্রুত | ধীর |
| খরচ | কম | বেশি |
| উপযুক্ত | ছোট-মাঝারি team, নতুন product | বড় org, বিশাল scale |

---

## Microservices-এর লুকানো খরচ

এই অংশটা অনেকে বলে না, কিন্তু interview-তে এটা বলতে পারলে পরিণত মনে হয়।

**১. Network অবিশ্বাস্য (Fallacies of Distributed Computing)**
Function call কখনো fail করে না; HTTP call যেকোনো সময় fail করতে পারে। প্রতিটা service-to-service call-এ retry, timeout, circuit breaker লাগবে।

**২. Latency যোগ হতে থাকে**
৫টা service একে অপরকে ডাকলে, প্রতিটার ২০ms মানে ১০০ms। Monolith-এ এটা ছিল প্রায় শূন্য।

**৩. Data duplication অনিবার্য**
Order service-এর user-এর নাম দরকার। প্রতিবার user service-কে ডাকবে? তাহলে ধীর আর ভঙ্গুর। তাই নামটা copy করে রাখতে হয় — আর তখনই sync-এর সমস্যা।

**৪. Testing কঠিন**
Integration test চালাতে ৮টা service চালু করতে হবে। তাই contract testing, service virtualization — নতুন সব tooling শিখতে হবে।

**৫. Distributed Monolith — সবচেয়ে খারাপ পরিণতি**
Service গুলো আলাদা, কিন্তু এতটাই জড়িয়ে আছে যে একটা deploy করতে গেলে সবগুলো একসাথে deploy করতে হয়। এতে monolith-এর সব অসুবিধা রইলো, **সাথে** distributed system-এর সব যন্ত্রণাও যোগ হলো। এটা এড়ানোই microservices-এর সবচেয়ে বড় চ্যালেঞ্জ।

**৬. Organizational খরচ**
Conway's Law বলে — system-এর গঠন organization-এর যোগাযোগের গঠনের মতোই হয়। Team যদি আলাদা না হয়, service আলাদা করেও লাভ নেই।

---

## কখন কোনটা বেছে নেবে

**Monolith দিয়ে শুরু করো যখন:**

- Startup, product-market fit এখনো পাওনি
- Team ১০ জনের কম
- Domain এখনো ভালোভাবে বোঝা হয়নি (কোথায় ভাগ করবে তুমি এখনো জানো না!)
- দ্রুত বাজারে যেতে হবে
- DevOps-এর অভিজ্ঞ লোক নেই

**Microservices-এ যাও যখন:**

- একাধিক team একই codebase-এ ধাক্কাধাক্কি করছে
- System-এর আলাদা আলাদা অংশের scaling চাহিদা সম্পূর্ণ আলাদা
- Deploy করা এত ঝুঁকিপূর্ণ হয়ে গেছে যে সবাই ভয় পায়
- আলাদা অংশের জন্য আলাদা technology সত্যিই দরকার
- Domain boundary গুলো এখন স্পষ্ট
- DevOps-এর সক্ষমতা আছে (CI/CD, monitoring, tracing)

> **সবচেয়ে ভালো পরামর্শ (Martin Fowler-এর):** "Monolith First." প্রায় সব সফল microservices system-ই একটা monolith হিসেবে শুরু হয়েছিল, তারপর প্রয়োজন অনুযায়ী ভাগ হয়েছে। শুরুতেই microservices দিয়ে শুরু করা বেশিরভাগ সময়ই ভুল সিদ্ধান্ত — কারণ তুমি তখনো জানো না সীমারেখা কোথায় টানতে হবে।

**মাঝামাঝি একটা পথ — Modular Monolith:**
একটাই deployable unit, কিন্তু ভিতরে কঠোরভাবে আলাদা করা module, যাদের নিজস্ব স্পষ্ট interface আছে এবং একে অপরের ভিতরে হাত দেয় না। Monolith-এর সরলতা পাবে, আর পরে ভাগ করতে চাইলে সীমারেখাও প্রস্তুত থাকবে। **বেশিরভাগ company-র জন্য এটাই সঠিক উত্তর।**

---

## Service কীভাবে ভাগ করবে

ভুল জায়গায় ভাগ করলে microservices আশীর্বাদের বদলে অভিশাপ।

**❌ ভুল উপায় — Technical layer ধরে ভাগ:**
```
UI Service → Business Logic Service → Data Access Service
```
এতে একটা ছোট feature যোগ করতেও তিনটা service বদলাতে হবে। কোনো লাভ নেই।

**✅ সঠিক উপায় — Business capability ধরে ভাগ:**
```
User Service | Order Service | Payment Service | Shipping Service
```
প্রতিটা service একটা পূর্ণ ব্যবসায়িক ক্ষমতার মালিক — নিজের UI থেকে নিজের database পর্যন্ত।

**Domain-Driven Design (DDD)-এর সাহায্য:**

**Bounded Context** — একটা নির্দিষ্ট সীমানা যার ভিতরে একটা শব্দের একটাই অর্থ। যেমন "Customer" শব্দটা Sales context-এ মানে একরকম (lead, deal), আর Support context-এ আরেকরকম (ticket, history)। এই দুটো আলাদা service হওয়া উচিত।

**ভালো service-এর লক্ষণ:**
- **High cohesion** — ভিতরের জিনিসগুলো একে অপরের সাথে ঘনিষ্ঠভাবে সম্পর্কিত
- **Low coupling** — অন্য service-এর উপর কম নির্ভরশীল
- **স্বাধীনভাবে deploy করা যায়** — অন্য কাউকে না জানিয়ে
- **একটা team-এর মালিকানায়**
- **"Two-pizza team" নিয়ম** — যে team-কে দুটো পিৎজায় খাওয়ানো যায় (৬-৮ জন), তারা যেন একটা service সামলাতে পারে

---

## Service-রা একে অপরের সাথে কীভাবে কথা বলে

### Synchronous — উত্তরের জন্য অপেক্ষা

**REST/HTTP** — সবচেয়ে সহজ ও প্রচলিত। যেকোনো ভাষা থেকে ব্যবহার করা যায়, debug করা সহজ। তবে তুলনামূলক ধীর (JSON parsing, HTTP overhead)।

**gRPC** — Protocol Buffers ব্যবহার করে binary format-এ, HTTP/2-এর উপর। REST-এর চেয়ে অনেক দ্রুত, strongly typed contract দেয়, streaming support করে। Internal service-to-service যোগাযোগে চমৎকার। তবে browser থেকে সরাসরি ব্যবহার কঠিন, আর debug করতে বিশেষ tool লাগে।

**GraphQL** — Client ঠিক যা চায় তাই চেয়ে নিতে পারে। অনেক service-এর data একসাথে আনার জন্য (aggregation) ভালো।

*Synchronous-এর সমস্যা:* Service A, B-কে ডাকছে, B ডাকছে C-কে। C ধীর হলে A-ও ধীর। এভাবে **cascading failure** হতে পারে।

### Asynchronous — উত্তরের জন্য অপেক্ষা নেই

Message queue বা event bus দিয়ে (Kafka, RabbitMQ)।

*সুবিধা:* পূর্ণ decoupling, একটা service down থাকলেও message জমা থাকে, নিজের গতিতে scale করা যায়।
*অসুবিধা:* Eventual consistency, debug কঠিন।

**নিয়ম:** যেখানে সাথে সাথে উত্তর লাগে (user-এর profile দেখানো) সেখানে synchronous, আর যেখানে লাগে না (email পাঠানো, analytics) সেখানে asynchronous। **যতটা সম্ভব asynchronous-এর দিকে ঝুঁকো** — তাতে system অনেক বেশি সহনশীল হয়।

---

## Database per Service

Microservices-এর সবচেয়ে কঠিন কিন্তু সবচেয়ে গুরুত্বপূর্ণ নিয়ম — **প্রতিটা service-এর নিজস্ব database, এবং অন্য কেউ সেটা সরাসরি ছোঁবে না।**

**কেন এত কড়াকড়ি:**
- একটা service নিজের schema বদলাতে পারবে অন্যদের না ভেঙে
- প্রতিটা service তার প্রয়োজন অনুযায়ী database বাছতে পারবে
- Database একটা service-এর ভিতরের ব্যাপার, বাইরের interface না
- শেয়ার করা database মানেই সবচেয়ে শক্ত coupling — তখন এটা আসলে distributed monolith

**কিন্তু এতে যেসব সমস্যা তৈরি হয়:**

**১. JOIN করা যায় না।** সমাধান — API Composition (দুটো service-কে ডেকে application-এ মিলিয়ে নাও), বা CQRS (আগে থেকেই একটা read-optimized view বানিয়ে রাখো)।

**২. Transaction নেই।** সমাধান — Saga pattern (`17. Message Queue`-এ বিস্তারিত)।

**৩. একই data একাধিক জায়গায়।** সমাধান — event দিয়ে sync রাখা, আর মেনে নেওয়া যে eventual consistency-ই বাস্তবতা।

---

## Resilience Pattern — ভাঙা ঠেকানোর কৌশল

Distributed system-এ **failure স্বাভাবিক ঘটনা**, ব্যতিক্রম না। তাই আগে থেকেই প্রস্তুত থাকতে হয়।

### Circuit Breaker — সবচেয়ে গুরুত্বপূর্ণ

বিদ্যুতের circuit breaker-এর মতোই। একটা service বারবার fail করলে তাকে ডাকা **বন্ধ** করে দাও কিছুক্ষণের জন্য।

```
CLOSED (স্বাভাবিক)
   │  পরপর ৫ বার fail
   ▼
OPEN (কল বন্ধ, সাথে সাথে error দাও)
   │  ৩০ সেকেন্ড পর
   ▼
HALF-OPEN (একটা কল করে দেখো)
   │
   ├── সফল → CLOSED-এ ফিরে যাও
   └── ব্যর্থ → আবার OPEN
```

**কেন দরকার:** মৃত service-কে বারবার ডাকা মানে নিজের thread গুলোও আটকে ফেলা। তখন তোমার service-ও মরে যায়, তার উপর নির্ভরশীল service-ও মরে — এভাবে **cascading failure**-এ পুরো system ধসে পড়ে। Circuit breaker এই শৃঙ্খল ভেঙে দেয়।

### Retry with Exponential Backoff

ব্যর্থ হলে সাথে সাথে না, ক্রমশ বেশি সময় অপেক্ষা করে আবার চেষ্টা — ১s, ২s, ৪s, ৮s। সাথে random **jitter** যোগ করো যাতে সব client একসাথে হামলে না পড়ে।

⚠️ শুধু **idempotent** operation-এ retry করবে। "Payment নাও" retry করলে দুবার টাকা কাটতে পারে!

### Timeout

প্রতিটা call-এ timeout দাও। Timeout ছাড়া call মানে অনির্দিষ্টকাল আটকে থাকা thread — আর ধীরে ধীরে সব thread শেষ।

### Bulkhead

জাহাজের জলনিরোধক কুঠুরির মতো — এক অংশে পানি ঢুকলেও পুরো জাহাজ ডোবে না। প্রতিটা downstream service-এর জন্য আলাদা thread pool/connection pool রাখো, যাতে একটার সমস্যা অন্যদের সম্পদ শেষ করে না দেয়।

### Graceful Degradation

একটা service মরলে কম গুরুত্বপূর্ণ feature বন্ধ রেখে বাকিটা চালু রাখা।
*উদাহরণ:* Recommendation service down? তাহলে শুধু "জনপ্রিয় পণ্য" দেখাও — পুরো page ভাঙার চেয়ে অনেক ভালো।

---

## Observability — তিন স্তম্ভ

Microservices-এ কী ঘটছে তা বোঝার একমাত্র উপায়।

**১. Logs** — কী ঘটেছে। সব service-এর log এক জায়গায় জমা করতে হবে (ELK stack, Loki)। প্রতিটা log-এ **correlation ID** রাখা বাধ্যতামূলক, নাহলে কোন request কোন log — বোঝাই যাবে না।

**২. Metrics** — কত ঘটেছে। Request rate, error rate, latency, resource ব্যবহার (Prometheus + Grafana)।

**গুরুত্বপূর্ণ:** গড় (average) latency দেখো না, **percentile** দেখো। p50 (মাঝামাঝি), **p95, p99** — কারণ গড় ভালো দেখালেও ৫% user ভয়াবহ অভিজ্ঞতা পেতে পারে।

**৩. Traces** — একটা request কোন কোন পথে গেলো। Distributed tracing (Jaeger, Zipkin, OpenTelemetry) দেখায় request-টা কোন service-এ কত সময় নিলো — bottleneck খুঁজে বের করার একমাত্র উপায়।

**Health Check:** প্রতিটা service-এর `/health` endpoint থাকতে হবে, যাতে load balancer আর orchestrator জানে সে বেঁচে আছে কিনা।

---

## Monolith থেকে Microservices-এ যাওয়ার পথ

কখনো একবারে সব ভেঙে ফেলো না ("big bang rewrite") — এটা প্রায় সবসময়ই ব্যর্থ হয়।

**Strangler Fig Pattern** — ধীরে ধীরে, একটা একটা করে:

```
ধাপ ১: Monolith-এর সামনে একটা proxy/gateway বসাও
       (সব traffic এখনো monolith-এ যাচ্ছে)

ধাপ ২: একটা feature বেছে নাও — ছোট, সবচেয়ে কম জড়ানো
       সেটা আলাদা service হিসেবে লিখো

ধাপ ৩: Proxy-তে বলে দাও ওই path-টা নতুন service-এ যাবে

ধাপ ৪: কাজ করছে নিশ্চিত হলে monolith থেকে ওই code মুছে দাও

ধাপ ৫: পরের feature-এ যাও, পুনরাবৃত্তি করো
```

গাছের গায়ে জড়ানো লতা যেমন ধীরে ধীরে গাছটাকে ঢেকে ফেলে — নাম এসেছে সেখান থেকে।

**কোন feature আগে বের করবে:**
- যেটার scaling চাহিদা সবচেয়ে আলাদা
- যেটা সবচেয়ে ঘন ঘন বদলায়
- যেটার অন্য অংশের উপর নির্ভরতা সবচেয়ে কম
- সবচেয়ে গুরুত্বপূর্ণ অংশ দিয়ে **কখনো শুরু করবে না**

---

## Interview Q&A

**প্রশ্ন ১: Monolith আর Microservices-এর মধ্যে কোনটা বেছে নেবে?**

এটা team-এর আকার, domain-এর স্পষ্টতা আর scaling-এর চাহিদার উপর নির্ভর করে। ছোট team, নতুন product আর অস্পষ্ট domain হলে monolith দিয়ে শুরু করাই সঠিক — কারণ তখনো জানা নেই সীমারেখা কোথায় টানতে হবে। একাধিক team যখন একই codebase-এ বাধা পাচ্ছে, deploy ঝুঁকিপূর্ণ হয়ে উঠছে, আর বিভিন্ন অংশের scaling চাহিদা আলাদা হয়ে যাচ্ছে — তখন microservices-এ যাওয়া যুক্তিসঙ্গত। আমি সাধারণত modular monolith দিয়ে শুরু করার পক্ষে, যাতে পরে ভাগ করতে চাইলে সীমারেখা প্রস্তুত থাকে।

**প্রশ্ন ২: Microservices-এর সবচেয়ে বড় চ্যালেঞ্জ কী?**

Data consistency আর operational জটিলতা। প্রতিটা service-এর আলাদা database হওয়ায় ACID transaction পাওয়া যায় না — Saga pattern দিয়ে compensating transaction লিখতে হয়, যা জটিল ও ভুলপ্রবণ। পাশাপাশি একটা request একাধিক service ঘুরে যায় বলে debug করতে distributed tracing লাগে, আর প্রতিটা service-এর জন্য আলাদা deployment, monitoring, alerting সামলাতে হয়। মোটকথা business logic-এর জটিলতা কমে, কিন্তু infrastructure-এর জটিলতা অনেক বেড়ে যায়।

**প্রশ্ন ৩: Distributed monolith কী এবং এটা খারাপ কেন?**

যখন service গুলো আলাদা করা হয়েছে ঠিকই, কিন্তু তারা এতটা পরস্পরনির্ভর যে একটাকে আলাদাভাবে deploy করা যায় না — সবগুলো একসাথে deploy করতে হয়। এটা সবচেয়ে খারাপ পরিণতি কারণ এতে monolith-এর সব সীমাবদ্ধতা (একসাথে deploy, শক্ত coupling) থেকেই যায়, আর তার উপর distributed system-এর সব সমস্যা (network failure, latency, debug করা কঠিন) যোগ হয়। সাধারণত এটা হয় ভুল জায়গায় service ভাগ করলে, বা database শেয়ার করলে।

**প্রশ্ন ৪: Service গুলো কীভাবে ভাগ করবে?**

Technical layer ধরে না, business capability ধরে। UI service, logic service, data service — এভাবে ভাগ করলে একটা feature যোগ করতে সবগুলো বদলাতে হয়, কোনো লাভ হয় না। বরং Domain-Driven Design-এর bounded context ধরে ভাগ করতে হয় — User, Order, Payment, Shipping — যেখানে প্রতিটা service একটা পূর্ণ ব্যবসায়িক ক্ষমতার মালিক, নিজের database সহ। ভালো ভাগের লক্ষণ হলো high cohesion, low coupling, আর স্বাধীনভাবে deploy করার ক্ষমতা।

**প্রশ্ন ৫: Circuit breaker pattern কী?**

একটা downstream service বারবার fail করলে তাকে ডাকা সাময়িকভাবে বন্ধ করে দেওয়ার কৌশল। তিনটা অবস্থা থাকে — CLOSED-এ স্বাভাবিক কল যায়; নির্দিষ্ট সংখ্যক ব্যর্থতার পর OPEN হয়ে যায় এবং তখন কল না করেই সাথে সাথে error ফেরত দেয়; কিছু সময় পর HALF-OPEN অবস্থায় একটা কল করে দেখে সেবা ফিরেছে কিনা। এটা জরুরি কারণ মৃত service-কে বারবার ডাকলে caller-এর thread গুলোও আটকে যায়, ফলে সে নিজেও মরে যায় এবং cascading failure-এ পুরো system ধসে পড়ে।

**প্রশ্ন ৬: Microservices-এ transaction কীভাবে handle করবে?**

প্রতিটা service-এর আলাদা database থাকায় প্রচলিত ACID transaction সম্ভব না। Two-phase commit তাত্ত্বিকভাবে সম্ভব হলেও ধীর, ভঙ্গুর এবং availability কমিয়ে দেয়, তাই বাস্তবে এড়ানো হয়। এর বদলে Saga pattern ব্যবহার করা হয় — কাজটাকে ধারাবাহিক local transaction-এ ভাগ করা হয়, প্রতিটা ধাপ নিজের database-এ commit করে, আর কোনো ধাপ ব্যর্থ হলে আগের ধাপগুলোর compensating transaction চালিয়ে পিছিয়ে আসা হয়।

**প্রশ্ন ৭: Service discovery কী?**

Microservices-এ instance গুলো auto-scaling আর restart-এর কারণে সারাক্ষণ আসছে-যাচ্ছে, তাই IP address hardcode করা অসম্ভব। Service discovery হলো সেই ব্যবস্থা যা track রাখে কোন service-এর কোন instance এখন কোথায় চলছে। Client-side discovery-তে client নিজে registry (Consul, Eureka) থেকে ঠিকানা নিয়ে সিদ্ধান্ত নেয়; server-side discovery-তে client শুধু একটা load balancer-কে ডাকে, সে-ই খুঁজে দেয়। Kubernetes-এ এটা built-in — service নামেই DNS resolution হয়ে যায়।

**প্রশ্ন ৮: Monolith থেকে microservices-এ কীভাবে migrate করবে?**

কখনো একবারে পুরোটা rewrite করবো না — সেটা প্রায় সবসময়ই ব্যর্থ হয়। Strangler Fig pattern ব্যবহার করবো: প্রথমে monolith-এর সামনে একটা gateway বসাবো, তারপর একটা ছোট ও তুলনামূলক স্বাধীন feature বেছে নিয়ে সেটাকে আলাদা service হিসেবে লিখবো, gateway-তে ওই path-টা নতুন service-এ পাঠাবো, এবং নিশ্চিত হওয়ার পর monolith থেকে পুরনো code মুছে দেবো। এভাবে একটা একটা করে এগোবো, সবচেয়ে critical অংশটা সবার শেষে।

**প্রশ্ন ৯: Microservices-এ একটা request debug করবে কীভাবে?**

Distributed tracing দিয়ে। প্রতিটা incoming request-এ একটা unique trace ID তৈরি হয় এবং সেটা প্রতিটা downstream call-এর header-এ পাঠানো হয়। এতে Jaeger বা Zipkin-এর মতো tool-এ পুরো request-এর যাত্রাপথ দেখা যায় — কোন service কত সময় নিলো, কোথায় error হলো। এর সাথে centralized logging লাগে যেখানে সব service-এর log এক জায়গায় জমা হয় এবং প্রতিটা log line-এ ওই correlation ID থাকে, তাহলে একটা ID দিয়ে খুঁজলেই পুরো ঘটনাটা পাওয়া যায়।

**প্রশ্ন ১০: প্রতিটা service-এর আলাদা database লাগে কেন? একটা শেয়ার করলে সমস্যা কী?**

Shared database হলে সবচেয়ে শক্ত coupling তৈরি হয় — একটা service schema বদলালে অন্য সব service ভেঙে যেতে পারে, ফলে স্বাধীনভাবে deploy করার ক্ষমতাই নষ্ট হয়ে যায়, যা microservices-এর মূল উদ্দেশ্য। তাছাড়া প্রতিটা service তার নিজের প্রয়োজন অনুযায়ী database প্রযুক্তি বাছতে পারে না। এর দাম অবশ্যই দিতে হয় — JOIN আর transaction হারাতে হয় — কিন্তু সেটাই microservices-এর মূল্য। Database শেয়ার করলে সেটা আসলে microservices না, distributed monolith।

---

## এক কথায় মনে রাখো

**Monolith** = একটা রান্নাঘর। সহজ, দ্রুত শুরু, কিন্তু বড় হলে ভারী।

**Microservices** = অনেক রান্নাঘর। স্বাধীনতা ও সহনশীলতা দেয়, কিন্তু বিশাল operational খরচ আনে।

**Monolith First** — প্রায় সব সফল microservices একটা monolith হিসেবেই শুরু হয়েছিল।

ভাগ করো **business capability ধরে**, technical layer ধরে না। প্রতিটা service-এর **নিজের database**। আর **Circuit Breaker, Retry, Timeout, Tracing** ছাড়া microservices চালানো আত্মহত্যার শামিল।
