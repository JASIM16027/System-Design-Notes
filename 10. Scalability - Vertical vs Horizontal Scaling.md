# Scalability — Vertical vs Horizontal Scaling

<!-- TOC START -->

**আলোচ্য বিষয়:**

* [Scalability আসলে কী?](#scalability-আসলে-কী)
* [Vertical Scaling (Scale Up) — মেশিন বড় করো](#vertical-scaling-scale-up--মেশিন-বড়-করো)
* [Horizontal Scaling (Scale Out) — মেশিন বাড়াও](#horizontal-scaling-scale-out--মেশিন-বাড়াও)
* [পাশাপাশি তুলনা](#পাশাপাশি-তুলনা)
* [কখন কোনটা বেছে নেবে](#কখন-কোনটা-বেছে-নেবে)
* [Horizontal Scaling-এর আসল চ্যালেঞ্জ — State](#horizontal-scaling-এর-আসল-চ্যালেঞ্জ--state)
* [একটা System কীভাবে ধাপে ধাপে বড় হয়](#একটা-system-কীভাবে-ধাপে-ধাপে-বড়-হয়)
* [Scalability-র কয়েকটা জরুরি শব্দ](#scalability-র-কয়েকটা-জরুরি-শব্দ)
* [Interview Q&A](#interview-qa)
* [এক কথায় মনে রাখো](#এক-কথায়-মনে-রাখো)

---

<!-- TOC END -->

## Scalability আসলে কী?

ধরো তোমার একটা ছোট চায়ের দোকান। প্রতিদিন 50 জন কাস্টমার আসে, তুমি একাই সামলাও।

হঠাৎ দোকান famous হয়ে গেলো — এখন প্রতিদিন 5,000 জন আসে। এখন কী করবে?

দুটো রাস্তা আছে।

**রাস্তা ১:** তুমি নিজে আরো দ্রুত কাজ শিখলে, বড় চুলা কিনলে, বড় কেটলি আনলে। — এটা **Vertical Scaling**।

**রাস্তা ২:** আরো 5 জন কর্মচারী নিয়োগ দিলে, 5টা আলাদা কাউন্টার বানালে। — এটা **Horizontal Scaling**।

Software system-এও ঠিক এই দুটোই রাস্তা। Scalability মানে হলো — **load বাড়লে system কি সেটা সামলাতে পারবে?**

---

## Vertical Scaling (Scale Up) — মেশিন বড় করো

একই server-এ আরো বেশি CPU, RAM, SSD লাগিয়ে দেওয়া।

আগে ছিল 4 CPU + 8 GB RAM। এখন করে দিলে 32 CPU + 128 GB RAM। Server একটাই, শুধু শক্তিশালী হলো।

**সুবিধা:**

- কোনো code change লাগে না। শুধু server upgrade করো, ব্যস।
- Data একটা জায়গায় থাকে, তাই consistency নিয়ে চিন্তা নেই।
- Architecture simple — কোনো load balancer, কোনো distributed complexity নেই।

**অসুবিধা:**

- **Hardware limit আছে।** পৃথিবীর সবচেয়ে বড় server-ও একটা জায়গায় গিয়ে থেমে যায়। তুমি অসীম RAM লাগাতে পারবে না।
- **দাম exponentially বাড়ে।** 2x powerful server-এর দাম 2x না, প্রায় 4x-5x হয়।
- **Single Point of Failure (SPOF)।** ওই একটা server down মানে পুরো system down।
- Upgrade করতে গেলে সাধারণত **downtime** লাগে (server restart)।

---

## Horizontal Scaling (Scale Out) — মেশিন বাড়াও

একটা বড় server-এর বদলে অনেকগুলো ছোট server রাখা, আর সামনে একটা **Load Balancer** বসিয়ে traffic ভাগ করে দেওয়া।

```
              ┌──────────────┐
  Users ────► │ Load Balancer│
              └──────┬───────┘
           ┌─────────┼─────────┐
           ▼         ▼         ▼
       Server-1  Server-2  Server-3
```

**সুবিধা:**

- **প্রায় unlimited scale।** আরো user? আরো server যোগ করো। Google, Facebook এভাবেই চলে।
- **Fault tolerance।** একটা server মরে গেলে বাকিগুলো কাজ চালিয়ে যায়। User টেরই পায় না।
- **সস্তা।** 10টা normal machine একটা super machine-এর চেয়ে কম দামি।
- **Zero downtime deployment** সম্ভব — একটা একটা করে server update করো।

**অসুবিধা:**

- Architecture **complex** হয়ে যায় — load balancer, service discovery, distributed cache সব লাগে।
- Application-কে **stateless** হতে হয় (এটাই সবচেয়ে বড় শর্ত)।
- Data consistency কঠিন — একাধিক server একই data নিয়ে কাজ করলে conflict হতে পারে।
- Network latency যোগ হয় — server-রা একে অপরের সাথে কথা বলে।

---

## পাশাপাশি তুলনা

| বিষয় | Vertical (Scale Up) | Horizontal (Scale Out) |
|---|---|---|
| কী করা হয় | একই মেশিন শক্তিশালী করা | আরো মেশিন যোগ করা |
| Limit | Hardware-এর ceiling আছে | প্রায় unlimited |
| খরচ | Exponentially বাড়ে | Linearly বাড়ে |
| Failure | Single point of failure | একটা গেলে বাকিরা চলে |
| Downtime | Upgrade-এ লাগে | লাগে না |
| Complexity | কম | বেশি |
| Load balancer | লাগে না | অবশ্যই লাগে |
| Data consistency | সহজ | কঠিন |
| উদাহরণ | ছোট startup, PostgreSQL primary DB | Google, Netflix, Facebook |

---

## কখন কোনটা বেছে নেবে

**Vertical scaling বেছে নাও যখন:**

- Traffic এখনো ছোট বা predictable
- Team ছোট, distributed system maintain করার লোক নেই
- Database (বিশেষ করে SQL) — DB সাধারণত প্রথমে vertically scale করা হয়
- দ্রুত একটা সমাধান দরকার

**Horizontal scaling বেছে নাও যখন:**

- Traffic দ্রুত বাড়ছে বা unpredictable (flash sale, viral post)
- High availability লাগবেই — downtime affordable না
- Global user base, বিভিন্ন region-এ server লাগবে
- Stateless application (REST API, microservices)

**বাস্তব সত্য:** বেশিরভাগ company দুটোই করে। প্রথমে vertical (সহজ), তারপর দেয়াল-এ ধাক্কা খেয়ে horizontal।

---

## Horizontal Scaling-এর আসল চ্যালেঞ্জ — State

এটাই সবচেয়ে গুরুত্বপূর্ণ অংশ, আর এখানেই বেশিরভাগ মানুষ আটকায়।

**সমস্যা:** User login করলো, Server-1 তার session memory-তে রাখলো। পরের request গেলো Server-2 তে। Server-2 বলে — "তুমি কে? Login করো।" User বিরক্ত।

**সমাধান ৩টা:**

**১. Sticky Session (Session Affinity)** — Load balancer মনে রাখে কোন user কোন server-এ গিয়েছিল, প্রতিবার সেখানেই পাঠায়।
*সমস্যা:* ওই server down হলে session হারিয়ে যায়। Load-ও সমানভাবে ভাগ হয় না। এটা খারাপ সমাধান।

**২. Centralized Session Store** — Session সব server-এর বাইরে Redis-এ রাখা। যেকোনো server Redis থেকে পড়ে নিতে পারে।
*এটা ভালো সমাধান,* তবে Redis-এ একটা extra network call লাগে।

**৩. JWT (Stateless Token)** — Server কিছুই মনে রাখে না। Token-এর মধ্যেই user info থাকে, signature দিয়ে verify হয়।
*এটাই best solution* horizontal scaling-এর জন্য। (বিস্তারিত: `4. Token-Based and JWT Authentication.md`)

**মূল কথা:** Horizontal scaling করতে চাইলে application **stateless** বানাতেই হবে।

---

## একটা System কীভাবে ধাপে ধাপে বড় হয়

এটা interview-তে খুব কাজে লাগে — evolution-টা দেখাতে পারলে interviewer খুশি হয়।

**Stage 1 — একটাই server (0 – 1,000 users)**
App + Database একই মেশিনে। সবচেয়ে সহজ।

**Stage 2 — Database আলাদা করো (1K – 10K)**
App server আর DB server আলাদা মেশিনে। এখন দুটোকে আলাদাভাবে scale করা যায়।

**Stage 3 — Load Balancer + একাধিক App server (10K – 100K)**
App stateless বানাও, session Redis-এ সরাও, সামনে load balancer বসাও।

**Stage 4 — Cache + CDN (100K – 1M)**
বারবার একই DB query? Redis cache বসাও। Image, CSS, JS? CDN-এ পাঠাও।

**Stage 5 — Database Replication (1M – 10M)**
এক Primary (write) + অনেক Replica (read)। কারণ বেশিরভাগ traffic read।

**Stage 6 — Sharding + Message Queue (10M+)**
Data ভাগ করে অনেক DB-তে রাখো। Heavy কাজ (email, video encoding) queue-তে পাঠিয়ে async করো।

**Stage 7 — Microservices + Multi-region (100M+)**
Service আলাদা আলাদা করো, পৃথিবীর বিভিন্ন region-এ deploy করো।

---

## Scalability-র কয়েকটা জরুরি শব্দ

**Elasticity** — Load বাড়লে automatically server বাড়ে, load কমলে কমে যায়। AWS Auto Scaling এই কাজটা করে। Scalability হলো ক্ষমতা, elasticity হলো সেটা automatic হওয়া।

**Bottleneck** — System-এর সবচেয়ে দুর্বল জায়গা, যেটা পুরো system-কে ধীর করে দেয়। 10টা app server থাকলেও যদি একটাই DB থাকে, DB-ই bottleneck।

**Single Point of Failure (SPOF)** — যে component টা মরে গেলে পুরো system মরে যায়। ভালো design-এ কোনো SPOF থাকে না।

**Redundancy** — একই জিনিসের backup copy রাখা, যাতে একটা fail করলে অন্যটা কাজ করে।

**Graceful Degradation** — একটা feature নষ্ট হলে পুরো site বন্ধ না করে শুধু ওই feature বন্ধ রাখা। যেমন YouTube-এর "recommended videos" fail করলেও video play হতে থাকবে।

---

## Interview Q&A

**প্রশ্ন ১: Vertical আর Horizontal scaling-এর মধ্যে পার্থক্য কী?**

Vertical scaling মানে একই machine-এ resource (CPU, RAM) বাড়ানো — সহজ কিন্তু hardware limit আর single point of failure আছে। Horizontal scaling মানে আরো machine যোগ করা — প্রায় unlimited scale আর fault tolerance দেয়, কিন্তু load balancer লাগে এবং application stateless হতে হয়।

**প্রশ্ন ২: তাহলে সবাই horizontal scaling-ই করে না কেন?**

কারণ complexity। Horizontal scaling মানে distributed system, আর distributed system মানে network failure, data consistency, debugging কঠিন হওয়া — এসব সমস্যা। ছোট system-এ vertical scaling-ই যথেষ্ট এবং সস্তা। যতক্ষণ না সত্যিকারের প্রয়োজন হয়, ততক্ষণ complexity যোগ করা উচিত না।

**প্রশ্ন ৩: Database কি horizontally scale করা যায়?**

Read traffic-এর জন্য সহজে যায় — read replica যোগ করলেই হয়। কিন্তু write traffic-এর জন্য কঠিন, কারণ তখন sharding করতে হয় — data ভাগ করে আলাদা আলাদা DB-তে রাখতে হয়। এতে JOIN, transaction সব কঠিন হয়ে যায়। তাই DB সাধারণত আগে vertically scale করা হয়, তারপর replication, সবার শেষে sharding।

**প্রশ্ন ৪: Stateless application মানে কী, আর এটা scaling-এর জন্য জরুরি কেন?**

Stateless মানে server কোনো request-এর তথ্য নিজের memory-তে জমা রাখে না — প্রতিটা request নিজেই সম্পূর্ণ তথ্য বহন করে। এটা জরুরি কারণ তাহলে যেকোনো server যেকোনো request handle করতে পারে। Server মরে গেলেও কিছু হারায় না, নতুন server যোগ করাও instant।

**প্রশ্ন ৫: Auto-scaling কীভাবে কাজ করে?**

একটা metric (সাধারণত CPU usage বা request count) নির্দিষ্ট threshold ছাড়ালে system নতুন server চালু করে, আর threshold-এর নিচে নামলে server বন্ধ করে দেয়। এতে খরচ কমে কারণ peak time-এ বেশি server, রাতে কম server চলে। তবে নতুন server চালু হতে কিছু সময় লাগে, তাই sudden spike সামলাতে কিছু buffer capacity রাখতে হয়।

---

## এক কথায় মনে রাখো

**Vertical = মেশিন বড় করো।** সহজ, কিন্তু একটা সীমা আছে আর একটা মরলে সব শেষ।

**Horizontal = মেশিন বাড়াও।** Complex, কিন্তু unlimited scale আর একটা মরলেও কিছু হয় না।

আর horizontal scaling-এর প্রথম শর্ত — **application কে stateless বানাও।**
